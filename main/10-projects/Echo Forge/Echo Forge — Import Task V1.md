---
type: project
schema_version: 1
owner: agent
root: false
status: active
priority: P0
area: "[[Personal]]"
parent: "[[Echo Forge]]"
sprint:
start: 2026-09-21
due:
progress: 75
repo: xKoRx/symphony
jira:
prs:
aliases: []
tags:
  - kind/project
  - area/personal
created: "2026-09-21"
updated: "2026-09-22"
---

# Echo Forge — Import Task V1

## 🎯 Objetivo

- Incorporar estrategias SQX ya existentes al pipeline durable de Forge SIN ejecutar Builder: IMPORT (productor de intake) → CLASSIFICATION → RANKING → SELECTION, con evidencia durable y certificación física por archivos auténticos. **Contrato final 2026-09-22 (D10, mandato owner): IMPORT TASK ABANDONED — replaced by WATCHER IMPORT INTAKE.** La importación ocurre ANTES de Temporal: el owner deposita `flow.json` + `*.cfx` + `import/*.sqx` en el watch_dir; el watcher congela, publica bajo `watcher.import.folder` (MinIO), adopta con rol IMPORTED y llena `SQXJobRequest.Input {Origin: watcher_import}`; el enrichment físico SQX es bootstrap interno de Temporal sólo si classifications/early_rankings consumen el cohort. Import NO es una TaskSpec; sin host affinity; el watcher nunca ejecuta SQX. Extensión funcional posterior a Factory V2; no reabre F05.

## 📊 Estado actual

- **G7 FÍSICO PASS + INTEGRADO EN MASTER + LIMPIEZA COMPLETA (2026-09-23): `origin/master` @ `9a69243` (FF puro `745bc8b..9a69243`, 35 commits; feature branch remota/local y worktree ELIMINADOS; `ls-remote` = sólo `refs/heads/master`). RESULT = G7 PASS.** CONFIRMED del owner sobre RT-1 v5 @ `a2b4bbd` (Kronos, ventana 2h, universo 8 USATECHIDXUSD_darwinex H1 L+S ratificado; `L` = etiqueta de bucket). Ejecución en VM 111 Kronos con SQX candidate-local (copia rsync del install de `kor` sin zip ni databanks ajenos; aislamiento reforzado: cero writes en `/home/kor/sqx`) bajo operador `echo-dev`. **7 defectos materiales de integración detectados EN VENTANA, todos fail-closed, corregidos con red→green y re-pins (`a6eab94` deadline ctx del intake · `ba8eb53` fallback request_id del dispatch · `d0f9748` verify write-once gateado a stream consumido · `172d514` task path del bootstrap = slot reservado `root/0` · `0a15e0b` identidad del carrier (fuente vs publicado) en persist · `b697815` alias `USATECHIDXUSD_darwinex→usatechidxusd` en CanonicalSymbol · `ca83947` restauración de Keys en la salida del bootstrap).** Campaña certificadora final = `g7r7`: FlowRun `7e2f4c05` COMPLETED, workflow `sqx-main-v1-5f6a0a08` COMPLETED, StageExecution `import@sqx-import.v1` COMPLETED; 8 evaluaciones `import@sqx-import.v1` `IMPORTED_HISTORICAL_RESULT` con provenance `minio_package` (keys+SHA256 exactos); 1 classification snapshot (indicator_signature.v1) → 3 ranking snapshots (imported_ranking per-type) → 1 selection snapshot con 6 finalists, refs exactas; config persistida sin ningún TaskSpec type=import. Read-back MinIO 8/8 objetos byte-exact contra el manifest congelado. Drill de idempotencia: resume de crash pre-publish converge mismo FlowRun + cohort 8; sondas `IMPORT_FROZEN_INPUT_EXTRA`/`MISSING`/`MUTATED` fallan cerrado sin tocar el cohort. Negativos de cierre: flota mismo PID 1400507/binario; `passed` 200 files/0 nuevos/bytes exactos; ETCD production 110 claves sin cambios de mod_revision; 0 pollers en `sqx-main-queue`; Echo 0 POST (fail-closed). Teardown: procesos detenidos, prefixes ETCD candidatos eliminados (34+26), proyecto efímero + databank `input` eliminados; watch_dir + MinIO + PG/Mongo conservados como evidencia. Binarios finales @ `ca83947` (`vcs.modified=false`): sqx-watcher `80bd7ec4…`, sqx-worker `2aa58ec0…`, copias durables `~/aranea/work/import-cert-g7-v5-20260922/`. Puerta abierta: campaña B (SELECTED COHORT → SQX TICK RETEST → MT5) y adopción formal de `usatechidxusd` en el catálogo Echo (owner).

- **CIERRE OPERACIONAL OWNER APLICADO (2026-09-22, pre-CONFIRMED): RT-1 v5 definitiva @ `a2b4bbd` (FF `4f00f65..a2b4bbd`, docs-only; pins @ `dd3eedb` conservados): estado corregido a `G7 BLOCKED / NOT CERTIFIED / NOT MERGE_READY` sin CONFIRMED (el "CERTIFIED/MERGE_READY" del v5 original era un defecto); registro de aprobaciones técnicas (MR-1/MR-2 ronda 3) separado explícitamente de la ratificación owner; negativo de databanks precisado (prohibido Retester/flota; única excepción = databank `input` interno del proyecto efímero `EchoForgeImportExporter` con aislamiento y teardown documentados); independencia worker/watcher precisada (sólo ArtifactRefs de MinIO, sin `watch_dir/import`, host compartido posible — independencia contractual, no física).**

- **CONTRATO FINAL IMPLEMENTADO Y PUBLICADO (2026-09-22, D10 — mandato owner one-shot): `origin/feature/sqx-import-task-v1` @ `1a2fe34` (FF desde `492250b`; `cf808a4` código + `1a2fe34` docs; master `745bc8b` intacto). RESULT software = PASS: failing set == baseline exacto (22/22 nombres, stash-diff), tests A–L nuevos verdes, G7 sigue SIN EJECUTAR y exige RT-1 v5 (v2/v3/v4 superseded).** Arquitectura: `watcher.import {enabled, folder, config, custom_analysis_plugin}`; step `import_intake` del watcher (freeze doble-hash + freeze-check de membresía ORIGIN + publish byte-exacto put-if-absent + AdoptStrategy IMPORTED + Input durable); productor reservado `watcher_import` para classifications/early_rankings; bootstrap interno de enrichment en el workflow (stage `import@sqx-import.v1`, sin TaskSpec) con `ingest_import_cohort` (membresía = exactamente Input) + `persist_import_evidence` (IMPORTED_HISTORICAL_RESULT); processed como unidad; `type=import`, `stage=import` declarable, `TaskSpec.InputFolder`, `00_inputs/import` y la constante `01_import` eliminados del contrato (fail-closed con guidance). RT-1 v3 marcada SUPERSEDED; runbook G7 reemitido con receta `watcher.import` + demostraciones worker-independence/no-TaskSpec.

- **CORRECCIÓN ARQUITECTÓNICA IMPLEMENTADA Y PUBLICADA (2026-09-22, mandato owner one-shot): `origin/feature/sqx-import-task-v1` @ `492250b` (FF desde `92bf0fd`; `6037bec` código + `492250b` docs; master `745bc8b` intacto). RESULT = PASS (software); G7 sigue SIN EJECUTAR y requiere RT-1 v4 (RT-1 v3 OBSOLETA: su receta/fuente corresponden a la arquitectura rechazada).** El owner rechazó la interfaz `Retester/databanks/import` host-local (distribución manual entre workers + afinidad de host = inaceptables en plataforma multi-worker). Nuevo contrato: task import declara `input_folder: "00_inputs/import"` (hermana de `00_configs` bajo la raíz del wave; `source_folder` PROHIBIDO en import); el step `ingest_import_package` inventaría el paquete vía MinIO, congela un manifest determinista sellado write-once en `01_import/ingest_manifest.json` (`sqx-import-ingest-manifest.v1`), descarga con SHA256 verificado al scratch per-FlowRun, deduplica byte-exact y prepara el databank del proyecto fijo bajo flock `.echo-import-exporter.lock`; reintentos recuperan el manifest congelado, input mutado ⇒ `IMPORT_INPUT_MUTATED` (falla cerrada), agregados tardíos ignorados; provenance = bloque `minio_package {package_prefix, input_folder, worker_host}` (el worker importador nunca es el host histórico de origen). Regresión: steps/runtime/binding/pipeline/worker suites PASS; failing sets de workflows (21) e internal idénticos al baseline; fix MR-2 (recovery) conservado. Un solo paquete puede consolidar estrategias de Zeus/Hera/Kronos en UN FlowRun (gap cross-host disuelto para la cohorte de import; binding single-FlowRun de snapshots intacto).

- **HISTÓRICO (2026-09-22 tarde): G7 NO EJECUTADO — RT1_PREFLIGHT PASS de la v3; MR-1/MR-2 APPROVED (ratificación owner pendiente). Preflight G7 COMPLETO = RT1_PREFLIGHT PASS; RT-1 **v3** re-emitida @ origin `92bf0fd` (delta docs-only). Correcciones materiales de la v3: (1) **binario**: el SHA `5f8df487…` de la v2 era un build de `cmd/symphony`, no del worker — el artefacto real es `sqx-worker` @ `f02bf39` SHA `3a31ebd0…` (`vcs.modified=false`) + `sqx-watcher` dispatch SHA `940d28b5…`, copias durables en `~/aranea/work/import-cert-g7-20260922/`; (2) **config**: allowlist de 28 keys con TRES overrides (`sqx/task_queue=sqx-import-cert-v1` crítico: `generic_workflow.go:1436` enruta sub-workflows); excluidos control/estado de flota; (3) **dispatch**: `sqx-watcher` efímero (prefix `/sqx-watcher/import-cert-dev/`); (4) **universo verificado POR ARCHIVO** (8 NDX_W3 de `passed` Kronos, bidireccional L+S, `L` = bucket de `BuildMinIOPath`); (5) **licencia**: `license.db` sin metadatos de concurrencia — aceptación al arranque. CFX vigente (SHA `64ab2674…`); flota Kronos intacta (PID 1400507, SHA `e1f62e06…`). Nada arrancado; cero writes en ETCD/MinIO/PG/Mongo/Temporal.

## 🧱 Entrega de desarrollo

| Aplicación / repo | Branch | Base | SPEC funcional | SPEC técnica | Estado |
|---|---|---|---|---|---|
| xKoRx/symphony | `master` @ origin `9a69243` | FF `745bc8b..9a69243` (integrado 2026-09-23) | Mandato owner 2026-09-21 (IMPORT V1) + D10 + fixes finales + ventana G7 | `specs/FEAT-SQX-IMPORT-TASK-V1/SPEC.md` (§14 + §14.9) + amendments | **G7 PASS · INTEGRADO · feature branch y worktree eliminados · sólo `master` en remoto** |

## ✅ Tareas

> [!example]- Fuente de tareas — editar / mover de estado aquí
> %% Estados: [ ] To Do · [/] WIP · [r] Review · [x] Done · [-] Canceled. %%
> - [x] G0/G1: auditoría contratos + topología hosts + SPEC freeze + amendments #owner/agent #type/dev
> - [x] G2–G5: task import, registro de productores, ranking per-type, selection_snapshot_v1 #owner/agent #type/dev
> - [x] G6: tests §14 nuevos + regresión completa (23 fallos pre-existentes idénticos al baseline, 0 nuevos) #owner/agent #type/dev
> - [x] Review independiente ronda 1 (2026-09-22, agentes verificadores contexto fresco): MR-1 **APPROVED**; MR-2 **CHANGES_REQUIRED** (MAJOR H-1: recovery import validaba MetricSet con constantes Builder ⇒ retry post-crash roto; sin test) #owner/agent #type/pr-review
> - [x] Correcciones ronda 2 @ `bed8d31`: Fix A recovery por contrato de productor + tests recovery/evidencia import + binding databank plugin (input para import / output para builder) + mensajes por contrato; regresión = fail-set baseline idéntico; re-review ronda 2 en curso #owner/agent #type/dev
> - [x] Preflight control-plane (RO): Temporal `sqx-dev` OK / flota=`sqx-prop`+`sqx-main-queue` (ETCD production), 0 pollers en cola por defecto; ETCD DEV 40 keys (PG 192.168.31.220/trading_systems, Mongo forge, MinIO 192.168.31.92:9000); MinIO: 11 estrategias .sqx auténticas en `running/wave_2/xau/base/` + convención `00_configs/` con .cfx por stage (sin `EchoForgeImportExporter.cfx` aún — paso owner); canales caídos: aranea-ssh 503 y Mongo RO session-not-found ⇒ preflight host-level (licencia, JAR, passed local, proceso flota) PENDING #owner/agent #type/dev
> - [x] MR-1/MR-2 veredicto formal ronda 2: **APPROVED ambos** (K1–K8 PASS, sin BLOCKER/MAJOR; run `dwfrun-3d728aaa`); headers de amendments actualizados (INDEPENDENT REVIEW APPROVED / RATIFICACIÓN OWNER PENDIENTE) @ `329ee94` #owner/me #type/pr-review
> - [x] Solicitud RT-1 precisa emitida: `specs/FEAT-SQX-IMPORT-TASK-V1/RT1-REQUEST-G7-OPCION-B.md` (host kronos propuesto, cola sqx-import-cert-v1@sqx-dev, prefix ETCD dedicado, lista cerrada de recursos, ≤2 h, teardown/rollback, condiciones previas) #owner/me #type/dev
> - [x] Mandato cierre III (2026-09-22): G1 cierre formal MR-1/MR-2 validado (delta post-review docs-only); G2 aranea-ssh RESTAURADO (pool 64 agotado → restart documentado por mcps-ops, smoke PASS) + Mongo RO vivo; G3 preflight host-level Kronos PASS (identidad triple VM 111 @ .121, 15 ítems, SHA críticos, cero XAUUSD en flota); G4 CFX generado+validado contra postProcessCFX+publicado en MinIO (SHA `64ab2674…`); G5 manifest congelado 8 NDX_W3 (SHA256); G6 build `329ee94` (SHA `5f8df487…`, vcs.modified=false) + regresión = baseline exacto; G7 RT-1 **v2 re-emitida** @ `f02bf39` con contrato loader corregido (`ENV`→`/sqx-worker/<ENV>/`) #owner/agent #type/dev
> - [x] Corrección arquitectónica de entrada (2026-09-22, mandato owner one-shot): paquete MinIO `00_inputs/import` reemplaza databank host-local; `input_folder` + `ingest_import_package` + manifest congelado write-once + staging per-FlowRun con flock + provenance `minio_package` @ `6037bec`; SPEC §13/D9/RT-1 v3 OBSOLETA/runbook G7 reemitido @ `492250b`; push FF; master intacto #owner/agent #type/dev
> - [-] RT-1 **v4**: superseded por D10 (era receta del paquete `00_inputs/import` para la task import abandonada) #owner/me #canceled
> - [x] Mandato fixes finales (2026-09-22): G1 wiring `import_intake` en `GetDefaultPipeline` + test de integración del pipeline completo; G2 freeze exacto con manifest técnico bajo `config_folder` (5 escenarios fail-closed/convergencia); G3 naming canónico S5 con `SanitizePublishedFilename` compartida; no-regresión ⊆ baseline; ronda 3 independiente APROBADA sin BLOCKER @ `4f00f65` #owner/agent #type/dev
> - [x] RT-1 **v5**: EMITIDA (`RT1-REQUEST-G7-V5.md`) sobre el contrato Watcher Import Intake; binarios @ `dd3eedb` congelados (watcher `d9a0602d…`, worker `e44c321e…`); preflight host/ETCD/cola/licencia de la v3 como insumo #owner/agent #type/dev
> - [ ] **CONFIRMED del owner para RT-1 v5** (host + ventana + universo calificado): única acción pendiente para ejecutar G7 físico #owner/me #blocked
> - [ ] G7: BLOCKED a CONFIRMED RT-1 v5. Demostraciones: watcher única frontera filesystem, worker sin acceso al filesystem del watcher, folder configurable, sin `TaskSpec type=import`, bootstrap interno antes de Classification, processed como unidad, drill idempotencia (kill/restart + added/removed/mutated fail-closed) #blocked
> - [ ] G8 cierre: merge owner + release según gates (push hecho 2026-09-21) #owner/me #type/dev
> - [x] D10 (2026-09-22, mandato one-shot): watcher import intake implementado end-to-end; regresiones == baseline; push FF `492250b..1a2fe34` #owner/agent #type/dev
> - [ ] Campaña B: congelar parámetros OOS/MT5 y ejecutar tras G7 #owner/me #type/dev

## 📆 Bitácora

- **2026-09-22 (mandato fixes finales — G1/G2/G3 + cert + RT-1 v5; sesión ZCode/GLM)** — Baseline revalidado (`master @ 745bc8b` local==origin; branch `1a2fe34` local==origin; worktree limpio; merge-base == master ⇒ FF posible). **G1:** `GetDefaultPipeline` añadía `import_intake` faltante (el binario real despachaba `BatchKeys{}`); test nuevo `TestGetDefaultPipeline_Order` (secuencia exacta) + `TestGetDefaultPipeline_ExecutesImportIntakeAndDispatchesImportedBatch` (pipeline REAL `RegisterSteps`+`GetDefaultPipeline` con fakes uploader/registry/control/storage: con import ⇒ `Input{Keys=2, StrategyArtifacts=2, Origin=watcher_import}` y processed como unidad; sin import ⇒ Input vacío, 0 objetos). **G2:** defecto confirmado — `verifyFrozenMembership` sólo chequeaba presencia ⇒ admitía crecimiento tras crash parcial; además `SourceDigest` de membresía NO es el SHA del artefacto y la fila estrategia no persiste digest ⇒ sin registro pre-publicación la mutación local pre-upload era indetectable. Fix: manifest técnico `watcher-import-freeze-manifest.v1` sellado write-once ANTES del primer upload bajo `config_folder|root` con `request_id` en el filename (nuevo puerto estrecho `WatcherFreezeManifestStore` implementado por el adapter MinIO reusando `putObjectIfAbsentAndReconcile`; SPEC §14.3 corregida + D11 justifican la insuficiencia); retry compara exactamente cantidad/filenames/SHA256/output keys (`EXTRA`/`MISSING`/`MUTATED`/divergencia ⇒ `CONTRACT_CONFLICT`); tests de los 5 escenarios del mandato con fail-injection (crash tras 5/10 → retry converge 10/10, mismo FlowRun, sin duplicados). **G3:** `ExactOutputName` pasaba el basename fuente crudo; fix: `PublishedName = SanitizePublishedFilename(CanonicalStrategyFilename(basename))` con la autoridad movida verbatim a `domain` (adapter MinIO delega; harness diferencial del revisor: 25 inputs idénticos) — la key pre-congelada del manifest coincide byte a byte con la publicada (`Strategy 4.53.669.sqx` → `Strategy_4.53.669.sqx`); canonical key ahora usa `canonical_strategy_id` (identidad, no nombre fuente); colisión canónica con bytes distintos ⇒ fail-closed; test con atributos de spec ≠ filename fuente demuestra nombre/key publicados y provenance del basename en el manifest. **No-regresión (G4):** comparación por pares (paquete, test) branch vs baseline en worktree detached `745bc8b` con `../sdk` sincronizado: fail-set del branch (22) ⊆ baseline (102); hallazgo ambiental diagnosticado y documentado (cuota tmpfs de /tmp + fetch de PG embebido 429 UA-dependiente ⇒ `registry-postgres` rojo espurio; con cache sembrado/`TEST_POSTGRES_DSN`: verde en ambos lados; 1 flake preexistente `TestDurableSelect_GroupSelectedCarrier`, 30 reps fallan en ambos lados, paquete no tocado por el delta). **Review independiente ronda 3** (verificador adversarial contexto fresco, 8 claims, tests propios): APROBADO SIN BLOCKER; hallazgos resueltos en ronda (F2: RT-1 v5 re-pinneada; F3: fórmula docs refrescada; NOTE-1/3/4: aserción de carriers, fake sanitiza, condición muerta eliminada). **RT-1 v5 emitida** (`RT1-REQUEST-G7-V5.md`): host Kronos propuesto (preflight v3 como insumo, re-verificación en ventana), sin databank/sin `cp -n`/sin `00_inputs/import`, watch_dir candidate-local con `import/` de 8 auténticas, cola `sqx-import-cert-v1`@`sqx-dev`, lista cerrada de recursos, drill idempotencia ampliado, aborto/teardown; binarios @ `dd3eedb` (`vcs.modified=false`): watcher `d9a0602d…`, worker `e44c321e…`, copias en `~/aranea/work/import-cert-g7-v5-20260922/`. **CONFIRMED del owner NO existe en ningún canal ⇒ G7 NO EJECUTADO**; cero writes en ETCD/MinIO/PG/Mongo/Temporal DEV; flota intocada. Limpieza: worktree baseline propio eliminado (fixtures restaurados antes), PGs embebidos huérfanos de las corridas terminados, /tmp limpio. Push FF `1a2fe34..4f00f65` (5 commits); master intacto. Siguiente acción exacta: CONFIRMED del owner sobre RT-1 v5.

- **2026-09-22 (D10 — IMPORT TASK ABANDONED → WATCHER IMPORT INTAKE, mandato owner one-shot)** — Verificado baseline (`origin == local == 492250b`, master `745bc8b`). **Código @ `cf808a4`:** (1) `WorkflowSpec.Watcher.Import` + validación runtime (`folder` segmento único arbitrario; `config` sólo si classifications/early_rankings consumen `watcher_import`; plugin fijo `EchoForgeOverviewExporter`; nombre de task `watcher_import` prohibido); (2) watcher step `import_intake` (freeze con doble hash estable ⇒ `IMPORT_INPUT_MUTATED`/`IMPORT_INPUT_EMPTY` fail-closed; freeze-check de membresía ORIGIN vía `FlowRunParticipationReader`+`StrategyIdentityReader` ⇒ `IMPORT_FROZEN_INPUT_MISSING`; publish byte-exacto por `UploadFromDiskExactWithBatchPreflight` con `ExactOutputName` + put-if-absent ⇒ `CONTRACT_CONFLICT`; `AdoptStrategy` rol IMPORTED; carriers {StrategyRef, CanonicalStrategyID, Key, Artifact}; CFX de enrichment publicado bajo config_folder; dispatch con `Input{Origin: watcher_import}` y FlowRun ya resuelto; processed unidad `processed/<ts>_<cfgid>/{flow.json,*.cfx,import/}`); (3) worker bootstrap: `ingest_import_cohort` (descarga refs exactas de Input, digest-verified, sin listados ni paquete) + `persist_import_evidence` (PersistOutputs contrato `sqx-import.v1`, provenance prefix+folder+host del enrichment, carriers actualizados con EvaluationRef/BuilderMetricSetRef); recovery COMPLETED reutilizada; (4) workflow `runWatcherImportCohortBootstrap` antes de la primera task sólo si `watcher_import` es consumido; `selectionSourceFolder` resuelve al folder configurado; hooks con familia de productores builder|watcher_import. **Eliminado:** `type=import` (validación ⇒ error con guidance), `stage=import` declarable, `TaskSpec.InputFolder`, `IsDurableImportTask`, branch import del workflow, `ingest_import_package`/manifest sellado §13, `upload_import_results`/`db_register_import` del worker, caso `import` en `ResolveLocalProjectNameByType`. **Tests:** runtime (receta intake, forma de sección, folder arbitrario banana/07_external/legacy_pool, reservas fail-closed, productor watcher_import), watcher (A no-op exacto, B/C publish bajo folder configurado + D cohorte mixta 1 FlowRun, E retry converge mismo FlowRun/cohort sin duplicados, F mutación entre intentos, G colisión, §12 Input en dispatch, J processed unidad/plano), worker (ingest refs exactas I, mutación, colisión basename, staging per-FlowRun H, collect joins, persist evidence sella carriers), workflows (K bootstrap con enrichment + sin consumidor ⇒ sin bootstrap; primera task recibe cohort enriquecido). **Regresión:** full suite `go test ./...` fail-set == baseline 492250b exacto (22 nombres, stash-diff por nombre; 0 nuevos, 0 fijados). Fix colateral: staging de publicación ahora usa `MkdirTemp` por ejecución (antes dir compartido en /tmp keyed por FlowRunRef — contaminación entre corridas). **Docs @ `1a2fe34`:** SPEC §14 (contrato final completo), D10, RT-1 v3 banner SUPERSEDED (v5 requerida), RUNBOOK G7 reescrito (receta `watcher.import` + 8 demostraciones), RUNBOOK-DEV banner. Push FF `492250b..1a2fe34`; master intacto; flota intocada; G7 NO EJECUTADO. Siguiente acción exacta: RT-1 v5.

- **2026-09-22 (corrección arquitectónica — paquete MinIO como autoridad de entrada)** — Mandato owner: la carpeta `Retester/import` fue una mala decisión arquitectónica y deja de ser la interfaz de entrada; las estrategias se entregan UNA vez en MinIO dentro del paquete de configuración del wave (`00_inputs/import`, directorio HERMANO de `00_configs` — corrección del propio mandato sobre su primera propuesta) y Forge distribuye al worker elegido. Verificado baseline (`origin == 92bf0fd == local`, master `745bc8b` intacto). **Implementación @ `6037bec`**: (1) `TaskSpec.InputFolder` + validación runtime (`validateTask` import: `input_folder` requerido con segmentos seguros, `source_folder` PROHIBIDO; selection intacta); (2) step `ingest_import_package` reemplaza a `enumerate_import_databank`: `ListStrategies` sobre el prefijo derivado de `BuildMinIOPath(...)+input_folder` (mismo contrato que `download_config`, sin convención paralela), descarga al scratch per-FlowRun con doble-hash verificado (`IMPORT_INPUT_EMPTY`/`IMPORT_INPUT_DOWNLOAD_FAILED`), dedupe byte-exact con aliases (mismo basename/bytes distintos ⇒ `CONTRACT_CONFLICT`), manifest determinista `sqx-import-ingest-manifest.v1` (entries {package_prefix, object_key, basename, size, sha256}; sin timestamps/host ⇒ dos workers congelan bytes idénticos) sellado WRITE-ONCE en `01_import/ingest_manifest.json` vía `ArtifactStorage.UploadArtifactFromPath` (put-if-absent + reconcile byte-exact: sellado concurrente idéntico converge, divergente ⇒ `CONTRACT_CONFLICT`); reintentos recuperan el manifest (clave ausente ⇒ `IMPORT_FROZEN_INPUT_MISSING`; bytes mutados ⇒ `IMPORT_INPUT_MUTATED`; agregados tardíos ignorados); pipeline reordenado (resolve/claim ANTES del ingest para sellar dentro del namespace reclamado); (3) staging per-FlowRun (`import_ingest/<flow>/`, `import_stage/<flow>/uNNNN/`) + flock exclusivo `.echo-import-exporter.lock` durante toda la activity (dos FlowRuns del mismo host serializados; cleanup propio best-effort tras éxito); (4) provenance: `ImportProvenance{PackagePrefix, InputFolder, WorkerHost}` y bloque evidencia `minio_package` (el worker importador no es el host histórico de origen; MR-2 recovery conservado); (5) `01_import/ingest_manifest.json` es la membresía durable del FlowRun, generada automáticamente (el owner nunca escribe manifests ni conoce directorios SQX). **Tests casos A–L** (`steps_import_test.go` reescrito con fake MinIO de semántica real: listing/download/put-if-absent): A pass sin ningún databank local, B/C determinismo + retry en worker nuevo (root vacío) recuperando el manifest, D mutación post-freeze falla cerrada, E descargas vacías/fallidas explícitas, F colisión basename/contenido determinista, G duplicados byte-exact con alias, H roots per-FlowRun disjuntos, I paquete mixto multi-host en UN FlowRun, J selection intacta (tests runtime), K regresiones Builder, L fix MR-2 preservado (`recovery_import_test.go` actualizado sólo en fixture de provenance). Regresión global: core/runtime/binding/pipeline/steps/worker suites PASS; workflows (21) e internal failing sets IDÉNTICOS al baseline `92bf0fd` (verificado por stash+diff). Efecto lateral de tests sobre 2 fixtures revertido (patrón ya conocido). **Docs @ `492250b`**: SPEC §1/§4.1/§4.2/§4.3/§10/§13, TOP-DECISIONS D9, RT-1 v3 con banner OBSOLETA (requisito: RT-1 v4), RUNBOOK G7 reescrito (paquete MinIO en §1, receta con `input_folder` en §2, demostración worker-independence en §3, alcance §5), RUNBOOK DEV con banner de sustitución. Push FF `92bf0fd..492250b`; master sin cambios; flota intocada; G7 NO EJECUTADO. Siguiente acción exacta: RT-1 v4.

- **2026-09-22 (mandato one-shot final — riesgos §1 cerrados, solicitud final única)** — Vigencia verificada: `origin/feature/sqx-import-task-v1` == `92bf0fd` == local (clean), `master` `745bc8b`; binarios durables íntegros contra `SHA256SUMS` (worker `3a31ebd0…`, watcher `940d28b5…`); delta `f02bf39..92bf0fd` docs-only. **Riesgo 1 PERMISOS — cerrado como exposición declarada** (auditoría RO del catálogo PG @ `.220`): rol `sqx` sin poderes de clúster PERO **owner de la DB `trading_systems`** y CREATE en `trading_systems` + `trading_systems_test`, CONNECT+TEMP en 10 bases ajenas ⇒ alcance de escritura mayor al wave, sin aislamiento por grants; Mongo y MinIO con credenciales compartidas de flota por referencia. No se crearon grants ni ACLs. **Riesgo 2 CONCURRENCIA — condición de aborto vigente**: `license.db` re-leída con SQLite `mode=ro`; 0 procesos `sqcli`/`java`; flota intacta (worker 0.2.105 PID 1400507, SHA `e1f62e06…`); hallazgo menor: 2 marcadores `.echo-*.lock` zero-byte en proyectos exporter — project-scoped, sin proceso que los sostenga, NO son locks de instancia SQX. **Vigencia física**: manifest 8/8 con SHA idénticos y `passed`=200 en Kronos; databank `import` y proyecto candidato ABSENT (negativos correctos); CFX único objeto del wave en MinIO (etag `652b9666…`, 1841 B); ETCD prefixes del candidato vacíos + `production` 80 keys; Temporal `sqx-dev` activo. **G7 NO EJECUTADO**: CONFIRMED del owner no existe en ningún canal. (Nota: tras este cierre, la arquitectura de entrada fue corregida por mandato posterior — ver bitácora de arriba; la preparación física «cp de las 8 a import» de esta entrada queda sin efecto.)

- **2026-09-22 (mandato cierre IV — preflight completo y RT-1 v3)** — Mandato de cierre RT-1/G7: gates técnicos todos resueltos, G7 sigue BLOCKED a owner. G1 ratificación: veredictos MR-1/MR-2 APPROVED conservados; delta `329ee94..f02bf39` docs-only ⇒ aprobaciones vigentes sin re-review; ratificación owner NO existe (ni inventada). G2 CONFIG_ISOLATION **PASS**: allowlist de 28 keys derivada de readers del código @ `f02bf39` (ETCD RO `/sqx-worker/production/` 80 keys leídas y clasificadas): 21 copiadas, 3 overrides (`temporal/namespace=sqx-dev`, `temporal/task_queue=sqx-import-cert-v1`, **`sqx/task_queue=sqx-import-cert-v1` NUEVO** — sub-workflows), 4 secretos por referencia (ventana); excluidas 52 (control/estado flota, proactiva, quiesce, dead keys `sqx/exec_base_path`/`log/json`/`name`); matriz de destinos sin secretos: PG `.220:5432/trading_systems_test` user `sqx` (corrección: v2 decía `trading_systems`), Mongo `forge`, MinIO `.92:9000`, Temporal `.46:7233`, OTEL `.60`. Hallazgo: `NewRuntimeConfig` con enum de instrumentos es dead code (sin callers) ⇒ `USATECHIDXUSD_darwinex` pasa validación activa sin cambio de código. Riesgo documentado: identidad `sqx` compartida ⇒ alcance de escritura mayor al wave (restringida requiere procedimiento owner, no creado). G3 UNIVERSE **PASS cualificado**: 8/8 inspeccionadas individualmente vía zip-interno en Kronos: SHA/bytes == manifest, `USATECHIDXUSD_darwinex`, H1, período 1451606400000–1776124799999 (2016-01-01→2026-04-14), engine MT5 hedged; dirección del contenido = 1 acción Long(1) + 1 Short(-1) por archivo ⇒ bidireccional; `L` = bucket del normalizador `BuildMinIOPath` (default no-Short); flota 100 % NDX_W3 (Kronos 200, Zeus 144, Hera 291 — sin cohortes Long-only auténticas posibles); cohorte homogénea conservada; calificación a ratificar por owner. G4 SQX_CONCURRENCY **PASS condicionado**: `license.db` SQLite 1 key `license` sin metadatos de concurrencia; 0 locks bajo `/home/kor/sqx`; 0 procesos sqcli/java; load 0,00; 123/125 GB RAM, 19 GB disco; verificación al arranque del candidato, aborto si SQX rechaza; nunca detener flota. G5 binario/CFX **PASS con corrección**: delta docs-only verificado; `5f8df487` = build `cmd/symphony` (trazabilidad), worker real = **`sqx-worker` @ `f02bf39` SHA `3a31ebd0…`** + **`sqx-watcher` SHA `940d28b5…`** (rebuild mismo entorno CGO 4.3.5, `vcs.modified=false`, copias en `~/aranea/work/import-cert-g7-20260922/SHA256SUMS`); CFX read-back: etag `652b9666…` + 1841 B idénticos ⇒ bytes intactos (SHA `64ab2674…` vigente); plugin jar `aaa30d44…` re-verificado. G6 RT-1 **v3** emitida @ `92bf0fd` (push FF `f02bf39..92bf0fd`) con §4 allowlist, §4.2 dispatch watcher (prefix `/sqx-watcher/import-cert-dev/`), §6 universo por archivo, §8 licencia, §9 arranque/aborto/teardown explícitos. G7 **BLOCKED**: ratificación owner + CONFIRMED no existen; nada arrancado; flota intocada (PID 1400507 SHA `e1f62e06…` re-verificado); cero writes ETCD/MinIO/PG/Mongo/Temporal; canal Mongo RO volvió a caer (`session not found`, colecciones code-proven; el canal ya fue recuperado por management path en cierre III).

- **2026-09-22 (mandato cierre III — RT-1 v2 + ambiente completo)** — G1 formal: delta `bed8d31..329ee94` docs-only ⇒ aprobaciones vigentes. G2: ssh-mcp con firma de pool agotado (503 `session limit (64)` + `/status` 200 + `connections:[]`) restaurado por management path (`hermes`→`mcps-ops`→`docker restart ssh-mcp`, imagen certificada intacta, baseline previo, smoke PASS); Mongo RO vivo. G3: identidad Kronos demostrada por triple vía (VM 111 `sqx-kronos` @ `192.168.31.121`, nodo `kronos`, MAC `bc:24:11:e2:35:ab`, fqdn `sqx-ulab-kron-0`); 15 ítems read-only PASS; hallazgo material: **cero XAUUSD en databanks de las 3 VMs** ⇒ fuente auténtica = `passed` local (≈200+ NDX_W3; universo real `USATECHIDXUSD_darwinex`/H1 leído del `settings.xml`). G4: CFX `EchoForgeImportExporter.cfx` derivado de la plantilla real del proyecto OverviewExporter (XML por `unzip -p`; base64 bloqueado por filtro de entropía del MCP), validado contra `postProcessCFX` real del repo (test temporal, idempotencia y fail-closed demostrados) y **publicado en MinIO** `wave_import_cert_v1/usatechidxusd_darwinex/l_h1/SQX/v1/00_configs/` (1841 B, SHA `64ab2674…`, read-back OK) — la condición «owner sube el .cfx» de la RT-1 v1 queda cubierta por el agente según mandato. G5: manifest congelado de 8 estrategias (6 `k0` + `h0`/`z0`) con SHA256 y tamaños; copias físicas a `import` quedan para la ventana autorizada. G6: build `sqx/cmd/sqx-worker` @ `329ee94` (SHA256 `5f8df487…`, `vcs.modified=false`, go1.27.1, entorno CGO de usuario para libzmq 4.3.5 sin sudo); regresión idéntica al baseline (21 workflows + 1 fixture-gate; 0 fallos en 7 paquetes). **Contrato del loader demostrado por código**: `sqx/cmd/sqx-worker` usa `di.InitSelective("sqx-worker", WithEnvironment(os.Getenv("ENV")))` ⇒ SDK `fmt.Sprintf("/%s/%s/")` ⇒ **`ENV=import-cert-dev` ⇒ `/sqx-worker/import-cert-dev/`** — la RT-1 v1 decía `ENV=import-cert` ⇒ `/symphony/import-cert-dev/` (prefix de otra app; con ENV vacío el candidato habría caído a development). G7: RT-1 **v2 re-emitida** (`213747c`+`f02bf39`, push FF) con host confirmado, binario/CFX/manifest reales, seed ETCD exacto (80 keys con 2 overrides Temporal) y negativos; queda BLOCKED a CONFIRMED del owner. Flota intocada (worker 0.2.105 PID 1400507, SHA `e1f62e06…` registrado como baseline de no-interferencia).

- **2026-09-22 (mandato cierre II — final)** — Ronda 2 del revisor independiente sobre delta `3d04d68..bed8d31` (run `dwfrun-3d728aaa`): **K1–K8 PASS ⇒ MR-2 APPROVED** (H-1 resuelto con recovery por contrato y ruta Builder byte-idéntica; H-2/H5 con tests; fail-set workflows 21 pre-existentes, causa raíz harness). Veredictos finales registrados en headers de ambos amendments (INDEPENDENT REVIEW APPROVED / RATIFICACIÓN OWNER PENDIENTE) y en el evidence pack. Preflight G7-B control-plane completo (§6 del anexo): flota=`sqx-prop`/`sqx-main-queue` vs candidato=`sqx-dev`/`sqx-import-cert-v1`, 11 `.sqx` auténticos en MinIO, convención `00_configs/` verificada (sin `EchoForgeImportExporter.cfx` aún); host-level bloqueado por canales caídos (aranea-ssh 503, Mongo RO session-not-found). Solicitud RT-1 precisa emitida (`RT1-REQUEST-G7-OPCION-B.md`). Push FF final `bed8d31 → 329ee94`; master intacto `745bc8b`; flota intocada; nada arrancado.
- **2026-09-22 (mandato cierre II)** — Reviews independientes y correcciones: ronda 1 por dos agentes verificadores adversariales de contexto fresco sobre `3d04d68` (run `dwfrun-aa80d4e6`): **MR-1 APPROVED** (10/10 claims PASS; F1/F2 MINOR, F3–F7 NOTE) y **MR-2 CHANGES_REQUIRED** (MAJOR H-1: `recovery.go` validaba el MetricSet de la recovery import con constantes Builder ⇒ retry post-crash de etapa import COMPLETED fallaba con CONTRACT_CONFLICT; sin test; además H-2 sin test del branch import del ranking). Fixes en rama `3d04d68 → 6ad247a → bed8d31` (push FF): recovery por contrato de productor (fail-closed no registrados; ruta Builder byte-idéntica), tests de recovery/evidencia import, binding del databank de entrada del plugin (`input` import / `output` builder — defecto de integración que habría dejado el plugin leyendo un databank vacío en G7), mensajes de error por contrato; fixture del benchmark restaurado tras efecto lateral. Regresión post-fix: fail-set idéntico al baseline (21 nombres) y dominio/binding/steps/runtime en 0. Re-review ronda 2 del delta en curso (`dwfrun-3d728aaa`). Preflight G7-B control-plane verificado RO (Temporal sqx-dev OK, flota sqx-prop/sqx-main-queue, MinIO con 11 .sqx auténticos y convención 00_configs; canales aranea-ssh 503 y Mongo RO caídos ⇒ preflight host-level pendiente); nada arrancado, flota intocada.
- **2026-09-21 (mandato cierre)** — Review y cierre físico preparado: branch actualizada `5e495ae → 3d04d68` (1 commit, push FF; master intacto `745bc8b`). Review técnica MR-1/MR-2 limpia con evidence pack; correcciones documentales: autoridad proyecto/plugin (`EchoForgeImportExporter` proyecto SQX local + plugin `EchoForgeOverviewExporter`; SPEC §4.1/4.2/§12 corregidas, runbook ya consistente), receta runbook con `custom_analysis_plugin` (fallo cerrado sin él), gap multi-host declarado con tests de binding single-FlowRun (SPEC §4.3 + D7 precisada + runbook §5: G7 de un host = parcial). Anexo runtime candidato DEV opción B (`RUNBOOK-DEV-AISLADO-OPCION-B.md`, PREPARED/PENDING_RT1; verificado RO: namespace Temporal `sqx-dev` existe, ETCD `/symphony/development/` 40 keys, cola compartida `sqx-main-queue` excluida para el candidato). Nada arrancado ni desplegado; flota 0.2.105 intocada.
- **2026-09-21** — Consolidación git previa a G7 (mandato owner): branch publicada en origin `5e495ae` (5 commits íntegros, push tras scan de secretos limpio); `master` promovido por FF puro a `745bc8b` (merge-base de esta rama == nuevo master, historia sin rebase); verificado vía GitHub API (`ahead=5 behind=0 merge_base=745bc8b`; sin commits de Import en master). G7 sigue bloqueado a runtime aislado del owner; MR-1/MR-2 siguen en review.
- **2026-09-21** — Sesión ZCode/GLM: G0–G6 completos en worktree `/home/kor/go/src/github.com/xKoRx/symphony-import-v1` (4 commits). Flota con worker 0.2.105 corriendo en Zeus ⇒ G7 requiere owner gate. Runbook y receta de config listos.

## 🧭 Decisiones

- D1 desacople por REGISTRO de productores (builder|import), no por relajar el `if` de stage. D2 `IMPORTED` activado como rol origin (reservado por CROSS-FLOWRUN-REUSE). D3 extractor único: plugin Java EchoForgeOverviewExporter modo databank. D4 métricas import = `IMPORTED_HISTORICAL_RESULT` (nunca evaluación Forge). D5 filenames/carriers `builder_*` congelados en V1. D6 selection = task control-plane con snapshot durable insert-once. D9 (2026-09-22) autoridad de entrada = paquete de campaña MinIO (`00_inputs/import`); databank host-local RECHAZADO; manifest congelado write-once = membresía del FlowRun. **D9 SUPERSEDED por D10 (2026-09-22): IMPORT TASK ABANDONED — Watcher Import Intake** (watch_dir `import/` → MinIO `watcher.import.folder` → Input; productor reservado `watcher_import`; bootstrap interno de enrichment; sin manifest redundante). Detalle: `specs/FEAT-SQX-IMPORT-TASK-V1/TOP-DECISIONS.md`.

## 🔗 Docs / Links

- Parent: [[Echo Forge]] · Entorno: [[Echo + Echo Forge — Environment Contract]]
- Repo specs: `specs/FEAT-SQX-IMPORT-TASK-V1/` (SPEC, TOP-DECISIONS, RUNBOOK G7), `specs/FEAT-SQX-IMPORT-CAMPAIGN-B/SPEC.md`
- Amendments manager review: `FEAT-SQX-DURABLE-CLASSIFICATION-EVIDENCE/AMENDMENT-IMPORT-PRODUCER.md`, `FEAT-SQX-DURABLE-EARLY-PER-TYPE-RANKING/AMENDMENT-IMPORT-PRODUCER.md`
