---
type: project
schema_version: 1
owner: agent
root: false
status: active
priority: P1
area: "[[Echo]]"
parent: "[[Echo — Live Platform V1]]"
sprint:
start: 2026-09-16
due:
progress: 25
repo: xKoRx/echo
jira:
prs:
aliases:
  - Echo E-06
  - Reference enrollment and binding
  - E-06 RuntimeBinding
  - FEAT-REFERENCE-ENROLLMENT-BINDING-E6
tags:
  - kind/project
  - area/echo
  - agent/owner
created: "2026-09-16"
updated: "2026-09-18"
cssclasses:
  - wide
---

# Echo — E-06 Reference Enrollment and Binding

%% Naming: Echo — E-06 Reference Enrollment and Binding es el link canónico del proyecto; aliases guarda variantes humanas; tags/slugs son solo automatización. %%

> [!info]+ Echo — E-06 Reference Enrollment and Binding
> **Área:** [[Echo]] · **Estado:** active · **Prioridad:** P1 · **Parent:** [[Echo — Live Platform V1]] · **Repo:** `xKoRx/echo`
> Subproyecto de **implementación** de la fase E-06 / Reference enrollment and binding. No es Integration. El contrato WHAT vive en el SPEC de Echo; esta nota es HOW / ORDER / GATES.

> [!abstract]- Ownership del proyecto (`owner`) — humano vs agente
> Este proyecto es `owner: agent`. El padre [[Echo — Live Platform V1]] enlaza aquí. La supervisión humana del track live sigue en [[Echo — Producto Integrado]].

## 🎯 Objetivo

Crear `RuntimeBinding` verificable: enrollment canónico por StrategyVersion, mapping observado account+broker+magic, operator ACK, read-back físico, coverage start barrier y attach manual verificado V1. Principio: **INGESTED / CONFIGURED / DB-BOUND ≠ OBSERVING**. Unlock: reloj forward atribuible para E-07/E-10.

## 📊 Estado actual

- **E06_T10_C1_ARCHITECTURE_BLOCKED (2026-09-18, NORMAL T10 correction C1 sobre `35db8b67` — RUTA B, cero commit).** Veredicto Manager `E06_T10_BLOCKED — CORRECTION_C1` invalida el STOP de replay del 2026-09-18 (interpretó incorrectamente el baseline: `35db8b67` es la entrega T10 publicada y el baseline obligatorio de C1, no drift; remoto verificado sin avance, parent exacto `891ab1d8`, worktree limpio). **Defecto CONFIRMADO (CONTRACT + PG REAL):** dos recepciones independientes del mismo hecho físico P (mismos bytes, mismo S) sellan `received_at` T1≠T2 ⇒ D1=`sha256:d12df650…` ≠ D2=`sha256:d0d4b07f…` (con received_at igualado D1==D2 ⇒ único campo divergente); `ReferenceReadbackStore.Insert` real sobre PG 17.11 descartable (cluster efímero `data-c1repro`, `run.sh` PASS completo): Insert R1 `created=true` con received_at/recorded_at originales intactos, Insert R2 ⇒ error real `CONTRACT_CONFLICT (same source_event_id, different payload)` con 1 fila; Caso F (mismo S, `position_count` 0→5, mismo received_at) ⇒ `CONTRACT_CONFLICT` (conflicto verdadero intacto); replay exacto converge. **Matriz A–F:** A (retry del mismo objeto) y E (duplicado Kafka del mismo mensaje) convergen — PublishSync no refresca received_at y el dedupe T09 cubre; B (retransmisión), C (reconexión pipe) y D (reinicio Bridge) caen en el defecto — Bridge no posee estado S→T1 y D es irrecuperable sin almacenamiento durable; F preserva conflicto. **Alternativas 1–5:** ninguna integral dentro del scope C1 — (1) preservar T1 en Bridge exige estado durable prohibido (SPEC §7.1 Transport: Bridge sin PG ni credenciales DB, boundary congelado; caché in-memory no sobrevive reinicio ni califica como integral); (2) excluir received_at sólo del digest deja el defecto intacto (el comparador T09 lo mantiene) y excluirlo de ambas superficies es cambio contractual T08/T09 READ ONLY con efecto a revisar sobre el dedupe cross-ID `uq_e6_readbacks_payload_digest` de 064; (3) idempotencia en el owner durable exige modificar T09/SDK/SPEC; (4) el protocolo collector no transporta el instante de primer ingreso (observed_at = reloj EA, semántica distinta; sustitución prohibida); (5) tocar 064 no ataca la causa y debilitaría la unicidad doble. **RUTA B:** repo `xKoRx/echo` intocado (tests temporales de reproducción eliminados, git status limpio @ `35db8b67`, sin commit ni push). **Decisión mínima requerida del Manager:** (i) redefinir la equivalencia de replay excluyendo `received_at` de la vista de digest (T08) Y de la comparación canónica (T09) — recomendada: un solo lugar de verdad, sin storage nuevo, revisando el efecto cross-ID; o (ii) alterar el boundary para que el owner durable selle/recupere el primer `received_at` (p.ej. Gateway, cambia §7.1 «primer Bridge UTC»). Deudas intactas: PHYSICAL=PENDING (GAP-ECHO-006), MQL_COMPILE_C2=NOT_RUN. E-06 NO CLOSED; T11 no autorizado; **siguiente gate: decisión Manager sobre C1.**
- **E06_T10_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL T10 sobre `891ab1d8`).** Bridge emitter zero-order + atestaciones implementado en `feature/e06-reference-enrollment-binding` @ `35db8b67` (push FF `891ab1d8..35db8b67`; SPEC v1.2.2 intacto; `messages.go`/`reference_readback.go`/topics/migration 064/Gateway/Forge/MQL collectors/`go.mod`/`go.sum` diff 0; delta = `reference_readback_emitter.go` nuevo + hook en `reference_pipe_handler.go` + `reference_readback_emitter_test.go` nuevo + TASKS/VERIFICATION, todo allowed). **Hook:** único `case "reference_status"` en el switch de `ReferencePipeHandler.handleMessage` — alcanzable sólo desde `handleSession` tras `WaitForConnection` de un pipe que `PipeManager` crea exclusivamente para cuentas `AccountTypeReference`; heartbeat/reference_event/trade_close/span_*/UnifiedBatch/config/snapshots/PG no disparan la ruta. **Coherencia de sesión (§7.2):** la etiqueta `account_role` del JSON no autentica sola — el emitter exige `account_login` observado == cuenta vinculada al handler (`RegisteredAccount.AccountID()`, mismo namespace decimal) y rol `reference`; contradicción ⇒ rechazo sin publicación; sin sistema de autenticación nuevo. **Transformación:** copia verbatim campo a campo (pointers tri-state, conteos, arrays, identidades producer-assigned; ausencia ≠ cero, false ≠ ausencia); `account_registration_ref` = `domain.AccountRegistrationRef(platform, ACCOUNT_SERVER, login)` sobre lo observado (desigualdad con la receta prohibida ACCOUNT_COMPANY demostrada en test); `received_at` = reloj UTC del Bridge en el límite de ingreso (top de `handleReferenceStatus`, antes del parseo y de I/O; seam `h.now` inyectable default `time.Now`; un intento de publicación jamás lo refresca; dedupe/replay permanece en Gateway/PG T09); `Validate()` ANTES de publicar con error explícito (`broker_connected=false` y KNOWN_EMPTY transportan; UNKNOWN jamás se vuelve KNOWN; atestaciones ausentes transportan); `PublishSync` sobre el producer inyectado del handler en `domain.Topics.ReferenceReadback` (`echo.reference-readback.v1`); key determinista = `source_event_id` (producer-assigned; retry del mismo hecho ⇒ misma key; IDs preservados en payload); cero producers nuevos, cero consumidores, cero topics de comandos. **Tests 27/27:** 7 canónicos TASKS + 20 negativos/de transporte (payload malformado; source_event_id/collector_id/collector_epoch/broker_connected ausentes ⇒ rechazo; KNOWN sin ambos conteos ⇒ rechazo; KNOWN 0/0 ⇒ publicación; UNKNOWN con conteos ausentes preservado jamás sintético; broker_connected=false preservado; capabilities tri-state false/nil/true; atestaciones vacías sin síntesis; magic máximo `9223372036854775807` string exacto jamás float; dos atestaciones mismo program_name distinto chart_ref ambas preservadas; registration ref desde ACCOUNT_SERVER; cuenta y rol incoherentes ⇒ rechazo; retransmisión conserva IDs y campos físicos con received_at por ingreso; fallo Kafka propagado; received_at = ingreso UTC inyectado ≠ observed_at; cero escrituras a comandos y cero invocaciones de trading); fixture = espejo del golden CONTRACT T09b contrastado contra la estructura del builder MQL real; sin snapshot de collector real ⇒ fixture CONTRACT, nunca evidencia PHYSICAL; fake Kafka (`mocks.MockProducer`) con inyección de `PublishSyncErr`, sin dependencia de producción. **Gates:** CONTRACT+TRANSPORT+SOURCE PASS (`bridge/internal` plain 0.474s / `-race` 1.885s; `sdk/domain` `-race` PASS 1.364s); PHYSICAL=**PENDING** (GAP-ECHO-006); MQL_COMPILE_C2=**NOT_RUN** (deuda heredada de T09b C2 — bloqueo de acceso al host de compilación —, no resuelta por T10); vet OK; builds sdk/bridge/gateway OK; failing sets `sdk+bridge+gateway` idénticos por nombre al baseline aprobado `891ab1d8` en worktree aislado `/tmp/echo-e06-baseline-t10` (4 colaterales preexistentes: automation/scratch/seed/jaeger). E-06 NO CLOSED; T11+ no iniciados; siguiente gate: Manager review T10 → autoriza T11 (Gateway consumer → OBSERVING).
- **E06_T09B_C2_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL T09b correction C2 sobre `fc2d4e5d`).** Dos defectos materiales de freshness corregidos en `feature/e06-reference-enrollment-binding` @ `891ab1d8` (push FF `fc2d4e5d..891ab1d8`; SPEC v1.2.2 intacto; `messages.go`/contrato T08/migration 064/Gateway/Bridge/Forge diff 0; delta = 4 MQL + `messages_test.go` + TASKS/VERIFICATION, todo allowed; identidad durable C1-1 intacta sin regresión). **C2-1 rollover fail-closed:** `g_LastStatusMs`/`g_PendingStatusBuiltMs` pasan de `ulong` a `uint` (tipo exacto de `GetTickCount()`, wrap ~49,7 d) y la edad es la resta MODULAR de 32 bits convertida a `long` después — jamás un underflow unsigned 64 bits decide (antes, edad negativa bajo el wrap ⇒ gate no descartaba ⇒ evidencia vencida al pipe); reloj grueso `TimeGMT()` anclado en `g_PendingStatusBuiltGmtS` SÓLO descalifica tramos indemostrables (retroceso `< 0`, o brecha `coarseS*1000 > remain`: suspensión/desconexión larga incl. ≥ 2^32 ms ⇒ `coarseS ≥ 4294967`) ⇒ fail-closed, jamás extiende vigencia; cadencia modular (`now - g_LastStatusMs < (uint)ECHO_REF_STATUS_INTERVAL_MS`) operativa tras el wrap; nuevo gate build→primera escritura (`buildAgeMs >= oldestRemainMs` ⇒ descarte sin transmisión, sin refrescar GVs ni tocar el JSON); remain<0 (sin atestaciones) ⇒ sin gate ⇒ zero-order no se bloquea. **C2-2 vigencia sólo de slots incorporados:** en `EchoAttestV1_Scan` el mínimo `outOldestRemainMs` corría ANTES de la correlación `chart_ref ∈ charts[]` y de la capacidad ⇒ un slot rechazado modificaba la vigencia de un hecho que no lo contiene; ahora el loop guarda `slotRemainMs` (mismo descuento conservador 1 s de `ts`) y el mínimo se aplica sólo dentro de la rama de incorporación a `outProgram[]/outChartRef[]/outMagic[]` — expirado/chart ajeno/capacidad no participan, cero emitidos conserva `-1`; validaciones, correlación, JSON contractual intactos. **Tests:** 3 nuevos (`TestSOURCE_C2_PendingEligibilityRolloverFailClosed` con el caso obligatorio 4294967000: cruce dentro de vigencia ⇒ mismos bytes/id, edad real 16 s ⇒ descartar y `ageMsBuggy` demuestra el bypass eliminado; remain 0; edad == deadline vs inmediatamente inferior; retroceso grueso; ms congelado + 30 d; desconexión ≥ ciclo; cadencia 5 s atravesando el wrap; sin atestaciones ⇒ sin gate — `TestSOURCE_C2_NoStaleWriteBetweenBuildAndSend`; `TestSOURCE_C2_OldestRemainOnlyFromIncludedSlots` con orden correlación<inclusión<mínimo, exactamente 2 asignaciones al mínimo, paridad byte-idéntica del scan MT5/MT4, tabla de 6 filas + vigencia 0 bloquea retry), tablas espejo con aritmética `uint32` real y pines SOURCE de tipos+operación; pin C1 del mínimo actualizado a `slotRemainMs < outOldestRemainMs` (misma semántica, placement C2). **Gates:** CONTRACT+SOURCE PASS (`sdk/domain` PASS plain/`-race`, goldens y tokens prohibidos intactos); MQL_COMPILE=**NOT_RUN** (host `mt5-kronos.lab.aranea.cl` alcanzable a red pero publickey denegada para todas las identidades locales, agente SSH vacío, sin capability MCP del host Windows, sin wine/MetaEditor local; key temporal C1 revocada post-uso y reprovisionar no es autoautorizable — riesgo de sintaxis declarado no certificado; delta sólo usa construcciones ya compiladas en C1); PHYSICAL=PENDING GAP-ECHO-006. Regresión: vet OK, builds sdk/gateway/bridge OK, failing sets skip-mode idénticos por nombre al baseline `fc2d4e5d` en worktree aislado `/tmp/echo-e06-baseline-c2` (colateral automation/etcd/postgres/telemetry). E-06 NO CLOSED; T10/T11+ no iniciados; siguiente gate: Manager review C2 → autoriza T10 (Bridge emitter).
- **E06_T09B_C1_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL T09b correction C1 sobre `4009db7a`).** Dos defectos de autoridad física del collector corregidos en `feature/e06-reference-enrollment-binding` @ `fc2d4e5d` (push FF `4009db7a..fc2d4e5d`; SPEC v1.2.2 intacto; `messages.go`/contrato T08/migration 064/Gateway/Bridge/Forge diff 0; delta = 4 MQL + `messages_test.go` + TASKS/VERIFICATION, todo allowed). **C1-1 identidad durable fail-closed:** `FileIsExist` precede a cualquier `FileOpen` — inexistente demostrado ⇒ nacimiento, existente-ilegible ⇒ fail-closed sin otro id ni sobrescritura, corrupto ⇒ fail-closed sin regeneración; validadores estrictos `ReferenceValidUUIDv7` (formato completo lowercase, versión 7, variante) y `ReferenceValidEpochText` (decimal canónico, sin overflow ni basura, `StringToInteger` prohibido) con rechazo de campos extra (`FileIsEnding`); `FileWrite` verificado + flush + relectura completa antes de `g_RefIdentityDurable=true`; intento único en OnInit (epoch incrementa exactamente una vez por sesión, sin retry por tick; legacy intocado). **C1-2 sin retransmitir evidencia caducada:** `EchoAttestV1_Scan` reporta la vigencia restante del slot más antiguo EMITIDO (descuento conservador de la resolución 1 s de `ts`, fuera del JSON contractual); el tick evalúa caducidad ANTES de retransmitir: hecho vigente ⇒ mismos bytes/`source_event_id` con `observed_at`/`ts` intactos; vencido ⇒ descarte sin transmitir y snapshot físico nuevo (id nuevo); atestación de 14 s al capturar ⇒ restante 0 ⇒ jamás reenviable; cero claves wire nuevas. **Gates:** CONTRACT+SOURCE PASS (4 tests C1 nuevos con pines SOURCE + tablas de decisión espejo, `sdk/domain` PASS plain/`-race`; failing sets idénticos por nombre al baseline `4009db7a` en worktree aparte); **MQL_COMPILE=PASS (primera del proyecto)** — MetaEditor64 5.0.0.6090 + metaeditor 5.0.0.2418 en `mt5-kronos.lab.aranea.cl` (host de laboratorio autorizado, sin deploy ni terminales), fuentes byte-exactos SHA-verificados con includes reales: `reference_v3.mq5` 0 errors/1 warning (w43 preexistente fuera del delta; ex5 203598 B sha256 `11eecbeb…`), `reference_v3.mq4` 0 errors/1 warning (w60 preexistente del scanner; ex4 197080 B sha256 `fdb38d96…`); sin probar expresamente (sin runtime MQL ejecutable): estados reales del archivo, relectura física y retry contra GVs reales — PHYSICAL PENDING GAP-ECHO-006. E-06 NO CLOSED; T10/T11+ no iniciados; siguiente gate: Manager review C1 → autoriza T10 (Bridge emitter).
- **E06_WPC2_T09B_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-C2 T09b sobre `32332c04`).** Collector `reference_status` zero-order + relay de atestaciones implementado en `feature/e06-reference-enrollment-binding` @ `4009db7a` (push FF `32332c04..4009db7a`; SPEC v1.2.2 intacto; migration 064, T08/T09, Gateway, Bridge, contracts, ClientConfig, Execution/seed, Forge diff 0; Bridge emitter es T10). **Emisión:** `type=reference_status` periódico por el pipe existente en OnTimer (cadencia nominal 5000 ms, OnTick intocado, cero interferencia TradeIntent/Close), zero-order obligatorio — se emite siempre con sesión pipe activa, incluso 0 posiciones/0 pendientes/`observed_magic=[]` (KNOWN_EMPTY). **Identidad (SPEC §7.5):** `collector_id` UUIDv7 nacido en el EA + contador de `collector_epoch` (OnInit +1 por restart) persistidos en `echo_reference_v3_mt{5,4}_identity_<account>.txt` en el sandbox Files del terminal (sin FILE_COMMON ni PG ni ClientConfig); archivo corrupto/escritura fallida ⇒ status deshabilitado fail-closed (nunca id inestable), reintento por tick; `source_event_id` nuevo por snapshot y retry del MISMO hecho con los mismos bytes (buffer FIFO, 12 reintentos). **Inventario:** MT5 `position_count=PositionsTotal()`/`pending_count=OrdersTotal()` (pendientes) y `observed_magic=POSITION_MAGIC ∪ ORDER_MAGIC` dedupe ≥1; MT4 clasifica OrdersTotal en mercado (OP_BUY/OP_SELL) vs 4 tipos pendientes — jamás confundidos; scan con fallo ⇒ UNKNOWN con conteos ausentes y sin array parcial; `charts[]` MT5 todos los charts (`ChartFirst/ChartNext`, decimal con signo, `CHART_EXPERT_NAME`) y MT4 V1 chart actual + `WindowExpertName`; truncamiento ⇒ attestations omitidas. **Scanner §7.2a read-only compartido en EchoCommon:** grammar exacto con fields literales (`EchoAttestFieldKind` rechaza `n01`/`n16`) y hex16 lowercase, filtro `acct16`==hex(ACCOUNT_LOGIN) + `sh`==FNV-1a32(ACCOUNT_SERVER observado) + `chart_ref ∈ charts[]` del mismo snapshot, lectura consistente c1→payload→c2 con 3 reintentos ante torn read, vigencia `age_s=TimeGMT()-ts` con ventana congelada 15000 ms (futuro/expirado ⇒ omitir; cero hora de acceso de GV), magic lossless por bits `uint(mh)<<32|uint(ml)` en 1..MaxInt64 decimal por entero (jamás double), program_name chunks big-endian con UTF-8 estricto RFC 3629/padding cero/sin NUL//\\; slots parciales/malformados/ajenos se OMITEN. **MT4:** sin slots válidos ⇒ `runtime_attestations=[]` fail-closed (las GV MT5 no son visibles; sin simular paridad con config). **Go:** `ReferenceStatusPayload`/`ParseReferenceStatus` en `messages.go` reutilizando `RuntimeAttestation`/`ReadbackChart` de T08 (sin duplicar el envelope; sin `account_registration_ref`/`received_at` — eso es T10); preserva false/arrays vacíos/magic decimal string/pointers (ausencia ≠ cero). **Gates:** CONTRACT=PASS (11 tests nuevos, `sdk/domain` PASS plain/`-race`, golden zero-order + `TestAttestation_TicklessWindow_SlotExpires` AC-37a con pin SOURCE de ventana+fórmula); SOURCE=PASS (tokens prohibidos = 0 en los 4 MQL, `g_ConfigNativeMagic` clavado al baseline 6/6, pines ejecutables en tests Go); MQL_COMPILE=NOT_RUN (sin MetaEditor/wine; riesgo de sintaxis declarado); PHYSICAL=PENDING (GAP-ECHO-006). Builds sdk/gateway/bridge OK; vet OK; failing sets skip-mode de bridge/gateway/sdk idénticos por nombre al baseline `32332c04` en worktree aparte (colateral preexistente automation/seed/scratch/jaeger) ⇒ cero regresiones; `go.mod`/`go.sum` delta 0. E-06 NO CLOSED; T10/T11+ no iniciados; siguiente gate: Manager review T09b → autoriza T10 (Bridge emitter).

- **E06_WPC1_C1_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-C1 corrección C1 de T08).** Manager auditó `1bebb67d`; defecto de completitud del envelope corregido en `32332c04` (push FF `1bebb67d..32332c04` sobre `feature/e06-reference-enrollment-binding`; mismo carril, SPEC v1.2.2 intacto, sin merge/master; sin reiniciar T08/T09, sin avanzar T09b, digest y persistencia sin rediseñar — `reference_readback_store.go` diff 0, migration 064/Gateway/Bridge/MQL/contracts diff 0). **C1-A:** `Validate()` exige `broker_connected` explícito — `BrokerConnected != nil` (§7.2 TERMINAL_CONNECTED bool), nil ⇒ `ErrInvalidReadback`; true y false válidos, false no implica OBSERVING ni bloquea el envelope; la conexión jamás se infiere desde `ea_session_active`, heartbeat ni ClientConfig. **C1-B:** con `inventory_completeness=KNOWN` exige `position_count != nil` y `pending_count != nil`, ambos >= 0 — cero válido, nil jamás se convierte en cero; UNKNOWN preserva conteos ausentes y los presentes siguen no negativos; sin valor nuevo del enum (KNOWN_EMPTY sigue siendo la representación `KNOWN + position_count=0 + pending_count=0`). **C1-C:** capacidades tri-state `*_trade_allowed` intactas (true/false/nil; ausencia ≠ true y no invalida el envelope). **Tests:** `TestReadback_RequiresBrokerConnected` (nil rechazado, true válido, false válido con round-trip JSON exacto) y `TestReadback_KnownRequiresBothCounts` (KNOWN ambos nil/sólo posiciones/sólo pendientes ⇒ rechazo con field culpable; ambos cero y positivos ⇒ válidos; UNKNOWN ambos nil ⇒ válido; conteo negativo ⇒ rechazo en KNOWN y UNKNOWN) + 3 casos en `TestReadback_ValidateRequiredFields`; fixtures T08/T09 actualizados con `broker_connected` declarado físicamente. **Golden digest recalculado por el campo explícito** (`sha256:4677c175…` reemplaza `sha256:8b7f5127…`): receta y canonicalización intactas, verificación independiente byte-exacto Go↔Python (vista reconstruida a mano, 748 = 748 bytes) + sha256sum CLI. **PG REAL PASS:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd); `run.sh` PASS ×2; `sdk/domain` PASS plain/race (14 tests T08); T09 5/5 `-race`; failing sets idénticos por nombre al baseline `1bebb67d` capturado pre-edit con el mismo PG (postgres 53=53, gateway 30=30 — colateral TC-06-1/identity_bwc + `TestScratch_QueryDB` + `TestAutomationHandler_HandleMessage_ValidAction`, este último reproducido en skip-mode sobre worktree puro `1bebb67d`); vet OK; builds sdk/gateway/bridge OK; `go.mod`/`go.sum` delta 0; tokens prohibidos 0. E-06 NO CLOSED; T09b/T10/T11+ no iniciados; siguiente gate: Manager review de la corrección C1 WP-C1 → autoriza T09b (collector `reference_status` MQL).

- **E06_WPC1_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-C1 T08/T09 sobre `cdb0c163`).** Envelope `REFERENCE_READBACK.v1` + store append-only implementados en `feature/e06-reference-enrollment-binding` @ `1bebb67d` (push FF `cdb0c163..1bebb67d`; SPEC v1.2.2 intacto; migration 064, binding store, Gateway, Bridge, MQL, contracts, core, go.mod/go.sum diff 0). **T08:** `v3/sdk/domain/reference_readback.go` — payload mínimo §7.2 con los 27 campos congelados; registration ref por receta `H("echo-account-registration.v1",[platform,broker_server_ref,account_login])` sobre lo observado (Validate exige match exacto; ACCOUNT_COMPANY como sustituto rechazado; goldens `sha256:055642d0…`/`sha256:7ecd6d46…` con sha256sum independiente); attestation §7.2a exactamente `{program_name, chart_ref, magic_decimal}` con `chart_ref` obligatorio, program_name 1..63 sin NUL//\\, slots parciales rechazados fail-closed; `RebuildAttestationMagic(mh,ml uint32)` por bits uint64, jamás float64 (round-trip `9223372036854775807` demostrado; 2^63 rechazado aunque exacto en double); capability `*_trade_allowed` tri-state `*bool`+omitempty (ausencia ≠ true, null válido); KNOWN_EMPTY aceptado explícitamente; Validate normaliza nil→vacío en los 3 arrays y JAMÁS exige magic del binding, attestations no vacías, casación program↔Version ni decide OBSERVING (T11); **digest** = convención SHA-256 del repo sobre canonical echo-wire-json.v1 cubriendo TODO el payload contractual MENOS `source_event_id` (identidad de transporte) para que el dedupe por digest de §7.1 converja el mismo hecho re-emitido — golden `sha256:8b7f5127…` byte-exacto con sha256sum independiente, sonda temporal borrada; topic `echo.reference-readback.v1` añadido al catálogo existente `domain.Topics` (una línea, sin producer/consumer). **T09:** `v3/sdk/postgres/reference_readback_store.go` — `Insert(ctx, *domain.ReferenceReadback) (*ReferenceReadbackRecord, created bool, error)` INSERT puro sobre `reference_readbacks` (received_at viaja en el payload, jamás `time.Now()` del store; recorded_at default now() del commit; match_status default UNBOUND; MATCH_FAILED durable habilitado por el schema sin API prematura — la atribución real es T11); dedupe doble resuelto atómicamente por los UNIQUEs de 064: replay exacto (mismo source_event_id + mismo digest) devuelve la fila del winner sin mover received_at/recorded_at/payload/match state; mismo digest bajo otra source_event_id converge SOLO con el payload canónico persistido byte-idéntico (`readbackPayloadEqual` con UseNumber, no re-hashea) — colisión con payload distinto ⇒ CONTRACT_CONFLICT fail-closed; misma identidad con payload distinto ⇒ CONTRACT_CONFLICT; resolución determinista del winner (primero por source_event_id, luego por digest). **Tests:** T08 12 nuevos (7 obligatorios TASKS + golden digest + tri-state + vista de digest + topic) y T09 5 (`TestReadback_ReplaySameReceivedAt`, `TestReadback_Dedupe` a/b/c, `TestReadback_DigestCollisionFailClosed` con INSERT SQL directo de digest reutilizado rechazado por el UNIQUE, `TestReadback_AppendOnly` 6 UPDATEs + DELETE rechazados por triggers 064, `TestReadback_KnownEmptyPersistsEmptyMagic`) PASS. **PG REAL PASS:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd); `run.sh` PASS ×2; `sdk/domain` PASS plain/race; failing sets idénticos por nombre al baseline `cdb0c163` corrido en worktree aparte con el mismo PG (postgres 53=53, gateway 29=29, colateral TC-06-1 + `TestScratch_QueryDB` — cero regresiones); vet OK; builds sdk/gateway/bridge OK (sin cambios); skip-mode PASS. E-06 NO CLOSED; T09b/T10/T11+ no iniciados; siguiente gate: Manager review WP-C1 → autoriza T09b (collector MQL).

- **E06_WPB_C1_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-B corrección C1 de T05/T06).** Manager auditó `cd2aca6`; 4 defectos corregidos en `cdb0c163` (push FF `cd2aca6..cdb0c163` sobre `feature/e06-reference-enrollment-binding`; mismo carril, SPEC v1.2.2 intacto, sin merge/master; WP-B ya aceptado no reabierto). **C1-1:** `proof.recorded_at` ya no es input del caller — su presencia en el JSON `proof` ⇒ 400 INVALID_INPUT `field_or_ref=proof.recorded_at` (futuro, pasado o null) con cero fila; el Gateway sella `ProofRecordedAt = h.now().UTC()` exactamente una vez en la primera creación PREPARED (§5.5 ya declaraba el reloj UTC server Gateway); `preparedSealedEqual` ya no compara el sello (metadata server-generated del winner, la comparación de campos declarados por el caller intacta) ⇒ un retry con re-sello jamás fabrica CONTRACT_CONFLICT; migration 064 intocada; goldens §17.1 recalculados por el fixture sin `recorded_at` (`sha256:40fc5cf7…`/`sha256:a7f25959…`, verificación independiente byte-exacto contra el canonical real). **C1-2:** el primer ACK revalida el catálogo con `binding.AccountID` — cuenta ausente 404 NOT_FOUND, `role≠reference` 409 IDENTITY_CONFLICT en `account_id` sin setear `acked_at`, fallo operacional del lookup 503 UNAVAILABLE retryable sin mutación; el replay de un ACK ya committeado no revalida (200, `acked_at` original) y el drift posterior al ACK/OBSERVING sigue siendo path system T14. **C1-3:** el pre-GET deja de ser autoridad del status; `InsertPreparedWithOutcome(...) (*ReferenceBinding, created bool, error)` decide CREATED/REPLAY en la misma operación atómica por el UNIQUE de PostgreSQL (pkey/`uq_e6_bindings_ref` + sealed-equal ⇒ `created=false` con la fila del winner; digest realmente distinto ⇒ CONTRACT_CONFLICT); `InsertPrepared` queda como wrapper compatible WP-A; sin cache, mutex process-local, tabla nueva, SERIALIZABLE ni sleep/retry. **C1-4:** `validateAckProof` retorna (field, err) — semánticos 400 con field culpable (method, hashes, version inexistente), operacionales 503 UNAVAILABLE retryable con `acked_at` NULL y error PG crudo no expuesto — mediante seam local `ackVersionLookup` en el handler, sin capa repository-wide. **Tests:** 7 nuevos gateway (`TestEnroll_RejectsClientProofRecordedAt` ×3 subtests, `TestEnroll_ServerStampsProofRecordedAt`, `TestEnroll_ReplayPreservesProofRecordedAt`, `TestEnroll_ConcurrentReplay_One201One200` — un 201 + un 200, misma fila única, mismo proof_recorded_at, cero CONTRACT_CONFLICT, `TestAck_AccountRoleRevalidated`, `TestAck_ReplayDoesNotRewriteOriginalAck`, `TestAck_VersionLookupUnavailable503`) + 2 store (`TestInsertPreparedWithOutcome_CreatedVsReplay`, `TestInsertPrepared_ReplayPreservesServerStamp`); todos los WP-B previos verdes. **PG REAL PASS:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd); `run.sh` PASS ×2; Go E-06 **17/17** `sdk/postgres` + **22/22** `gateway/internal` `-race`; failing set idéntico al baseline `cd2aca6` (29=29 gateway, 53=53 postgres, colateral TC-06-1 + `TestScratch_QueryDB`); vet OK; builds sdk/gateway/bridge OK. SOURCE: delta = 4 productivos + TASKS/VERIFICATION, todos allowed; contracts/061–064/Bridge/MQL/core/ClientConfig/server.go diff 0; `go.mod`/`go.sum` delta 0. E-06 NO CLOSED; T08–T22 no iniciados; siguiente gate: Manager review de la corrección C1 WP-B.

- **E06_WPB_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-B T04–T07 enrollment HTTP).** Implementado en `feature/e06-reference-enrollment-binding` @ `cd2aca6` (push FF `1614028b..cd2aca6`; base `5dd998f1`; sin merge/master). **T04:** pin E-06 aditivo en ClientConfig (`runtime_binding_id`/`strategy_version_ref`/`magic_decimal`/`map_revision`) omitempty, validación estricta sólo al presence (UUID lowercase, `sha256:<64hex>`, decimal canónico 1..MaxInt64 sin float), legacy intacto (AC-14). **T05:** POST PREPARED siempre vía store 064; idempotencia §17.1 determinista — `attach_request_digest = sha256(canonical body echo-wire-json.v1)`, `Idempotency-Key = H("echo-reference-enroll.v1",[ns,version_ref,acct_reg,class,digest])` con golden cruzado Go/Python/sha256sum (`sha256:bd342f5b…`/`sha256:92f65c7e…`); header ausente 400 / distinto 409 CONTRACT_CONFLICT; `proof_recorded_at` dentro del proof canónico para convergencia de replay; `binding_id` en el body ⇒ STOP `CONTRACT_IDEMPOTENCY` no aplicó; verifica antes de persistir: version_ref sintáctico 400, Version E-03 correspondiente (canonical/strategy_ref/platform) 409, account 404, role=reference 409, pin magic 409, map_revision declarado 409, MT4 ⇒ Version MT4 y magic ≤ int32 sin truncado (§15/AC-24); body con `acked`/`state`/`coverage_started_at` 400 (§8); GET by-id y by-version lista pura sin latest implícito; auth E-02 REUSE (POST=config_operator, GET=CONFIG|READ vía `referenceReadAuth` con primitivas existentes); 201 creación / 200 replay. **T06:** ACK valida proof `MANUAL_VERIFIED` + hashes EX5/inputs/deps == fila Version (400 con field culpable); `acked_at` seteado, PREPARED intacto; replay conserva `acked_at` (AC-02). **T07:** drain sella `accepts_opens_to`; suspend operator auditado **actor=config_operator, reason=OPERATOR_SUSPEND** (decisión Manager WP-B) con path `system/STALENESS` intacto para T14 (store mínimo: helper parametrizado + `SuspendByOperator`, sin tocar triggers); close PREPARED permitido (watermark NULL), OBSERVING→CLOSED y DRAINING sin evidencia 409 NOT_OBSERVING, DRAINING con fixture CONTRACT de `reference_readbacks` KNOWN_EMPTY (C1-2) ⇒ CLOSED; publish del pin exacto por `echo.account-configs.v1` post-PREPARED (creación y replay) sin cambiar estado, fallo ⇒ 503 retryable con fila PREPARED y republicación en retry, cero `echo.commands.*`/`echo.reference-events.v1`; `hasura_handler.go` intocado; store mínimo autorizado: `ListByVersion` query pura. **PG REAL PASS:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd); `run.sh` PASS ×2 completo; Go E-06 **15/15** `sdk/postgres` + **15/15** `gateway/internal` PASS `-race`; vet OK; builds sdk/gateway/bridge OK (Bridge sin cambios); failing set `gateway/internal` idéntico al baseline `1614028b` (29=29, colateral preexistente clase TC-06-1 en harnesses forge; `TestForgeIngest_CertPack` inestable de orden, idéntico aislado en ambos); `sdk/postgres` 0=0; skip-mode idéntico (sólo `TestScratch_QueryDB` documentado); bug propio corregido: conflicto de patrones del mux en fallback sin PG. SOURCE: delta = 7 archivos productivos + TASKS/VERIFICATION, todos allowed; contracts/061–064/Bridge/MQL/core/hasura diff 0; tokens prohibidos 0; `go.mod`/`go.sum` delta 0. E-06 NO CLOSED; T08–T22 no iniciados; siguiente gate: Manager review WP-B (autoriza WP-C read-back).
- **E06_WPA_C2_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-A corrección C2 de T01–T03).** Manager auditó `c021983f`; 1 defecto residual corregido en `1614028b` (push FF sobre `feature/e06-reference-enrollment-binding`; mismo carril, SPEC v1.2.2 intacto, sin merge/master). **C2:** `uq_e6_canonical_admission` pasa de `state <> 'CLOSED'` a `state IN ('PREPARED','OBSERVING','SUSPENDED')` — la reserva ya no se retiene en DRAINING (SPEC §11: el conflicto CANONICAL protege al CANONICAL OPEN-admitting; drenar sella `accepts_opens_to` y el switch de cuenta canónica explícito admite el sucesor PREPARED en OTRA physical account durante DRAINING; CLOSED libera; SUSPENDED sigue sin reemplazo implícito). Ninguna transición enumerada §6 re-entra al índice desde DRAINING/CLOSED (DRAINING→SUSPENDED no existe) ⇒ `uq_e6_canonical_open_admitting` permanece defensa en profundidad sin camino de fallo; `uq_e6_overlap_live` sin cambios (SUSPENDED y DRAINING siguen reteniendo la overlap key same-account ⇒ handover sólo hacia otra physical account; same-account rollover exige CLOSED + inventario vacío C1-1/C1-2). `reference_binding_store.go` sin cambios (ya clasifica el constraint como `ErrCanonicalExists`). **Evidencia A–G** en `13_uniqueness.sql` (A/B/C conflictos con titular PREPARED/OBSERVING/SUSPENDED; D sucesor admitido con viejo DRAINING en otra physical key; E sucesor observa porque el viejo DRAINING ya no es OPEN-admitting; F overlap same-account rechazada con viejo DRAINING; G CLOSED libera y admite sucesor fresco) y `TestCanonicalUnique` refleja el mismo lifecycle; `10_migrate_up.sql` aserta el predicado exacto sin DRAINING/CLOSED. **PG REAL PASS:** PG 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd); `run.sh` PASS ×2 completo (rebuild, probe, up, aserciones 10, idempotencia, 12, 13 A–G, 14, down fail-closed, down ok, up/down/up final); Go E-06 13/13 PASS `-race`; failing set del paquete idéntico al baseline `c021983f` (53 = 53, colateral TC-06-1 + `TestScratch_QueryDB` preexistentes ⇒ cero regresiones); vet OK; builds sdk/gateway/bridge OK; skip-mode PASS. SOURCE: delta C2 = 6 archivos allowed; contracts/061–063 diff 0; tokens prohibidos 0. `TEST_CHANGE_REQUEST.md` intacto. Nota colateral documentada: el comentario de `14_watermark_view_revoke.sql` dice "binding 102 permanece vivo" y tras C2 el vivo es el 109 (14 no es allowed file de C2; fixture SHADOW funciona idéntico). E-06 NO CLOSED; T04–T22 no iniciados; siguiente gate: Manager review C2.
- **E06_WPA_C1_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-A corrección C1 de T01–T03).** Manager auditó `429e5c03`; 4 defectos corregidos en `c021983f` (push FF sobre `feature/e06-reference-enrollment-binding`; mismo carril, SPEC v1.2.2 intacto, sin merge/master). **C1-1:** `uq_e6_overlap_live` retiene la overlap key también en SUSPENDED (`{PREPARED, OBSERVING, DRAINING, SUSPENDED}`); liberación = CLOSED con inventario vacío. **C1-2:** trigger DRAINING→CLOSED exige read-back durable `MATCHED` al binding, misma physical key, collector epoch autorizado, `KNOWN` con position_count=0 ∧ pending_count=0 y `received_at >=` instante auditado OBSERVING→DRAINING (fila append-only) ⇒ `E06_CLOSE_REQUIRES_EMPTY_INVENTORY`; protege UPDATE SQL directo; PREPARED→CLOSED nunca observado sigue sin evidencia. **C1-3:** SUSPENDED→OBSERVING exige `coverage_quality=PROVEN` (`E06_RECOVERY_QUALITY`); `Recover()` sella PROVEN + `accepts_opens_from=received_at` + `accepts_opens_to=NULL`, watermark inmutable, gap queda UNKNOWN. **C1-4:** nueva `uq_e6_canonical_admission` UNIQUE `(ns, version_ref) WHERE CANONICAL AND state<>'CLOSED'` serializa el segundo PREPARED CANONICAL desde el POST (§14) ⇒ `ErrCanonicalExists` en `InsertPrepared`; SUSPENDED sin reemplazo implícito; switch explícito tras DRAINING/CLOSED; SHADOW coexiste; `uq_e6_canonical_open_admitting` queda como defensa en profundidad. **PG REAL PASS:** PG 17.11 descartable (Zonky + PGDG psql, cluster efímero `/tmp/e06-pg17`); `run.sh` PASS completo (rebuild, probe, up, aserciones 10 con las dos nuevas, idempotencia, 12 con C1-2/C1-3 neg+pos, 13 con C1-4/C1-1/switch post-CLOSED, 14, down fail-closed, up/down/up final); Go E-06 13/13 PASS `-race`; vet OK; builds sdk/gateway/bridge OK. SOURCE: delta C1 = 9 archivos allowed; contracts/061–063 diff 0; tokens prohibidos 0. Fixtures cross-file 004/201 → SHADOW (probaban borrado/watermark, no la clase). `TEST_CHANGE_REQUEST.md` intacto. E-06 NO CLOSED; T04–T22 no iniciados; siguiente gate: Manager review C1.
- **E06_WPA_READY_FOR_MANAGER_REVIEW (2026-09-17, NORMAL WP-A Persistencia T01–T03).** Implementado en `feature/e06-reference-enrollment-binding` @ `429e5c03` (push FF sobre `acf996ad`; base `5dd998f1`; sin merge/master). **T01 064:** `reference_bindings`/`reference_readbacks`/`binding_transitions`; FK compuesta a tupla Version 061 + FK accounts; UNIQUE parciales CANONICAL OPEN-admitting / collector claim / overlap vivo; triggers INSERT-siempre-PREPARED, write-once identidad, transiciones enumeradas §6 con auditoría obligatoria (SECURITY DEFINER, actor/reason por GUC) y watermark inmutable `max(proof,barrier)` validado por trigger; vista `v_reference_bindings` sin proof crudo; revokes; widen `active_positions.strategy_id`→text; down fail-closed abort con `octet_length>50`. **T02 store:** `InsertPrepared` con recetas §5.1/§13 computadas server-side (pin contra `strategy_identity_mappings`), replay same key+digest convergente, conflict digest distinto, Ack/MarkObserving/Recover/Drain/Suspend/Close/AdvanceReadback; errores `CANONICAL_EXISTS`/`COLLECTOR_CONFLICT`/`ACCOUNT_MAGIC_OVERLAP`/`NOT_OBSERVING`. **T03:** unicidad CANONICAL/COLLECTOR/OVERLAP con evidencia UNIQUE SQL directa + API. **PG REAL PASS:** PostgreSQL 17.11 descartable (binarios Zonky + cliente PGDG, cluster efímero local) — harness `tests/reference_binding_e6/run.sh` PASS completo (rebuild 061→062→063, probe interrupción, up, idempotencia, máquina de estados, unicidad, watermark/vista/revokes, down fail-closed, up/down/up final); tests Go E-06 13/13 PASS con DATABASE_URL y `-race`; go vet OK. SOURCE: diff contracts y 061–063 = 0; OrderSend/forbidden tokens = 0. Tests ajenos rotos por la existencia de 064 (helper `identityBWCDB`, harnesses identity_bwc/analytics_a0) documentados en `TEST_CHANGE_REQUEST.md` sin editar archivos ajenos. T02 queda `[/]` sólo por AC-18 (HTTP, cierra con T05). E-06 NO CLOSED; T04–T22 no iniciados.
- **E06_FORGE_PRODUCER_C1_READY_FOR_MANAGER_REVIEW (2026-09-17, corrección C1 lane Forge).** Manager auditó `508b4a2` y mandó 4 defectos; corregidos en `b738a6d` (push FF sobre `feature/e06-runtime-attestation-exporter`; mismo delta de 2 archivos). C1-1: injector preserva control flow de OnInit — return de éxito guardado por if/else sin llaves se reescribe como bloque compuesto `{ Publish(); return X; }`, boundary no demostrable rechaza fail-closed, cadenas if/else terminales instrumentables (prueba acotada), returns de fallo byte-idénticos. C1-2: invalidación `c=0` es hard gate antes de cualquier payload. C1-3: `SetField` comprueba retorno documentado de `GlobalVariableSet` (`datetime != 0`) + `GetLastError`; cleanup de chunks distingue inexistente (`GlobalVariableCheck`) de fallo real de `GlobalVariableDel`; fallo material de cleanup impide commit. C1-4: generate+assert+instrumentación en staging aislado (mismo basename) y promoción por rename atómico (fallback mismo-volumen) sólo tras post-condiciones; exportación fallida no deja MQ5 publicable ni sobrescribe preexistente. Tests C1: 9 casos (guard same/next-line, if/else, else-if, nested, bloque llaves, boundary ambiguo, fault injection GV CONTRACT/model 10 escenarios + pins textuales, publicación staged). Suite §17 A–O + RobustRun/WFM + JUnit trades 13/13 PASS; build PASS; `verify_build.sh` FAIL_BASELINE_KNOWN reproducido 3/3 en `508b4a2` puro. PHYSICAL_PENDING (AC-34/35 sin terminal MT5/SQX). E-06 NO CLOSED.
- **E06_FORGE_PRODUCER_IMPLEMENTED_PHYSICAL_PENDING (2026-09-17, lane A NORMAL).** Producer atestación §7.2a inyectado en `EchoForgeMT5Exporter.java` (Forge `feature/e06-runtime-attestation-exporter` @ `508b4a2` desde `3d0e8c9` F-05-I; push FF; sin merge). Contract §17 A–O PASS, build plugin PASS, idempotencia byte-stable, MagicNumber/mmLots intactos. PHYSICAL_PENDING (sin terminal MT5/SQX aquí; AC-34/AC-35 esperan físico). Auditado por Manager ⇒ corrección C1 (ver bullet superior). E-06 NO CLOSED; NORMAL Echo (WP-A…WP-E) no lanzado.
- **E06_PLANNING_READY_FOR_IMPLEMENTATION_REVIEW (2026-09-16, docs-only, SPEC v1.2.2).** Erratum de liveness del refresh §7.2a.4: MQL5 no garantiza ticks cada 5 s ⇒ el `MUST` de republicación vía `OnTick` era garantía irrealizable. Decisión Manager frozen: `OnTimer` existente realmente activado con período ≤5 s **preferido** (prohibido modificar `EventSetTimer`/crear timer); si sólo `OnTick`, renovación **oportunista** — 5 s = intervalo mínimo entre publicaciones, no frecuencia; sin tick ni timer compatible **sin promesa de liveness continua** (expiración 15 s fail-closed `SUSPENDED + UNKNOWN`; recovery §6 con nueva atestación matching; ninguna actualización de `ts` desde Echo/Bridge/Gateway/config). Tests AC-37a…d. CASE B **intacto**; encoding v1 intacto. Corrección previa del transporte §7.2a: encoding `echo.attest.v1.<acct16>.<chart16>.<field>` con `mh`/`ml` uint32 exactos, commit par, `ts` escrito (nunca `GlobalVariableTime`), `chart_ref` obligatorio, FNV-1a32 de `ACCOUNT_SERVER`. Inyección Forge: post-`generate`+`assertNonZeroLots` en `EchoForgeMT5Exporter.java`, scanner estructural, fail-closed, **no implementada**. Branch Forge observada `codex/f05-release-prep` @ `0ddd4db` (blob `cfbd5b78`); `master` `0b9742b` no es la de desarrollo. HEAD Echo `acf996ad043f87d6bbe6ae7b6190d1eb801e908a` (contrato `28afc47faf72b70e67b141b39224b8674f98458b`; old `336c723b`; push FF). Baseline `origin/master` `5dd998f16aea7b2821f460188718d7a6d279829c`. **0 líneas productivas en `v3/**`; 0 bytes Forge mutados.** Master intacto. NORMAL no lanzado. No E-07. No PR. Next gate = IMPLEMENTATION REVIEW.
- **RuntimeBinding key:** PK `binding_id` UUID; pin S0 `binding_ref = H("echo-reference-binding.v1",[ns,binding_id,version_ref,account_registration_ref,broker_server_ref,platform,magic_decimal,observation_class])`.
- **Lifecycle:** PREPARED → (ACK + matching read-back) → OBSERVING → DRAINING → CLOSED; staleness → SUSPENDED + UNKNOWN. PREPARED/ACK solos ≠ OBSERVING.
- **Read-back authority:** Echo collector `reference_status` → Bridge `REFERENCE_READBACK.v1` → Gateway. Operator ACK es CONFIG, no suficiente. Heartbeat/UnifiedBatch/config **no** son OBSERVING.
- **Coverage barrier:** `coverage_started_at = max(proof.recorded_at, capture_barrier_at)`. Prohibido created_at/ingestion/ACK clocks.
- **CANONICAL/SHADOW:** 1 CANONICAL OPEN-admitting por Version; SHADOW no suma. Duplicate collector y same-account overlap fail-closed.
- **Migración:** `064_reference_enrollment_binding` exclusiva E-06; FK a 061; SHARED DEV apply gated por 061 APPLIED (hoy NOT_APPLIED). 062/063 no-touch.
- **PHYSICAL:** AC-01…15 + AC-26…32; producer real `reference_status`; fakes ≠ OBSERVING. GAP-ECHO-006 = PHYSICAL_PENDING, no PASS.
- **Hipótesis accounts/policies solos:** REFUTADA en source/PG. Bastan accounts/policies (intent) + `reference_bindings` (hecho). No `deployments`.
- **AUTHORITY_CONFLICT:** ninguno. S0 ACTIVE/CLOSED = envelope de pin; lifecycle operacional = Live Authority §5. O1/O3 technical default Fable.

## 🧱 Entrega de desarrollo

| Aplicación / repo | Branch | Base | SPEC funcional | SPEC técnica | Estado |
|---|---|---|---|---|---|
| xKoRx/echo | `feature/e06-reference-enrollment-binding` | `5dd998f16aea7b2821f460188718d7a6d279829c` | [[Echo — Forge Ingestion, Runtime Identity and Live Authority Contract V1]] §§5–6; O1/O3 Fable; C-3 collector + excepción Manager §7.2a | `specs/FEAT-REFERENCE-ENROLLMENT-BINDING-E6/SPEC.md` v1.2.2 (encoding v1 + erratum liveness refresh; HEAD `acf996ad`, contrato `28afc47f`; previo `336c723b`) | E06_T10_C1_ARCHITECTURE_BLOCKED @ `35db8b67` (correction C1: defecto received_at↔digest↔T09 confirmado CONTRACT+PG REAL; RUTA B sin commit; decisión Manager pendiente; T10 Bridge emitter `reference_status` → `REFERENCE_READBACK.v1` → `echo.reference-readback.v1` intacto; PHYSICAL PENDING GAP-ECHO-006; MQL_COMPILE_C2 NOT_RUN deuda T09b; T11–T22 pendientes) |
| xKoRx/symphony (Forge) | `feature/e06-runtime-attestation-exporter` @ `508b4a2` (parent `3d0e8c9` = cierre F-05-I; push FF; base `codex/f05-release-prep`) | `3d0e8c958765da23840e00bf1a0ac017fc47597a` (blob exporter `cfbd5b78` verificado pre-write) | F-04 sello/readback magic | SPEC §7.2a v1.2.2 implementada: inyección post-`generate`+`assertNonZeroLots` en `EchoForgeMT5Exporter.java` (scanner estructural no-regex, fail-closed, idempotente `ECHO_ATTEST_V1_*`, encoding v1, refresh §7.2a.4). Tests §17 A–O contract PASS; build plugin PASS; **PHYSICAL_PENDING** (sin terminal MT5/SQX) | E06_FORGE_PRODUCER_IMPLEMENTED_PHYSICAL_PENDING · lane propio, gate Manager pendiente |

## 🗺️ Source map (baseline `5dd998f1` + PG/Hasura)

- Accounts/policies: intent mutable; sin version/hash/terminal/read-back.
- ClientConfig/Kafka/handshake: config delivery; login+company; **sin** ACCOUNT_SERVER; **sin** ACK de EA.
- Bridge heartbeat: liveness vestigial; MQL Reference **no emite** heartbeat; payload ignorado. UnifiedBatch = Execution-only. AccountRegistry no es unique físico.
- 061 source sí / SHARED DEV no. 062+063 DEV sí. `reference_bindings` ausente.
- S0 `RuntimeBinding` READ ONLY. E-04 INGESTED no-touch. E-02 CONFIG REUSE. Hook SQX de coverage OUT_OF_SCOPE (C-3 DEFER). Echo collector `reference_status` + relay de atestaciones IN SCOPE (§7.2a). Producer del strategy EA = Forge exporter (`EchoForgeRobustRunExporter` estampa `MagicNumber` input; `EchoForgeMT5Exporter` genera; `magic-readback` verifica; sello F-04 exige allocated==readback).
- `reference_v3.mq{4,5}`: sin magic input propio; `GetEffectiveMagicNumber` = POSITION_MAGIC o fallback ClientConfig (prohibido como atestación). Cero `GlobalVariable*`/`ACCOUNT_SERVER`/`CHART_EXPERT_NAME` hoy: helpers net-new T09b (reconstrucción encoding v1, sólo lectura).

## 🎯 Target physical state

```text
v3/sdk/postgres/migrations/064_reference_enrollment_binding.{up,down}.sql
echo.reference_bindings + reference_readbacks + binding_transitions
echo.v_reference_bindings
POST /api/v1/reference/bindings[+ /ack /drain /suspend /close]
echo.reference-readback.v1   # Bridge → Gateway, solo desde reference_status
ClientConfig additive pin fields
v3/clients/mt{4,5}/reference_v3.mq{4,5}  # reference_status mínimo + relay verbatim atestaciones §7.2a (NORMAL futuro)
v3/hasura/metadata/tables/reference_bindings.yaml  # SELECT only
(xKoRx/symphony) EchoForgeMT5Exporter.java  # inyección atestación §7.2a — lane Forge, un archivo
```

Ningún cambio a `v3/sdk/contracts/**`. MQL sólo el delta collector T09b. Ningún E-07 DEAL. Ningún apply 061/064 a Aranea en NORMAL. Resto de Forge intocado.

## 🕸️ Dependency graph

Ver TASKS.md. T01 persistencia → T05 HTTP → T09b collector status → T11 OBSERVING. Emitter T10 ∥ HTTP. PHYSICAL T21 al final con producer real.

No ejecutar E-07…E-13. No reabrir E-01…E-05. E-04 T21 no bloquea. 061 DEV NOT_APPLIED no bloquea development (descartable); bloquea apply 064 SHARED DEV. No abrir hook SQX.

## Allowed scope NORMAL

Exacto PLAN.md. Development en `feature/e06-reference-enrollment-binding` desde `5dd998f1`. Worktree dedicado; no reutilizar checkout de otra fase. Prohibido `origin/master` push/merge. Prohibido `v3/sdk/contracts/**`. Prohibido 061/062/063. 064 exclusiva. Prohibido hook SQX exportado. Permitido `reference_status` en Echo collector. Prohibido Aranea PROD. Prohibido apply 064 SHARED DEV hasta 061 APPLIED (ops aparte).

## 📦 Work packages

- **WP-A Persistencia** T01–T03. 064 + stores + UNIQUEs.
- **WP-B Enrollment HTTP** T04–T07. PREPARED/ACK/drain + ClientConfig.
- **WP-C Read-back** T08–T11 + T09b. Envelope, collector `reference_status`, Bridge emitter, Gateway consumer, OBSERVING.
- **WP-D Gates negativos** T12–T15. Duplicate, overlap, mismatch, stale, DB-only.
- **WP-E READ/BWC/cert** T16–T22. Hasura, widen, SOURCE, coverage, PHYSICAL.

## TOP / NORMAL boundaries

- TOP: SPEC, esta nota, TASKS, PLAN puente, linkage padre. No source Go/SQL/HTTP productivo (cumplido). Corrigió el transporte §7.2a a encoding v1 (sin cambiar CASE B). No implementó la inyección.
- NORMAL: **no lanzado** (gate Manager). T01–T22+T09b desbloqueados por v1.2.0: zero-order = atestación runtime §7.2a + KNOWN_EMPTY; fail-closed `RUNTIME_ATTESTATION_ABSENT`/`MISMATCH`. No sintetizar OBSERVING, no hook SQX de coverage, no E-07, no apply 061. La inyección Forge (un archivo) corre en lane Forge con gate propio.
- GOD: NONE.

## Frozen decisions

1. `reference_bindings` es el hecho; accounts/policies son intent; no `deployments`.
2. OBSERVING exige ACK CONFIG **y** `reference_status` matching. Heartbeat/UnifiedBatch/config/PG no bastan.
3. Un CANONICAL OPEN-admitting por Version; SHADOW no suma.
4. Duplicate collector: UNIQUE physical key + epoch **del producer EA**; segundo 409. Gateway no mint identity.
5. Same-account overlap magic prohibido hasta CLOSED+inventario vacío.
6. Pin Version write-once en PREPARED; config posterior no reinterpreta.
7. `coverage_started_at = max(proof.recorded_at, capture_barrier_at)`.
8. Stale > 15000ms sin `reference_status` → SUSPENDED + UNKNOWN.
9. Migración 064; FK 061; interlock SHARED DEV explícito.
10. Certificación sin órdenes. Unit fakes ≠ PHYSICAL OBSERVING. PHYSICAL exige producer `reference_status` + Version con inyección §7.2a.
11. `observed_magic` = inventario broker; `runtime_attestations` = reconstrucción lossless encoding v1 §7.2a; sin backfill/mint desde config/PG/Version. **Zero-order (Manager CASE B): `effective_magic == binding.magic_decimal` atestiguado por la instancia EA strategy; ausente/mismatch ⇒ fail-closed. Nunca trade/pending/DEAL artificial.** `chart_ref` obligatorio. Vigencia = `ts` escrito, nunca `GlobalVariableTime`.
12. `*_trade_allowed` se capturan; no gatean OBSERVING. VALID_NO_SIGNAL es E-07.
13. C-3: hook SQX de **coverage** DEFER; excepción §7.2a = **sólo** atestación de identidad runtime mínima (sin signals/deals/coverage/métricas/lifecycle/telemetría/estado). Echo collector status + relay IN SCOPE.
14. Identidad de Version: inyectar atestación cambia bytes ⇒ **nuevo** `strategy_version_ref` (canonical_strategy_id estable). Versions históricos nunca se mutan; fail-closed `RUNTIME_ATTESTATION_ABSENT`. Re-enroll = nueva exportación + ingestion + nuevo `binding_id`.

## Migrations

Una: `064_reference_enrollment_binding` (exclusiva E-06). Depende de 061 **para apply**. Independiente de 062/063 semánticamente. 065+ fuera. Gate up/down/up en PG descartable 061→062→063→064. No apply PROD. No apply SHARED DEV sin 061.

## Dependency delta

NONE de terceros. `v3/sdk` importa contracts (mismo parent). Kafka topic nuevo sobre producer existente.

## Compatibility strategy

HTTP nuevo. ClientConfig additive. Hasura SELECT nueva. EA/journal legacy intactos. `active_positions.strategy_id` → text (deuda E-03).

## Testing / gates

SPEC AC-01…AC-25 + AC-26…AC-33. SOURCE + CONTRACT + PG REAL + HTTP + PHYSICAL producer `reference_status` zero-order + HASURA yaml. Ver VERIFICATION.md stub.

## Prohibited files

`v3/sdk/contracts/**`, `migrations/061-063_*`, `v3/clients/**` salvo collector `reference_status`+relay §7.2a, `v3/front/**`, `v3/core/**`, Docker/Compose/deploy/workflows, Forge/symphony **salvo la inyección de un archivo §7.2a en lane Forge propio**, `sync.sh`.

## Blockers

**Planning: ninguno** (transporte v1.2.1 cerrado; CASE B intacto). Development: ninguno adicional (PG descartable incluye 061). SHARED DEV 064 apply: **061 NOT_APPLIED** (ops/E-03). PHYSICAL producer real: GAP-ECHO-006 si no hay terminal collector **y/o** Version Forge con inyección §7.2a.5; **no** se sustituye por fake. E-04 T21 no bloquea. La inyección Forge requiere lane/gate Forge propio **y** reidentificar branch de WRITE (`codex/f05-release-prep` observada; `master` no autorizada por el hecho de ser master) — bloquea sólo los AC PHYSICAL de atestación, no CONTRACT.

## Handoff requirements

Implementation review de v1.2.2 (erratum liveness + encoding v1 + CASE B intacto) → autoriza NORMAL. **NORMAL no arranca antes.** No merge/deploy. No E-07. Hook SQX de coverage sigue DEFER. No apply 061/064 Aranea. La inyección Forge se ejecuta como expansión de borde de un archivo con gate Manager/Forge **después** de reidentificar la branch autorizada.

## Closure conditions

Esta sesión TOP **no** cierra E-06. Cierre futuro: T01–T22 `[x]`; AC-01…AC-25; independent verifier; Manager; controlled integration. PHYSICAL SHARED DEV/PROD son gates aparte.

## 🧩 Subproyectos

_No aplica — hijo de implementación de E-06; no crea Integration ni más hijos._

## ✅ Tareas

> [!example]- Fuente de tareas — editar / mover de estado aquí
> Checklist atómico en `xKoRx/echo` `specs/FEAT-REFERENCE-ENROLLMENT-BINDING-E6/TASKS.md`. Aquí sólo work packages. NORMAL no arranca hasta manager review.
> - [x] TOP planning SPEC/PLAN/TASKS/VERIFICATION v1.0.0 → Manager Review #owner/agent #type/docs #area/echo
> - [x] TOP planning correction #1 v1.1.0 zero-order physical authority → Manager Review #owner/agent #type/docs #area/echo
> - [x] TOP authority reconciliation zero-order magic → CASE C blocked #owner/agent #type/docs #area/echo
> - [x] TOP planning correction #2 v1.2.0 Manager CASE B + producer discovery §7.2a → Manager Review #owner/agent #type/docs #area/echo
> - [x] TOP transport contract correction v1.2.1 encoding GV lossless → Manager Review #owner/agent #type/docs #area/echo
> - [x] TOP refresh liveness erratum v1.2.2 (OnTimer compatible / OnTick oportunista / sin liveness continua prometida) → Implementation Review #owner/agent #type/docs #area/echo
> - [r] WP-A Persistencia 064 + stores + UNIQUEs #owner/agent #type/dev #area/echo
> - [r] WP-B Enrollment HTTP PREPARED/ACK/drain + ClientConfig #owner/agent #type/dev #area/echo
> - [ ] WP-C Read-back Bridge→Gateway desde `reference_status` y transición OBSERVING #owner/agent #type/dev #area/echo
> - [ ] WP-D Gates negativos duplicate/overlap/mismatch/stale/DB-only #owner/agent #type/dev #area/echo
> - [ ] WP-E Hasura/BWC/SOURCE/PHYSICAL no-trading #owner/agent #type/dev #area/echo

```dataviewjs
const meta={" ":["To Do","var(--text-muted)","var(--background-modifier-border)"],"/":["WIP","#ba7517","rgba(234,124,12,.18)"],"r":["Review","#185fa5","rgba(55,138,221,.18)"],"x":["Done","#3b6d11","rgba(99,153,34,.18)"],"X":["Done","#3b6d11","rgba(99,153,34,.18)"],"-":["Canceled","var(--text-faint)","var(--background-modifier-border)"]};
function linkify(s){return String(s).replace(/\[\[([^\]|]+)(?:\|([^\]]+))?\]\]/g,(m,a,b)=>`<a class="internal-link" href="${a}" data-href="${a}">${b||a}</a>`).replace(/#[\w/-]+/g,m=>`<span style="opacity:.55;font-size:12px">${m}</span>`).replace(/📅\s*(\d{4}-\d{2}-\d{2})/g,(m,d)=>`<span style="opacity:.7;font-size:12px">📅 ${d}</span>`).replace(/[⏫🔼🔽⏬🔺]/g,"").replace(/✅\s*(\d{4}-\d{2}-\d{2})/g,"");}
function has(t,tag){return new RegExp(`(^|\\s)#${tag}(\\s|$)`).test(String(t.text));}
function render(tasks){const el=dv.el('div','');el.innerHTML=tasks.map(t=>{const[label,fg,bg]=meta[t.status]||["?","var(--text-muted)","var(--background-modifier-border)"];return `<div style="display:flex;align-items:center;gap:8px;margin:5px 0;"><span style="font-size:11px;font-weight:600;padding:1px 9px;border-radius:999px;background:${bg};color:${fg};min-width:56px;text-align:center;flex:none;">${label}</span><span>${linkify(t.text)}</span></div>`;}).join("");}
function board(tasks){const cols=[[" ","🟦 To Do"],["/","🟡 WIP"],["r","🔵 Review"]];let any=false;for(const[st,label]of cols){const c=tasks.filter(t=>t.status===st);if(c.length){any=true;dv.el('h4',label);render(c);}}const done=tasks.filter(t=>t.status==="x"||t.status==="X");if(done.length){any=true;dv.el('h4',"✅ Done");render(done);}if(!any)dv.paragraph("_Sin tareas._");}
const owner=((dv.current().owner)==="agent")?"agent":"me";
const all=dv.current().file.tasks.array();
const primary=all.filter(t=>has(t,`owner/${owner}`));
const loose=all.filter(t=>!has(t,"owner/me")&&!has(t,"owner/agent"));
dv.header(3, owner==="agent"?"🤖 Tareas del agente":"🧍 Mis tareas");
board(primary);
if(loose.length){dv.header(3,"🧺 Sin owner (clasificar)");render(loose);}
```

## 📆 Bitácora

- **2026-09-18 (MANDATO T10 CORRECTION C1 — `E06_T10_C1_ARCHITECTURE_BLOCKED`, RUTA B sin delta).** Bootstrap con reconocimiento explícito de que el STOP anterior interpretó incorrectamente el baseline (`35db8b67` = entrega T10 publicada y baseline obligatorio de C1; fetch de origin sin avance material, parent exacto, worktree limpio ⇒ G0 PASS). Reproducción determinista P/S/T1/T2 con el camino real `handleMessage → handleReferenceStatus → emitter` (reloj inyectado, fixture CONTRACT T10) y funciones reales del SDK: identidades preservadas, R1.ReceivedAt≠R2.ReceivedAt, D1=`sha256:d12df650…` ≠ D2=`sha256:d0d4b07f…`, payload canónico distinto y D1==D2 al igualar received_at (único campo divergente). Store T09 real contra PostgreSQL 17.11 descartable (cluster efímero local, schema por `run.sh` PASS completo): Insert R1 `created=true`; Insert R2 ⇒ `CONTRACT_CONFLICT (same source_event_id, different payload)`; 1 fila con received_at/recorded_at originales; Caso F ⇒ `CONTRACT_CONFLICT` (conflicto verdadero preservado); replay exacto converge. Matriz A–F (A/E convergen por diseño T09; B/C/D defectuosos sin estado S→T1 en Bridge, D irrecuperable sin storage durable; F intacto) y alternativas 1–5 ⇒ ninguna corrección integral dentro del scope C1 (T08/T09 READ ONLY, boundary sin-PG de Bridge congelado, protocolo collector sin instante de primer ingreso). RUTA B: repo intocado (tests temporales de reproducción eliminados, `git status` limpio @ `35db8b67`, cero commit/push), cluster PG detenido. Registros: agent_run `2026-09-18-zcode-glm-5.3-flash-e06-t10-c1-architecture-blocked`, change_log `2026-09-18-e06-t10-c1-architecture-blocked`. **Siguiente gate: decisión Manager sobre C1 (equivalencia de replay vs boundary durable) — no autoriza T11.**
- **2026-09-18 (MANDATO T10 REENVIADO — STOP con evidencia §0, replay detectado, sin delta).** El mandato NORMAL T10 (baseline `891ab1d8`, estado autorizado `E06_T09B_APPROVED — RELEASE_T10`) llegó reenviado con la condición §0 «si el baseline remoto avanzó, STOP con evidencia». Verificación fresca en el worktree dedicado `/tmp/echo-e06-reference-enrollment`: `git fetch` → `origin/feature/e06-reference-enrollment-binding` = `35db8b67b64c154579399310cd149ae01c2c786b` (la entrega T10 del 2026-09-17), ahead/behind `0 0`, `merge-base --is-ancestor` confirma `891ab1d8` como parent exacto, worktree limpio; `git show --stat 35db8b67` = exactamente los 5 archivos allowed del mandato (`reference_readback_emitter.go` nuevo + hook en `reference_pipe_handler.go` + `reference_readback_emitter_test.go` nuevo + TASKS/VERIFICATION); las 7 tests canónicas TASKS + 20 negativas/de transporte presentes con los nombres exactos del mandato; `go test ./v3/bridge/internal/ -run 'TestEmitter_' -count=1` → ok; agent_run `2026-09-17-zcode-glm-5.3-flash-e06-t10-bridge-emitter` y change_log `2026-09-17-e06-t10-bridge-emitter` presentes. Conclusión: replay de un mandato ya cumplido — STOP sin re-implementación, sin commit, sin push, repo `xKoRx/echo` intocado; el estado E-06 NO cambia; PHYSICAL sigue PENDING (GAP-ECHO-006) y MQL_COMPILE_C2 NOT_RUN (deuda heredada T09b C2). Registros del día: agent_run/change_log `2026-09-18-e06-t10-replay-stop`. **Siguiente gate: Manager review T10 (`35db8b67`) → autoriza T11.**
- **2026-09-17 (NORMAL T10 — Bridge emitter zero-order + runtime attestations, `E06_T10_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` @ `891ab1d880846e89d96e2e45dedd25a91faad214` == origin, parent `fc2d4e5d37d04f2e535e80d5f34fda1d34324fe2`, merge-base master `5dd998f1`, worktree limpio, sin commits posteriores; checkout E-02 (`~/go/src/github.com/xKoRx/echo`, carril `feature/e02-control-safety-journal-recovery`) sin tocar. **Hook real:** único `case "reference_status"` añadido al switch de `ReferencePipeHandler.handleMessage` → nuevo `handleReferenceStatus`; el switch sólo es alcanzable desde `handleSession`, que corre tras `WaitForConnection` de un pipe que `PipeManager.CreatePipeForAccount` crea exclusivamente para cuentas `AccountTypeReference` (el transporte existente ES la sesión Reference conectada — sin sistema de autenticación nuevo); heartbeat/reference_event/trade_close/span_*/default siguen sus rutas originales; UnifiedBatch de Execution, configuración, snapshots y PostgreSQL no tienen ningún camino hacia el emitter. **Coherencia de sesión:** `RegisteredAccount.AccountID()` (número de login decimal con el que se construyó el pipe) == `payload.AccountLogin` observado y `account_role == "reference"` exigidos antes de construir; contradicción ⇒ rechazo sin publicación (la etiqueta del JSON no autentica sola). **Nuevo emitter (`reference_readback_emitter.go`):** `ParseReferenceStatus()` → `buildReferenceReadback` copia los 27 campos verbatim (pointers tri-state, conteos, arrays, identidades producer-assigned intactos; ausencia ≠ cero, false ≠ ausencia; cero campos inventados); `account_registration_ref` derivado exclusivamente con el helper congelado `domain.AccountRegistrationRef(platform, broker_server_ref, account_login)` sobre lo observado (helper y receta intocados; test demuestra ≠ receta sobre ACCOUNT_COMPANY); `received_at` capturado en el límite de ingreso del hecho (top de `handleReferenceStatus`, antes del parseo y de cualquier I/O) con seam mínimo `h.now func() time.Time` en el handler (default `time.Now`, inyectable en tests; sin infraestructura de clocks) — un intento de publicación jamás lo refresca; `observed_at`, `timestamp_ms` del pipe y hora del ACK Kafka excluidos por test; `ReferenceReadback.Validate()` ANTES de publicar: error de parseo/validación ⇒ error explícito sin publicación, cero defaults silenciosos (`broker_connected=false` y KNOWN_EMPTY transportan; UNKNOWN jamás se vuelve KNOWN; atestaciones ausentes transportan; KNOWN sin ambos conteos ⇒ rechazo); publicación síncrona `PublishSync` sobre el `messaging.Producer` inyectado del handler (cero producers nuevos, cero consumidores) en `domain.Topics.ReferenceReadback`; key determinista = `source_event_id` (identidad producer-assigned §7.1; el retry del mismo hecho converge a la misma key; IDs preservados en el payload; sin IDs artificiales); fallo Kafka ⇒ error propagado con telemetría `RecordKafkaError`; cero dedupe/cache en Bridge (append-only + dedupe = Gateway/PostgreSQL T09). **Tests (27 nuevos, `reference_readback_emitter_test.go`):** los 7 canónicos TASKS + 20 negativos/de transporte, todos manejando `handler.handleMessage` con el camino real (hook → parse → coherencia → build → Validate → publish) y fake Kafka `mocks.MockProducer` (inspección topic/key/payload/cantidad + `PublishSyncErr`); fixture = espejo byte del golden CONTRACT T09b (`referenceStatusZeroOrderPayload`, `sdk/domain/messages_test.go`), a su vez contrastado contra la estructura de campos del builder MQL real `reference_v3.mq5`/`.mq4`; limitación declarada: NO existe snapshot capturado de un collector real en ejecución ⇒ fixture CONTRACT, nunca evidencia PHYSICAL. **Archivos modificados:** `v3/bridge/internal/reference_readback_emitter.go` (nuevo), `v3/bridge/internal/reference_pipe_handler.go` (campos + constructores + case + método; 96 líneas de delta), `v3/bridge/internal/reference_readback_emitter_test.go` (nuevo), TASKS/VERIFICATION — todo allowed; SPEC, `messages.go`, `reference_readback.go`, `snapshots.go`, migration 064, Gateway, Forge, MQL collectors, `go.mod`/`go.sum` diff 0. **Gates:** CONTRACT=PASS; TRANSPORT=PASS (fake Kafka); SOURCE=PASS (delta Go inspeccionado: sin OrderSend, sin topics de comandos, sin PG, sin bindings, sin StrategyVersions, sin OBSERVING, sin ACK, sin fabricación de atestaciones — no-effects §8 por código y tests); `bridge/internal` PASS plain (0.474s) y `-race` (1.885s) con 27/27; `sdk/domain` `-race` PASS (1.364s); vet OK (sdk/domain + bridge/internal + sdk/messaging); builds sdk+bridge+gateway OK (go.work); failing sets `sdk+bridge+gateway` `-count=1` idénticos por nombre al baseline aprobado `891ab1d8` corrido en worktree aislado `/tmp/echo-e06-baseline-t10` (worktree eliminado post-comparación; colateral preexistente en AMBOS lados: `TestAutomationHandler_HandleMessage_ValidAction`, `TestScratch_QueryDB`, `TestSeedEchoConfig_Production`, `TestVerifyDevJaegerReceivesTraces`) ⇒ cero regresiones. PHYSICAL=PENDING GAP-ECHO-006; MQL_COMPILE_C2=NOT_RUN (deuda heredada de T09b C2, no abordada por T10). Commit `35db8b67b64c154579399310cd149ae01c2c786b`, push FF `891ab1d8..35db8b67`, sin merge/PR/master; post-push: parent exacto `891ab1d8`, HEAD==origin, worktree limpio. Entidad/bitácora actualizadas, agent_run `2026-09-17-zcode-glm-5.3-flash-e06-t10-bridge-emitter` y change_log `2026-09-17-e06-t10-bridge-emitter` registrados. E-06 NO CLOSED; T11+ no iniciados. **Siguiente gate: Manager review T10 → autoriza T11 (Gateway consumer → OBSERVING).**
- **2026-09-17 (NORMAL T09b correction C2 — expiración rollover-safe fail-closed + vigencia sólo de slots realmente incorporados, `E06_T09B_C2_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` @ `fc2d4e5d37d04f2e535e80d5f34fda1d34324fe2` == origin, parent `4009db7a`, worktree limpio. **C2-1:** causa — `(long)(GetTickCount() - g_PendingStatusBuiltMs)` con `g_PendingStatusBuiltMs` en `ulong` promovía la resta a 64 bits; bajo el wrap de `GetTickCount()` (`uint` 32 bits, ~49,7 d) la conversión a `long` producía edad negativa ⇒ el gate no descartaba y el pendiente retransmitía evidencia vencida (mismo riesgo en `g_LastStatusMs`: resta `ulong` gigante bajo el wrap falsaba la cadencia). Cambio exacto — ambas marcas pasan a `uint`; edad = resta modular 32 bits `(long)(nowTick - g_PendingStatusBuiltMs)` (el wrap ocurre en dominio `uint` donde es exacta para tramos < 2^32 ms; la conversión a `long` se aplica al resultado, jamás a un underflow de 64 bits); ancla gruesa nueva `g_PendingStatusBuiltGmtS = (long)TimeGMT()` seteada junto al instante ms; elegibilidad completa antes del bloque de retry con `discardPending = coarseS < 0 || coarseS*1000 > remain || ageMs >= remain` — el grueso SÓLO descalifica (retroceso, o brecha mayor que la ventana residual: suspensión/desconexión larga incl. ≥ un ciclo completo) ⇒ fail-closed, jamás extiende vigencia; deadline con `>=` (edad == restante descarta, inmediatamente inferior conserva); descarte sin transmisión y sin `return` (cae al snapshot nuevo respetando cadencia); cadencia modular `now - g_LastStatusMs < (uint)ECHO_REF_STATUS_INTERVAL_MS`; gate nuevo build→primera escritura `buildAgeMs >= oldestRemainMs ⇒ WARN + return` sin refrescar GVs ni tocar el JSON; remain<0 ⇒ sin gate de frescura (zero-order nunca bloqueado). Prueba de rollover (caso obligatorio): construido en `4294967000` (296 ms antes de 2^32); retry tras el wrap con edad modular 396 ms ⇒ mismos bytes/id; mismo cruce con edad real 16 s (`now=15704`, modular 16000) ⇒ descartar, mientras `ageMsBuggy` (int64(uint64(now)-uint64(built))) da negativo y habría dejado pasar el hecho vencido — bypass eliminado demostrado; cadencia: `cadenceDue(4294967000, 200)` espera (496 ms) y `(4294967000, 6000)` emite; la resta del defecto habría emitido snapshot extra tras el wrap. **C2-2:** ubicación final — en `EchoAttestV1_Scan` (ambos EchoCommon) el loop de lectura sólo guarda `slotRemainMs = max(0, 15000 − (ageS+1)·1000)` (pin C1 del descuento intacto, tras el check de ts futuro) y el mínimo `outOldestRemainMs` se aplica ÚNICAMENTE dentro de la rama de incorporación, después de la correlación `chart_ref ∈ charts[]` del mismo snapshot y del límite de capacidad: `outProgram[outCount]/outChartRef[outCount]/outMagic[outCount] += …; outCount++; if(outOldestRemainMs < 0 || slotRemainMs < outOldestRemainMs) …`. Pruebas de inclusión: 1 slot incluido ⇒ su vigencia; 2 incluidos ⇒ mínimo correcto; expirado/omitido no participa aunque su restante sería menor; chart ausente del inventario no participa; rechazado por capacidad no participa; cero emitidos ⇒ `-1`; incluida a 14 s ⇒ vigencia 0 que bloquea el retry; exactamente 2 asignaciones al mínimo (sentinel + incorporación real); scan byte-idéntico MT5/MT4 (y tick también, diff awk). **Archivos modificados:** `v3/clients/mt5/reference_v3.mq5`, `v3/clients/mt4/reference_v3.mq4`, `v3/clients/mt5/EchoCommon.mqh`, `v3/clients/mt4/EchoCommon.mqh`, `v3/sdk/domain/messages_test.go`, TASKS/VERIFICATION — todo allowed; SPEC, `messages.go`, stores, migration 064, Gateway, Bridge, Forge, `go.mod`/`go.sum` diff 0. **Gates:** CONTRACT+SOURCE PASS — `sdk/domain` PASS plain (0.031s) y `-race` (1.139s) con 3 tests C2 nuevos + 11 T09b + 4 C1 (pin C1 del mínimo actualizado a `slotRemainMs < outOldestRemainMs`, misma semántica con placement C2); pines SOURCE de tipos (`uint` declaraciones) y operación temporal efectiva; tokens prohibidos = 0; identidad C1-1 sin regresión. MQL_COMPILE=**NOT_RUN**: host autorizado `mt5-kronos.lab.aranea.cl` alcanzable a red (192.168.31.128, ping OK) pero publickey denegada para todas las identidades locales (`id_ed25519` ofrecida y rechazada; `id_ed25519_hermes_ariadna` rechazada; agente SSH vacío), sin credenciales en sesión, sin capability MCP del host Windows, sin wine/MetaEditor local; la key temporal de C1 fue revocada y verificada post-uso y reprovisionarla no es autoautorizable ⇒ bloqueo documentado, jamás PASS inventado; mitigación declarada: el delta usa sólo construcciones ya compiladas en C1 en estos mismos archivos (declaraciones `uint` del scanner, cast `(long)TimeGMT()`, `IntegerToString(long)`); SHA256 de fuentes C2: mq5 `371eee14…`, mq4 `63ba26e3…`, EchoCommon.mq5 `2b303b6c…`, EchoCommon.mq4 `268aaa03…`, messages_test.go `a758dd32…` (sin binarios nuevos — los ex5/ex4 de C1 NO se reutilizan como evidencia C2). PHYSICAL=PENDING / GAP-ECHO-006. Regresión: vet OK, builds sdk/gateway/bridge OK, failing sets skip-mode `bridge/gateway/sdk` idénticos por nombre al baseline puro `fc2d4e5d` en worktree aislado `/tmp/echo-e06-baseline-c2` (`TestAutomationHandler_HandleMessage_ValidAction`, `TestSeedEchoConfig_Production`, `TestScratch_QueryDB`, `TestVerifyDevJaegerReceivesTraces`) ⇒ cero regresiones. Commit `891ab1d880846e89d96e2e45dedd25a91faad214`, push FF `fc2d4e5d..891ab1d8`, sin merge/PR/master; post-push: parent exacto, HEAD==origin, worktree limpio. E-06 NO CLOSED; T10/T11+ no iniciados. **Siguiente gate: Manager review C2 T09b → autoriza T10 (Bridge emitter).**
- **2026-09-17 (NORMAL T09b correction C1 — identidad durable fail-closed + retry sólo con atestaciones vigentes, `E06_T09B_C1_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` @ `4009db7ab7ecabe86dcf9816e8d9a76369a7e990` == origin, parent `32332c04`, merge-base master `5dd998f1`, worktree limpio. **C1-1:** `ReferenceLoadOrCreateCollectorIdentity` reescrita con tres estados inequívocos — `FileIsExist(fname)` antes de cualquier `FileOpen` (A: ausencia demostrada ⇒ nacimiento `GenerateUUIDv7`/epoch 0+1; B: existente ilegible ⇒ ERROR fail-closed sin crear otro id ni sobrescribir; C: presente corrupto ⇒ ERROR fail-closed sin regeneración); `ReferenceValidUUIDv7` (36 chars, guiones 8/13/18/23, hex lowercase, versión '7', variante 8/9/a/b) y `ReferenceValidEpochText` (sólo dígitos, sin cero a la izquierda salvo "0", tope 18 dígitos ⇒ overflow imposible; `StringToInteger` = 0 en el path — recortaba prefijos y saturaba) + `FileIsEnding` tras línea 2 ⇒ campos extra no autorizados rechazados y línea faltante cae en rechazo; persistencia verificada (resultado de cada `FileWrite` > 0, `FileFlush`, `FileClose`, relectura completa con id+epoch idénticos y sin extras antes de `g_RefIdentityDurable=true`); intento único por OnInit — `ReferenceStatusTick` jamás llama al loader (sin identidad durable ⇒ sin status toda la sesión; epoch incrementa exactamente una vez por sesión). **C1-2:** `EchoAttestV1_Scan` añade `outOldestRemainMs` (init −1; mínimo sobre slots EMITIDOS tras todos los checks; `remain = max(0, 15000−(age_s+1)·1000)` — descuento conservador de la resolución de 1 s de `ts`, el techo plano de 15 s desde el build era insuficiente con atestación de 14 s al capturarse); builder lo retorna vía `outOldestRemainMs` y el tick guarda `g_PendingStatusBuiltMs`+`g_PendingStatusAttestRemainMs` fuera del JSON contractual (cero claves wire nuevas — claves del payload congeladas por test); el check `g_PendingStatusAttestRemainMs >= 0 && edad ≥ remain` precede a cualquier `PipeWriteLn(g_PendingStatusMsg)`: hecho vigente ⇒ mismos bytes/mismo `source_event_id` con `observed_at`/`ts` intactos; vencido ⇒ WARN, descarte sin transmitir y caída al flujo normal (snapshot siguiente = hecho físico nuevo con id nuevo); hecho sin atestaciones (remain −1) ⇒ no caduca por vigencia; paridad MT5/MT4 byte-identical (diff de funciones). **Tests (4 nuevos, `sdk/domain` PASS plain/`-race`):** `TestSOURCE_C1_IdentityFileStatesFailClosed` (orden FileIsExist→lectura→salidas→mint→write; mint exactamente 1), `_IdentityStrictValidators` (guards pineados + tablas espejo ejecutables: 7 UUID inválidos, `12abc`/`007`/`1.5`/19 dígitos/`+5`/`-1`/vacío ⇒ rechazo), `_IdentityDurableOnlyAfterReadback` (2 aperturas de lectura, orden mismatch→durable, 1 llamada en OnInit, 0 en el tick), `_RetryEligibilityRespectsOldestAttestation` (fórmula pineada tras el check de ts futuro; caducidad antes de retransmisión; bloque de descarte sin `return;`; claves congeladas; tabla espejo: 14 s+retry 3 s ⇒ descartar, fresco+3 s ⇒ mismo hecho, 14000 ms ⇒ descartar, emitido a 15 s ⇒ restante 0, sin atestaciones ⇒ no caduca) — pines SOURCE + tablas espejo, no sustituyen la ejecución del productor. **MQL_COMPILE=PASS (primera del proyecto):** host Windows de laboratorio autorizado `mt5-kronos.lab.aranea.cl` (el de evidencia física E-03; sin deploy, sin lanzar terminales, sin estrategias ni órdenes); fuentes subidos byte-exactos vía SFTP (SHA256 verificados en destino: mq5 `130f1466…`, mq4 `a1a2a3c2…`, includes Echo×7 reales por plataforma + stdlib del terminal de referencia `C:\MT{5,4}\e03-correction-1`); layout terminal `MQL{5,4}\Experts|Include` con `/inc` a la raíz MQL; comandos `/compile:… /inc:… /log:…` con `Start-Process -Wait`; compiladores MetaEditor64 5.0.0.6090 y metaeditor 5.0.0.2418; resultados: MT5 **0 errors/1 warning** (w43 `ulong→long` EchoPersistence:101, preexistente fuera del delta; 11272 ms; exit 1; `reference_v3.ex5` 203598 B sha256 `11eecbeb74ce365faad50f6e811ab011527818b8f37e7a4638a8939e2b3afae4`) y MT4 **0 errors/1 warning** (w60 posible `chunks` no inicializada, patrón preexistente del scanner baseline — se llena/lee sólo hasta K; 148 ms; exit 1; `reference_v3.ex4` 197080 B sha256 `fdb38d96356cafdf8e16a06637ce94cbefb14bce9de3696b637c34b751c90b7a`); logs completos y workspaces de compilación quedan como evidencia en el host (`C:\MT5\e06-t09b-c1\`, `C:\MT4\e06-t09b-c1\`); key SSH temporal instalada para la transferencia REVOCADA y verificada post-uso. **Sin probar expresamente (sin runtime MQL ejecutable; PHYSICAL GAP-ECHO-006):** los tres estados reales del archivo de identidad, la relectura física de persistencia y el retry contra GVs reales de slots caducando. **Regresión:** builds sdk/gateway/bridge OK; vet OK; failing sets de `sdk/... bridge/... gateway/...` idénticos por nombre al baseline `4009db7a` corrido en worktree aparte (colateral preexistente automation/seed/scratch/jaeger); `go.mod`/`go.sum` delta 0. Commit `fc2d4e5d37d04f2e535e80d5f34fda1d34324fe2`, push FF `4009db7a..fc2d4e5d`, sin merge/PR/master. E-06 NO CLOSED; T10/T11+ no iniciados. **Siguiente gate: Manager review C1 T09b → autoriza T10 (Bridge emitter).**
- **2026-09-17 (NORMAL WP-C2 — T09b collector `reference_status`, `E06_WPC2_T09B_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `32332c04b9dfb14708099686bc50975f5876b43c` == origin, worktree limpio, parent `1bebb67d`, merge-base `origin/master` = `5dd998f1`. **Emisión zero-order:** `ReferenceStatusTick()` en OnTimer (conectado y recién-conectado) con cadencia nominal `ECHO_REF_STATUS_INTERVAL_MS=5000` — se emite SIEMPRE con sesión pipe activa, incluso 0 posiciones/0 pendientes/`observed_magic=[]` (KNOWN_EMPTY, caso crítico E-06); builder `BuildReferenceStatusMessage` compone §7.2 desde `AccountInfo*/TerminalInfo*/PositionsTotal/OrdersTotal/ChartGetString` + `broker_connected=TERMINAL_CONNECTED` explícito + capacidades `TERMINAL_TRADE_ALLOWED`/`MQL_TRADE_ALLOWED`/`ACCOUNT_TRADE_ALLOWED` bool explícitos + `ea_session_active=IsPipeConnected()` + `observed_at=FormatRFC3339()` (TimeGMT)/`clock_basis=TERMINAL_GMT`/`snapshot_interval_ms=5000`/`terminal_build`/`platform`/`account_role=reference`/`broker_company` auditoría; OnTick intocado, cero interferencia con TradeIntent/Close. **Identidad (SPEC §7.5):** `collector_id` UUIDv7 nacido en el EA (`GenerateUUIDv7`) y contador de `collector_epoch` (persistido + 1 por OnInit) en `echo_reference_v3_mt{5,4}_identity_<account>.txt` — sandbox Files del terminal, mismo almacenamiento local que TradeMapper, sin FILE_COMMON ni PG ni ClientConfig; archivo corrupto (id≠36 chars/epoch<0) o escritura fallida ⇒ `g_RefIdentityDurable=false` ⇒ status deshabilitado fail-closed con reintento por tick (nunca un id inestable); primer arranque genera id nuevo con epoch 1; `source_event_id` nuevo por snapshot; fallo de escritura del pipe retiene los bytes completos en `g_PendingStatusMsg` y los reenvía idénticos (mismo source_event_id, ≤12 reintentos, FIFO del pipe) — snapshot nuevo recibe identidad nueva. **Inventario:** MT5 `position_count=PositionsTotal()`, `pending_count=OrdersTotal()` (pendientes), `observed_magic=POSITION_MAGIC ∪ ORDER_MAGIC` (dedupe, sólo ≥1 — 0 no es magic canónico y T08 lo rechazaría); MT4 recorre `OrdersTotal()` con `OrderSelect` clasificando OP_BUY/OP_SELL (mercado) vs 4 tipos pendientes — jamás confundidos (§10); cualquier fallo de selección ⇒ scan incompleto ⇒ `inventory_completeness=UNKNOWN` con conteos ausentes (pointers) y sin array parcial (jamás parcial como KNOWN); `charts[]` MT5 vía `ChartFirst/ChartNext` (`chart_id` decimal con signo, symbol, `period=EnumToString`, `expert_name=CHART_EXPERT_NAME`) y MT4 V1 chart actual + `WindowExpertName()` (SPEC §7.2); guard 4096/techo 128 con truncamiento ⇒ attestations omitidas (correlación no demostrable). **Scanner §7.2a read-only compartido (EchoCommon, `EchoAttestV1_Scan`):** filtra grammar exacto (split 6 partes, fields literales `c/mh/ml/nl/ts/sh/n0..n15` con `EchoAttestFieldKind` que rechaza `n01`/`n16`/desconocidos, hex16 lowercase exacto), descarta slots de otra cuenta (`acct16` ≠ `ToHex((ulong)ACCOUNT_LOGIN,16)`), lectura consistente c1→payload(mh/ml/nl/ts/sh/n0..nK con K=ceil(nl/4)-1)→c2 con 3 reintentos ante torn read (`c2≠c1` ⇒ reintento, sigue ⇒ omitir), dominios enteros exactos en double (`c` par [2,2^53), mh/ml/sh [0,2^32), nl [1,63], ts ≥0), `sh` == `EchoFnv1a32(ACCOUNT_SERVER observado)` (leftover ajeno omitido), vigencia `age_s = TimeGMT() - ts` con `age_s<0` futuro omitido y `age_s*1000 > 15000` expirado omitido (ventana congelada `#define ECHO_ATTEST_FRESHNESS_MS 15000`; jamás la hora de acceso de la GV — token = 0 en el delta), magic lossless `u=(uint(mh)<<32)|uint(ml)` con `u==0 || u>MaxInt64` omitido y decimal por `IntegerToString((long)u)` (jamás double), program_name desde chunks big-endian (`byte j = (cu >> 8*(3-j)) & 0xFF`) con padding fuera de nl obligado a cero, sin NUL, sin `/` ni `\`, sin control (<0x20/0x7F), `EchoUtf8Valid` RFC 3629 estricto (sin overlongs/surrogates), `chart_ref` decimal con signo de los bits del ChartID y gate `chart_ref ∈ charts[]` del MISMO snapshot; sin slots live ⇒ `runtime_attestations=[]` transporte válido. **MT4:** sin slots válidos ⇒ array vacío (las GV de MT5 no son visibles desde MT4; fail-closed sin simular paridad con config). **Go parser:** `messages.go` añade `ReferenceStatusPayload` (26 campos físicos §7.2, sin `account_registration_ref`/`received_at`/`contract_version` que son del envelope — transformación de T10) y `ParseReferenceStatus` en el patrón `PipeMessage` existente, reutilizando `RuntimeAttestation`/`ReadbackChart` de T08 sin duplicar el envelope; preserva bool false (tri-state `*bool`), arrays vacíos, magic como decimal string y pointers para epoch/conteos/build (ausencia ≠ cero); sin timestamps Bridge, sin derivar registration, sin crear atestaciones; `contracts/wire` diff 0. **Tests (11 nuevos en `messages_test.go`, `sdk/domain` PASS plain y `-race`):** `TestParseReferenceStatus_ZeroOrder` (payload completo 0/0/[] con atestación de magic máximo `9223372036854775807` + `chart_ref`, golden re-marshal), `_NoAttestationsAndNoSynthesis` ([] y ausente, cero entradas sintetizadas), `_PartialSlotOmitted` (output post-omisión parsea), `TestAttestation_TicklessWindow_SlotExpires` (AC-37a CONTRACT: pin SOURCE de `#define ECHO_ATTEST_FRESHNESS_MS 15000` + fórmula `TimeGMT() - (long)(datetime)ts` + comparación en el scanner real de AMBAS plataformas + tabla de decisión −1/0/15/16 + payload post-expiración vacío), `_TwoChartsSameExpertName` (mismo program_name, chart_ref distinto, sin dedupe), `_MagicLosslessNeverFloat` (string exacto, sin float), `_SourceEventIdentity` (replay conserva id, snapshot nuevo ⇒ id nuevo), `_NoConfigBackfillChannel` (re-marshal sin ninguna key de config; attestations exactamente las 3 claves físicas), `_CapabilitiesAndUnknown` (false conservado como false; UNKNOWN preserva nil, jamás cero, no se convierte en KNOWN), `TestSOURCE_CollectorNoAttestWritesOrTrading` (tokens `OrderSend`/`GlobalVariableSet`/`GlobalVariableDel`/`GlobalVariableTime` = 0 en los 4 MQL; `g_ConfigNativeMagic` clavado al baseline 6/6 por collector — uso nuevo rompe el pin; builder/scanner sin referencias a config/`GetEffectiveMagicNumber`/Version) y `TestSOURCE_CollectorIdentityPersistedInProducer` (FILE_TXT, `GenerateUUIDv7`, `prevEpoch + 1`, sin FILE_COMMON) — pines SOURCE ejecutables en tests Go, no greps sueltos. **Gates de validación:** `go test`/`go test -race ./v3/sdk/domain/` PASS; `go vet` OK (sdk/gateway/bridge); builds sdk/gateway/bridge OK; failing sets skip-mode de `bridge/... gateway/... sdk/...` idénticos por nombre de test al baseline puro `32332c04` corrido en worktree aparte `/tmp/echo-e06-baseline-32332c04` (colateral preexistente sin servicios: `TestAutomationHandler_HandleMessage_ValidAction`, `TestSeedEchoConfig_Production`, `TestScratch_QueryDB`, `TestVerifyDevJaegerReceivesTraces`) ⇒ cero regresiones; `MQL_COMPILE=NOT_RUN` (sin MetaEditor/wine en el entorno, verificado `which`; riesgo residual de sintaxis MQL declarado, no certificado); `PHYSICAL=PENDING` (GAP-ECHO-006: sin terminal MT5/MT4 ni Version Forge con inyección §7.2a; goldens y pines son CONTRACT/SOURCE, nunca PHYSICAL — AC-32). **SOURCE:** delta vs `32332c04` = 4 archivos MQL allowed + `messages.go` + `messages_test.go` (nuevo) + TASKS/VERIFICATION, todos allowed; migration 064, T08/T09, Gateway, Bridge, contracts, ClientConfig, core, hasura, Execution/seed, Forge diff 0; `go.mod`/`go.sum` delta 0; `TEST_CHANGE_REQUEST.md` intacto. Commit `4009db7a`, push FF `32332c04..4009db7a`, sin merge/PR/master. E-06 NO CLOSED; T10/T11+ no iniciados. **Siguiente gate: Manager review T09b → autoriza T10 (Bridge emitter).**

- **2026-09-17 (NORMAL WP-C1 corrección C1 — audit fix de completitud del envelope, `E06_WPC1_C1_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `1bebb67dd6d38c70b6a4616a4fa05e3d6472846b` == origin, worktree limpio, parent `cdb0c163`, merge-base `origin/master` = `5dd998f1`. Defecto único del Manager corregido sin reiniciar T08/T09, sin avanzar T09b y sin rediseñar digest ni persistencia. **C1-A broker_connected explícito:** `Validate()` exigía solamente no-negatividad si el campo venía presente, así un envelope sin `broker_connected` (nil/ausente en el wire) validaba — el SPEC §7.2 declara el bool explícito de `TERMINAL_CONNECTED`; ahora `BrokerConnected == nil` ⇒ `ErrInvalidReadback` con mensaje que nombra las fuentes de inferencia prohibidas (`ea_session_active`, heartbeat, ClientConfig); true y false válidos — false es un hecho físico legítimo que no implica OBSERVING ni bloquea el envelope (eso lo decide T11; el dominio no conoce bindings) — y el round-trip demuestra que false viaja literal `"broker_connected":false` (omitempty no lo descarta por ser puntero no-nil). **C1-B KNOWN exige inventario declarado:** con `inventory_completeness=KNOWN` ahora exige `position_count != nil` y `pending_count != nil`, ambos >= 0 — cero válido, nil jamás se convierte en cero (rechazo con field culpable: ambos nil ⇒ error de `position_count`, sólo posiciones ⇒ error de `pending_count`, sólo pendientes ⇒ error de `position_count`); con UNKNOWN los conteos pueden ausar (transporte preservado) y los presentes siguen sujetos a no negatividad en ambos estados; sin valor nuevo del enum — KNOWN_EMPTY sigue siendo la representación `KNOWN + position_count=0 + pending_count=0` (comentarios del struct actualizados para decirlo explícito). **C1-C tri-state intacto:** `terminal_trade_allowed`/`ea_trade_allowed`/`account_trade_allowed` sin cambios — `TestReadback_CapabilityTriState` pasa sin modificación (true/false/nil distinguibles, ausencia ≠ true, nada invalida el envelope). **Tests:** +2 obligatorios (`TestReadback_RequiresBrokerConnected`, `TestReadback_KnownRequiresBothCounts` con los 7 casos del mandato) y +3 casos en `TestReadback_ValidateRequiredFields` (`broker_connected presente`, `known exige position_count`, `known exige pending_count`); 14 tests T08 en domain. **Fixtures:** `fixtureReadback` (domain) y `fixtureStoreReadback` (postgres, nuevo helper `storeBool`) declaran `broker_connected` físicamente. **Golden digest:** el campo explícito cambia los bytes canónicos de la vista ⇒ recalculado con sonda temporal Go (borrada) y verificado de forma independiente — vista reconstruida A MANO en Python sin salida Go: bytes idénticos 748 = 748 y `sha256sum` CLI ⇒ `sha256:4677c1756274b9b8ebafcdfc7d4e76db65cda855f3afe20305c75a820e5e840a` reemplaza `sha256:8b7f5127…`; la receta no cambia (mismos campos de vista, misma C(), `source_event_id` sigue excluido; el campo ya estaba en la vista desde T08 — el golden cambió porque el FIXTURE cambió, no la función). **PG REAL:** PostgreSQL 17.11 descartable (Zonky 17.11 + PGDG psql 17.9, cluster efímero `/tmp/e06-pg17/data-wpc1c1` recién initdb'd, cero SHARED DEV/PROD): `run.sh` PASS completo ×2; `sdk/domain` PASS plain y `-race`; T09 5/5 PASS `-race` (`TestReadback_ReplaySameReceivedAt`, `TestReadback_Dedupe` a/b/c, `TestReadback_DigestCollisionFailClosed`, `TestReadback_AppendOnly` 6 UPDATEs + DELETE, `TestReadback_KnownEmptyPersistsEmptyMagic`). **Regresión:** failing sets de paquetes completos idénticos por nombre de test al baseline puro `1bebb67d` capturado en este mismo worktree ANTES de editar, con el mismo PG — `sdk/postgres` 53=53 (colateral identity_bwc + `TestScratch_QueryDB`) y `gateway/internal` 30=30 (colateral Forge-ingest clase TC-06-1 + `TestForgeIngest_CertPack` + `TestAutomationHandler_HandleMessage_ValidAction`) ⇒ cero regresiones; nota: el fallo de automation en skip-mode fue reproducido idéntico en worktree aparte al commit puro `1bebb67d` (colateral preexistente ambiental, no introducido por C1; con `DATABASE_URL` el paquete entero corre y su set coincide). **SOURCE:** delta vs `1bebb67d` = 3 archivos productivos/tests + TASKS/VERIFICATION, todos allowed; `reference_readback_store.go` diff 0; migration 064, Gateway, Bridge, MQL, contracts, core, ClientConfig diff 0; `go.mod`/`go.sum` delta 0; tokens prohibidos en el delta = 0; `TEST_CHANGE_REQUEST.md` intacto. Commit `32332c04b9dfb14708099686bc50975f5876b43c`, push FF `1bebb67d..32332c04`, sin merge/PR/master. E-06 NO CLOSED; T09b/T10/T11+ no iniciados. **Siguiente gate: Manager review de la corrección C1 WP-C1 → autoriza T09b (collector `reference_status` MQL).**

- **2026-09-17 (NORMAL WP-C1 — T08/T09 read-back contract + store, `E06_WPC1_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `cdb0c16384b0373a8c641c6369c6a5757246ac96` == origin, worktree limpio, parent `cd2aca674a085cb9912949620e73dc85e012afcd`, merge-base `origin/master` = `5dd998f1`. **T08:** `v3/sdk/domain/reference_readback.go` con el payload mínimo §7.2 v1.2.2 completo (27 campos congelados; `recorded_at` excluido como autoridad producer — commit PG en T09; sin lifecycle de binding). `account_registration_ref` por la receta `H("echo-account-registration.v1",[platform,broker_server_ref,account_login])` con `wire.HashTagged` sobre lo observado — Validate exige match exacto y rechaza un ref derivado de `ACCOUNT_COMPANY`/catálogo/ClientConfig; goldens MT5 `sha256:055642d0…` y MT4 `sha256:7ecd6d46…` verificados con sha256sum independiente y permutación de campos no convergente. `RuntimeAttestation` = exactamente `{program_name, chart_ref, magic_decimal}`: program_name 1..63 bytes sin NUL/`/`/`\`, `chart_ref` decimal int64 con signo obligatorio (misma representación que `charts[].chart_id` para la correlación §7.2a.3), `magic_decimal` canónico 1..MaxInt64; slots parciales/malformados que lleguen al envelope se rechazan fail-closed (el collector los omite). Helper `RebuildAttestationMagic(mh, ml uint32)` por bits uint64 (resultado 1..MaxInt64, decimal vía entero, jamás float64): round-trip del máximo `2147483647/4294967295 → 9223372036854775807` demostrado y 2^63 rechazado aunque exactamente representable en double. Capability `terminal_trade_allowed`/`ea_trade_allowed`/`account_trade_allowed` (+`broker_connected`/`ea_session_active`) como `*bool` con omitempty: true/false/ausente distinguibles en JSON, ausencia ⇒ nil (UNKNOWN), jamás true, y ningún valor bloquea el envelope. KNOWN_EMPTY aceptado explícitamente; Validate normaliza nil→vacío en los tres arrays (patrón `validatePrepared` con el proof `{}`) y JAMÁS exige magic del binding, attestations no vacías, casación program↔Version ni decide OBSERVING (T11). **Digest:** convención SHA-256 del repo sobre el canonical echo-wire-json.v1 (`"sha256:"+hex(sha256(C(view)))`, análoga a `attachRequestDigest` del enrollment), cubriendo TODO el payload contractual MENOS `source_event_id` — identidad de transporte — para que el UNIQUE(payload_digest) de 064 converja el mismo hecho re-emitido bajo otra identidad (§7.1 Dedupe + §14 del mandato); sin dependencia de recorded_at/match_status/matched_binding_id/metadata PG (no existen en el dominio); golden `sha256:8b7f512767fc443ca5768812113a95245a2cb84bc9c73dcd609a1a44b41de5f9` derivado por sonda temporal y verificado byte-exacto con sha256sum independiente; `TestReadback_DigestViewKeys` demuestra por key sets que la vista = envelope − source_event_id. Topic `echo.reference-readback.v1` añadido como única línea al catálogo existente `domain.Topics` (`snapshots.go`), sin segundo catálogo ni producer/consumer. **T09:** `v3/sdk/postgres/reference_readback_store.go` — `Insert(ctx, *domain.ReferenceReadback) (*ReferenceReadbackRecord, created bool, error)`: valida, computa digest/payload canónicos y hace INSERT puro sobre `echo.reference_readbacks` con las 29 columnas denormalizadas de 064; `received_at` viaja en el payload (primer receipt Bridge — jamás `time.Now()` del store), `recorded_at` es el default `now()` del commit PG, `match_status` el default UNBOUND; MATCH_FAILED durable queda habilitado por el schema (el trigger append-only permite mutar sólo la atribución) sin API prematura — la lógica de atribución real llega con T11. **Dedupe doble §14:** (a) first insert ⇒ una fila `created=true`; (b) replay exacto (mismo source_event_id + mismo digest) ⇒ fila del winner sin mover received_at/recorded_at/payload/match state; (c) mismo digest bajo otra source_event_id ⇒ converge SOLO si el payload canónico persistido es byte-idéntico al nuevo menos source_event_id (`readbackPayloadEqual` compara formas canónicas con `UseNumber` — re-hashear sería circular), colisión con payload distinto ⇒ `CONTRACT_CONFLICT` fail-closed; (d) misma identidad con payload distinto ⇒ `CONTRACT_CONFLICT`; resolución determinista del winner: primero por source_event_id, sólo si no existe por digest. **Tests:** T08 — 12 tests nuevos (`TestReadback_RegistrationRecipe`, `TestReadback_RejectsEmptyEpoch`, `TestReadback_EmptyMagicKnownEmptyAccepted`, `TestReadback_AttestationsOptional_NoSynthesis`, `TestAttestation_MaxMagicRoundTrip`, `TestAttestation_RejectsTruncatedOrPartial`, `TestAttestation_MagicNeverViaDouble` + `TestReadback_ValidateRequiredFields`, `TestReadback_CapabilityTriState`, `TestReadback_PayloadDigestGolden`, `TestReadback_DigestViewKeys`, `TestReadback_TopicCatalog`); T09 — 5 (`TestReadback_ReplaySameReceivedAt`, `TestReadback_Dedupe` a/b/c, `TestReadback_DigestCollisionFailClosed` con INSERT SQL directo de digest reutilizado rechazado por el UNIQUE + comparador Go byte-a-byte, `TestReadback_AppendOnly` con 6 UPDATEs + DELETE rechazados por triggers 064 sin tocar la migration, `TestReadback_KnownEmptyPersistsEmptyMagic` con relectura física exacta). Dos bugs propios detectados por los propios gates y corregidos: digest no idempotente si el caller no validaba antes (vista de digest ahora normaliza nil→vacío por sí misma) y caso de test con `(MaxUint32, 0)` clasificado erróneamente como válido (0xFFFFFFFF00000000 > MaxInt64 — la función lo rechazaba correctamente). **PG REAL:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero `/tmp/e06-pg17/data-wpc1` recién initdb'd, cero SHARED DEV/PROD): `run.sh` PASS completo ×2; T09 5/5 PASS `-race` con `DATABASE_URL`; `sdk/domain` PASS plain y `-race`; failing sets idénticos por nombre de test al baseline puro `cdb0c163` corrido en worktree aparte con el mismo PG (`sdk/postgres` 53=53, `gateway/internal` 29=29, todo colateral TC-06-1 + `TestScratch_QueryDB`) — cero regresiones; skip-mode PASS (gateway PASS completo, T09 skip); `go vet` OK; builds sdk/gateway/bridge OK — Gateway/Bridge compilan sin modificaciones. **SOURCE:** delta vs `cdb0c163` = 4 archivos productivos nuevos + `snapshots.go` (sólo el topic) + TASKS/VERIFICATION, todos allowed; migrations 061–064, binding store, gateway, Bridge, MQL, Forge, core, contracts, hasura diff 0; `go.mod`/`go.sum` delta 0; tokens prohibidos en el delta = 0 (las únicas menciones de `time.Now()` son comentarios de prohibición); sin OrderSend, sin commands producer, sin transición a OBSERVING; TC-06-1/`TestScratch` siguen fuera de scope; `TEST_CHANGE_REQUEST.md` intacto. Commit `1bebb67dd6d38c70b6a4616a4fa05e3d6472846b`, push FF `cdb0c163..1bebb67d`, sin merge/PR/master. E-06 NO CLOSED; T09b/T10/T11+ no iniciados. **Siguiente gate: Manager review WP-C1 → autoriza T09b (collector `reference_status` MQL).**

- **2026-09-17 (NORMAL WP-B corrección C1 — audit fix de T05/T06, `E06_WPB_C1_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `cd2aca674a085cb9912949620e73dc85e012afcd` == origin, worktree limpio, parent `1614028b`, merge-base `origin/master` = `5dd998f1`. Los 4 defectos del Manager corregidos sin reabrir el SPEC v1.2.2 ni las decisiones ya aceptadas de WP-B (T04 ClientConfig, auth E-02, GETs, MT4 magic, map_revision, operator suspend, drain, close, config publish, envelope local, zero commands); `client_config.go`/`client_config_test.go`/`server.go` sin tocar. **C1-1:** `proof.recorded_at` era aceptado desde HTTP y copiado a `PreparedBinding.ProofRecordedAt`, dejando un reloj del caller dentro del watermark `max(proof.recorded_at, capture_barrier_at)`; ahora `clientProofRecordedAt` rechaza su presencia en el proof JSON crudo en la fase estructural (400 INVALID_INPUT `proof.recorded_at`, cualquier valor incluido null, análogo a `forbiddenLifecycleField`) y el Gateway sella `ProofRecordedAt = h.now().UTC()` exactamente una vez en la primera creación PREPARED; en replay el store devuelve la fila del winner con su sello original — `preparedSealedEqual` ya no participa con `proof_recorded_at` (metadata server-generated; los campos realmente declarados por el caller siguen comparándose todos, incluido el proof JSON) y un re-sello de retry jamás fabrica CONTRACT_CONFLICT; migration 064 sin cambios. Goldens §17.1 recalculados porque el proof fixture ya no contiene `recorded_at` (attach digest `sha256:40fc5cf7…`, key `sha256:a7f25959…`), derivados byte-exacto del canonical echo-wire-json.v1 real impreso por una sonda temporal del paquete (borrada) y verificados en Python/sha256sum independiente — la receta y la canonicalización no cambian. **C1-2:** `handleAck` revalida el catálogo con `lookupAccount(binding.AccountID)` antes del primer `PREPARED + acked_at NULL → ACK`: cuenta ausente ⇒ 404 NOT_FOUND `account_id`; `role≠reference` ⇒ 409 IDENTITY_CONFLICT `account_id` sin `acked_at`; fallo operacional del lookup ⇒ 503 UNAVAILABLE retryable sin mutación; el replay de un ACK ya committeado salta revalidaciones (200, `acked_at` original — sin reescritura retrospectiva); el role drift posterior al ACK/OBSERVING no se implementa aquí (path system T14 futuro). **C1-3:** el handler hacía GET(binding_id) → decide `replay` ANTES del INSERT, así dos POST idénticos podían ver ambos NOT_FOUND y terminar 201+201; eliminado el pre-GET como autoridad: nuevo `InsertPreparedWithOutcome(ctx, p) (*ReferenceBinding, created bool, error)` entrega el outcome en la misma operación atómica — INSERT exitoso ⇒ created=true; PK/`uq_e6_bindings_ref` + sealed-equal ⇒ created=false con la fila existente; body realmente distinto ⇒ `ErrContractConflict`; `InsertPrepared` queda como wrapper (WP-A y callers intactos); PostgreSQL UNIQUE sigue siendo la única autoridad (sin cache, sin mutex process-local, sin tabla nueva, sin SERIALIZABLE global, sin sleep/retry). **C1-4:** `validateAckProof` consultaba la Version y cualquier error no-NotFound volvía como 400 INVALID_INPUT; ahora retorna `(field string, err error)` — semánticos (method, hashes ausentes/mismatch, version inexistente) ⇒ 400 con `field_or_ref` preciso; operacionales (PG/query/conexión/contexto) ⇒ 503 UNAVAILABLE retryable, `acked_at` permanece NULL, error PG crudo no expuesto; seam mínimo = campo `ackVersionLookup` en el handler (default `NewStrategyVersionRepository(db).Get`), sin framework nuevo ni capa repository-wide. **Tests:** gateway +7 (`TestEnroll_RejectsClientProofRecordedAt` con futuro/pasado/null y cero fila; `TestEnroll_ServerStampsProofRecordedAt` con reloj inyectado y proof persistido sin recorded_at; `TestEnroll_ReplayPreservesProofRecordedAt` con reloj avanzado 1 h; `TestEnroll_ConcurrentReplay_One201One200` — dos POST concurrentes mismo key/body/binding_id ⇒ exactamente un 201 + un 200, mismo binding_id/binding_ref/prepared_at/proof_recorded_at, una sola fila PG, cero CONTRACT_CONFLICT, publish MAY en ambos; `TestAck_AccountRoleRevalidated`; `TestAck_ReplayDoesNotRewriteOriginalAck`; `TestAck_VersionLookupUnavailable503`) y store +2 (`TestInsertPreparedWithOutcome_CreatedVsReplay`, `TestInsertPrepared_ReplayPreservesServerStamp`); robustez del harness: producer mock con mutex (concurrencia bajo `-race`), seed de cuentas upsert del rol y restore del drift en `t.Cleanup` (el DELETE de cuentas del cleanup falla silencioso por FK con bindings vivos y un drift residual contaminaba el test siguiente — detectado y corregido en esta sesión). **PG REAL:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero `/tmp/e06-pg17/data-c1fix` recién initdb'd, cero SHARED DEV/PROD): `run.sh` PASS completo ×2 (rebuild 001–063, probe, up, aserciones 10, idempotencia, 12, 13 A–G, 14, down fail-closed, down ok, up/down/up final); Go E-06 **17/17** `sdk/postgres` y **22/22** `gateway/internal` PASS con `DATABASE_URL` y `-race`; `sdk/domain` PASS plain/race; `go vet` OK; builds sdk/gateway/bridge OK. **Regresión:** failing set de ambos paquetes completos idéntico por nombre de test al baseline puro `cd2aca6` corrido en worktree aparte con el mismo PG (29=29 gateway, 53=53 postgres; todo colateral preexistente clase TC-06-1 + `TestScratch_QueryDB`) — cero regresiones nuevas; skip-mode idéntico (gateway PASS, postgres sólo `TestScratch_QueryDB`); TC-06-1/TestScratch no arreglados (fuera de scope). **SOURCE:** delta vs `cd2aca6` = 4 archivos productivos + TASKS/VERIFICATION, todos allowed; contracts/migrations 061–064/Bridge/MQL/core/hasura/ClientConfig/server.go diff 0; `go.mod`/`go.sum` delta 0; `TEST_CHANGE_REQUEST.md` intacto. Commit `cdb0c16384b0373a8c641c6369c6a5757246ac96`, push FF `cd2aca67..cdb0c163`, sin merge/PR/master. E-06 NO CLOSED; T08–T22 no iniciados. **Siguiente gate: Manager review de la corrección C1 WP-B.**

- **2026-09-17 (NORMAL WP-B — Enrollment HTTP T04–T07, `E06_WPB_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `1614028b` == origin, worktree limpio, parent `c021983f`, merge-base `origin/master` = `5dd998f1`. **T04:** pin E-06 aditivo en ClientConfig — campos `runtime_binding_id`/`strategy_version_ref`/`magic_decimal`/`map_revision` omitempty; `Validate()` estricta sólo cuando están presentes (`ErrInvalidPinField`: UUID lowercase, `sha256:<64hex>`, decimal canónico 1..9223372036854775807 sin pasar por float); `Clone()` preserva el pin; sin pin la config legacy valida y serializa igual (AC-14). **T05:** handler nuevo `reference_binding_handler.go` montado en `server.go` (POST=config_operator vía authMiddleware E-02; GET=CONFIG|READ vía `referenceReadAuth` con las mismas primitivas; sin PG toda la superficie 503 fail-closed). POST siempre PREPARED vía store 064. **Idempotencia §17.1 determinista:** `attach_request_digest = "sha256:"+hex(sha256(canonical body echo-wire-json.v1))` (keys ordenadas; `proof_recorded_at` viaja DENTRO del proof para que el replay canónico converja) y `Idempotency-Key = H("echo-reference-enroll.v1",[ns,version_ref,account_registration_ref,observation_class,attach_request_digest])`; header ausente ⇒ 400, header ≠ receta ⇒ 409 CONTRACT_CONFLICT (cubre mismo key + body distinto §14); golden cruzado Go/Python/sha256sum (`sha256:bd342f5b…` / `sha256:92f65c7e…`); `binding_id` es campo del body ⇒ se conserva entre retries por PK+digest sellado del store ⇒ **STOP CONTRACT_IDEMPOTENCY no aplicó**; 201 primera creación / 200 replay (GET previo distingue; en carrera de replay idéntico la convergencia la garantiza la PK — nunca segunda fila). Verificaciones pre-persist: version_ref presente y sintáctico 400, Version E-03 existente y correspondiente (canonical/strategy_ref/platform) 409, account catálogo 404, role=reference 409, pin magic 409 (store), map_revision declarado 409 con el correcto aceptado, MT4 ⇒ Version MT4 y magic ≤ int32 sin truncado (§15/AC-24); body con `acked`/`state`/`coverage_started_at` ⇒ 400 INVALID_INPUT (§8). GET by-id; GET by-version lista pura de todas las filas (`ListByVersion`, expansión mínima autorizada, sin latest implícito). **T06:** ACK valida proof `MANUAL_VERIFIED` + hashes EX5/inputs/deps == fila Version E-03 (400 con field culpable); `acked_at` seteado, PREPARED intacto, watermark NULL; replay conserva `acked_at` (AC-02). **T07:** drain sella `accepts_opens_to`; **suspend operator auditado actor=config_operator, reason=OPERATOR_SUSPEND** (decisión Manager WP-B) vía `SuspendByOperator` — helper privado parametrizado, path `system/STALENESS` intacto para T14, sin cambios a máquina de estados ni triggers; close PREPARED→CLOSED permitido (watermark NULL), OBSERVING→CLOSED directo y DRAINING sin evidencia ⇒ 409 NOT_OBSERVING, DRAINING con fixture CONTRACT de `reference_readbacks` KNOWN_EMPTY (C1-2; nunca PHYSICAL, AC-32) ⇒ CLOSED. **Config publish:** tras PREPARED exitoso (creación y replay) publica por el mecanismo existente `echo.account-configs.v1` con el pin E-06 exacto igual a la fila del binding y campos legacy del catálogo; publish NO cambia estado (demostrado por GET); fallo del publisher ⇒ 503 retryable con la fila PREPARED y republicación en el retry idempotente; `hasura_handler.go` intocado; cero `echo.commands.*`/`echo.reference-events.v1`/topic nuevo. Errores: envelope local `{code,retryable,field_or_ref?,binding_id?}`; sin códigos nuevos en `v3/sdk/contracts/wire`. **PG REAL:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero recién initdb'd, cero SHARED DEV/PROD): harness `run.sh` PASS completo ×2 (rebuild 001–063, probe, up, aserciones 10, idempotencia, 12, 13 A–G, 14, down fail-closed, down ok, up/down/up final). Go E-06 **15/15** `sdk/postgres` (13 previos + `TestSuspendByOperator_Audit` + `TestListByVersion`) y **15/15** `gateway/internal` PASS con `DATABASE_URL` y `-race`; `sdk/domain` PASS plain/race; `go vet` OK; builds sdk/gateway/bridge OK (Bridge sin modificaciones). **Regresión:** failing set `gateway/internal` idéntico al baseline puro `1614028b` en worktree aparte (29=29; todo colateral preexistente clase TC-06-1 — truncate-vs-FK de 064 en harnesses forge; `TestForgeIngest_CertPack` con inestabilidad de orden falla idéntico en ambos aislado); `sdk/postgres` 0=0; skip-mode idéntico (sólo `TestScratch_QueryDB` documentado E-05) — cero regresiones nuevas. Defecto propio detectado por skip-mode y corregido: conflicto de patrones del mux en el fallback sin PG (panic `TestServer_MountsForgeIngest`) — resuelto con patrón subtree `/api/v1/reference/bindings/`. **SOURCE:** delta vs `1614028b` = 7 archivos productivos + TASKS/VERIFICATION, todos allowed; contracts/061–064/Bridge/MQL/core/hasura diff 0; `OrderSend`/`deployments`/`AllocateMagic`/`echo.commands.`/`GlobalVariableSet` = 0 en el delta; `go.mod`/`go.sum` delta 0; `TEST_CHANGE_REQUEST.md` intacto. Commit `cd2aca674a085cb9912949620e73dc85e012afcd`, push FF `1614028b..cd2aca6`, sin merge/PR/master. E-06 NO CLOSED; T08–T22 no iniciados. **Siguiente gate: Manager review de WP-B (autoriza WP-C read-back).**

- **2026-09-17 (NORMAL WP-A corrección C2 — audit fix de la corrección C1, `E06_WPA_C2_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `c021983f` == origin, worktree limpio, parent `429e5c03`, merge-base `origin/master` = `5dd998f1`. Defecto único del Manager corregido sin reabrir el SPEC ni tocar otras semánticas: el predicado de `uq_e6_canonical_admission` (C1-4) `state <> 'CLOSED'` retenía la reserva CANONICAL durante DRAINING, contradiciendo §11 (el conflicto protege al CANONICAL OPEN-admitting; el switch de cuenta canónica es explícito con el viejo DRAINING/CLOSED). **Fix:** predicado ahora `state IN ('PREPARED','OBSERVING','SUSPENDED')`; DRAINING/CLOSED liberan la titularidad; se demuestra en el trigger que ninguna transición enumerada §6 re-entra al índice desde DRAINING/CLOSED (DRAINING→SUSPENDED no existe) ⇒ `uq_e6_canonical_open_admitting` queda sin camino de fallo alcanzable (defensa en profundidad, sin cambios); `uq_e6_overlap_live` sin cambios (SUSPENDED y DRAINING siguen reteniendo la overlap key ⇒ handover en DRAINING sólo hacia otra physical account; same-account rollover exige CLOSED + inventario vacío por C1-1/C1-2). `reference_binding_store.go` sin cambios (ya clasifica el constraint como `ErrCanonicalExists`). **Tests:** `13_uniqueness.sql` añade casos A (titular PREPARED ⇒ conflicto en INSERT), B (OBSERVING ⇒ conflicto), C (SUSPENDED ⇒ conflicto), D (sucesor CANONICAL PREPARED admitido en otra physical key con viejo DRAINING), E (sucesor pasa a OBSERVING con el viejo aún DRAINING — ya no es OPEN-admitting — con gates normales ACK/barrier/watermark), F (overlap same-account+same-magic rechazada con viejo DRAINING), G (cerrados viejo y sucesor con evidencia C1-2 cada uno, un CANONICAL PREPARED fresco inserta — switch post-CLOSED intacto); `TestCanonicalUnique` reescrito con el mismo lifecycle (conflictos PREPARED/OBSERVING/SUSPENDED, sucesor admitido y observando durante DRAINING, sucesor fresco post-CLOSED); `10_migrate_up.sql` aserta el índice con el conjunto exacto y sin DRAINING/CLOSED. **PG REAL:** PostgreSQL 17.11 descartable (Zonky + PGDG psql, cluster efímero `/tmp/e06-pg17/data-c2` recién initdb'd, cero SHARED DEV/PROD): `run.sh` PASS completo ×2 (rebuild 001–063, probe BEGIN/ROLLBACK, up `-1`, aserciones 10, idempotencia, 12 máquina de estados, 13 unicidad A–G, 14 watermark/vista/revokes, down fail-closed >50 bytes, down ok 31, up/down/up final). Go E-06 **13/13 PASS** con `DATABASE_URL` y `-race`. **Regresión:** failing set del paquete `sdk/postgres` completo idéntico al baseline puro `c021983f` corrido en worktree aparte (53 = 53; todo colateral TC-06-1 `identityBWCDB` + `TestScratch_QueryDB` preexistentes) — cero regresiones nuevas. `go vet` OK (sdk/gateway/bridge); builds sdk/gateway/bridge OK; skip-mode CONTRACT PASS (sólo falla el `TestScratch_QueryDB` preexistente). **SOURCE:** delta vs `c021983f` = 6 archivos (064 up, 10, 13, store test, TASKS, VERIFICATION), todos allowed; contracts y 061–063 diff 0; `OrderSend`/`deployments`/`AllocateMagic` = 0 en el delta (única mención: la lista documental de tokens en VERIFICATION, patrón C1); `TEST_CHANGE_REQUEST.md` intacto. Nota colateral documentada en VERIFICATION: el comentario de `14_watermark_view_revoke.sql` (no allowed file) dice "el binding 102 permanece vivo" y tras C2 el 102 termina CLOSED mientras el CANONICAL vivo al cierre de 13 es el 109; su fixture 201 SHADOW funciona idéntico. Commit `1614028bc2c3831a689f76094ccd7a5a8e0577c4`, push FF `c021983f..1614028b`, sin merge/PR/master. E-06 NO CLOSED; T04–T22 no iniciados. **Siguiente gate: Manager review de la corrección C2.**

- **2026-09-17 (NORMAL WP-A corrección C1 — audit fix de T01–T03, `E06_WPA_C1_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `429e5c03` == origin, worktree limpio, parent `acf996ad`, merge-base `origin/master` = `5dd998f1`. Cuatro defectos del Manager corregidos sin reabrir el SPEC ni introducir estados/políticas nuevas. **C1-1:** `uq_e6_overlap_live` pasa de `{PREPARED, OBSERVING, DRAINING}` a `{PREPARED, OBSERVING, DRAINING, SUSPENDED}` — un binding suspendido aún puede conservar posiciones, así que la overlap key sólo se libera con CLOSED (decisión frozen 5 del proyecto). **C1-2:** la transición DRAINING→CLOSED en `fn_e6_bindings_before_update` ahora exige evidencia durable consultando `echo.reference_readbacks`: fila `match_status='MATCHED'` con `matched_binding_id` del binding, igualdad de `broker_server_ref`/`account_registration_ref`/`platform` (otra cuenta rechazada), igualdad de `collector_id`/`collector_epoch` con el claim autorizado (otro epoch rechazado), `inventory_completeness='KNOWN'` con `position_count=0 AND pending_count=0` (UNKNOWN/NULL rechazados) y `received_at >=` al `at` de la fila OBSERVING→DRAINING de `binding_transitions` (append-only, immutable — read-back anterior al drain rechazado; sin fila de auditoría ⇒ `E06_CLOSE_DRAIN_AUDIT_MISSING` fail-closed); sin evidencia ⇒ `E06_CLOSE_REQUIRES_EMPTY_INVENTORY`, gate vigente también ante UPDATE SQL directo; `PREPARED→CLOSED` nunca observado permanece permitido y la evidencia exigida cabe completa en el schema congelado (sin columnas nuevas; down sin cambios). **C1-3:** trigger SUSPENDED→OBSERVING exige `coverage_quality='PROVEN'` (`E06_RECOVERY_QUALITY`) y `Recover()` la sella junto con `accepts_opens_from=received_at`, `accepts_opens_to=NULL`, `last_physical_readback_at=received_at`; `coverage_started_at`/`capture_barrier_at` inmutables (ya bloqueados por trigger) y `suspended_at` preservado; el gap queda UNKNOWN sin reescribir historia. **C1-4:** nueva UNIQUE parcial `uq_e6_canonical_admission` sobre `(registry_namespace, strategy_version_ref) WHERE observation_class='CANONICAL' AND state <> 'CLOSED'` — el segundo POST PREPARED CANONICAL de la misma Version (distintas physical keys, distinto binding_id) viola el UNIQUE en el INSERT y `InsertPrepared` lo clasifica como `ErrCanonicalExists` (SPEC §14: serialización desde POST; también mapeado en `classifyTransitionError`); SUSPENDED no habilita reemplazo implícito; el switch sigue explícito tras DRAINING/CLOSED; SHADOW coexiste en otras cuentas; `uq_e6_canonical_open_admitting` se conserva como defensa en profundidad (sin camino de fallo posible bajo la reserva). **Tests:** SQL — `12_state_machine.sql` añade cierre sin evidencia (rechazo) + fixture de read-back de cierre CONTRACT/PG REAL y recovery sin PROVEN (rechazo) + assertions de calidad vigente; `13_uniqueness.sql` reescribe el caso canónico (segundo PREPARED falla en INSERT por `uq_e6_canonical_admission`, SUSPENDED reintenta y sigue rechazado, recovery C1-3, drain → cierre con evidencia C1-2 → re-insert positivo del switch post-CLOSED) y añade overlap con titular SUSPENDED; `10_migrate_up.sql` aserta las dos nuevas propiedades de índices; `14` ajusta su fixture a SHADOW (mismo motivo cross-file que el binding 004 de `12`: la reserva C1-4 opera sobre fixtures preexistentes que probaban borrado/watermark, no la clase). Go — `TestCanonicalUnique` reescrito (POST ⇒ `CANONICAL_EXISTS` sin fila persistida, evidencia SQL directa, SUSPENDED sin reemplazo, recovery PROVEN, switch con evidencia), `TestOverlapUnique` añade SUSPENDED-retain + liberación real, `TestTransitions_EnumeratedOnly`/`TestCollectorUnique` añaden negativo+positivo de C1-2, `TestCoverageStartImmutable` aserta PROVEN post-recovery, helper `seedCloseEvidence` documenta que el fixture es CONTRACT/PG REAL nunca PHYSICAL (AC-32). **PG REAL:** PostgreSQL 17.11 descartable (mismo patrón `/tmp/e06-pg17`, cluster efímero recién initdb'd, cero SHARED DEV/PROD): `run.sh` PASS completo dos veces (rebuild 001–063, probe interrupción, up, aserciones 10 actualizadas, idempotencia, máquina de estados, unicidad, watermark/vista/revokes, down fail-closed >50 bytes, down ok, up/down/up final). Go E-06 **13/13 PASS** con `DATABASE_URL` y `-race`; `go vet` OK (sdk/gateway/bridge); builds sdk/gateway/bridge OK; skip-mode CONTRACT PASS. **SOURCE:** delta vs `429e5c03` = 9 archivos (064 up, store, store test, 4 SQL del harness, TASKS, VERIFICATION), todos allowed; contracts y 061–063 diff 0 vs `5dd998f1`; `OrderSend`/`deployments`/`AllocateMagic` = 0 en el delta; `TEST_CHANGE_REQUEST.md` intacto (TC-06-1/TC-06-2 preservados sin arreglos ajenos). Commit `c021983f` push FF `429e5c03..c021983f`, sin merge/PR/master. E-06 NO CLOSED; T04–T22 no iniciados. **Siguiente gate: Manager review de la corrección C1.**

- **2026-09-17 (NORMAL WP-A — Persistencia T01–T03, `E06_WPA_READY_FOR_MANAGER_REVIEW`)** — Baseline verificado pre-write: worktree dedicado `/tmp/echo-e06-reference-enrollment` en `feature/e06-reference-enrollment-binding` @ `acf996ad` == origin, merge-base `origin/master` = `5dd998f1`, worktree limpio. **T01:** migración `064_reference_enrollment_binding.{up,down}` con tablas §16, FK tupla Version 061, UNIQUE parciales §5.1/§9/§10, triggers de lifecycle §6 (INSERT siempre PREPARED `E06_INSERT_NOT_PREPARED`; write-once identidad `E06_WRITE_ONCE`; transiciones enumeradas con precondiciones; auditoría obligatoria `binding_transitions` via AFTER trigger SECURITY DEFINER con actor/reason por GUCs `echo.e6.*`; watermark `E06_WATERMARK_RECIPE` validado en trigger como `GREATEST(proof_recorded_at, capture_barrier_at)`; recovery mueve `accepts_opens_from` y reabre `accepts_opens_to` sin mover watermark; DELETE/TRUNCATE denegados), vista `v_reference_bindings` redactando proof a `proof_sha256_hex`, revokes patrón 061/063 (roles con guard de existencia + PUBLIC), widen `active_positions.strategy_id`→text con recreación de `v_trade_stream` (057) y down fail-closed `MIGRATION_064_DOWN_ABORTED` si `octet_length(strategy_id)>50` (umbral exacto 50). Decisiones de diseño bajo SPEC: collector claim claim-together (`E06_COLLECTOR_CLAIM_PARTIAL`) e inmutable una vez seteado; pin del mapping `E06_MAGIC_PIN_MISMATCH` exigido en INSERT (igualdad string decimal == bigint::text); recetas `fn_e6_hash_tagged`/`fn_e6_binding_ref`/`fn_e6_map_revision` en SQL con escaping `to_json` == `wire.appendJSONString` (verificado golden cruzado SQL==Go `sha256:7e03590a…`). **T02:** `reference_binding_store.go` — `InsertPrepared` (validación S0, recetas, replay §14 por PK+digest sellado, clasificación de UNIQUE/FK por constraint name), `Ack` (idempotente, conserva `acked_at` original), `MarkObserving` (exige ACK; sella barrier/watermark/accepts_from/collector/last_readback; `CANONICAL_EXISTS`/`COLLECTOR_CONFLICT` mapeados), `Recover`, `Drain`, `Suspend`, `Close`, `AdvanceReadback` (no-op ante replay); transacciones ReadCommitted con GUCs de auditoría en la misma txn. **T03:** tests Go con evidencia UNIQUE directa (`uq_e6_canonical_open_admitting`, `uq_e6_collector_claim`, `uq_e6_overlap_live` via UPDATE/INSERT SQL) y positiva SHADOW coexistente + reattach post-CLOSED. **PG REAL:** PostgreSQL 17.11 descartable en `/tmp/e06-pg17` (binarios server Zonky 17.11 + cliente PGDG psql 17.9; sin docker/sudo; cluster efímero local, cero Aranea): harness `run.sh` PASS — rebuild 001–063, seed pre-064, probe BEGIN/ROLLBACK, up `-1`, aserciones 10 (objetos/FK/uniques/triggers/vista/widen/golden), segundo apply idempotente, 12 máquina de estados (≈25 negativos + positivos), 13 unicidad (incl. overlap cross-namespace O3), 14 watermark/vista/revokes `has_table_privilege`, down fail-closed con fila de 60 bytes (aborta, objetos intactos), down ok → 31 (objetos ausentes, varchar(50) exacto, `v_trade_stream` recreada), up final. Go: 13/13 PASS `DATABASE_URL` + `-race` PASS; `go vet` OK; build sdk/gateway/bridge OK; skip-mode sin DATABASE_URL = sólo `TestScratch_QueryDB` FALLA (DSN hardcodeado preexistente, bit-idéntico a master, documentado E-05). **SOURCE:** diff `v3/sdk/contracts` y 061–063 vs `5dd998f1` = 0; delta = 17 archivos allowed; `OrderSend`/`deployments`/`AllocateMagic` = 0 en delta. **Colateral documentado en `TEST_CHANGE_REQUEST.md` (TC-06-1/TC-06-2, repro incluido):** `identityBWCDB` TRUNCATE falla en schema-064 por FK nueva; harnesses `identity_bwc`/`analytics_a0` aplican 064 sin 061 (interlock §16 opera) — fixes recomendados, no aplicados (fuera de allowed files). Commit `429e5c035d471c83ebc50af5e9637d1e7a283189`, push FF `acf996ad..429e5c03`, sin merge/PR/master. T01 `[x]`, T02 `[/]` (AC-18 → T05), T03 `[x]`. **Siguiente gate: Manager review de WP-A → autoriza T04+ (WP-B).** E-06 NO CLOSED.

- **2026-09-17 (corrección C1 — Forge producer, audit fix)** — Manager auditó `508b4a2` y detectó 4 defectos materiales; corregidos en el worktree dedicado `symphony-e06-attestation` (branch `feature/e06-runtime-attestation-exporter`, delta sólo los 2 archivos autorizados; fixture ajeno del worktree principal preservado intocado). **C1-1:** la inserción textual antes de un `return INIT_SUCCEEDED` guardado por `if` sin llaves (same-line o next-line) desasociaba el return de su guard y lo volvía incondicional. Scanner ahora clasifica el boundary de cada return de éxito: previo `;`/`{`/`}`/inicio ⇒ inserción plana segura; cuerpo directo de `if/else/for/while/switch` sin llaves (palabra guard obtenida del par de paréntesis cerrado inmediatamente anterior, con pila para no dejar que llamadas anidadas la sombreen; o `else` literal) ⇒ reescritura del span exacto del statement como `{ EchoAttestV1_Publish(); return X; }`; cualquier otro boundary (`case:`/label/expresión) ⇒ rechazo fail-closed. Fall-through: además de último segmento `return`, cadenas if/else terminales se aceptan con prueba estructural acotada (regionTerminates/ifTerminates: sólo `return`, bloques y cadenas if/else; cualquier otra forma ⇒ false ⇒ fail-closed); segmentos que empiezan con `else` se fusionan con su `if` (el splitter partía el statement en el `;` del arma then). Returns de fallo quedan byte-idénticos (sólo se clasifica boundary donde se inserta). **C1-2:** la invalidación `c=0` ignoraba su resultado ⇒ ahora hard gate: si falla, Publish retorna false sin escribir payload ni commit. **C1-3:** `SetField` sólo chequeaba `GetLastError` ⇒ ahora comprueba el retorno documentado (`datetime != 0`) además del error; cleanup residual de chunks distingue inexistente (`GlobalVariableCheck`) de fallo real de `GlobalVariableDel` y un fallo material de cleanup bloquea el commit; commit final mantiene chequeo + invalidación best-effort + false; Revoke sigue contrato §7.2a.2 (c=0 basta, Del residual tolerado). **C1-4:** exporter generaba directo al path final y luego instrumentaba ⇒ si el injector fallaba quedaba MQ5 sin instrumentar publicable. Nuevo flujo: staging aislado `.echoforge-e06-staging-<rand>/` (mismo basename), generate→assertNonZeroLots→instrument→validación→promoción `Files.move` con `ATOMIC_MOVE` (fallback renombrado mismo-volumen documentado; staging es hijo del outDir, mismo filesystem), limpieza recursiva en finally restringida a su propio namespace. Evidencia no-STOP: staging sólo cambia el directorio destino de `SourceCode.generate` (parámetro explícito; suite existente ya generaba a paths arbitrarios), `.sqx` de entrada/basename/bytes sellados sin cambio. **Tests (+9):** guard same-line y next-line (el defecto reproducido y ahora rechazado por simulador de control flow), if/else success-failure, cadena else-if, nested if sin llaves, return en bloque con llaves, boundary ambiguo (case-label) fail-closed, fault injection GV model 10 escenarios (invalidación ⇒ cero escrituras posteriores; mh; ml; chunk; cleanup real; ts; sh; commit final; chunk inexistente no es fallo; happy path con residuos) clasificados CONTRACT/model con pins textuales del bloque emitido, publicación staged (rechazo ⇒ nada publicable ni residuo; preexistente preservado byte-idéntico; éxito ⇒ sólo MQ5 instrumentado, mismo nombre, MagicNumber/mmLots intactos; fallo del generador ⇒ nada). Simulador de control flow en tests (parser acotado a las formas de los fixtures) demuestra equivalencia, no presencia textual. **Verificación:** build.sh PASS; suite main 32/32 OK (23 previas + 9 C1); RobustRun/WFM OK; JUnit trades 13/13; `verify_build.sh` FAIL_BASELINE_KNOWN reproducido 3/3 en baseline puro `508b4a2` (carrera SIGPIPE conocida) con verificación semántica manual 5/5 exporters y 0 test-support en prod. Regresiones §8: idempotencia, encoding v1, magic int64 máximo, FNV golden, UTF-8, OnDeinit, timers, OnTick, zero trading calls — todas verdes. PHYSICAL_PENDING (sin terminal MT5/SQX; AC-34/AC-35 esperan físico). Commit `b738a6db19bc19b4b12362583f50bee85a3790af`, push FF `508b4a2..b738a6d`, sin merge/PR/master. **Verdict: `E06_FORGE_PRODUCER_C1_READY_FOR_MANAGER_REVIEW`**; E-06 NO CLOSED.

- **2026-09-17 (NORMAL Forge lane A — producer atestación §7.2a)** — Implementada la inyección del producer `echo.attest.v1` en `EchoForgeMT5Exporter.java` (único archivo productivo, repo `xKoRx/symphony`). Baseline verificado pre-write: `origin/codex/f05-release-prep` @ `3d0e8c958765da23840e00bf1a0ac017fc47597a` (parent `0ddd4db`), blob exporter `cfbd5b78` exacto; worktree dedicado limpio (el worktree principal tenía un fixture de benchmark ajeno modificado, preservado sin tocar). Branch `feature/e06-runtime-attestation-exporter` @ `508b4a253dfb6cf6419dc5fc3f1be94801745a93`, push FF, sin merge/PR/master. Implementación: inserción por offsets tras `generate`+`assertNonZeroLots` (no movido), scanner estructural state-machine (comentarios/strings/chars/preprocessor, braces, duplicados), helper MQL5 `EchoAttestV1_*` con encoding v1 congelado (split mh/ml uint32 exactos — roundtrip max int64 verificado, `ts` escrito con `TimeGMT`, commit par ≥2, invalidación c=0, cleanup residual n(K+1)..n15, FNV-1a32 UTF-8 de ACCOUNT_SERVER con goldens, chunking big-endian n0..n15, límites nl 1..63), publish sólo en `return INIT_SUCCEEDED` literal (returns no literales o fall-through ⇒ fail-closed export), revoke primera sentencia de OnDeinit (append mínimo si falta), refresh §7.2a.4 v1.2.2 (OnTimer compatible preferido: `EventSetTimer(P)` único literal, statement top-level de OnInit, 0<P≤5, sin tocar EventSetTimer/EventKillTimer; si no OnTick oportunista; sin sitio exporta sin liveness prometida), idempotencia byte-stable por marcadores, post-condiciones byte-level (readback FromMQ5 espejo, línea MagicNumber y mmLots idénticas), cero llamadas de trading en el bloque. Contract tests §17 A–O + missing-OnInit PASS (22/22 suite), JUnit trades 13/13, RobustRun/WFM OK; build.sh PASS; `verify_build.sh` FAIL_BASELINE_KNOWN (carrera SIGPIPE `grep -q`+`pipefail`, falla igual en baseline puro 3/3; verificación semántica equivalente a mano: 5/5 exporters, 0 test-support). PHYSICAL_PENDING: sin terminal MT5 ni SQX Build 142 en esta máquina (no compile MQ5, no export real; AC-34/AC-35 esperan entorno físico; JDK portable Temurin 17 en /tmp fue requerido). Echo repo y resto de Forge intactos; Versions históricos intactos (identidad nueva por export futuro). Estado del lane: `IMPLEMENTED` + `SOURCE_VERIFIED` + `PHYSICAL_PENDING`. **E-06 NO CLOSED**; handoff al Manager.

- **2026-09-16 (TOP refresh liveness erratum — v1.2.2)** — Defecto: §7.2a.4 v1.2.1 exigía «MUST republicar al menos cada 5 s» con sitio `OnTick`; MQL5 no garantiza ticks cada 5 s ⇒ garantía temporal irrealizable. Decisión Manager frozen (sólo semántica de refresh): `OnTimer` existente realmente activado con período ≤5 s preferido; prohibido modificar `EventSetTimer` existente ni crear timer; si sólo `OnTick`, renovación oportunista — 5 s = intervalo mínimo entre publicaciones, no frecuencia garantizada; sin tick ni timer compatible nunca se promete liveness continua; expiración 15 s fail-closed `SUSPENDED + UNKNOWN`; recovery exige nueva atestación física matching con las transiciones §6 congeladas; ninguna actualización de `ts` desde Echo/Bridge/Gateway/config; cero cambios de trading. Tests AC-37a…d (contractual+físico; PHYSICAL gated por Version con inyección §7.2a.5). Scanner §7.2a.5: ausencia de sitio de refresh deja de ser fail-closed en export. CASE B, encoding v1 y resto de gates intactos. Source mutations 0 (Echo y Forge). HEAD `acf996ad043f87d6bbe6ae7b6190d1eb801e908a` (contrato `28afc47faf72b70e67b141b39224b8674f98458b`; old `336c723b`); push FF. Estado `E06_PLANNING_READY_FOR_IMPLEMENTATION_REVIEW`.
- **2026-09-16 (TOP transport contract correction — encoding v1)** — Defecto: `GlobalVariableSet` no transporta `{program_name, chart_ref, magic}` ni int64 magic sin pérdida; `GlobalVariableTime` no es publicación. SPEC v1.2.1 cierra encoding fragmentado lossless, vigencia `ts`, correlación chart/cuenta, inyección estructural Forge (no implementada). CASE B intacto. Source mutations 0 (Echo y Forge). HEAD `336c723ba46a7c04a1a6cc4390c6d5a4f15b56d7` (contrato `662c0dcef9fb17a5308ddcb974eabb6074c748aa`; old `e8fba410`); push FF. Estado `E06_TRANSPORT_CONTRACT_READY_FOR_MANAGER_REVIEW`.
- **2026-09-16 (TOP planning correction #2 — Manager CASE B)** — Manager resolvió CASE C: zero-order exige `effective_magic` físicamente atestiguado por la instancia strategy EA (excepción C-3 §7.2a: bootstrap runtime identity attestation, sin signals/deals/coverage/métricas/lifecycle/telemetría/estado). Discovery físico READ ONLY: producer = exporter Forge `EchoForgeMT5Exporter.java` (stamp `MagicNumber` input por `EchoForgeRobustRunExporter`; verificado por `magic-readback`; sello F-04). Transporte: variables globales terminal → relay verbatim collector `reference_status`. Identidad: bytes cambian ⇒ nuevo `strategy_version_ref`; históricos intactos fail-closed. SPEC v1.2.0; caso AC-26a/b/c; bloque CASE-C removido; reconciliación preservada en VERIFICATION.md. AutoTrading B2 intacto; coverage hook sigue DEFER. Source mutations 0 (Echo y Forge). HEAD `e8fba410347f6c60d036f9d03b2d0b1946d53e28` (contrato `3d5a5d627f5ed66e48479e24f6b664c5a973899b`; old `349b6ac8`); push FF. Estado `E06_PLANNING_V1_2_READY_FOR_MANAGER_REVIEW`.
- **2026-09-16 (TOP authority reconciliation)** — CASE C. Live Authority §5 vs Fable C-3 no cierran si zero-order exige echo físico de magic. SPEC v1.1.0 sin bump; §7.3 KNOWN_EMPTY no es implementable. AutoTrading B2 intacto. C-3 DEFER intacto. HEAD `349b6ac8` (contrato `1c794d5a`; old `3e190d86`). Source delta `v3/` = 0. Estado `E06_PLANNING_BLOCKED — MANAGER_DECISION_REQUIRED`. *(Resuelto después por Manager CASE B; ver entrada superior.)*
- **2026-09-16 (TOP planning correction #1)** — v1.1.0 @ `3e190d86` (contrato `ef8a96f3`; old `9989f399`). Heartbeat/UnifiedBatch no son autoridad zero-order. Producer congelado: `reference_status` Echo collector. Magic = inventario; KNOWN_EMPTY no se fabrica. AutoTrading = capability (B2). C-3 DEFER SQX conservado. Source mutations 0. Estado `E06_PLANNING_CORRECTED_READY_FOR_MANAGER_REVIEW`.
- **2026-09-16 (TOP planning one-shot)** — SPEC/PLAN/TASKS/VERIFICATION v1.0.0 @ `9989f399`. Hipótesis accounts/policies solos refutada; `reference_bindings` + intent. O1/O3 applied. 064 reservada. 0 source. Estado `E06_PLANNING_READY_FOR_MANAGER_REVIEW`. Puente padre → Review.

## 🧭 Decisiones (ejecución, no semántica nueva)

- Hijo de implementación de E-06; ownership sigue en [[Echo — Live Platform V1]], no Integration.
- OBSERVING nunca se sintetiza desde SQL/ACK/config/heartbeat/UnifiedBatch.
- Apply 064 SHARED DEV es ops gated por 061, no NORMAL.
- PHYSICAL V1 exige producer `reference_status`; fake ≠ PHYSICAL. Terminal MT5 real GAP-ECHO-006 pendiente no se disfraza de PASS.
- Hook SQX de coverage **no** se abre (C-3 DEFER). La excepción §7.2a es identidad runtime mínima, registrada como decisión Manager posterior explícita (2026-09-16), no reescritura del freeze C-3.
- Zero-order magic = fail-closed congelado (CASE B): atestación runtime == pin o no hay OBSERVING.
- Transporte §7.2a = encoding v1 sobre GVs del mismo terminal; magic nunca viaja como `double`; una GV existente no es instancia viva.
- Liveness §7.2a.4 (erratum v1.2.2): refresh = `OnTimer` compatible preferido sin tocar `EventSetTimer` / `OnTick` oportunista (5 s = intervalo mínimo entre publicaciones); sin sitio ⇒ sin liveness continua prometida; la expiración de 15 s es la autoridad fail-closed.

## 🔗 Docs / Links

- [[Echo — Live Platform V1]]
- [[Echo — E-04 Forge Ingestion E1]]
- [[Echo — E-03 Identity and BWC Foundation E0]]
- [[Echo — E-02 Control Safety, Auth and Journal Recovery]]
- [[Echo — E-05 Analytics Convergence A0]]
- [[Echo — Forge Ingestion, Runtime Identity and Live Authority Contract V1]]
- [[Echo + Echo Forge — Architecture Durability and Contract Review — Fable 5.1]]
- [[Echo — Access & Physical Capability Matrix]]
- [[Echo — Producto Integrado]]

## 💡 Ideas

### Backlog de ideas

- Hook EA SQX exportado de **coverage**: DEFER (C-3), no V1. La excepción §7.2a (identidad runtime mínima) NO es ese hook y no lo preimplementa.

### Motivos / principios

- Ingestion no inicia el reloj. Una fila SQL no observa.

### Memoria pública / interna

- **Memoria pública:** contratos enlazados + SPEC del repo.
- **Memoria interna:** continuidad en esta nota.
- **Motivo:** el padre conserva el roadmap; este hijo conserva HOW/GATES.
