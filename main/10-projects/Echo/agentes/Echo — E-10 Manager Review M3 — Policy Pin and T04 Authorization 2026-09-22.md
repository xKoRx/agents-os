# Echo — E-10 Manager Review M3 · 2026-09-22

**Authority:** manager técnico, carril único Echo. **Repo:** `xKoRx/echo`; única branch remota de desarrollo `feature/e09-execution-copy-reconciliation-fidelity`. **Baseline revisado:** `34876939696ec11dab8b5d42b86f3a2715947ffc`; **HEAD remoto revisado:** `d27544153d795dbd041125d6312c0fcdc1481a69`; `master` remoto `5dd998f16aea7b2821f460188718d7a6d279829c` intacto. **Decisión:** `M2_SOURCE_REVIEWED · M2_C1_A_B_D_C2_PRESENT · RATIFICATION_UNACCREDITED · POLICY_PIN_GATE_PENDING · T04_CONDITIONALLY_AUTHORIZED_AFTER_PIN · T05_T10_NOT_AUTHORIZED · PHYSICAL_PENDING · INTEGRATION_PENDING · FINAL_CLOSED=NO`. M3 prevalece en autorización/secuencia sobre mensajes históricos que habilitan T04–T10 en bloque. No crear nuevas branches.

## Contraste del handoff M2

GitHub compare `34876939...d2754415`: status ahead, 3 commits FF, behind=0, 9 archivos modificados, todas rutas en scope; ningún archivo de migraciones 001–068 modificado. GitHub branches: única feature @ `d2754415`, master @ `5dd998f1`, dos rescue intactas. `strategy_quality.go` ahora computa policy_ref con `{policy_version,content}` y policy_digest desde content usando S0; `strategy_quality_stores.go` recomputa identidad version+content, comprueba digest, enlaza assessment con Expectation strategy/version y decisión con assessment+Expectation+outcome+inputs. `economic_commands_e8/run.sh` excluye 069 en rebuild inicial y pre-down; `execution_fidelity_e9/run.sh` igualmente. Agent reporta `build/vet/domain race/11 PG/2 harnesses PASS`, 121 tests cross-lane rechazan base E-10 como gate de identidad y failing set E-08 preexistente sin delta. Manager revisó source/refs, **NO** ejecutó tests/PG Daedalus ni certificó CI (sin statuses publicados para SHA). `T00–T03` tienen implementación con M2 aplicada, cierre de seguridad/autoridad real pendiente.

## M3-P1 — defecto de enlace de política congelada descubierto en re-review

SPEC S8 y nota canónica establecen que EligibilityDecision resuelve **la política referida por `StrategyExpectation.minimum_observation_policy_ref` con el `policy_digest` sellado**. `domain/strategy_quality.go` incluye ambos campos en contenido canónico/digest write-once de Expectation. El store `EligibilityDecisionStore.PutDecision` en `d2754415` verifica que decision policy ref/digest coincidan con **assessment** y que Expectation citada pertenezca a misma strategy/version, pero **no compara el pin de política de la Expectation**. Si el assessment y la decisión se construyen coherentemente con otra política existente, los actuales checks no detectan policy-shopping post-forward.

**Gate obligatorio antes de T04:** leer campos canónicos de la Expectation durable re-verificada y exigir que `decision.policy_ref == expectation.minimum_observation_policy_ref` y `decision.policy_digest == expectation.policy_digest` antes del INSERT; comprobar asociación equivalente cuando se emite assessment autoritativo, sin impedir diagnósticos tipados explícitos `POLICY_UNAVAILABLE`/`POLICY_DIGEST_MISMATCH` con política ausente. No cambiar S0 ni política owner ni crear identidad sintética. Test RED reproducible con Expectation sellada a A y assessment+decision válidos bajo B: antes de fix se escribe, después error tipado y 0 rows; controles positivos policy A y negativo digest discrepante. No introducir una definición arbitraria de ratificación.

## Decisión de ratificación M3-R

M2 C1-C quedó **demostrada, no resuelta**: `PutPolicy()` y `ratified_at DEFAULT now()` acreditan inserción en base, NO consentimiento del owner. Decisión arquitectónica mínima: mantener **dos estados conceptuales independientes**: política almacenada write-once ≠ política ratificada. Para autorizar decisiones reales, se exigirá evidencia owner-operated **autenticada, inmutable y verificable**, vinculada a `policy_ref` exacto, `policy_digest` exacto y versión, con identidad del aprobador y timestamp de aprobación de autoridad; la ruta de aplicación debe impedir que el writer general o fixture se autoatribuya aprobación. Una marca JSON suministrada por el caller o mero `ratified_at` no sirve. El backend exacto (registro durable dedicado dentro del boundary owner, integración con mecanismo de aprobación existente, roles/permissions) y contenido/umbrales de política requieren definición contractual posterior, antes de T06/T07, no se improvisan en T04. Hasta entonces: sólo fixtures de clase A, no políticas RATIFIED reales, no decisiones reales consumibles por E-11, no activación.

## Autorización de ejecución limitada

**Un NORMAL, un worktree de sesión aislado:** baseline obligatorio `d27544153d795dbd041125d6312c0fcdc1481a69`, origin==HEAD limpio, master intacto; STOP ante drift/dirty no atribuible; no reset/rebase/force. Dos commits separados: (1) M3-P1 fix y regresiones, exigir gate PASS; (2) T04 exclusivamente (SPEC S1/S3, nota canónica §PLAN): ensamblador de ventana/cobertura READ ONLY sobre binding 064, `reference_coverage` 065 y lifecycle 065; digests reproducibles y UNKNOWN-first con fixtures deterministas etiquetados. No tocar T05 writer/calc, T06 policy thresholds, T07 decisión/reset, T08 driver, T09 coverage final ni T10 cierre. T04 usa únicamente observación Reference CANONICAL y versión probada: no lectura de E08/E09, ninguna inferencia desde SHADOW, dedupe intervalos, ausencia de reporte = UNKNOWN, gaps/intersecciones/solapes honestos, `VALID_NO_SIGNAL` cuenta exposición sólo si evidencia positiva, `MARKET_CLOSED`/`STRATEGY_DISABLED` se reportan separadas sin exposición, `UNKNOWN` excluido de denominador, time basis no verificada => UNKNOWN, no NULL→0. Identidades reales del binding y event-time `[from,to)`; no generar hechos o forzar estado físico. Los tests usan PG17 descartable propio de identidad E10, con gate antes de cualquier DDL/DML y cleanup, y fixtures rotulados, nunca instancias previas ni DEV/PROD. Consultas parametrizadas; preservar migraciones 001–068 byte-identical, 069 schema sin cambios salvo necesidad demostrada que requiere STOP. Construir, vet, race/test por paquete, harness E10, regressions E08/E09 en sus instancias exclusivas si se ejecutan, failing sets apples-to-apples. No usar `go test ./...` indiscriminado contra servicios compartidos. Documentar coverage por archivo como informativa sin proclamar G4 >=95 (T09).

**STOP tras T04 y handoff Manager:** commits atómicos, push FF, readback, E10 SPEC/VERIFICATION/TASKS + nota Agents-OS + agent_run/change_log/session-close; resultados verificables: policy-pin gate, coverage matrix casos UNKNOWN/PARTIAL/KNOWN/NO_SIGNAL/closed/disabled/overlap/gap/wrong version/time basis, cambios exactos, tests/failing set y flags pendientes. No cambiar master/Forge/S0, no shared DEV/PROD ni activación. `PHYSICAL_PENDING`, `INTEGRATION_PENDING`, `POLICY_RATIFICATION_UNACCREDITED`, `T05–T10=NOT_AUTHORIZED`, `FINAL_CLOSED=NO`.
