---
type: project
schema_version: 1
owner: agent
root: false
status: active
priority: P1
area: "[[Echo]]"
parent: "[[Echo Forge — Factory V2 Completion]]"
sprint:
start: 2026-09-13
due:
progress: 85
repo: xKoRx/symphony
jira:
prs:
aliases:
  - F-05-I
  - Echo Forge F-05-I
  - release prep read surfaces
tags:
  - kind/project
  - area/echo
  - agent/owner
created: "2026-09-13"
updated: "2026-09-16"
---

# Echo Forge — F-05-I Cohesive release and read surfaces

%% Naming: Echo Forge — F-05-I Cohesive release and read surfaces es el link canónico del proyecto; aliases guarda variantes humanas; tags/slugs son solo automatización. %%

> [!info]+ Echo Forge — F-05-I Cohesive release and read surfaces
> **Área:** [[Echo]] · **Estado:** active · **Prioridad:** P1 · **Parent:** [[Echo Forge — Factory V2 Completion]] · **Repo:** `xKoRx/symphony`
> Preparación de implementación de F-05 (split F-05-I desarrollo / F-05-C certificación diferida). SPEC: [[Echo Forge — F-05-I Release Matrix and Read Surface Contract]]. Veredicto objetivo: `F-05-I IMPLEMENTED / SOURCE VERIFIED`. Nada físico se marca PASS.

> [!abstract]- Ownership del proyecto (`owner`) — humano vs agente
> Este proyecto es `owner: agent`. El padre humano tiene la tarea puente `#type/supervision`. Las fases internas no inundan el cockpit.

## 🎯 Objetivo

Dejar inspectable, sin abrir bases a mano y sin publicar nada: matriz determinística de release por capacidad, read surface CLI JSON (campaña/run/estrategia/matrix) sobre read models existentes, funnel projection honesta, conformance checklist y manifest template para F-05-C. Usar sólo outputs/fixtures existentes; `fixture != authentic physical golden`.

## 📊 Estado actual

- **Planificación TOP completa (2026-09-13):** SPEC frozen, tareas atómicas F05I-T1…T7, allowed files freeze, matriz de tests congelada. NORMAL puede ejecutar sin decisiones arquitectónicas.
- **Baseline:** `xKoRx/symphony@b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` (HEAD `origin/feature/f04-magic-version-handoff`; release `0.2.98`). Branch objetivo `codex/f05-release-prep` desde el SHA exacto. El checkout local puede estar divergido (`9fad768` no contiene C5): siempre fetch + branch por SHA.
- **Decisiones frozen:** `DATABASE MIGRATION: NONE`; arquitectura CLI JSON (sin HTTP); funnel como proyección pura; release matrix = artefacto declarativo validado; sin writes runtime.
- **Implementación T2–T4 (2026-09-16):** T2, T3 y T4 en estado tablero **Review** en `codex/f05-release-prep` (commits `7d073b3`, `65c879a`, `d77342d`; branch publicada en GitHub, HEAD remoto `d77342d`). Distinción de estado: IMPLEMENTATION REPORTED + SOURCE VERIFIED BY AGENT · MANAGER REVIEW PENDING · PHYSICAL CERTIFICATION NOT RUN. Tests enfocados unit + persistencia embedded-postgres PASS, `-race` PASS, vet PASS. Físico: nada certificado. Pendiente: aprobación manager de T2–T4 y ejecución de T1, T5, T6, T7.
- **Corrección T2 (2026-09-16, commit `3da8b47`):** defecto confirmado por manager en la paginación de campañas: el predicado tuple `(created_at, id) < ($1,$2)` compara `id` descendente contra el orden contractual `created_at DESC, id ASC`, perdiendo silenciosamente filas empatadas en `created_at` con `id > cursor.id`. Corregido a `created_at < $1 OR (created_at = $1 AND id > $2)` en `forge_campaign_list.go` (sin tocar orden, cursor, firma, validaciones ni esquema). Test de regresión `TestListForgeCampaignsPersistenceTieTimestampsKeysetTraversal` (embedded-postgres, limit=1, grupo de empate + cruces de timestamp, orden exacto, sin pérdida/duplicados, página final vacía): falla contra el predicado anterior, PASS con la corrección. T2 permanece en Review; manager review pending.
- **Manager approval T2–T4 (2026-09-16):** SOURCE REVIEW APPROVED para T2, T3 y T4; tablero movido a Done (implementación/source review). Distinción explícita: tests locales PASS **reportados por el agente de desarrollo** (no certificación física); PHYSICAL CERTIFICATION NOT RUN; T7 verificación integral pendiente; F-05-I sigue abierta. La aprobación NO constituye certificación física ni cierre de F-05-I.
- **Planning C1 (2026-09-16, corrección contractual dentro de F-05-I, no nueva fase):** seis defectos del contrato de Release Matrix y CLI resueltos con evidencia de source @ `3da8b47`. C1.1: artefacto movido de `deploy/release-matrix.json` a `sqx/core/releasematrix/release-matrix.json` (`deploy/` es el WatchRoot del deployer-watcher y staging de `deploy_release.sh`). C1.2: carga portable por `go:embed` + override `--matrix` tipado; eliminado el default relativo-repo. C1.3: `deployed` con `deploy_targets[]` por target (release/release_present OBSERVED|NOT_OBSERVED/process RUNNING|NOT_OBSERVED + evidencia; sin inferir deploy de publicación ni proceso de archivos). C1.4: semántica DONE/OPEN/DEFERRED/NOT_APPLICABLE con historia física preservada (allowlist b1a/b1b/b2/f03; DONE físico exige `certification_record`+`as_of`; nunca certifica release actual por arrastre). C1.5: autoridad de evidencia por dimensión (`evidence_kind` enum; RELEASED≠DEPLOYED; SOURCE VERIFIED≠PHYSICAL). C1.6: dispatch-first con grafo mínimo de boot por subcomando (`release-matrix` cero DI; inspect PG-only; `run get` +Mongo; push-output intacto) — eliminado el boot DI completo pre-dispatch. SPEC actualizada; contratos T1/T5 cerrados para NORMAL.
- **Implementación T5 (2026-09-16, commit `eed8000`):** CLI `sqx-flowkit` en estado tablero **Review** en `codex/f05-release-prep`. Dispatch-first (C1.6): `main.go` es sólo despacho+composición, el boot DI completo pre-dispatch fue eliminado y cada subcomando inicializa su grafo frozen — `release-matrix` cero DI (embebido por defecto u override `--matrix`; salida byte-idéntica al marshaling canónico, probada desde CWD vacío sin checkout); `campaign get|list`, `run stages`, `strategy get` con boot PG mínimo (`WithEtcd+WithTelemetry(telemetry.Postgres)+WithPostgres` → `NewControlPlaneFromClient`) reutilizando `LoadForgeCampaignResult` y `InspectService` de T4 con ports nil fail-closed; `run get` añade únicamente el Mongo de ranking (`sharedmongo.New` + `metadatamongo.NewRankingSnapshotStore`) como primer caller productivo de `forge.Service.Result`; `push-output` extraído a `push_output.go` con flags/defaults/salida/exit codes byte-idénticos. JSON versionado en stdout (encoder determinístico, sin timestamps de consulta), errores stderr `<code>:<kind>:<mensaje>`, exit codes 0/2/4/10/50 con mapping frozen ErrorKind, identidad sólo por refs exactos validados antes de boot (`latest` → exit 2 sin infraestructura). Cursor keyset de `campaign list` codificado base64url-JSON opaco. Tests: 68 casos PASS (`go test ./sqx/cmd/sqx-flowkit/...`), `-race` PASS, vet PASS, gofmt OK, `git diff --check` OK; smoke del binario real con matriz byte-idéntica desde `/tmp`; golden bytes de `sqx-strategy-inspect.v1`/`sqx-run-stage-timeline.v1`/`sqx-campaign-list.v1` con fakes sintéticas; grafo mínimo verificado estructuralmente sobre el AST (release-matrix sin DI, PG sin Mongo/Temporal/MinIO, run get añade sólo Mongo, push-output conserva su boot). NOT_RUN: golden del flujo exitoso de push-output (requiere etcd/MinIO reales) y suite global `go build ./sqx/...` falla sólo en `sqx/tools` (mains duplicados preexistentes al baseline, BASELINE_KNOWN; paquetes consumidores de T5 compilan por separado). Distinción de estado: IMPLEMENTATION REPORTED + SOURCE VERIFIED BY AGENT · MANAGER REVIEW PENDING · PHYSICAL CERTIFICATION NOT RUN. F-05-I sigue abierta.
- **Corrección manager C1 de T5 (2026-09-16, commit `cbf520b`):** tres defectos confirmados por manager en SOURCE REVIEW de T5 @ `eed8000`, corregidos sólo en `inspect.go`/`inspect_test.go`. Causa 1: los FlagSets inspect usaban `flag.ExitOnError`, que ejecuta `os.Exit` dentro de `flag.Parse`, saltándose commandFunc, writers inyectados, `emitError` y el stderr contractual `<code>:<kind>:<mensaje>` (reproducido: `release-matrix --flag-inexistente` moría con el formato del paquete flag). Corregido con helper `newInspectFlagSet` (ContinueOnError + salida del paquete flag suprimida) y `exitForFlagError` (flag.ErrHelp → usage stdout exit 0; resto → `emitError` 2:INVALID_ARGUMENT); push-output conserva su `ExitOnError` brownfield. Causa 2: el paquete flag estándar deja de parsear tras el primer positional, así que la sintaxis frozen `run get <FlowRunRef> --ranking name` fallaba; corregido con parser puro `parseRunGetArgs` que aísla el primer positional contractual, parsea sólo el tail de flags y rechaza positional extra, manteniendo la validación del ref ANTES del boot. Causa 3: help incompleto (`campaign -h` → exit 2; leaves `-h` → usage default del paquete flag por stderr); corregido: grupos campaign/run/strategy y todos los leaves inspect responden `-h`/`--help` con usage en stdout, exit 0, stderr limpio y sin DI. Guard estructural nuevo: AST de `inspect.go` con cero `flag.ExitOnError` y cero `os.Exit`. Tests de regresión nuevos (7 funciones) FALLAN contra `eed8000` (verificado en worktree del baseline; uno mataba el binario de test dentro de flag.Parse) y PASS con la corrección. T5 permanece Review; manager review pending de `cbf520b`.
- **Manager approval T1 (2026-09-16):** SOURCE REVIEW APPROVED @ `2c5d34f` del paquete `releasematrix` (incluida la corrección trailing-data); tablero T1 movido a Done (implementación/source review). La aprobación NO constituye certificación física: PHYSICAL CERTIFICATION NOT RUN; los gates físicos de la matriz siguen OPEN/DEFERRED.
- **Implementación T6 (2026-09-16, commit `1a1926a`):** handoff documental F-05-C en estado tablero **Review** en `codex/f05-release-prep`. Tres archivos nuevos en `docs/echo-forge/`: `f05-read-surface.md` (contrato read-only: delimitación de qué es/no es F-05-I, 6 comandos con `push-output` delimitado como brownfield no-read, 7 schemas JSON con shapes reales confirmados contra source @ `cbf520b` — `sqx-forge-campaign-result.v1`, `sqx-campaign-list.v1`, `forge-result.v1|v2`, `sqx-run-stage-timeline.v1`, `sqx-forge-funnel-projection.v1`, `sqx-strategy-inspect.v1`, `sqx-release-matrix.v1` —, reglas de identidad frozen, tabla de ausencia/error con exit codes 0/2/4/10/50 y formato stderr `<code>:<kind>:<mensaje>`, semántica funnel, release matrix portable/embedded, boundary del frontend futuro: consumir los read services, jamás PG/Mongo directo ni recomputar membership/ranking/funnel), `f05-conformance-checklist.md` (checklist ejecutable por superficie A–J con qué verificar/input/expected/autoridad/evidencia/PASS/FAIL/qué NO demuestra; distinción obligatoria SOURCE CHECK PASS ≠ DEPLOYED/PHYSICALLY_CERTIFIED/CROSS_LANE_CERTIFIED; sección DEFERRED NO EJECUTAR con los 7 gates canónicos del backlog CERT-F04-01/02, CERT-E04-01, CERT-F04-03, CERT-F05-01/02/03 con precondiciones/evidencia/dimensión-a-DONE/pendiente-post-PASS y `NO ESPECIFICADO EN AUTORIDAD` donde el backlog no soporta el dato; fixture warning `fixture != authentic physical golden`), y `f05-certification-manifest-template.json` (template vacío válido: schema documental `sqx-f05-certification-manifest-template.v1` registrado como template —la SPEC no congela schema runtime para el manifest—, `certification_status: OPEN`, los 7 gates con `status: OPEN` y toda evidencia física en `null`, orden frozen de ejecución, `release_under_certification`/`cross_lane_receipt`/`authentic_golden` null; parse y invariantes verificados con script). Sin cambios Go, sin tocar release-matrix.json, sin ejecutar certificación, sin publicar release. Distinción de estado: DOCUMENTATION IMPLEMENTED · MANAGER REVIEW PENDING · PHYSICAL CERTIFICATION NOT RUN. F-05-I sigue abierta.
- **Review contractual F05I-PAGINATION-C1 (2026-09-16, sesión TOP, sin tocar source):** la manager review de T6 @ `1a1926a` quedó bloqueada por una contradicción real: el checklist C1 exige "recorrer todas las páginas usando los cursores retornados", pero `sqx-campaign-list.v1` sólo expone `schema`+`campaigns[]`, `runCampaignList` no emite cursor y `encodeCampaignCursor` tiene cero callers productivos (sólo el codec test) — la receta es inejecutable vía contrato público. Decisión con evidencia source @ `1a1926a`/`cbf520b`: **PASS — OPTION A — SERVER-ISSUED OPAQUE CURSOR** (contrato completo y impacto exacto en el change_log `2026-09-16-f05i-pagination-contract-review`). Resumen: `sqx-campaign-list.v1` gana `next_cursor: string|null` siempre presente (aditivo pre-release, permanece v1 — nunca publicada); el servicio pagina con probe `LIMIT N+1` (≤N filas ⇒ `next_cursor: null` exacto; N+1 ⇒ emite N y cursor del boundary = última fila emitida); página vacía sigue válida; el consumidor nunca construye ni interpreta el token (encoding queda informativo, detalle reservado del servidor); port y adapter NO cambian (T2 frozen). D1: wording — `run get` conserva `schema_version` (ForgeResult F-02 congelado, no se renombra); se corrige la frase "campo `schema` obligatorio" en docs/SPEC con la excepción. D2: BWC push-output se expresa como "flags/ruta de éxito/códigos propios byte-idénticos, salvo el único delta pre-dispatch aprobado (exit 1 → exit 10 `config_error`, SPEC C1.6)". Reaperturas: T4 SÍ formal (delta en `InspectService.Campaigns` + shape del documento); T5 scope extendido (sigue Review); T2/T3 NO; T6 aplica las correcciones dentro de su Review; T7 continúa después del fix, sin rediseño. Golden `sqx-campaign-list.v1` cambia (`{"schema":"sqx-campaign-list.v1","campaigns":[],"next_cursor":null}`); SPEC y nota de proyecto requieren registrar el delta aditivo. PHYSICAL CERTIFICATION NOT RUN; ningún gate marcado; ninguna tarea movida de estado.
- **Implementación F05I-PAGINATION-C2 (2026-09-16, sesión NORMAL, corrección pre-release sobre `1a1926a`):** implementa el veredicto OPTION A de F05I-PAGINATION-C1. Shape `CampaignListDocument` = `schema` + `campaigns[]` + `next_cursor *string` sin omitempty (nil → JSON `null`; orden de campos frozen: schema, campaigns, next_cursor). Algoritmo N+1 en `InspectService.Campaigns`: validación `limit ≥ 1` + overflow-safe (`limit == MaxInt` rechazado) ANTES del reader — cierra la regresión `limit=0` → query `limit=1` exitoso —; probe `LIMIT N+1` contra el port existente (sin cambio de port, sin SQL, sin count query); `len(items) ≤ limit` ⇒ visible completo + `next_cursor: nil`; `len(items) > limit` ⇒ emite las primeras N y `next_cursor = EncodeCampaignCursor(boundary)` con boundary = ÚLTIMA FILA EMITIDA (la fila N+1 jamás es cursor); imposibilidad de codificar un boundary durable válido ⇒ `CONTRACT_INCONSISTENCY`. Codec `EncodeCampaignCursor`/`DecodeCampaignCursor` movido del CLI a `sqx/core/forge/inspect.go` (misma codificación base64url de JSON compacto `created_at` RFC3339Nano + `ref`; Encode valida el boundary; token inválido sigue fallando `INVALID_ARGUMENT` en la superficie CLI). CLI `runCampaignList`: consumidor/composición puro — parse de `--limit`/`--cursor` antes de DI, decodifica con `forge.DecodeCampaignCursor`, reenvía el documento del service tal cual; guard estructural AST nuevo: inspect.go sin segunda implementación de encoding y con delegación en `forge.DecodeCampaignCursor`. Tests nuevos: casos A–G (página corta reader llamado con limit+1 y `next_cursor` nil; página llena [A,B,C] ⇒ emite [A,B] con boundary decodificado == B, nunca C; página vacía con golden exacto `{"schema":"sqx-campaign-list.v1","campaigns":[],"next_cursor":null}`; limit 0/-1 y overflow fallan `ErrInvalidArgument` con reader NO invocado; codec round-trip UTC/RFC3339Nano + tokens inválidos; boundary con created_at cero ⇒ `CONTRACT_INCONSISTENCY`) + traversal hermético keyset (`TestInspectServiceCampaignsKeysetTraversalUsesReturnedCursorOnly`: 5 campañas, dos pares empatados en created_at, limit=1, usando EXCLUSIVAMENTE el `next_cursor` retornado: orden exacto, cero duplicados, cero pérdidas, página final `next_cursor: null`). Docs T6 corregidos dentro de su Review: `f05-read-surface.md` (campo de versión general con excepción ForgeResult `schema_version`; shape/semántica cursor opaco server-issued/algoritmo N+1/página vacía; BWC push-output sin afirmar exit codes totalmente byte-idénticos — flags/defaults/ruta de éxito/códigos propios 0/2/20/30/40/50 preservados con único delta aprobado boot pre-dispatch exit 1→10) y `f05-conformance-checklist.md` (C1 traversal EXCLUSIVAMENTE con `next_cursor` retornado sin derivación manual; C2 autoridad codec core + guard; C3 golden con `next_cursor: null`; I5 golden campaign list actualizado; H1 BWC acotada a H2; H2 conceptualmente igual; manifest template intacto). Validación: gofmt OK · `go test ./sqx/core/forge/...` PASS · `go test ./sqx/cmd/sqx-flowkit/...` PASS · `go test ./sqx/adapters/registry-postgres/... -run TestListForgeCampaigns` PASS · `-race` PASS · vet PASS · build flowkit PASS · `git diff --check` OK · `go build ./sqx/...` falla sólo en `sqx/tools` (mains duplicados BASELINE_KNOWN preexistentes al baseline). Sin migration, sin port, sin adapter/SQL, sin `internal/di`, sin release-matrix, sin ForgeResult, sin push-output, sin gates físicos. Distinción de estado: IMPLEMENTATION REPORTED + SOURCE VERIFIED BY AGENT · MANAGER REVIEW PENDING · PHYSICAL CERTIFICATION NOT RUN. F-05-I sigue abierta.
- **Implementación T1 (2026-09-16):** T1 en estado tablero **Review** en `codex/f05-release-prep` (commit `5295f1c`; push normal, HEAD remoto `5295f1c`). Paquete `sqx/core/releasematrix`: schema `sqx-release-matrix.v1`, API frozen `Embedded` (copia defensiva) / `LoadEmbedded` / `Parse` (estricto: unknown fields y trailing data rechazados) / `Load` (override explícito sin fallback) / `Validate` (puro, sin derivación entre dimensiones) + `MarshalCanonical` (indent 2 + newline final; artefacto committed byte-idéntico, round-trip idempotente). Artefacto embebido con las 17 filas frozen: historia física preservada con records reales (B1A/B1B/B2 PASS/CLOSED as_of 2026-09-06 vía agent-runs 2026-09-06; F-03 PHYSICAL PASS as_of 2026-09-10, job `14m51.98s` 3000/3000); released DONE por `0.2.98` as_of 2026-09-13; deployed OPEN con targets stager (linux OBSERVED/RUNNING 2026-09-13, windows NOT_OBSERVED por viewer policy gap mt5-kronos); F-04 físico DEFERRED CERT-F04-01/02/03 y cross-lane handoff DEFERRED CERT-E04-01; pipeline/release-pipeline/f05i físico DEFERRED CERT-F05-02/CERT-F05-01; release-pipeline released NOT_APPLICABLE (es la autoridad que publica); f05i implemented/source_verified OPEN con SHA vacío (T5–T7 pendientes). Allowlist histórica física DONE `{b1a,b1b,b2,f03}` vive en guards del test. Tests reales: `go test ./sqx/core/releasematrix/...` PASS (17 funciones, 51 casos), `-race` PASS, vet PASS, build PASS, gofmt OK, `git diff --check` OK, `go list -deps` sin `internal/di`/DB/Temporal/MinIO/etcd. Distinción de estado: IMPLEMENTATION REPORTED + SOURCE VERIFIED BY AGENT · MANAGER REVIEW PENDING · PHYSICAL CERTIFICATION NOT RUN. F-05-I sigue abierta.
- **Manager approval T4–T6 y arranque T7-A (2026-09-16):** SOURCE REVIEW APPROVED @ `0ddd4db` (HEAD de `codex/f05-release-prep`; parent `1a1926a`; commit F05I-PAGINATION-C2) para T4 (delta pagination), T5 (scope completo) y T6; tablero movido a Done. T7 arranca como **T7-A verificación integral source/contract** con matriz frozen de gates A–K sobre HEAD exacto `0ddd4db`: el agente es verification agent NORMAL — produce evidencia, NO cierra la release matrix (`f05i-read-surface` implemented/source_verified permanecen OPEN y `TestGuardF05INotDeclaredDone` debe seguir existiendo), NO emite SOURCE REVIEW APPROVED, NO certifica físico, NO commitea. Tras la evidencia, el manager autorizará T7-CLOSE para actualizar la declaración. PHYSICAL CERTIFICATION NOT RUN; F-05-I abierta; dirty ajeno `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` debe permanecer intacto.

## 🧱 Entrega de desarrollo

| Aplicación / repo | Branch | Base | SPEC funcional | SPEC técnica | Estado |
|---|---|---|---|---|---|
| xKoRx/symphony | `codex/f05-release-prep` (creada desde SHA; publicada en origin 2026-09-16) | `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` | [[Echo Forge — Factory V2 Completion]] F-05 | [[Echo Forge — F-05-I Release Matrix and Read Surface Contract]] | EN CURSO: T1–T6 Done (manager SOURCE REVIEW APPROVED; T4–T6 @ `0ddd4db`); T7 WIP (T7-A verificación integral, evidencia para manager review; la release matrix NO se cierra aquí); físico NOT RUN |

## 🧩 Subproyectos

Sin subproyectos; fases internas = tareas F05I-T1…T7 abajo.

## ✅ Tareas

> [!note]+ Ownership y tarea puente
> Este board muestra `#owner/agent`. El humano sigue el curro desde [[Echo — Producto Integrado]].

> [!example]- Fuente de tareas — editar / mover de estado aquí
> %% Estados: [ ] To Do · [/] WIP · [r] Review · [x] Done · [-] Canceled. %%
> - [x] F05I-T1 Release matrix: paquete validador + artefacto embebido `sqx/core/releasematrix/release-matrix.json` (contrato corregido Planning C1 2026-09-16; prohibido deploy/) — implementada 2026-09-16 (`5295f1c`), corrección trailing-data (`2c5d34f`); manager SOURCE REVIEW APPROVED 2026-09-16 @ `2c5d34f`; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [x] F05I-T2 Read ports + implementaciones PG (campaign list, stage list, participaciones, versiones/handoff reads) — implementada 2026-09-16 (`7d073b3`), corrección keyset (`3da8b47`); manager SOURCE REVIEW APPROVED 2026-09-16; tests locales PASS reportados; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [x] F05I-T3 Funnel projection pura (topología dinámica, orden determinístico) — implementada 2026-09-16 (`65c879a`); manager SOURCE REVIEW APPROVED 2026-09-16; tests locales PASS reportados; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [x] F05I-T4 Read services: strategy inspect + stage timeline + campaign list service (provenance por refs exactos) — implementada 2026-09-16 (`d77342d`); manager SOURCE REVIEW APPROVED 2026-09-16; reabierta únicamente por el delta pagination F05I-PAGINATION-C2 (shape `next_cursor` + N+1 server-issued en `InspectService.Campaigns` + codec movido a core); manager SOURCE REVIEW APPROVED del delta 2026-09-16 @ `0ddd4db`; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [x] F05I-T5 CLI `sqx-flowkit` subcomandos inspect + wiring (push-output intacto; dispatch-first + boot mínimo por subcomando; release-matrix embebido — contrato corregido Planning C1 2026-09-16) — implementada 2026-09-16 (`eed8000`), corrección manager C1 2026-09-16 (`cbf520b`: error contract de parsing, orden frozen `run get <ref> --ranking`, help grupos/leaves, guard AST sin ExitOnError/os.Exit en inspect.go), scope extendido F05I-PAGINATION-C2 2026-09-16 (`runCampaignList` consumidor puro del `next_cursor` server-issued vía `forge.DecodeCampaignCursor`; guard sin segunda implementación de encoding); manager SOURCE REVIEW APPROVED 2026-09-16 @ `0ddd4db`; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [x] F05I-T6 Handoff F-05-C: read-surface contract doc + conformance checklist + cert manifest template — implementada 2026-09-16 (`1a1926a`: `docs/echo-forge/f05-read-surface.md`, `f05-conformance-checklist.md`, `f05-certification-manifest-template.json`; shapes confirmados contra source @ `cbf520b`; gates deferred citados sin redefinir; template 100% OPEN/null), correcciones F05I-PAGINATION-C2 2026-09-16 (read-surface: campo versión con excepción ForgeResult `schema_version`, shape/algoritmo campaign list con `next_cursor`, página vacía, BWC push-output acotada; checklist: C1/C2/C3/I5/H1; manifest template intacto); manager SOURCE REVIEW APPROVED 2026-09-16 @ `0ddd4db`; certificación física NOT RUN #owner/agent #type/dev #area/echo
> - [/] F05I-T7 Verificación completa enfocada (build/tests/race/vet/diff-check/BWC/fixtures) + evidencia — WIP 2026-09-16 (T7-A): verificación integral source/contract frozen sobre HEAD `0ddd4db` con gates A–K; produce evidencia para manager review, NO cierra la release matrix (`f05i-read-surface` implemented/source_verified siguen OPEN) ni emite autoaprobación; certificación física NOT RUN; incluye re-verificación de los invariantes C1 (grafo mínimo, portabilidad, guards) #owner/agent #type/dev #area/echo

```dataviewjs
const meta={" ":["To Do","var(--text-muted)","var(--background-modifier-border)"],"/":["WIP","#ba7517","rgba(234,124,12,.18)"],"r":["Review","#185fa5","rgba(55,138,221,.18)"],"x":["Done","#3b6d11","rgba(99,153,34,.18)"],"X":["Done","#3b6d11","rgba(99,153,34,.18)"],"-":["Canceled","var(--text-faint)","var(--background-modifier-border)"]};
function linkify(s){return String(s).replace(/\[\[([^\]|]+)(?:\|([^\]]+))?\]\]/g,(m,a,b)=>`<a class="internal-link" href="${a}" data-href="${a}">${b||a}</a>`).replace(/#[\w/-]+/g,m=>`<span style="opacity:.55;font-size:12px">${m}</span>`).replace(/📅\s*(\d{4}-\d{2}-\d{2})/g,(m,d)=>`<span style="opacity:.7;font-size:12px">📅 ${d}</span>`).replace(/[⏫🔼🔽⏬🔺]/g,"").replace(/✅\s*(\d{4}-\d{2}-\d{2})/g,");}
function has(t,tag){return new RegExp(`(^|\\s)#${tag}(\\s|$)`).test(String(t.text));}
function render(tasks){const el=dv.el('div','');el.innerHTML=tasks.map(t=>{const[label,fg,bg]=meta[t.status]||["?","var(--text-muted)","var(--background-modifier-border)"];return `<div style="display:flex;align-items:center;gap:8px;margin:5px 0;"><span style="font-size:11px;font-weight:600;padding:1px 9px;border-radius:999px;background:${bg};color:${fg};min-width:56px;text-align:center;flex:none;">${label}</span><span>${linkify(t.text)}</span></div>`;}).join("");}
function board(tasks){const cols=[[" ","🟦 To Do"],["/","🟡 WIP"],["r","🔵 Review"]];let any=false;for(const[st,label] of cols){const c=tasks.filter(t=>t.status===st);if(c.length){any=true;dv.el('h4',label);render(c);}}const done=tasks.filter(t=>t.status==="x"||t.status==="X");if(done.length){any=true;dv.el('h4',"✅ Done");render(done);}if(!any){dv.paragraph("_Sin tareas._");}}
const owner=((dv.current().owner)==="agent")?"agent":"me";
const all=dv.current().file.tasks.array();
const primary=all.filter(t=>has(t,`owner/${owner}`));
const loose=all.filter(t=>!has(t,"owner/me")&&!has(t,"owner/agent"));
dv.header(3, owner==="agent"?"🤖 Tareas del agente":"🧍 Mis tareas");
board(primary);
if(loose.length){dv.header(3,"🧺 Sin owner (clasificar)");render(loose);}
```

## 📋 Tareas atómicas (freeze TOP 2026-09-13)

Orden por dependencia. Cada tarea lista objective / allowed files / inputs / requirement / tests / negative assertions / done / stop. Global: repo `xKoRx/symphony`, branch `codex/f05-release-prep` desde `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` (`git fetch origin` y verificar `git rev-parse origin/feature/f04-magic-version-handoff` == baseline; si difiere materialmente o el SHA no existe: `STOP — MANAGER REVIEW — BASELINE_MOVED`). Prohibido: push release, `deploy_release.sh`, stager, writes DB, migrations, tocar `internal/di`, `deployer/`, `deploy/manifest.json`, contratos frozen F-01…F-04/S0/B1A/B1B/B2. Todos los tests usan fixtures sintéticas etiquetadas (`fixture != authentic physical golden`).

### F05I-T1 — Release matrix (validador + artefacto) — contrato corregido Planning C1 2026-09-16

- **Objective:** schema `sqx-release-matrix.v1` en código puro + artefacto declarado con las capacidades y estados de verdad inicial, embebido en el binario.
- **Allowed files:** `sqx/core/releasematrix/releasematrix.go`, `sqx/core/releasematrix/releasematrix_test.go`, `sqx/core/releasematrix/release-matrix.json`. **Prohibido `deploy/`** (C1.1: es el WatchRoot del deploy pipeline).
- **Inputs:** SPEC §Release matrix contract CORREGIDO (ubicación C1.1, carga portable C1.2, dimensiones/estados C1.3/C1.4, autoridades C1.5, contenido de verdad inicial congelado, guard tests); [[Echo Forge — Factory V2 Completion]] y [[Echo + Echo Forge — Deferred Certification Backlog]] como fuente de evidencia.
- **Requirement:** tipos frozen: `Status` (DONE|OPEN|DEFERRED|NOT_APPLICABLE), `Dimension` (implemented|source_verified|released|deployed|physically_certified|cross_lane_certified), `EvidenceKind` (git|tests|release_manifest|deployment_proof|runtime_observation|certification_record|cross_lane_receipt|none), `State {status, evidence, evidence_kind, gate omitempty, as_of omitempty}`, `DeployTarget {target, release, release_present OBSERVED|NOT_OBSERVED, process RUNNING|NOT_OBSERVED, evidence, as_of omitempty}`, `Capability {capability, contract_refs[], producer_paths[], persistence_authority, read_surface, required_refs[], implementation_sha, states map[Dimension]State, deploy_targets[] omitempty}`, `Matrix {schema "sqx-release-matrix.v1", capabilities[] orden ASC}`. API: `Embedded()/LoadEmbedded()/Parse/Load(path)/Validate` + `//go:embed release-matrix.json`; marshal canónico indent-2 + newline (artefacto committed == salida del marshal, test byte a byte). Validaciones: schema exacto; ids únicos y ordenados; refs ≥1; producer_paths sin wildcards; `implementation_sha ^[0-9a-f]{7,64}$` o vacío; evidence siempre no vacío; DONE sin `gate` y sin `evidence_kind=none`; DONE en released/deployed/physical/cross-lane exige `as_of YYYY-MM-DD`; physical DONE exige `certification_record` y cross-lane DONE exige `cross_lane_receipt`; DEFERRED exige `gate ^CERT-[A-Z0-9]+(-[A-Z0-9]+)*$`; `deployed.status=DONE` ⇒ `deploy_targets ≥1`; target OBSERVED ⇒ `release` no vacío; estados jamás derivados entre dimensiones. 17 filas con contenido de verdad inicial tal como lo congela la SPEC (allowlist histórica física DONE: b1a/b1b/b2/f03; F-04/pipeline/release-pipeline/f05i físico ∈ {OPEN,DEFERRED}).
- **Tests:** unit validador ok/corrupt/enum inválido/duplicado/desorden/wildcard/DEFERRED sin gate/DONE con gate/physical DONE sin record o sin as_of/deployed DONE sin targets/target OBSERVED sin release; guard de contenido committed (allowlist histórica; F-04 y f05i sin física DONE; gates CERT-* correctos); determinismo de marshaling (idempotente byte a byte); embebido == artefacto del repo (`LoadEmbedded` vs `Load` del mismo dir en test).
- **Negative assertions:** el validador NO deriva dimensiones entre sí; ninguna fila nueva con física DONE fuera de la allowlist; ningún estado nuevo fuera de los enums frozen.
- **Done:** `go test ./sqx/core/releasematrix/...` PASS; artefacto válido y canónico.
- **Stop:** si el contenido exigiera declarar física DONE fuera de la allowlist histórica para ser coherente → `STOP — MANAGER REVIEW — FROZEN_CONTRACT_COLLISION`; si necesitara un cuarto estado o séptima dimensión → STOP con contradicción concreta (SPEC §Invariantes).

### F05I-T2 — Read ports + PG implementations

- **Objective:** ports read-only nuevos + SELECTs en el adapter existente (archivos nuevos, cero edits a stores existentes).
- **Allowed files:** `sqx/core/capabilities/forge_inspect_query.go` (+ `_test.go`), `sqx/adapters/registry-postgres/forge_campaign_list.go` (+ `_test.go`), `stage_execution_list.go` (+ `_test.go`), `flow_run_strategy_list.go` (+ `_test.go`), `strategy_handoff_reads.go` (+ `_test.go`). Si una fixture brownfield nueva es imprescindible: `sqx/adapters/registry-postgres/postgrestest/testdata/f05i_inspect_synthetic.sql`.
- **Inputs:** SPEC §Read surface contract; schemas migrations 001/011/015 (`flow_run_strategies`, `forge_campaigns`, `stage_executions`, `strategy_versions`, `handoff_manifests`, `handoff_deliveries`).
- **Requirement:** ports: `ForgeCampaignListReader.ListForgeCampaigns(ctx, limit, cursor)` (orden `created_at DESC, id ASC`; cursor opaco `(created_at,id)`); `StageExecutionListReader.ListStageExecutionsByFlowRun(ctx, flowRunRef)` (orden `created_at, stage_instance_key`; expone status/error_code/error_message/started_at/finished_at/subject/generation/temporal cols); `FlowRunParticipationReader.ListParticipationsByFlowRun` + `ByStrategy` (orden `participated_at, flow_run_id`); `StrategyProvenanceReader.ListStrategyVersions(strategyRef)` (orden `sealed_at, version_ref`) + `ListHandoffByStrategy(strategyRef)` (orden `created_at, idempotency_key`, incluye delivery state). Implementaciones sobre `ControlPlane` con `observe(...)` + `requireDB` + `ValidatePersistenceContext` como los reads existentes. Read-only: sin INSERT/UPDATE, sin outcome.
- **Tests:** persistence con harness postgrestest (brownfield + fixture sintética): happy path, ausencia (found=false), orden determinístico, cursor paginación, límites.
- **Negative assertions:** sin writes (assert SQL sólo SELECT en tests de statements si aplica); ref malformado → error invalid arguments sin query.
- **Done:** focused `go test ./sqx/adapters/registry-postgres/... -run 'TestListForgeCampaigns|TestListStageExecutionsByFlowRun|TestListParticipations|TestStrategyProvenanceReads'` PASS.
- **Stop:** si algún listado exige una columna inexistente (schema gap) → `STOP — MANAGER REVIEW — FROZEN_CONTRACT_COLLISION` (migration NONE es frozen).

### F05I-T3 — Funnel projection

- **Objective:** proyección pura `sqx-forge-funnel-projection.v1` desde vistas de stage executions + participaciones.
- **Allowed files:** `sqx/core/forge/funnel.go` (+ `_test.go`).
- **Inputs:** SPEC §Funnel projection; tipos view de T2.
- **Requirement:** función pura `ProjectFunnel(stages []StageExecutionListItem, participations []ParticipationItem) FunnelProjection`; boundaries por `stage_key` observado; orden `(min(created_at), stage_key)`; `by_status` completo; `strategy_subjects_seen/completed` (subject_kind STRATEGY, dedupe por ref); `error_codes` dedup ordenados; sin reloj, sin IO.
- **Tests:** stages repetidos agregan; stage ausente no aparece (no zero-fill); FAILED con error_code visible; orden determinístico estable; input vacío → boundaries vacíos (válido).
- **Negative assertions:** no inventa refs; no hardcodea nombres de stage; no marca "supervivencia" entre boundaries.
- **Done:** `go test ./sqx/core/forge/... -run TestProjectFunnel` PASS.
- **Stop:** si requiere conocer topología estática → mal análisis → replantear sin hardcode (no STOP de manager; corregir en scope).

### F05I-T4 — Read services de inspección

- **Objective:** composición por refs exactos: strategy inspect, run stage timeline (+funnel), campaign list service; clasificación de errores según taxonomía existente.
- **Allowed files:** `sqx/core/forge/inspect.go` (+ `_test.go`).
- **Inputs:** ports T2; `forge.Service` existente (no editar `result.go`); `LoadMagicAllocation`, `LoadStrategyIdentity`/`StrategyManifestIdentityReader`, `ListStrategyVersions`, `ListHandoffByStrategy` (T2).
- **Requirement:** `InspectService` con: `Strategy(ctx, strategyRef)` → documento `sqx-strategy-inspect.v1` (shape exacto SPEC: identity desde fila durable — nunca parse de CanonicalStrategyID —, participation, magic|null, versions, handoff+delivery); `RunStages(ctx, flowRunRef)` → `sqx-run-stage-timeline.v1` + funnel embebido; `Campaigns(ctx, limit, cursor)` → `sqx-campaign-list.v1`. Reuso de `classified(...)`/ErrorKind existente. Ausencia requerida → `ErrNotFound`; opcional → `null`. Deadline default como `Service.Result`.
- **Tests:** unit con fakes de ports: happy, not-found, magic null (pre-F-04), decoy canonical no altera instrument/direction/timeframe, orden determinístico documentos, provenance exacta (refs devueltos = refs persistidos, sin transformación).
- **Negative assertions:** "latest"/ref malformado → INVALID_ARGUMENT; sin Ref por filename/timestamp; incompleto contraditorio → CONTRACT_INCONSISTENCY y no JSON parcial inventado.
- **Done:** `go test ./sqx/core/forge/...` PASS (nuevo subset).
- **Stop:** si el shape exige mutar `ForgeResult`/`ForgeCampaignResult` → `STOP — MANAGER REVIEW — FROZEN_CONTRACT_COLLISION`.

### F05I-T5 — CLI sqx-flowkit inspect — contrato corregido Planning C1 2026-09-16

- **Objective:** subcomandos `campaign get|list`, `run get|stages`, `strategy get`, `release-matrix`; JSON determinístico; exit codes; wiring sin `internal/di`; dispatch primero, boot mínimo por subcomando (SPEC §C1.6).
- **Allowed files:** `sqx/cmd/sqx-flowkit/main.go` (sólo dispatch + boot por subcomando: extraer push-output a archivo propio preservando flags/salida/códigos), `sqx/cmd/sqx-flowkit/push_output.go`, `sqx/cmd/sqx-flowkit/inspect.go` (+ `_test.go`, tests de emisión/exit codes con services fake), `sqx/cmd/sqx-flowkit/testdata/` (fixtures sintéticas pequeñas).
- **Inputs:** T1 (API `releasematrix.LoadEmbedded/Parse/Load`), T3/T4 (funnel + `InspectService` con deps nil fail-closed), SPEC §C1.6 grafo mínimo de inicialización; patrón wiring `sqx/cmd/sqx-worker/persistence.go`.
- **Requirement:** `main()` despacha `os.Args[1]` ANTES de bootear; el `Init()` completo pre-dispatch se elimina. Grafo por subcomando (frozen, ver SPEC): `release-matrix` cero DI (embebido + override `--matrix`; override inválido → exit 2; NUNCA path default relativo al repo ni acceso a `deploy/`); `campaign get|list`/`run stages`/`strategy get` boot PG (`WithEtcd+WithTelemetry(telemetry.Postgres)+WithPostgres` → `NewControlPlaneFromClient(di.Container.Postgres)`, sin Mongo); `run get` boot PG + `sharedmongo.New` → `forge.NewService(control, rankingStore, control)`; `push-output` conserva su boot interno actual. Salida stdout JSON con `schema`, sin timestamps de query; stderr `<code>:<kind>:<mensaje>`; exit codes `0/2/4/10/50` con mapping frozen (INVALID_ARGUMENT→2, NOT_FOUND→4, CONTRACT_INCONSISTENCY/INFRASTRUCTURE/AMBIGUOUS→50, boot DI→10). `run get` cablea `forge.Service` (primer caller productivo); `--ranking` opcional; help por subcomando; unknown subcommand → exit 2 con usage completo.
- **Tests:** unit dispatch (push-output intacto: mismos flags/salida/exit codes vía FlagSet reflection + golden stdout); JSON emitters (golden bytes deterministas); exit code mapping por ErrorKind con fakes; **portabilidad release-matrix**: handler corre con CWD en temp dir vacío sin repo ⇒ bytes idénticos al embebido, y override `--matrix` válido/inválido (missing/corrupt/invalid → 2); **BWC**: `--help`/args/exit codes de push-output no cambian; grafo mínimo: el path de `release-matrix` no invoca `di.InitSelective` (estructura de código + T7 re-verifica).
- **Negative assertions:** `campaign get latest`/ref malformado → exit 2; ausente → exit 4 con mensaje claro; cero writes (sólo readers en el grafo); ningún subcomando de inspección inicializa Mongo/Temporal/MinIO salvo `run get` (Mongo) — y ninguno MinIO/Temporal.
- **Done:** `go test ./sqx/cmd/sqx-flowkit/...` PASS; `go build ./sqx/...` OK.
- **Stop:** si wiring exige modificar `internal/di` o agregar server HTTP, o si un subcomando de inspección necesita un servicio fuera del grafo mínimo → `STOP — MANAGER REVIEW` (arquitectura frozen).

### F05I-T6 — Handoff documental F-05-C

- **Objective:** contrato read-surface + checklist conformance + template manifest certificación.
- **Allowed files:** `docs/echo-forge/f05-read-surface.md`, `docs/echo-forge/f05-conformance-checklist.md`, `docs/echo-forge/f05-certification-manifest-template.json`.
- **Inputs:** SPEC completa; [[Echo + Echo Forge — Deferred Certification Backlog]] CERT-F05-01…03.
- **Requirement:** read-surface doc = esquemas JSON finales, comandos, tabla error/empty, reglas provenance (para el front futuro: consumir CLI/services, nunca DB directa). Checklist = por superficie: qué verificar, con qué refs/evidencia, qué NO valida F-05-I. Template = campos físicos `null` + `"status": "OPEN"`, sin datos inventados.
- **Tests:** validación JSON del template (parse + campos requeridos) dentro de T1 o T4 tests si aplica; consistencia cross-ref con comandos reales de T5.
- **Negative assertions:** ningún campo físico pre-llenado; ninguna promesa PRODUCT CAPABILITY.
- **Done:** docs committed y consistentes con lo implementado.
- **Stop:** n/a (documental). Si descubre gap real de certificación nueva → registrar en [[Echo + Echo Forge — Deferred Certification Backlog]] y avisar en bitácora (no improvisar gates).

### F05I-T7 — Verificación completa + evidencia

- **Objective:** pasar la matriz congelada y capturar evidencia para manager review.
- **Allowed files:** ninguno nuevo (sólo corrección de los anteriores si falla algo).
- **Commands (freeze, desde repo root):**
```text
go build ./sqx/... ./deployer/...
go test ./sqx/core/releasematrix/... ./sqx/core/forge/... ./sqx/core/capabilities/...
go test ./sqx/adapters/registry-postgres/... -run 'TestListForgeCampaigns|TestListStageExecutionsByFlowRun|TestListParticipations|TestStrategyProvenanceReads|TestForgeCampaignResult|TestFlowRunResult'
go test ./sqx/cmd/sqx-flowkit/...
go test -race ./sqx/core/forge/... ./sqx/core/releasematrix/... ./sqx/cmd/sqx-flowkit/...
go vet ./sqx/core/... ./sqx/adapters/registry-postgres/... ./sqx/cmd/sqx-flowkit/...
git diff --check
```
- **Notas:** suite full de registry-postgres tiene timeout conocido de embedded-postgres (preexistente); el subset focused es el gate. `-race` aplica a áreas concurrentes afectadas (read paths paralelos); si no hay concurrencia nueva, `-race` igual corre verde.
- **DEFERRED CERTIFICATION (NO son gates de F-05-I):** T2.11/T2.12/T2.13, E-04 T21/AC-37, físico Windows/MT5, FULL golden, publish/release.
- **Done:** todos verdes + reporte en bitácora (comandos, resultados, SHA branch). Veredicto: `F-05-I IMPLEMENTED / SOURCE VERIFIED` (no CLOSED, no PHYSICAL PASS, no PRODUCT CAPABILITY PASS).
- **Stop:** rojo estructural que exceda allowed files → `STOP — MANAGER REVIEW` con diagnóstico.

## ✅ Completion criteria (F-05-I)

Matriz release + provenance reproducibles (deterministas); read surfaces inspectables vía CLI; tests/source checks del scope PASS; cero cambios a F-01…F-04/S0 frozen; cero publicación desde la branch; ningún gate físico marcado PASS; handoff F-05-C presente.

## 🛑 Stop conditions (globales)

`STOP — MANAGER REVIEW — FROZEN_CONTRACT_COLLISION`: cualquier necesidad de modificar semántica/schema frozen o migration ≠ NONE. `STOP — MANAGER REVIEW — BASELINE_MOVED`: `b57bfb2` inexistente o `origin/feature/f04-magic-version-handoff` movida materialmente. Cualquier presión de publicar release o marcar gates físicos desde esta branch se rechaza y escala a manager.

## 📆 Bitácora

- **2026-09-16 — F05I-PAGINATION-C1 micro contract review (sesión TOP, read-only).** Contradicción manager review T6 resuelta con veredicto `PASS — OPTION A — SERVER-ISSUED OPAQUE CURSOR`. Evidencia decisiva: `CampaignListDocument` sin cursor (`sqx/core/forge/inspect.go:131`), `runCampaignList` sin emisión de cursor (`inspect.go:215`), `encodeCampaignCursor` con cero callers productivos (sólo el codec test `inspect_test.go:266`) — helper huérfano que prueba el diseño intentado — y docs que llaman "opaco" al cursor a la vez que documentan su encoding (`f05-read-surface.md:34/81`). Los tests derivan el cursor del último item (`forge_campaign_list_test.go:158/162/219`): derivación posible pero knowledge de test, no de contrato. Contrato final: `next_cursor: string|null` siempre presente, server-issued, probe `LIMIT N+1` con semántica null exacta (null ⟺ no hay fila posterior al boundary), página llena nunca seguida de vacía por construcción, página vacía válida, consumidor pass-through puro, keyset con empates intacto (predicate T2 + boundary = última fila emitida), determinismo por fila durable, sin v2 (v1 nunca publicada). D1 por wording (ForgeResult.schema_version no se renombra); D2 como excepción explícita del delta pre-dispatch aprobado. Impacto y files en `80-agents/journal/logs/2026-09-16-f05i-pagination-contract-review.md`; agent-run `2026-09-16-zcode-glm-5.3-flash-f05i-pagination-contract-review`. Repo sin cambios; baseline `1a1926a` y dirty ajeno preservados; T6 sigue `[r]`; nada Done; ningún gate físico marcado. Siguiente paso: tarea correctiva (delta T4 + CLI + tests + docs T6 + registro SPEC) y luego T7.

- **2026-09-16 — F05I-T6 IMPLEMENTED (sesión NORMAL, documental).** Handoff documental F-05-C: tres archivos nuevos en `docs/echo-forge/` del repo. Baseline verificado: `git fetch` sin avance concurrente; HEAD local == `origin/codex/f05-release-prep` == `cbf520b9663fa3c1a3a927c27bd7240b521446bf`; `eed8000`, `2c5d34f` y baseline F-04 `b57bfb2` ancestros verificados; dirty ajeno `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado intacto. Autoridades leídas antes de escribir: SPEC canónica, proyecto F-05-I y Deferred Certification Backlog; shapes confirmados contra source real @ `cbf520b` (`sqx/cmd/sqx-flowkit/{main,inspect,push_output}.go`, `sqx/core/forge/{inspect,funnel,result,campaign_result}.go`, `sqx/core/domain/{forge_result,forge_campaign}.go`, `sqx/core/releasematrix/{releasematrix.go,release-matrix.json}`) — sin reconstrucción de memoria; corrección aplicada durante la redacción al descubrir que los reasons reales de promotion son `TOP_PROJECTION_PROMOTED`/`TOP_PROJECTION_EMPTY` (v1) y `STRUCTURAL_PROMOTED`/`STRUCTURAL_EMPTY` (v2). Entregables: `f05-read-surface.md` (contrato consumible por operadores y front futuro sin acceso a PG/Mongo: delimitación explícita de qué es y qué no es F-05-I, 6 comandos con usos contractuales y help, 7 schemas con shapes/campos obligatorios/opcionales-null/arrays-válidos/orden/provenance/estados, identidad frozen —refs exactas, `latest` prohibido, filename/timestamp no-identity, canonical opaco, ranking no-autoridad-de-membership—, tabla error/empty frozen con exit codes y formato stderr, funnel como proyección pura, release matrix embedded/override/portable con seis dimensiones y reglas RELEASED≠DEPLOYED y SOURCE VERIFIED≠PHYSICALLY CERTIFIED, y boundary del frontend futuro: consumir los read services o una API que los envuelva, jamás PG/Mongo directo, jamás reinterpretar identidad ni recomputar membership/ranking/funnel, jamás escribir desde la superficie), `f05-conformance-checklist.md` (25 checks ejecutables organizados A–J, cada uno con qué verificar/input exacto/expected/autoridad/evidencia/PASS/FAIL/qué NO demuestra; distinción obligatoria SOURCE CHECK PASS ≠ DEPLOYED/PHYSICALLY_CERTIFIED/CROSS_LANE_CERTIFIED; sección DEFERRED — NO EJECUTAR EN F-05-I que cita los 7 gates canónicos del backlog con nombre exacto, precondición, evidencia física esperada, dimensión que podría pasar a DONE y qué queda sin probar, con `NO ESPECIFICADO EN AUTORIDAD` donde el backlog no soporta el dato y sin redefinir/renombrar/inventar gates; fixture warning destacado), y `f05-certification-manifest-template.json` (template vacío JSON válido: schema documental `sqx-f05-certification-manifest-template.v1` explícitamente registrado como template y no autoridad runtime —la SPEC no congela schema para este manifest—, `certification_status: OPEN`, gates en orden frozen del backlog con `status: OPEN` y TODA la evidencia física en `null` (timestamps, campaign/run refs, actor/host/worker/result, release observada, receipt cross-lane, golden identifiers/digests), más referencias documentales estáticas permitidas; invariantes verificados con script Python: JSON parseable, todos los gates OPEN, toda evidencia null, set de gates == orden frozen). Validación: consistencia cross-ref de schemas/comandos/exit codes/enums contra source (`HandoffDeliveryState` 6 estados, `ParticipationRole` 4 roles, `StageSubjectKind` 4 kinds, reasons v1/v2, exit codes 0/2/4/10/20/30/40/50 con 20/30/40 acotados a push-output). Commit `1a1926a339a2436669be4d5f77da520b0f9bd871` atómico (3 archivos, 1034 inserciones), push normal fast-forward `cbf520b..1a1926a` a `origin/codex/f05-release-prep`, HEAD remoto verificado == local, `cbf520b` ancestro, dirty ajeno preservado. NOT_RUN: certificación física (prohibida), release (prohibida), modificación de release-matrix.json (prohibida — la fila `f05i-read-surface` sigue OPEN hasta T7 con veredicto manager). T6 `[r]` Review con manager review pending; T1–T4 Done sin cambios; T5 `[r]` sin cambios; T7 pendiente; F-05-I abierta; ningún gate físico marcado. Siguiente paso: revisión manager de `1a1926a`; luego T7.

- **2026-09-16 — Corrección F05I-T5-C1 publicada (sesión NORMAL): parsing/error contract/help del CLI inspect.** Defectos confirmados por manager en SOURCE REVIEW de T5 @ `eed8000` y reproducidos localmente con el binario del baseline: (1) los seis FlagSets inspect usaban `flag.ExitOnError`, cuyo `os.Exit` dentro de `flag.Parse` saltaba writers inyectados, `emitError` y el stderr contractual — `release-matrix --flag-inexistente` moría exit 2 con el formato del paquete flag, sin `2:INVALID_ARGUMENT:`; (2) la sintaxis frozen `run get <FlowRunRef> --ranking name` fallaba porque el paquete flag deja de parsear tras el primer positional (usage exit 2, y `latest --ranking foo` jamás llegaba a validación de ref); (3) help incoherente: `campaign|run|strategy -h` → exit 2 "no reconocido" y `release-matrix -h`/`run get -h` → exit 0 pero con el usage default del paquete flag por stderr (stdout vacío). Corrección mínima en los allowed files (`inspect.go`, `inspect_test.go`): helper `newInspectFlagSet` (ContinueOnError + `SetOutput(io.Discard)`) para todos los FlagSets inspect con `exitForFlagError` (flag.ErrHelp → usage en stdout exit 0 sin DI; parse error → `emitError` 2:INVALID_ARGUMENT con stdout vacío), push-output conserva su `ExitOnError` brownfield; parser puro `parseRunGetArgs` + `firstContractualPositional` que aísla el ref contractual (consumiendo el valor de `--ranking`), parsea sólo el tail y rechaza positional extra — la variante `--ranking foo <ref>` sale gratis sin ambigüedad — y validación de ref sigue ANTES de todo boot; usos contractuales como constantes; grupos campaign/run/strategy responden `-h`/`--help` con usage en stdout exit 0. Tests nuevos (7 funciones en `inspect_test.go`): contrato unknown-flag en las 6 superficies inspect (exit 2, stdout vacío, stderr prefijo contractual, sin terminar el proceso), parsing C2 por helper puro (orden frozen, variante pre-ref, sin ranking, positional extra, sin ref, segundo positional), `latest --ranking foo` → 2:INVALID_ARGUMENT pre-boot, `--ranking` sin valor → 2 contractual, help de grupos, help de los 6 leaves, y guard AST: `inspect.go` con cero `flag.ExitOnError` y cero `os.Exit` (push_output.go queda fuera del guard). Evidencia de regresión: los tests conductuales nuevos FALLAN contra `eed8000` (worktree del baseline; el probe de unknown-flag no alcanzaba sus aserciones porque el binario de test moría por os.Exit dentro de flag.Parse) y PASS con la corrección. Decisiones de diseño: la prueba handler-level de orden frozen con ref válido es estructural (delegación handler→parser) porque un ref válido bootearía DI real y no sería hermético; un smoke accidental de esa ruta booteó etcd/telemetry/PG del entorno hasta fallar en la config de Mongo (sólo lectura, sin queries ni writes) y motivó hermetizar el test. Validación: gofmt OK · `go test ./sqx/cmd/sqx-flowkit/...` PASS · `-race` PASS · `go vet` PASS · `go build` paquete PASS · `go test ./sqx/core/releasematrix/... ./sqx/core/forge/...` PASS · `git diff --check` OK · smoke binario: unknown flag contractual en release-matrix y campaign list, 12 superficies help → exit 0 stdout con stderr limpio, `release-matrix` byte-idéntico al artefacto · `go build ./sqx/...` global FALLA sólo en `sqx/tools` (mains duplicados preexistentes, BASELINE_KNOWN; flowkit+releasematrix+forge+capabilities+registry-postgres compilan). Commit `cbf520b9663fa3c1a3a927c27bd7240b521446bf` atómico (2 archivos, 369 inserciones / 31 deletions), push normal fast-forward `eed8000..cbf520b`, HEAD remoto verificado == local, `eed8000` y baseline `b57bfb2` ancestros, dirty ajeno preservado. T5 permanece `[r]` Review con manager review pending de `cbf520b`; T1–T4 Done sin cambios; T6/T7 pendientes; F-05-I abierta; ningún gate físico marcado. Siguiente paso: revisión manager de `cbf520b`; luego T6.

- **2026-09-16 — F05I-T5 IMPLEMENTED / SOURCE VERIFIED BY AGENT (sesión NORMAL).** CLI `sqx-flowkit` con read surfaces JSON completas y dispatch-first. Baseline verificado: HEAD local `2c5d34f` == `origin/codex/f05-release-prep` tras `git fetch` (sin avance concurrente), baseline `b57bfb2` ancestro, dirty ajeno `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado. Implementación (sólo allowed files; 7 archivos, 1787 inserciones / 391 deletions): `main.go` reducido a despacho+composición (boot `Init()` completo pre-dispatch eliminado; unknown subcommand → exit 2 con usage completo; `help`/`-h` → usage exit 0); `push_output.go` con el comportamiento previo byte-a-byte (flags/defaults/validaciones/stdout/códigos 0/2/10/20/30/40/50; boot interno etcd+telemetry Document+MinIO intacto; único delta aprobado: etcd caído antes de parsear pasa de exit 1 a exit 10 `config_error`); `inspect.go` con `release-matrix` (cero DI: `LoadEmbedded()` u override `--matrix` tipado → exit 2 sin fallback; salida == `MarshalCanonical`), `campaign get` (`LoadForgeCampaignResult`), `campaign list` (`--limit` default 50, `--cursor` base64url-JSON opaco del keyset (created_at, ref), página vacía exit 0), `run stages` (`InspectService` con flowRuns+stages+participations, resto nil), `strategy get` (`InspectService` con identities+manifests+magic+participations+provenance) y `run get` (grafo PG + `sharedmongo.New` + `NewRankingSnapshotStore` → `forge.NewService(control, rankingStore, control)`, primer caller productivo; `--ranking` selector). Emisión JSON determinística (encoder struct-order, escape HTML off, sin timestamps de consulta), errores stderr `<code>:<kind>:<mensaje>` (kinds de la taxonomía `forge.Error`), exit codes frozen (INVALID_ARGUMENT→2, NOT_FOUND→4 nuevo, boot/config→10 `config_error`, CONTRACT_INCONSISTENCY/INFRASTRUCTURE_FAILURE/AMBIGUOUS_RESULT→50), identidad sólo por refs exactos con validación pre-boot (`latest`/malformado → 2 sin infraestructura). Validación real: gofmt OK · `go test ./sqx/cmd/sqx-flowkit/...` PASS (68 casos: dispatch/routing con tabla de comandos, portabilidad release-matrix con CWD en temp vacío y salida byte-idéntica al embebido, overrides válido/equivalente-no-canónico/inexistente/corrupto/schema-inválido/trailing-data/campo-desconocido, golden bytes de `sqx-strategy-inspect.v1` (magic null, delivery null), `sqx-run-stage-timeline.v1` (funnel con error_codes) y `sqx-campaign-list.v1` (página vacía exit 0), mapping ErrorKind 2/4/50/50 sin infraestructura, codec de cursor, validación de identidad pre-boot en 17 casos, BWC de flags push-output por reflexión, grafo mínimo por subcomando verificado sobre AST y ausencia de `Init()`) · `-race` PASS · `go vet` PASS · `go build` paquete PASS · `go test ./sqx/core/releasematrix/... ./sqx/core/forge/...` PASS · `git diff --check` OK · smoke binario real: `release-matrix` desde `/tmp` produce bytes idénticos al artefacto del repo (diff vacío), `campaign get latest` → exit 2. `go build ./sqx/...` global falla exclusivamente en `sqx/tools` (mains duplicados preexistentes al baseline, BASELINE_KNOWN, fuera de alcance; `go build` de flowkit+releasematrix+forge+capabilities+registry-postgres OK). Commit `eed8000eb0202c7dd4dc04458cefd7b265b53dc0` atómico, push normal fast-forward `2c5d34f..eed8000` a `origin/codex/f05-release-prep`, HEAD remoto verificado == local, `2c5d34f` y `b57bfb2` ancestros, dirty ajeno preservado. NOT_RUN: golden del stdout exitoso de push-output (requiere boot DI físico), certificación física (prohibida). T5 `[r]` Review con manager review pending; T1 `[x]` Done por manager approval @ `2c5d34f`; T2–T4 sin cambios; T6/T7 pendientes; F-05-I abierta; ningún gate físico marcado. Siguiente paso: revisión manager del commit `eed8000`; luego T6.

- **2026-09-16 — Corrección F05I-T1 publicada (sesión NORMAL): trailing data en `releasematrix.Parse`.** Defecto confirmado por manager (reproducido con el decoder estándar de Go): `Parse` verificaba trailing data con `dec.More()` tras el primer `Decode`, y `More()` hace peek del siguiente byte devolviendo false ante `}` o `]` pendientes — una matriz JSON válida seguida de `}` o `]` era aceptada, violando el contrato (rechazar TODO contenido no-whitespace posterior al único objeto JSON permitido; el test anterior de dos objetos concatenados no detectaba el problema). Corrección mínima en `Parse`: segundo `Decode` en variable descartable (`var trailing any`) que debe terminar exactamente en `io.EOF`; nil (otro documento) u otro error (contenido inválido) rechazan el documento. Sin cambios de API pública, modelos, estados, matriz de 17 capacidades, marshaling canónico, embedding, loader ni contratos históricos; `DisallowUnknownFields` y `Validate` intactos; sin dependencias externas. Commit `2c5d34f5acde958e93a8e5382bca35be5c96b667` (sólo los 2 archivos autorizados: `releasematrix.go`, `releasematrix_test.go`; 52 inserciones, 3 deletions), push normal fast-forward `5295f1c..2c5d34f` a `origin/codex/f05-release-prep`, HEAD remoto verificado, `5295f1c` ancestro y baseline `b57bfb2` ancestro; dirty ajeno `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado; `release-matrix.json` sin cambios. Test de regresión `TestParseTrailingDataAfterValidMatrix` con inputs desde `Embedded()`: comprobado que FALLA contra el código con `dec.More()` exactamente en `}` y `]` (los casos segundo-objeto y texto-inválido ya eran rechazados por el código viejo) y PASS con la corrección en los 5 casos obligatorios (`}` → ERROR, `]` → ERROR, segundo objeto JSON → ERROR, texto inválido → ERROR, sólo espacios/newlines → PASS). Validación real: gofmt OK · `go test ./sqx/core/releasematrix/...` PASS · `-race` PASS · `go vet` PASS · `go build` PASS · `git diff --check` OK. NOT_RUN: suite global y certificación física (fuera de alcance/prohibidas en la misión). T1 permanece `[r]` Review con manager review pending; T2–T4 sin cambios; T5/T6/T7 pendientes; F-05-I abierta; ningún gate físico marcado. Siguiente paso: revisión manager del commit `2c5d34f`; luego T5.

- **2026-09-16 — F05I-T1 IMPLEMENTED / SOURCE VERIFIED BY AGENT (sesión NORMAL).** Paquete `sqx/core/releasematrix` (schema `sqx-release-matrix.v1`) + artefacto embebido con las 17 filas frozen, conforme a Planning C1: `//go:embed release-matrix.json` en el paquete (ruta canónica fuera de `deploy/`), API `Embedded()` con copia defensiva, `LoadEmbedded()`, `Parse` estricto (unknown fields y trailing data rechazados), `Load(path)` override explícito sin fallback silencioso, `Validate` puro (6 dimensiones exactas por fila, sin derivación entre sí; tabla C1.5 de `evidence_kind` admisible por dimensión con `none` sólo en status ≠ DONE; DONE sin gate ni `none`; DONE released/deployed/physical/cross-lane exige `as_of` YYYY-MM-DD real; físico DONE ⇒ `certification_record`; cross-lane DONE ⇒ `cross_lane_receipt`; DEFERRED ⇒ gate `^CERT-[A-Z0-9]+(-[A-Z0-9]+)*$`; deployed DONE ⇒ `deploy_targets ≥ 1`; target OBSERVED ⇒ release identificada; producer_paths sin wildcards; `implementation_sha ^[0-9a-f]{7,64}$` o vacío) y `MarshalCanonical` (indent 2 + newline final). Artefacto: historia física preservada con records reales — B1A/B1B/B2 PASS/CLOSED as_of 2026-09-06 (agent-runs `2026-09-06-zcode-glm-5.3-flash-echo-forge-mt5-{ownership-b1a,b1b,b2}`), F-03 PHYSICAL PASS as_of 2026-09-10 (job `14m51.98s` COMPLETED 3000/3000); released DONE por `0.2.98` as_of 2026-09-13 (lineage); deployed OPEN con targets stager linux OBSERVED/RUNNING 2026-09-13 y windows NOT_OBSERVED (viewer policy gap mt5-kronos), época B sin targets (incertidumbre conservada); F-04 físico DEFERRED CERT-F04-01/02/03 y cross-lane handoff DEFERRED CERT-E04-01; pipeline/f05i físico DEFERRED CERT-F05-02, release-pipeline CERT-F05-01; release-pipeline released NOT_APPLICABLE (es la autoridad que publica); f05i implemented/source_verified OPEN con SHA vacío. Commit `5295f1ca91f41cc28f999f517c5bbae425f86b1b` (3 archivos autorizados, 2182 inserciones), push normal fast-forward `3da8b47..5295f1c` a `origin/codex/f05-release-prep`, HEAD remoto verificado; baseline `b57bfb2` ancestro y `origin/feature/f04-magic-version-handoff` == baseline; dirty ajeno `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado. Validación real: gofmt OK · `go test ./sqx/core/releasematrix/...` PASS (17 funciones, 51 casos: validador estructural 34 subtests, determinismo, round-trip byte-idéntico, embed==archivo, copia defensiva, portabilidad de Load y 7 guards de contenido) · `-race` PASS · `go vet` PASS · `go build` PASS · `git diff --check` OK · `go list -deps` sin `internal/di`/DB/Temporal/MinIO/etcd (79 deps, stdlib+embed). NOT_RUN: suite global (`go build ./sqx/...` falla sólo en `sqx/tools`, mains duplicados preexistentes al baseline, fuera de alcance) y certificación física (prohibida en T1). T1 `[r]` Review; T2–T4 sin cambios; T5/T6/T7 pendientes; F-05-I abierta; ningún gate físico marcado. Siguiente paso: revisión manager del commit `5295f1c`; luego T5.

- **2026-09-16 — Planning C1: corrección contractual Release Matrix + CLI (sesión TOP, sin tocar source).** Manager aprobó T2/T3/T4 (SOURCE REVIEW APPROVED; tablero a Done; tests locales PASS reportados por el agente, certificación física NOT RUN, T7 pendiente, F-05-I abierta). Misión: resolver los seis defectos C1.1–C1.6 con evidencia de source @ `3da8b470239a15f62d87f16559feada409e2d611` (HEAD verificado sin cambios; dirty ajeno `specs/.../phase4_performance.json` preservado). **C1.1:** `deploy/` es WatchRoot default del deployer-watcher (`main.go` fallback `./deploy`), snapshot recursivo (`source-fsnotify` `WalkDir`) y staging de `deploy_release.sh`; hoy `deploy/release-matrix.json` no se subiría sólo por la forma de `ComputeKey` (`<root>/<semver>/<platform>/<file>` o manifest raíz) — protección accidental que además dispararía ciclos de plan por edición ⇒ ruta canónica `sqx/core/releasematrix/release-matrix.json` (fuera del WatchRoot y del staging; dos garantías independientes). **C1.2:** carga portable por `//go:embed` (idioma existente en migrations/postgrestest) + API `Embedded/LoadEmbedded/Parse/Load/Validate`; override `--matrix` con error tipado exit 2; default relativo-repo eliminado; test de portabilidad con CWD vacío. **C1.3:** `deployed` gana `deploy_targets[]` (`release`, `release_present OBSERVED|NOT_OBSERVED`, `process RUNNING|NOT_OBSERVED`, evidence, as_of); sin inferir deploy de publicación ni proceso de archivos; incertidumbre explícita (windows `NOT_OBSERVED` por viewer policy gap; linux OBSERVED vía `stager-runtime.service` 0.2.98). **C1.4:** semántica por estado (DONE=require evidencia verificable registrada; histórico ⇒ `certification_record`+`as_of` y alcance de época, nunca certifica la release actual; OPEN=no verificado hoy; DEFERRED=gate CERT obligatorio; NOT_APPLICABLE justificado); guard corregido con allowlist histórica `{b1a,b1b,b2,f03}` (la prohibición absoluta de física DONE borraba certificaciones históricas verdaderas: F-03 PHYSICAL PASS job real y B1A/B1B/B2 CLOSED); certificación no comprobable queda pendiente de verificación, no inventada. **C1.5:** autoridad por dimensión con enum `evidence_kind` (git/tests/release_manifest/deployment_proof/runtime_observation/certification_record/cross_lane_receipt/none); RELEASED≠DEPLOYED, SOURCE VERIFIED≠PHYSICAL; `persistence_authority` nombra storage concreto (PG sqx/Mongo forge/MinIO/Temporal cols/etcd) sin intercambiabilidad; cero consultas de infraestructura (MCPs no usados). **C1.6:** hoy `main.go` bootea DI completo (etcd/telemetry/MinIO/Postgres/Temporal) antes de despachar y `runPushOutput` re-inicializa sin guard; contrato nuevo: dispatch-first, `release-matrix` cero DI, inspect PG-only vía `NewControlPlaneFromClient` + `InspectService` con deps nil fail-closed, `run get` añade `sharedmongo.New` + `NewRankingSnapshotStore` (implementa el port read), push-output intacto; único delta documentado: falla de etcd pre-dispatch pasa de exit 1 (boot viejo) a exit 10 `config_error` (tabla frozen); `internal/di` intocado. Validación CONTRACT REVIEW (no BUILD PASS): SPEC y tablero coherentes, T1/T5 sin decisiones abiertas, sin cambios source, sin certificaciones físicas nuevas, contratos frozen ajenos intactos. Siguiente paso: NORMAL ejecuta F05I-T1 con el contrato corregido.

- **2026-09-16 — Corrección F05I-T2 publicada (sesión NORMAL): keyset pagination en empates de `created_at`.** Defecto confirmado por manager: `forge_campaign_list.go` ordenaba `created_at DESC, id ASC` pero filtraba con `(created_at, id) < ($1,$2)` (comparación `id` descendente), excluyendo en cada página las campañas empatadas en `created_at` con `id > cursor.id` — pérdida silenciosa de registros al paginar. Corrección mínima al predicado contractual `created_at < $1 OR (created_at = $1 AND id > $2)`; sin cambios de orden, cursor, firma, validaciones, taxonomía, proyección ni esquema (migration NONE intacto). Commit `3da8b470239a15f62d87f16559feada409e2d611` (solo los 2 archivos autorizados), push normal a `origin/codex/f05-release-prep`, HEAD remoto verificado `3da8b47`, ancestro de `d77342d` verificado, dirty preexistente `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado. Test de regresión nuevo `TestListForgeCampaignsPersistenceTieTimestampsKeysetTraversal` (embedded-postgres aislado + fixture sintética con constraints reales: 3 campañas empatadas en `created_at`, una anterior y una posterior; recorrido completo con `limit=1`): verifica orden contractual exacto, cero pérdidas, cero duplicados y página final vacía; comprobado que FALLA contra el predicado anterior (página vacía donde debía haber fila del empate) y PASS con la corrección. sqlmock de cursor actualizado al SQL nuevo. Validación: gofmt OK · `go test -run 'TestListForgeCampaigns'` PASS · build consumers OK · `-race` PASS · `go vet` PASS · `git diff --check` OK. NOT_RUN: suite completa registry-postgres (timeout preexistente conocido; el gate es el subset enfocado) y suite global (fuera de scope por prompt). T2 permanece en Review; T3/T4 sin cambios de estado; F-05-I/F-05-C no cerradas; ningún gate físico marcado. Siguiente paso: revisión manager del commit `3da8b47` antes de Planning C1.

- **2026-09-16 — T2–T4 publicadas en GitHub y movidas a Review; corrección de trazabilidad L0 (sesión NORMAL, sin tocar source).** Branch `codex/f05-release-prep` publicada en `origin` (push normal, sin force, sin tags, sin PRs): HEAD remoto verificado `d77342d5a49cb393f92e1a85f998c5614a1cd6bb` == HEAD local; baseline `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` ancestro (GitHub compare: 3 commits ahead, 0 behind, 13 archivos); `git diff --check` baseline..HEAD limpio; commits accesibles por SHA. Dirty preexistente `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado sin commitear. Tareas T2/T3/T4 movidas de Done a Review (tablero `[r]`): IMPLEMENTATION REPORTED + SOURCE VERIFIED BY AGENT; MANAGER REVIEW PENDING; PHYSICAL CERTIFICATION NOT RUN. Corrección Agents OS: el L0 `2026-09-16-f05i-t2-t4-implementation-raw.md` era una reconstrucción sin transcript (prohibido por contrato session-close) → hechos operativos únicos fusionados en el L1 existente y L0 eliminado; detalle en `80-agents/journal/logs/2026-09-16-f05i-publication-review-corrections.md`. F-05-I NO cerrada; ningún gate físico marcado PASS. Siguiente paso: revisión manager del diff en GitHub.
- **2026-09-16 — F05I-T2 + T3 + T4 IMPLEMENTED / SOURCE VERIFIED (sesión NORMAL).** Baseline verificado `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` == `origin/feature/f04-magic-version-handoff` tras `git fetch` (sin BASELINE_MOVED); branch `codex/f05-release-prep` creada desde el SHA exacto (HEAD local previo `9fad768`, detrás del baseline; dirty preexistente `specs/.../phase4_performance.json` preservado sin commitear). Commits: `7d073b3` (T2: ports `capabilities/forge_inspect_query.go` + 4 adapters PG read-only con orden/paginación frozen), `65c879a` (T3: `core/forge/funnel.go` proyección pura `sqx-forge-funnel-projection.v1`), `d77342d` (T4: `core/forge/inspect.go` InspectService con `sqx-strategy-inspect.v1` / `sqx-run-stage-timeline.v1` / `sqx-campaign-list.v1`). Diff: 13 archivos nuevos autorizados, 2526 inserciones, 0 modificaciones a archivos existentes; sin migraciones, sin `internal/di`/`deploy`/`deployer`/`cmd`, contratos frozen intactos. Tests: gate enfocado `go test ./sqx/adapters/registry-postgres/ -run 'TestListForgeCampaigns|TestListStageExecutionsByFlowRun|TestListParticipations|TestStrategyProvenanceReads'` PASS (sqlmock + persistencia embedded-postgres aislado, fixtures sintéticas etiquetadas); `go test ./sqx/core/forge/... ./sqx/core/capabilities/...` PASS; `-race` PASS en los tres paquetes; `go vet` PASS; `gofmt` OK; `git diff --check` OK. NOT_RUN: `go build ./sqx/...` completo falló sólo en `sqx/tools` (mains duplicados preexistentes al baseline, fuera de scope); suite completa de registry-postgres no corrida (timeout conocido preexistente, el gate es el subset enfocado); certification física/CLI (T5+) no aplica a esta sesión. Dependencia técnica registrada: `InspectService` define port local `MagicAllocationReader` (consumidor) satisfecho estructuralmente por `ControlPlane.LoadMagicAllocation`; namespace magic se recibe por wiring (`MagicRegistryNamespaceLive`). Sin certificación física: ningún gate T2.11/T2.12/T2.13 ni golden marcado. Siguiente paso: revisión manager del handoff; luego T1/T5/T6/T7 según asignación.
- **2026-09-13 — TOP planificación completa.** Recon source read-only @ `b57bfb2`: read models existentes sin callers (`forge.Service`, `LoadForgeCampaignResult`), cero HTTP/API, tools informales; authorities mapeadas (PG 19 tablas sqx, Mongo 9 colecciones, MinIO, Temporal cols, etcd). SPEC frozen [[Echo Forge — F-05-I Release Matrix and Read Surface Contract]]: CLI JSON + release matrix declarativa + funnel proyección pura; `DATABASE MIGRATION: NONE`. Tareas F05I-T1…T7, allowed files y matriz de tests congelados. NORMAL pendiente autorización manager.

## 🧭 Decisiones

- Paginación `campaign list` = cursor opaco **server-issued** (F05I-PAGINATION-C1): `next_cursor: string|null` siempre presente en `sqx-campaign-list.v1`, generado por el read service con probe `LIMIT N+1` (null exacto = no hay fila posterior); el consumidor nunca construye ni interpreta el token; permanece v1 (corrección aditiva pre-release).

- Arquitectura read surface = CLI JSON determinístico sobre ports/services (sin HTTP; front futuro envuelve los mismos services).
- Release matrix vive en repo (`sqx/core/releasematrix/release-matrix.json`, embebida en el CLI) + validador puro; jamás en `deploy/` (WatchRoot del deploy pipeline); no es autoridad de release.
- Carga de la matriz: embebida por defecto (`go:embed`), override explícito `--matrix` con error tipado; cero dependencia de checkout/CWD (Planning C1.2).
- Boot del CLI: dispatch primero, boot mínimo por subcomando; `release-matrix` sin DI; inspect PG-only; `run get` añade Mongo; push-output intacto (Planning C1.6).
- Estados de la matriz: DONE/OPEN/DEFERRED/NOT_APPLICABLE con `evidence_kind` y `as_of`; historia física certifiable se conserva (allowlist b1a/b1b/b2/f03); flota por `deploy_targets[]` con enums de observación (Planning C1.3/C1.4/C1.5).
- Funnel derivado de stage_executions (topología dinámica), nunca hardcoded.
- `PARTIAL` no se inventa: estados existentes por nivel representan parcialidad.
- Exit code `4` = NOT_FOUND (aditivo).

## 🔗 Docs / Links

- [[Echo Forge — F-05-I Release Matrix and Read Surface Contract]] (SPEC)
- [[Echo Forge — Factory V2 Completion]] (parent/roadmap)
- [[Echo + Echo Forge — Deferred Certification Backlog]] (F-05-C)
- [[echo-forge]] · [[echo-forge-integration-boundary]]

## 💡 Ideas

### Backlog de ideas

- Front métricas/cohorts y SQX viewer `-gui` post-F-05 (no son F-05-I).

### Motivos / principios

- Membership ≠ rank; projection ≠ authority; zero finalists = resultado válido; ausencia ≠ cero.

### Memoria pública / interna

- **Memoria pública:** SPEC/contract docs.
- **Memoria interna:** ninguna nueva (sin delta operativo más allá del proyecto).
- **Motivo:** el estado y la historia viven en el proyecto y las SPECs.
