---
type: doc
schema_version: 1
status: active
area: "[[Echo]]"
related:
  - "[[Echo — Producto Integrado]]"
  - "[[Echo Forge — Factory V2 Completion]]"
  - "[[Echo Forge — F-04 Magic allocation, version seal and handoff]]"
  - "[[Echo — E-04 Forge Ingestion E1]]"
  - "[[AGENT-PLATFORM - MCP Access Plane]]"
aliases:
  - Echo certification backlog
  - F-04 deferred certification
tags:
  - kind/doc
  - area/echo
  - project/echo
created: "2026-09-13"
updated: "2026-09-20"
---

# Echo + Echo Forge — Deferred Certification Backlog

## Propósito

Mantener en un carril único, ordenado y no ejecutado las certificaciones físicas y cross-lane que quedaron fuera del critical path de desarrollo de Echo/Echo Forge.

## Contenido

### Decisión de gestión congelada

La certificación física/de infraestructura se retira temporalmente del critical path de desarrollo mientras se construye el Aranea MCP Access Plane; no se waiva. `IMPLEMENTED != CERTIFIED`. No se cambia ningún contrato frozen, no se inventa evidencia y no se marcan PASS T2.11, T2.12 ni T2.13.

### Taxonomía de estado

| Estado | Significado | Evidencia mínima |
|---|---|---|
| PLANNED | Trabajo definido, no ejecutado | alcance y dependencias |
| IMPLEMENTED | Código/configuración materialmente implementado | diff y tests del scope |
| SOURCE VERIFIED | Fuente y contratos verificados | revisión/gates source |
| RELEASED | Artefacto publicado desde SHA autorizado | manifest y digest |
| DEPLOYED | Runtime objetivo ejecuta el artefacto autorizado | deployment proof |
| PHYSICALLY CERTIFIED | Receta real ejecutada sobre runtime físico | evidencia durable end-to-end |
| CROSS-LANE CERTIFIED | Handoff real aceptado por el lane consumidor | receipt y read-back |
| CLOSED | Todos los estados requeridos por la fase están satisfechos | veredicto final y referencias |

Los estados son dimensiones de evidencia; `CLOSED` nunca se deriva de `IMPLEMENTED` o `RELEASED` por sí solos.

### Estado de entrada

F-04: `IMPLEMENTED` y `SOURCE VERIFIED` en product SHA `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43`, release `0.2.98` publicada y desplegada en flota completa (Linux Zeus/Hera/Kronos + Windows `worker-kronos` converged con modelo stager aceptado por owner). CERT-F04-01 fue ejecutado el 2026-09-17 (misión EXEC) y terminó **`BLOCKED`** por un defecto determinístico de la release en el stage `mt5_compile` (detalle en el delta EXEC de esta fecha): la cadena física alcanzó y certificó parcialmente campaign→wave→FlowRun→funnel completo→**allocation durable Magic V1 (3 allocations reales)**→final reretester→trade list→MQ5 export, pero `mt5_compile_artifact` falla 100% reproducible (`persistence_deadline_required` en la descarga de source durable) y sin fix de source + release nueva el gate no puede completar seal/handoff. **blocker source fix = `SOURCE VERIFIED` 2026-09-17** (misión F05C-CERT-F04-01-C1: fix en `DownloadArtifactTaskSource` @ commit `49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1`, branch `codex/f04-cert-f04-01-deadline-fix`, tests T1–T6 + repro del defecto contra el source 0.2.98; detalle en el delta de corrección de esta fecha). **Release `0.2.99` publicada y desplegada en flota completa 2026-09-17** (misión F05C-CERT-F04-01-C2, veredicto `RELEASE/ROLLOUT PASS`: fix branch y `codex/f05-release-prep` fast-forward a `49fce32…` verificables en origin, 6 artifacts byte-exactos, Linux Zeus/Hera/Kronos + Windows `worker-kronos` converged sobre `0.2.99`; detalle en el delta de release de esta fecha). El re-run físico del gate se ejecutó el 2026-09-17 (misión F05C-CERT-F04-01-RERUN, identidades todas nuevas) y **demostró físicamente la regresión del fix C1** (descarga durable OK, sin `persistence_deadline_required`, MetaEditor real `0 errors`, EX5 176730 B + log durables, allocation Magic V1 `26090011004` con read-back), pero terminó **`BLOCKED`** por un **segundo defecto determinístico de la release 0.2.99** en `mt5_compile_persist_v1`: el gate de seal `VerifyCompiledArtifactForSeal` matchea el compile log crudo UTF-16LE de MetaEditor sin normalizar (el gate local del compile sí normaliza) → `CONTRACT_CONFLICT compile log does not report 0 errors` y FlowRun/Campaign `FAILED/WAVE_FLOW_RUN_FAILED` antes de backtest/seal/handoff (detalle en el delta RERUN de esta fecha). **blocker source fix encoding = `SOURCE FIX VERIFIED` 2026-09-17** (misión F05C-CERT-F04-01-C3: criterio único `CompileLogReportsZeroErrors` en `sqx/core/capabilities/strategy_version.go` invocado por compiler y seal, parser duplicado del adapter eliminado, commit `18d09054630f1da0f3c44a13c6132af3e17b0596`, branch `codex/f04-cert-f04-01-encoding-fix`, tests T1–T8 + repro del defecto contra el source `49fce32`; detalle en el delta de corrección encoding de esta fecha). F-04 sigue `NOT PHYSICALLY CERTIFIED`, `NOT CROSS-LANE CERTIFIED` ni `CLOSED`. El microfix C4 sobre C3 (`a440ac4ed730747b1ae6b07557cb6204102f16ac`, UTF-8 estricto en logs sin BOM) se publicó como release `0.2.100` desplegada en flota completa 2026-09-17 (misión F05C-CERT-F04-01-C4, veredicto `RELEASE/ROLLOUT PASS`). El re-run físico #3 del gate se ejecutó 2026-09-17/18 (misión F05C-CERT-F04-01-RERUN-3, identidades todas nuevas) y avanzó más que ningún intento previo: funnel completo → Decision V2 → **allocation durable Magic V1 `26090011005`** con read-back → Apply COMPLETED → final reretester → **compile físico Windows limpio (`0 errors, 1 warnings`, EX5 176426 B `6d667d70…`) → `mt5_compile_persist_v1` COMPLETED con EvaluationRef `sha256:6d1a37ce…` (fixes C1+C3+C4 validados físicamente, cero `persistence_deadline_required` y cero `artifact_integrity` falso) → backtest físico real de 54 min en worker-kronos (HTM durable 13074872 B)** — pero terminó **`BLOCKED`** por un **tercer defecto determinístico (entorno×contrato)**: `mt5_reconcile_v1` falló non-retryable `contract_error: mt5 report: build not supported: build=6182` — el terminal MT5 de worker-kronos auto-actualizó a build 6182, fuera de la allow-list fail-closed de builds certificados `{6090, 6140, 6180}` de `mt5-report.v1` (`sqx/adapters/mt5/report/types.go:13` @ `a440ac4`), antes de scores/ranking/promotion/seal/handoff (detalle en el delta RERUN-3 de esta fecha). **CERT-F04-01 = `BLOCKED`**. El 2026-09-18 la misión F05C-CERT-F04-01-C5 intentó el camino owner (a) (certificar build 6182 contra `mt5-report.v1`) y terminó **`INSUFFICIENT_EVIDENCE`**: la provenance física completa del HTM de RERUN-3 quedó recuperada y anclada al resultado durable del child workflow (bucket `sqx-strategies`, key completa `…/09_mt5_backtest/final-b94f1400-….htm`, 13074872 B, SHA256 completo `21917e14…8b5c`, EX5 input `6d667d70…`, child WF/RunID, timezone `GMT`, producer build 6182 worker-kronos), pero las cuatro vías de lectura autorizadas (ArtifactRef persistida, CLI, MinIO RO 403 estructural, copia física Windows ya limpiada sin ninguna coincidencia de hash) no permiten obtener los bytes auténticos, así que la certificación no es ejecutable: blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` (detalle en el delta C5 de esta fecha). **La re-adquisición C5A del 2026-09-18 distinguió LIST de GET en la capability MinIO RO y cerró la causa del 403: el conector preconfigurado ya apunta al MinIO correcto (`192.168.31.92:9000`), la identidad RO es válida (ListBuckets OK sobre `deploy`/`examples`) y la denegación es exclusivamente de política IAM — `HeadObject`, `GetObject` y GET presignado por la propia identidad sobre el objeto exacto devuelven todos HTTP 403, y `sqx-strategies` no es listable — de modo que el cambio mínimo del owner es una statement `s3:GetObject` del objeto puntual en la policy embedded del principal RO, sin cambios de conector; el blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` permanece y la compatibilidad de 6182 sigue sin demostrar ni refutar (detalle en el delta C5A de esta fecha).** **La verificación C5B del 2026-09-18 resolvió el gate A0 de autorización: la concesión sigue sin aplicar (HeadObject/GetObject 403 con RequestIDs nuevos `18D66E30A94AC147`/`18D66E3697C8719F`), no existe autorización expresa del owner para la operación exacta ni copia byte-exacta entregada por canal autorizado, y el agente carece de capability administrativa legítima para el cambio IAM, de modo que la misión C5B terminó `OWNER_APPROVAL_REQUIRED` con la solicitud mínima re-entregada (detalle en el delta C5B de esta fecha).** **La certificación + source fix C6 del 2026-09-18 cerró el ciclo de adquisición por la vía owner: el HTM auténtico de RERUN-3 fue entregado físicamente a `daedalus` (integridad verificada contra el SHA256 durable `21917e14…8b5c`, 13074872 B, BOM `FF FE`; sin cambios IAM ni credenciales) y la misión demostró compatibilidad estructural completa del build 6182 con `mt5-report.v1` — parseo completo del parser real del baseline con 7/7 crosschecks PASS y deltas < 0.005 — e incorporó 6182 a la allow-list en la rama `codex/f05-build6182-parser-cert` @ commit `bcf67be…` publicada en origin (suite de certificación con goldens auténticos 30/30 PASS, vecinos 6181/6183 rechazados, CORPUS §5.6 + SPEC-PARSER actualizados; los 13 MB de bytes del fixture NO se versionaron en Git por falta de autorización y quedaron como gap del corpus con tests reproducibles vía `SQX_MT5_B6182_FIXTURE`; detalle en el delta C6 de esta fecha).** **La corrección de revisión C6R del 2026-09-18 cerró los cuatro pendientes del manager sobre el fix C6 (R1 diagnóstico genérico de build ausente sin duplicar la matriz, R2 SPEC §3.2 con 6182, R3 modo obligatorio `SQX_MT5_REQUIRE_B6182_FIXTURE=1` con cero skips y fail-closed ante fixture ausente/incorrecto, R4 coverage 81.6% → 99.1% con 18 tests de contrato enfocados) en el commit adicional `c1d24c1…` sobre `bcf67be…` (sin reescritura) publicado en origin con read-back; veredicto `SOURCE_READY`, paquete completo 53/53 PASS en modo release con fixture auténtico, detalle en el delta C6R de esta fecha.** **La release + rollout C7 del 2026-09-18 publicó `0.2.101` desde el source aprobado — `codex/f05-release-prep` fast-forward `a440ac4…`→`c1d24c1…` (verificado en origin) — tras el release test gate obligatorio con fixture auténtico (`SQX_MT5_REQUIRE_B6182_FIXTURE=1`, 53/53 PASS, 0 skips, coverage 99.1%) y la desplegó en flota completa con veredicto `RELEASE/ROLLOUT PASS` (read-back `EXACT_MATCH/CONSISTENT`; Linux Zeus/Hera/Kronos + Windows `worker-kronos` convergidos sobre `0.2.101` con SHA instalado == artifact; detalle en el delta C7 de esta fecha).** E-04 está `INTEGRATED`, pero T21/AC-37 y `FINAL CLOSED` siguen pendientes. F-05 conserva su split implementación/certificación. **El re-run físico #4 del gate se ejecutó 2026-09-18/19 (misión F05C-CERT-F04-01-RERUN-4, identidades todas nuevas, una campaña `8a6b7dda…` sobre `0.2.101`) y logró la cadena más profunda de la serie — funnel completo, allocations durables `26090011006–009`, 4 compiles físicos Windows limpios, 4 backtests físicos reales, 4 HTM auténticos build 6182 byte-verificados contra el SHA256 durable vía la lectura MinIO ya autorizada (cierre material del blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` de C5A/C5B), `mt5_reconcile_v1` ×4 COMPLETED con 6182 aceptado en producción (7/7 crosschecks), ranking y promotion — pero terminó **`BLOCKED`** por un **cuarto defecto determinístico (producto)**: `forge_seal_handoff_v1` rechaza el manifest porque el assembler le entrega como `ApplyEvaluationRef` la Evaluation que el final reretester reemplazó por diseño (`CONTRACT_CONFLICT: member 107f1da5-cf85-443f-8fbf-55ba1dbb551e source Evaluation is project@sqx-final-reretester.v1, apply authority required`, evento 492, 2026-09-19T03:15:56Z), antes de StrategyVersion seal/HandoffManifest (detalle en el delta RERUN-4 de esta fecha).** **La corrección source C9 del 2026-09-19 cerró el cuarto defecto en el source con veredicto `SOURCE_FIX_READY_FOR_REVIEW`: nuevo campo `StrategyArtifact.ApplyEvaluationRef` (`apply_evaluation_ref,omitempty`) fijado exclusivamente desde el resultado exitoso de Durable Apply, assembler del seal consumiendo el campo fijado con fail-closed ante autoridad Apply ausente y ante miembros duplicados, productor del final reretester intacto (la copia por valor del carrier en `steps.go` preserva físicamente el campo), consumer `forge_seal_handoff.go` sin cambios; commit único `87a3ab944c5ce706ac3fe7766fa86af32189cc05` en la rama `codex/f05-seal-apply-authority-fix` desde el parent exacto `c1d24c1…`, publicado en origin con read-back; tests T1–T7 con demostración RED contra baseline y suites con conjuntos de fallo idénticos al baseline (detalle en el delta C9 de esta fecha).** **La corrección de revisión C9R del 2026-09-19 cerró los dos hallazgos de la revisión del source C9 con veredicto `SOURCE_READY_FOR_MANAGER_REVIEW`: el fan-in del final reretester ahora exige `ApplyEvaluationRef` byte-exact (fail-closed `contract_conflict` ante drop/replace/fabricación, con `EvaluationRef` reemplazándose por contrato y G22 intacta), con tests negativos RED/GREEN demostrados contra `87a3ab9`, un test compuesto real Apply → fan-out/fan-in → assembler que intercepta el request auténtico a `forge_seal_handoff_v1` sin cruces entre miembros, y los 37 fallos históricos clasificados idénticos por nombre y causa contra `c1d24c1`; commit único `66faa42debba18cd0dd09de127ae1374ea2a07f0` sobre `87a3ab9…` publicado en origin con read-back (detalle en el delta C9R de esta fecha).** **La release + rollout C10 del 2026-09-19 publicó `0.2.102` desde el source aprobado C9+C9R — `codex/f05-release-prep` fast-forward `c1d24c1…`→`66faa42…` (verificado en origin) — tras el verification gate (tests C9/C9R con los 21 fallos de workflows idénticos al baseline por nombre y causa normalizada; worker y runtime PASS completos con el fixture auténtico restaurado; release test gate 53/53 PASS, 0 skips, coverage 99.1%; vet/gofmt/builds con los 2 fallos preexistentes documentados; anti-test-masking PASS 805+/0−) y la desplegó en flota completa con veredicto `RELEASE/ROLLOUT PASS` (read-back `EXACT_MATCH/CONSISTENT`; Zeus/Hera/Kronos + Windows convergidos sobre `0.2.102` con SHA instalado == artifact, `MT5_PROCS=0`, 0 Running en `sqx-prop`, releases históricas intactas; detalle en el delta C10 de esta fecha).** **El re-run físico #5 del gate se ejecutó 2026-09-19/20 (misión F05C-CERT-F04-01-RERUN-5, identidades todas nuevas, una campaña `c42dd637…` sobre `0.2.102`) y superó por primera vez el gate que mató a RERUN-4 — el request real de `forge_seal_handoff_v1` llegó con `ApplyEvaluationRef != CompileEvaluationRef` ×3 desde Durable Apply, validando el fix C9/C9R en producción — y avanzó en verde hasta promotion inclusive con una cohorte honesta de 3 miembros (funnel, allocations `26090011010–012` por Apply, 3 compiles físicos Windows, 3 backtests reales secuenciales ~54–70 min con `LIVE_PHYSICAL_MT5_EXECUTIONS<=1` demostrado en 4 muestras vivas, 3 HTM auténticos 6182 byte-verificados con 7/7 crosschecks, `mt5_reconcile_v1` ×3 `structural_identity: MATCH`, scoring/ranking/promotion con 3 finalists_v2 y warnings `PNL_SIGN_FLIP`), pero terminó **`BLOCKED`** por un **quinto defecto determinístico (producto)**: `forge_seal_handoff_v1` rechaza el read-back de magic porque `magicreadback.FromSQX` parsea como XML plano el `strategy.sqx` que el producer Apply emite como contenedor ZIP (`XML syntax error on line 3: illegal character code U+0003` sobre el header `PK\x03\x04`; evento 492, 2026-09-20T00:49:08Z), antes de StrategyVersion seal/HandoffManifest (detalle en el delta RERUN-5 de esta fecha).** **La corrección source C11 del 2026-09-20 cerró la mitad source del defecto #5 con veredicto `SOURCE_FIX_READY_FOR_MANAGER_REVIEW`: `FromSQX` ahora detecta la firma `PK\x03\x04`, abre el contenedor en memoria (`archive/zip`, sin extracción al filesystem), resuelve la entrada canónica exacta `strategy_Portfolio.xml` (duplicada → ambigua, ausente → missing, sin búsqueda alternativa por nombre parcial), aplica un límite documentado de descompresión (16 MiB vs 32618 B observados) con CRC y truncamiento surfaced como errores de contenedor, y sólo entonces alimenta el parser XML existente (`findMagicVariable`/`parseMagicValue` intactos; magic decimal sin coerción int32); el path XML plano histórico queda como ruta explícita probada y nunca como fallback de un ZIP corrupto (sentinels nuevos `ErrSQXZipContainer`/`ErrSQXZipEntryMissing`/`ErrSQXZipEntryAmbiguous`/`ErrSQXZipEntryLimit`); commit único `2993da07d453521d81589413edb344e16ac1a9c6` en la rama `codex/f05-sqx-zip-magic-readback-fix` desde el parent exacto `66faa42debba18cd0dd09de127ae1374ea2a07f0`, publicado en origin con read-back (HEAD/parent/3 archivos autorizados); RED contra `66faa42` reproduce la firma exacta del error de producción (T1 auténtico, T2 ZIP controlado, T3 seal), GREEN con gate auténtico sobre los bytes reales de RERUN-5 (4506976 B, SHA256 `9ac377b9…` → `26090011010` byte-exacto, determinístico, artefacto no mutado; fixture ausente/incorrecto = FAIL, nunca SKIP), matriz negativa completa y cobertura 100% del paquete `magic-readback` (detalle en el delta C11 de esta fecha).**  **La misión F05C-CERT-F04-01-C12/RERUN-6 del 2026-09-20 ejecutó el ciclo completo preflight→release→rollout→campaña física #6 y terminó `BLOCKED` por un sexto defecto determinístico (producto, entrega fail-closed), con el source fix ya entregado para revisión.** Gates previos todos PASS: **G0** verificó `2993da0…` (parent exacto de `66faa42…`, diff de 3 archivos autorizados, dirty operacional preservado) con el fixture auténtico RERUN-5 (`9ac377b9…`), seal 19/19, release gate 6182 en modo obligatorio 53/53 PASS/0 skips/coverage 99.1% y cero regresiones (fallos idénticos al baseline en worker/workflows, 1 flaky documentado pasa aislado en ambos árboles); **G1** construyó el ensayo integral del seal sin efectos reales con la actividad REAL, contratos reales y codec Mongo productivo sobre las 6 evaluaciones durables y 12/12 bytes auténticos de RERUN-5 — obteniendo por primera vez las SQX de los miembros 2 y 3, que leyeron `26090011011`/`26090011012` con el fix C11 — y completó 3/3 miembros (versions+manifests+effective-inputs+INGESTED simulada) con fail-closed ante corrupción de un byte; **G2** publicó `0.2.103` (authority CONSISTENT candidata 0.2.103, FF puro `66faa42…`→`2993da0…` verificado en origin, `deploy_release.sh --release-only`, 6/6 SHA256 == manifest, `vcs.revision=2993da0`, históricas intactas); **G3** certificó rollout Stager automático 4/4 sobre `0.2.103` (Zeus 2816225 / Hera 1403947 / Kronos 1366053 / Windows 3828 con evidence SYSTEM fresca, CURRENT=0.2.103, PENDING.next MISSING, SHA==manifest, pollers ESTAB, `MT5_PROCS=0`); **G4** despachó RERUN-6 UNA sola vez a las 2026-09-20T03:38:42Z con identidades todas nuevas (CampaignRef `f50e5cdb…`, IntentToken `4cdf96fc…`, FlowRunRef `c71687ca…`, request `cert-f04-01-rerun6-20260920T031526Z-1ed05240`; diff semántico exacto de 4 paths vs receta RERUN-5; `VALIDATION_OK` @ 2993da0 con negativos 3/3) y la cadena física avanzó íntegra en verde con cohorte honesta de 6 Apply → 5 promotion (allocations write-once `26090011013–018` leídas de las evaluaciones durables; 5 compiles Windows; 5 backtests reales secuenciales con `LIVE_PHYSICAL_MT5_EXECUTIONS<=1` en 13 muestras vivas; reconciles/scores/ranking/promotion con DecisionRef `sha256:ed703eeb…`; request real del seal capturado del evento 640) — **el fix C11 quedó validado en producción leyendo el Magic ZIP de TODOS los miembros, superando el punto exacto donde murió RERUN-5** — pero la campaña terminó **`BLOCKED`** por el **sexto defecto determinístico (producto, entrega fail-closed)**: con 0 keys `/sqx-worker/production/echo` (wiring diseñado 0 POST), `DeliverHandoff` clasifica la negativa pre-commit de `FailClosedIngress` como `HandoffUnavailable` + error crudo en vez del terminal contractual `HANDOFF_CREATED` + `ErrDeliveryAwaitingReceipt` (F-04 §11), y el consumidor — que sólo tolera awaiting-receipt — entra en reintento infinito: `member 7508a66c… deliver: echo ingest base url / bearer not configured: echo ingest ingress fail-closed` (attempts 1-6+ en el log del worker zeus), campaña sin estado terminal, Running en retry benigno idempotente. **G5** clasificó código, reprodujo RED con la firma física exacta y publicó el fix acotado (44+/1−, 3 archivos: sentinela `ErrIngressNotConfigured` en `capabilities`, clasificación `HANDOFF_CREATED`+awaiting-receipt en `DeliverHandoff`, test aditivo del terminal) en el branch aislado `codex/f05-seal-failclosed-delivery-fix` @ `25a5122baec197ab67bb38eec454d6e62400a472` (origin read-back OK; SIN desplegar; suites worker/core/echo-handoff/magic-readback 25/25 con fixtures auténticos, workflows 21 fallos idénticos al baseline, anti-masking 100% aditivo). Brecha honesta registrada: el ensayo G1 modeló un ingress que acepta (INGESTED) en vez del FailClosedIngress real de producción, por lo que este defecto no fue demostrado pre-campaña pese a estar declarado el terminal esperado en la receta RERUN-5. `CERT-F04-01 = BLOCKED / SOURCE_READY_FOR_MANAGER_REVIEW (defecto #6)`; allocations `26090011001–018` write-once; el delta completo vive en `~/aranea/work/f04-cert-f04-01-rerun6/` y `~/aranea/work/f04-cert-f04-01-g5/`.** **La misión F05C-CERT-F04-01-C13 del 2026-09-20 (tras la aprobación del manager del source C13) ejecutó release `0.2.104` + recuperación natural de RERUN-6 + veredicto de certificación y terminó `CERT_F04_01_PASS` con `RERUN7_NOT_REQUIRED` y `RERUN6_RECOVERED`.** G0 inventario vivo PASS (seal en attempt 16 con retry infinito benigno 1m/2.0/30m; miembro `7508a66c…` estado C —versión sellada + manifest write-once + delivery `UNAVAILABLE`— y miembros 2–5 estado A intactos por el loop fail-fast; ETCD echo 0 keys; `MT5_PROCS=0`; sin campañas competidoras); G1 ensayo realista PASS con los 5 miembros auténticos de RERUN-6 y el `FailClosedIngress` real de producción (20 artefactos SHA256-verificados contra las refs durables de las 10 evaluaciones Mongo; estado RERUN-6 reconstruido byte-exacto — el effective-inputs regenerado igualó los bytes reales `33937102…` de MinIO, ancla de determinismo de identidad; 5/5 `HANDOFF_CREATED`, `ingested=false`, 0 POST, sin duplicación, replay convergente, conflicto same-key/different-payload preservando evidencia; suites C11+C13 20/20, gate 6182 obligatorio coverage 99.1%, fallos idénticos al baseline `2993da0` por nombre, anti-masking aditivo); G2 publicó `0.2.104` (authority CONSISTENT candidate 0.2.104, FF puro `2993da0…`→`25a5122…` en origin, 6/6 SHA256 == manifest, `vcs.revision=25a5122`, históricas 0.2.99–0.2.103 intactas, cero FlowRuns iniciados); G3 rollout Stager 4/4 sobre `0.2.104` (Zeus 2839157 / Hera 1421854 / Kronos 1383513 / Windows 40192 con evidence SYSTEM `partial=false errors=[]`, SHA==manifest incluido `exe_sha256 c250d7dd…` EXACT, `MT5_PROCS=0`); **G4 recuperación natural sin reiniciar nada: el attempt 17 a las 14:23:32Z fue atendido por los workers `0.2.104` desplegados, el seal completó 5/5 miembros `HANDOFF_CREATED` + `ingested=false` con 5 VersionRefs/IdempotencyKeys distintos, el effective-inputs de `7508a66c` siguió byte-inmutable desde 08:22:29Z (write-once), la campaña `f50e5cdb…` terminó `COMPLETED/TARGET_REACHED` con 5 finalistas, ambos workflows terminales, Running=0, 0 POST y 0 backtests adicionales**; G5 declaró `CERT_F04_01_PASS` por lectura literal: los 4 criterios PASS del backlog están satisfechos (SHA homogéneo 4/4 en la flota en cada instante —productoras bajo `2993da0`, seal/delivery bajo `25a5122` con atribución por etapa—, flujo real completo sin bypass, bytes/refs/allocation/seal/manifest verificables, cardinality-safe); el criterio de "release cohesiva" pertenece a CERT-F05-01 y ninguna cláusula exige campaña de release única; F-04 autoriza el retry post-`UNAVAILABLE` con convergencia write-once por identidad de contenido. `HANDOFF_CREATED ×5` es el terminal contractual con Echo sin configurar (INGESTED pertenece a CERT-E04-01/CERT-F04-03). G7: `RERUN-7 NOT_REQUIRED` (cero campañas nuevas). Evidencia: `~/aranea/work/f04-cert-f04-01-rerun6/g1-stage/`; agent-run `2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-01-c13-recovery-certified`.**

### Delta de release, recuperación natural y certificación C13 — 2026-09-20 (misión NORMAL F05C-CERT-F04-01-C13; release 0.2.104 + recuperación de RERUN-6 sin reiniciarla + veredicto de certificación; veredicto `CERT_F04_01_PASS` / `RERUN6_RECOVERED` / `RERUN7_NOT_REQUIRED`)

Ejecutó el mandato maestro C13 tras la aprobación del manager del source `25a5122baec197ab67bb38eec454d6e62400a472` (fix del defecto #6): publicó la release corregida, recuperó la ejecución física existente RERUN-6 por la recuperación natural de Temporal sin cancelar/reiniciar/re-dispatchear nada, y completó la certificación con la evidencia recuperada. Sin campañas nuevas, sin cambiar workflow IDs, sin reutilizar allocations, sin configurar Echo. **Se evitó un séptimo backtest físico completo: los 5 backtests de ~1 h cada uno de RERUN-6 se aprovecharon íntegros.**

- **G0 inventario vivo PASS:** CampaignRef `f50e5cdb-9495-46c1-9a6d-96b0bc562fe4` y FlowRunRef `c71687ca-8811-4c9a-86fe-1788e2d4264c` resueltos desde la evidencia canónica; workflows `sqx-forge-campaign-v1-4cdf96fc…` (run `01a0bce5-0b8e-7f33-af02-fd6bc20f48e9`) y `sqx-main-v1-6fd4912a…` (run `01a0bce5-0cee-70f9-9067-dddac9f76bc6`) Running desde 03:38:42Z y únicos en `sqx-prop`; `forge_seal_handoff_v1` pending `Scheduled` attempt 16, retry policy unlimited (initial 1m, coefficient 2.0, max 30m, schedule_to_close/start_to_close efectivamente ilimitados), último error fresco = firma exacta del defecto #6 sobre `7508a66c-4c46-4328-9454-135c24c4d1d3`; task queues con 3 pollers Linux + 1 Windows (PIDs de 0.2.103) frescos; clasificación por miembro (PG del flujo sin canal autorizado, igual que C12; MinIO+Mongo+Temporal+describe gRPC): `7508a66c` = estado C (versión sellada + manifest + delivery `UNAVAILABLE` + effective-inputs `durable/forge-seal/v1/c71687ca…/7508a66c…/effective-inputs-339371024440bae66a7c6d106939f2439012a82c364a4bcb2ec57a851b31a780.json` de 08:22:29Z), `960d39f8`/`9a7e5cbb`/`ce307453`/`ddef83aa` = estado A (cero efectos: loop fail-fast — los 15 attempts previos murieron idénticos en el deliver del primer miembro); ningún estado con posibilidad de POST real (ingress fail-closed por diseño + ETCD `/sqx-worker/production/echo` = 0 keys fresco); `MT5_PROCS=0` y sin trabajos físicos competidores; allocations `26090011013–018` write-once confirmadas (evaluaciones Apply leídas de Mongo con `magic_number` exacto por miembro; `5a63da72`/`015` reservada-no-usada por el funnel, válida por contrato).
- **G1 ensayo realista PASS (corrige la brecha del ensayo C12):** arnés temporal creado y eliminado del worktree sin residuo (árbol == `25a5122` tras la corrida). Reconstruyó el estado durable exacto de RERUN-6 y ejecutó la actividad REAL `ForgeSealHandoffActivity`, la máquina de estados REAL `DeliverHandoff` (código C13) y el `FailClosedIngress` REAL de producción (`echohandoff.NewFailClosedIngress("echo ingest base url / bearer not configured")`, el wiring de `cmd/sqx-worker/main.go:409`), sobre los 5 miembros auténticos del request del evento 640 (refs exactas), 20 artefactos reales (SQX/MQ5/EX5/compile.log ×5) descargados de MinIO por presign GET y verificados por SHA256 contra las refs durables de las 10 evaluaciones Mongo (5 apply `6f544836/30459a19/ec16794a/95626d82/60175e66` + 5 compile `22632e23/d78b5f1b/7b81c55c/a3be851a/9bf902a9`, `_id` exactos), allocations reales `26090011013–018` sembradas desde la evidencia con verificación `stamped == allocated` por miembro, stores locales simulados y HTTP sin salida a red. Fase 0 (reproducción del comportamiento 0.2.103 con ingress legado sin sentinela): firma física exacta reproducida — fail-fast en el primer miembro, 1 versión + 1 manifest + fila `UNAVAILABLE` — y **el effective-inputs regenerado resultó byte-idéntico al objeto real de MinIO (`33937102…`): ancla de determinismo de identidad entre el binario productor y el código C13**. Fase 1 (recuperación con C13): 5/5 outcomes `HANDOFF_CREATED` con `ingested=false`, `ErrDeliveryAwaitingReceipt` tolerado por el caller, transición `UNAVAILABLE→CREATED` de la fila del miembro 1 con la negativa registrada en `last_error`, 5 llamadas Deliver todas rechazadas pre-commit (0 POST por construcción), exactamente 5/5/5 versiones/manifests/deliveries sin duplicación, el miembro preexistente conservó su VersionRef e IdempotencyKey originales y 4 effective-inputs nuevos en sus locators canónicos. Fase 2 (replay misma identidad/payload, con delivery `CREATED` preexistente y manifest write-once existente): convergencia total sin duplicación ni re-POST. Fase 3 (same-key/different-payload): `CONTRACT_CONFLICT` preservando fila de delivery y digest del manifest original. Verificación del árbol C13: suites seal C11+C13 20/20 PASS con fixture auténtico RERUN-5 (`9ac377b9…`), `magic-readback` PASS, `echo-handoff` PASS, `core/...` PASS, gate 6182 obligatorio PASS coverage 99.1% (fixture `21917e14…` re-verificado), `mt5-export.htm` restaurado byte-exacto desde `b5c71d5` (`090ca4d1…`), gofmt/vet limpios en los archivos del fix, comparación amplia contra baseline `2993da0` en worktree gemelo: 21 fallos de `workflows` idénticos por nombre (sólo dígitos de timing) y `worker` idéntico (0 fallos con fixture); anti-test-masking: diff `2993da0→25a5122` 100% aditivo (44+/1−, sin skips ni eliminaciones).
- **G2 release PASS:** FF puro `git merge --ff-only 25a5122` en `codex/f05-release-prep` (2993da0→25a5122) desde el checkout principal, push sin force, read-back `origin == 25a5122baec197ab67bb38eec454d6e62400a472` en ambas ramas; dirty operacional (`deploy/manifest.json` + `phase4_performance.json`) preservado sin stage. `release-authority` preflight `CONSISTENT` (published 0.2.103, candidate 0.2.104 resuelta en vivo, no presupuesta). Publicación canónica `./deploy_release.sh --release-only ""` + deployer-watcher (screen vivo desde el 17/09); MinIO confirmó manifest `0.2.104` 14:08Z. Certificación: authority post-publish `published_version=0.2.104 CONSISTENT` (candidate siguiente 0.2.105); manifest 6/6 con SHA256 local == manifest (symphony `a81aa5a1…`, sqx-watcher `c4417993…`, start-sh `9f956384…`, run-symphony `8f240eb2…`, promtail `eaa4e1b2…`, `sqx-mt5-worker.exe` `c250d7dd…`); `vcs.revision=25a5122baec197ab67bb38eec454d6e62400a472` en ambos binarios (`vcs.modified=true` por dirty operacional no-Go, mismas condiciones que la build 0.2.103); releases históricas 0.2.99–0.2.103 intactas; cero FlowRuns iniciados por la publicación (Temporal siguió con sólo los 2 workflows de RERUN-6).
- **G3 rollout PASS 4/4 con workflow existente activo:** compatibilidad demostrada antes del deploy — el diff `2993da0→25a5122` no toca `sqx/workflows/**` (código determinista idéntico al binario desplegado → replay seguro) ni la firma/serialización de la actividad, el modelo de worker es plano sin versioning (un retry pendiente es atendible por cualquier poller actualizado) y el único cambio semántico productivo es la clasificación del terminal de entrega bajo ingress sin autoridad, path exclusivo del defecto; sin otras actividades sensibles afectadas. Certificación: Zeus CURRENT `0.2.104` PID 2839157 (14:08:48Z), Hera PID 1421854 (14:10:30Z), Kronos PID 1383513 (14:09:57Z) con SHA symphony/watcher == manifest ×3; Windows evidence SYSTEM 14:13:06Z `partial=false errors=[]`, `CURRENT=0.2.104`, `sqx-mt5-worker` PID 40192 desde 14:09:02Z @ `releases\0.2.104\bin` con `exe_sha256 c250d7dd06f5cfb5aeecca0922ad2c9e817236a86900d6765a10d17e57421335` == manifest EXACT (37249024 B), padre stager-runtime Running/Automatic `.\kor`, poller ESTAB, singleton, `MT5_PROCS=0`; pollers frescos en Temporal con los 4 PIDs nuevos a las 14:14–14:15Z; sin binarios antiguos residuales en CURRENT.
- **G4 recuperación natural PASS (cero intervención, cero cancel/terminate/reset/re-dispatch):** el attempt 16 disparó 13:53:32Z en un worker viejo (Hera PID 1403947 pre-rollout; fallo benigno idéntico esperado) y el **attempt 17 a las 14:23:32Z fue atendido por los workers `0.2.104` ya desplegados**: el seal completó los 5 miembros y `sqx-main-v1-6fd4912a…` pasó a `Completed` en segundos. Resultado físico del seal (payload real de cierre): 5/5 miembros `HANDOFF_CREATED` + `ingested=false` con VersionRef/IdempotencyKey propios y distintos — `7508a66c`→`sha256:6d3cab1a…`/`718c4aeda…`, `960d39f8`→`04ab1176…`/`9f79226c…`, `9a7e5cbb`→`d41f3468…`/`2f76cc56…`, `ce307453`→`491c2094…`/`2686db6e…`, `ddef83aa`→`eefefdbd…`/`b8451d84…` — y `skipped` vacío; campaña `f50e5cdb…` → `{"status":"COMPLETED","stop_reason":"TARGET_REACHED","effective_unique_finalists":5,"waves_started":1,"waves_completed":1}`; ambos workflows terminales y `Running=0` en `sqx-prop`; MinIO `durable/forge-seal/v1/c71687ca…` con exactamente 5 objetos (el de `7508a66c` byte-inmutable desde 08:22:29Z, etag intacto; 4 nuevos 14:23:32Z); 0 POST reales (ETCD echo 0 keys fresco post-recuperación); 0 backtests adicionales — la física previa se aprovechó íntegra.
- **G5 veredicto `CERT_F04_01_PASS` con atribución por etapa:** lectura literal de la SPEC (criterios PASS del backlog + contrato F-04). (1) "El mismo SHA corre en los runtimes requeridos": flota homogénea 4/4 en cada instante — 0.2.103/`2993da0` durante toda la cadena física productora (03:38–08:22Z), 0.2.104/`25a5122` durante seal/delivery y al cierre. (2) "El flujo real completa sin bypass": `COMPLETED/TARGET_REACHED` con el terminal contractual `HANDOFF_CREATED ×5` + 0 POST para autoridad E-04 sin configurar; la failure matrix de F-04 autoriza retry post-`UNAVAILABLE` y el terminal `INGESTED` exige el consumer E-04 real (CERT-E04-01/CERT-F04-03; "no fingir INTEGRATION PASS"). (3) "Bytes, refs, allocation, seal y manifest verificables": todo anclado por SHA256 y refs durables (detalle en G4/G1). (4) "Cardinality-safe": write-once convergido, sin duplicados, cohorte honesta de 5. El criterio de "release cohesiva" NO está en CERT-F04-01 (pertenece a CERT-F05-01) y ninguna cláusula exige campaña de release única; la recuperación entre releases queda autorizada contractualmente por la matriz de retry + convergencia write-once por identidad de contenido (la identidad sellada es content-addressed, no build-addressed), con atribución registrada: etapas productoras (funnel→promotion) bajo `2993da0`, seal/delivery terminal bajo `25a5122`, artefactos anclados por bytes. `CERT-F04-01 = PHYSICALLY CERTIFIED (PASS)`; scope Físico de T2.12 satisfecho; T2.13/CERT-E04-01/CERT-F04-03 permanecen para el join real con Echo.
- **G6 seguridad PASS:** terminal real de RERUN-6 (ambos workflows Completed, sin pending activities, sin activities físicas pendientes); `MT5_PROCS=0` fresco post-rollout y post-recuperación; 0 workflows inesperados Running; allocations `26090011001–018` write-once intactas sin reutilización; 0 POST de FailClosedIngress; versiones/manifests/effective-inputs write-once íntegros (etag del objeto preexistente sin cambio tras la recuperación).
- **G7 `RERUN-7 NOT_REQUIRED`:** RERUN-6 terminal y reconciliada + SPEC sin exigencia de release única + vía contractual de certificación existente + release corregida desplegada 4/4 + sin ejecución competidora + ensayo integral PASS ⇒ no se creó ninguna campaña nueva (ningún CampaignRef/FlowRunRef/workflow ID nuevo).
- **Continuidad del orden frozen:** el siguiente gate es **CERT-F04-02 (golden auténtico / T2.11), ahora desbloqueado**: los preimages completos del run físico (5 StrategyVersions selladas, 5 manifests, 5 effective-inputs, artefactos compile/readback, 10 EvaluationRefs, DecisionRef `sha256:ed703eeb…`) están durables y SHA-anclados bajo el FlowRun `c71687ca…`. CERT-E04-01 y CERT-F04-03 siguen dependiendo del golden + join real; CERT-F05-01 mantiene su orden frozen.

Evidencia de la misión: agent-run `2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-01-c13-recovery-certified`, change_log `2026-09-20-echo-forge-cert-f04-01-c13-recovery-certified`, workdir `~/aranea/work/f04-cert-f04-01-rerun6/` (`g1-stage/` con los 20 artefactos verificados, evaluaciones y ancla `effective_inputs_m16_REAL.json`).

### Delta de golden auténtico CERT-F04-02 — 2026-09-20 (misión NORMAL F05C-CERT-F04-02; captura→integridad→corpus→certificación; veredicto `CERT_F04_02_PASS` / T2.11 satisfecho)

Ejecutó el mandato maestro CERT-F04-02 sobre el golden físico de RERUN-6 sin ejecutar campañas, sin backtests, sin releases, sin POST a Echo y sin tocar fuentes write-once. **Entregable: corpus real, durable y verificable en `~/aranea/work/f04-cert-f04-02/corpus` (permisos 700, destino autorizado por precedente C6; el vault conserva sólo metadatos y referencias).**

- **G0 autoridad PASS (evidencia fresca):** ambos workflows de RERUN-6 (`sqx-forge-campaign-v1-4cdf96fc…`/`01a0bce5-0b8e…` y `sqx-main-v1-6fd4912a-b85b-477e-8640-7c6d4823823d`/`01a0bce5-0cee…`) `Completed` 14:23:33Z con 0 pending activities (describe gRPC propio, ns `sqx-prop`); resultado de campaña `COMPLETED/TARGET_REACHED` con `effective_unique_finalists=5`; DecisionRef completa `sha256:ed703eeb5b96b0bd55da77cdcb479611f067526f1598e4bc80850854b8191312` resuelta del resultado durable de promotion (finalists_v2 con ranks/score_refs/warnings `PNL_SIGN_FLIP`); resultado del seal con 5/5 `HANDOFF_CREATED` + `ingested=false` (VersionRefs/IdempotencyKeys completos); request del seal re-extraído del evento 640; ETCD `/sqx-worker/production/echo` = 0 keys; 10 evaluaciones durables (`forge.evaluations`, `_id` == EvaluationRef) con refs de artefactos/magic/productor; 5 effective-inputs en MinIO con atribución verificada (miembro `7508a66c` @ 08:22:29Z bajo 0.2.103; miembros 2–5 @ 14:23:32Z bajo 0.2.104; etags coincidentes).
- **G1 inventario PASS:** recorrido del contrato del golden (`BuildHandoffManifest` + `HandoffManifestV1` @ `25a5122`): el manifest declara por miembro EX5 (ExecutableArtifact), effective-inputs (EffectiveInputsArtifact), dependency artifacts (source `.sqx` + `.mq5`), SourceArtifact, lineage (Apply/Compile EvaluationRefs, DecisionRef, MemberProof). Inventario: 25 preimages + 10 evaluaciones + evidencia Temporal/release/control-plane; HTM físicos/scores/snapshots de wave **omitidos del corpus autocontenido porque el contrato demuestra que no son preimages del manifest** (refs durables registradas en el manifest del corpus).
- **G2 adquisición byte-exacta PASS:** 25/25 objetos descargados por GET presignado fresco (identidad RO existente, URLs presign no persistidas) con `actual_size == declared_size` ∧ `actual_sha256 == declared_sha256` contra las refs durables de las evaluaciones y los digests embebidos en las keys `durable/forge-seal/...`; cross-check contra las copias C13 de `g1-stage/` 20/20 idénticas + ancla `33937102…`; releases verificadas: `0.2.103` symphony `f5965d81…`/exe `b29a4fd5…` y `0.2.104` symphony `a81aa5a1…` (== manifest C13 EXACT)/exe `c250d7dd…` (== rollout C13 EXACT), `vcs.revision` `2993da0`/`25a5122` 4/4 — sucesión 0.2.103→producción física, 0.2.104→seal/delivery registrada por etapa sin falsearla.
- **G3 identidad/provenance PASS (recomputación con el código real):** arnés `goldenrecompute` (recetas exactas del source `25a5122`, worktree desechable creado y eliminado sin residuo) sobre los bytes del corpus: readback SQX == MQ5 == magic asignado (13/14/16/17/18) 5/5; compile logs `0 errors` con el gate compartido real `CompileLogReportsZeroErrors` 5/5; **effective-inputs reproducidos byte-exacto** (canonicalización S0 desde config de apply + readback) 5/5; **VersionRefs recomputados == los producidos físicamente por el seal (Temporal)** 5/5; **IdempotencyKeys recomputados == Temporal** 5/5; allocation_refs recomputados por receta `forge-magic-allocation.v1`; sin cruces de artefactos entre estrategias (cada key/eval pertenece a su StrategyRef). Producer `echo-forge-handoff` demostrado: identidad frozen en source + log físico del worker en sqx-zeus 14:23:32Z (26 líneas: fetches de exactamente los 20 objetos con keys+sizes de la cohorte y uploads de los 4 effective-inputs de recuperación con etags == MinIO) + resultado del seal en Temporal.
- **G4 corpus PASS con limitación declarada:** índice machine-readable `CORPUS-MANIFEST.json` + `README.md` + `PROVENANCE.md` + `VERIFICATION.md` + `HANDOFF-E04-PACKAGE.md` + tools (validador + arnés de recomputación + plan de descarga por refs). **Objetos por referencia:** los bodies canónicos de los 5 `HandoffManifestV1` y las 5 filas `StrategyVersion` viven write-once en PostgreSQL `192.168.31.220/trading_systems_test` (`sqx.handoff_manifests`/`sqx.strategy_versions`, migration 015) **sin canal de lectura autorizado en esta sesión** (igual que C12/C13; el MCP PG sólo expone la BD `echo` como `mcp_echo_prod_ro`); su existencia/identidad quedan demostradas por Temporal + recomputación y su inmutabilidad por contrato; capturar sus bytes exige identidad de lectura `sqx` (capability requerida registrada en el corpus). `payload_digest` de los manifests no recomputable sin el body (depende de `Decision.ContentDigest`/Evidence en PG).
- **G5 verificación independiente PASS:** validador contractual propio (`tools/validate.py`, lee sólo el corpus, falla exit≠0 ante objeto ausente/hash incorrecto/referencia inconsistente) sobre el corpus final: **PASS**; negativos demostrados (byte mutado → exit 1; objeto ausente → exit 1; identidad manipulada → detectada); recomputación re-ejecutada desde el corpus final: exit 0, 5/5, 0 fallos. Tree digest de preimages `4a266cba45a6b07a5e9f699a9583a63e8f7b945035dbd9ee895381ca3604a36d`.
- **G6 veredicto `CERT_F04_02_PASS`:** criterios del backlog satisfechos — todos los bytes que el manifest/refs declaran existen y verifican; producer es `echo-forge-handoff`; el corpus no es copia de S0 ni de fakeconsumer (bytes de los objetos durables de RERUN-6 consumidos por el seal físico, cross-check C13, anclas de contenido). Los 10 criterios del mandato: 10/10 (detalle en `VERIFICATION.md`). `HANDOFF_CREATED ≠ INGESTED`: no se marcó CERT-E04-01 ni CERT-F04-03.
- **G7 handoff a CERT-E04-01:** paquete completo en `HANDOFF-E04-PACKAGE.md` (referencia estable del corpus, identidades por miembro, evidencia de producer, requisitos E-04 `POST /api/v1/forge/promotions` + GET by-key con pin S0 `91671f6f`, canales de lectura autorizados existentes, qué vale como fixture y qué exige POST real, y las dependencias pendientes: identidad `sqx` RO sobre `trading_systems_test` para los bodies + runtime Echo desplegado). Sin POST, sin join F04-03.
- **Fricción registrada:** el MCP `aranea-mongo-forge-ro/rw` falló en todo intento con `session not found` (incluido `connect`); se sustituyó por un lector propio RO con la URI sin credenciales de ETCD. El `sftp-download` del MCP SSH devuelve el contenido inline sin archivo local y el `run-command` redacta base64 de alta entropía; el log del worker se persistió desde la salida sftp con una key redactada por el sanitizador (`ce307453` SQX, size 2017405 identifica el objeto).

**Próxima tarea única recomendada para NORMAL:** **CERT-E04-01 — Echo cross-lane golden / T21** con el paquete G7 (su primer prerrequisito material es resolver la identidad de lectura `sqx` sobre `trading_systems_test` para obtener los bodies canónicos de los manifests, o autorización owner equivalente). T2.11 = satisfecho; T2.12 conserva CERT-F04-01 PASS; T2.13 permanece OPEN para el join real.

Evidencia de la misión: agent-run `2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-02-golden-corpus-pass`, change_log `2026-09-20-echo-forge-cert-f04-02-golden-corpus-pass`, corpus `~/aranea/work/f04-cert-f04-02/corpus/`.

### Delta de preflight y bloqueo CERT-E04-01 — 2026-09-20 (misión NORMAL CERT-E04-01 G0/G2; veredicto `CERT_E04_01_BLOCKED`, sin POST, fail-closed)

Ejecutó el mandato maestro CERT-E04-01 (T21/AC-37) con autonomía acotada: G0 auditoría de la brecha de bytes del golden F04-02 y G2 preflight completo del runtime Echo, con todas las verificaciones read-only independientes; G1 re-verificado vía validador del corpus (PASS hoy, exit 0); G3–G7 NO_RUN por dependencia de G0/G2; cero efectos productivos (0 POST, 0 deployments, 0 cambios de config/schema).

- **G0 `GOLDEN_MANIFEST_BYTES_BLOCKED`:** los bodies canónicos de los 5 `HandoffManifestV1` viven sólo en `sqx.handoff_manifests.canonical_body` @ PostgreSQL `192.168.31.220/trading_systems_test` (write-once migration 015) y ninguna vía autorizada de la sesión los alcanza: los MCP PG (`aranea-postgres-ro/rw`) están scoped a `echo`/`echo-develop` (`mcp_echo_prod_ro`/`mcp_echo_dev_rw`; verificado con `current_database()`/`inet_server_addr()`/`current_user()`), el Access Plane no registra capability alguna sobre `trading_systems_test`, la read surface F-05-I (`sqx-flowkit strategy get`) expone `handoff[]` con `payload_digest` pero sin `canonical_body`, y la evidencia durable del corpus (seal-result.json, request ev640, MinIO `durable/forge-seal`) contiene refs pero no bodies. Solicitud owner mínima preparada: `~/aranea/work/cert-e04-01/owner-action-01-sqx-ro-trading-systems-test.md` (SELECT de las 5 filas exactas por `idempotency_key`, vía capability RO temporal o entrega byte-exacta por canal owner con precedencia C6/HTM; procedimiento de verificación por `payload_digest` + advertencia JSONB round-trip).
- **G2 `ECHO_RUNTIME_BLOCKED` (independiente y concurrente):** (1) el gateway PROD (`192.168.31.71:8090`, systemd `echo-gateway.service`, `/home/kor/echo/echo-gateway` PID 713 corriendo desde 2026-08-09) no monta la ruta forge — `GET /api/v1/forge/promotions/by-key/<probe>` sin auth → 404 «page not found» vs baseline montado `GET /api/v1/webhooks/account-config` → 405; el binario desplegado es ~1 mes anterior a la integración E-04 (2026-09-12). (2) `origin/master` actual `5dd998f1` contiene E-04: ancestry `a99f9a63 ∈ 5dd998f1` PASS (shallow clone en /tmp, eliminado sin residuo) y `v3/gateway/internal/server.go` con `ForgeIngestConfig` + `forge_ingest_handler.go` presentes. (3) Ninguna base compartida tiene el schema v3: ni `echo` PROD ni `echo-develop` DEV contienen `strategy_identity_mappings`/`strategy_versions`/`promotion_records` en schema alguno — la migration 061 nunca se aplicó fuera de los clusters descartables de los gates. (4) Config E-04 ausente en ETCD `/echo/production` (sin `gateway/forge_ingest/namespace`/`artifact_root`/`store_allowlist`; el token es secreto-nombrado y el RO lo excluye por diseño). (5) Lado producer virgen: `/sqx-worker/production/echo` = 0 keys (cero POSTs previos; las 5 IdempotencyKeys siguen vírgenes). Owner action de deployment preparada: `~/aranea/work/cert-e04-01/owner-action-02-echo-e04-runtime-deployment.md` (source/release v3, migración 061 a base compartida, config §7 del SPEC, ArtifactSource contra `sqx-strategies`, secreto `forge:ingest` por canal owner).
- **G8 veredicto `CERT_E04_01_BLOCKED`:** T21/AC-37 permanece OPEN; no se declaró PASS por el validador local, ni por HTTP, ni se fabricó el body con `BuildHandoffManifest`; CERT-F04-02 PASS queda confirmado (su limitación fue declarada honestamente en su delta y no omitió requisito de su propio alcance — los bodies son persistencia del seal, no preimages declarados por el manifest; sin errata, la brecha ya estaba registrada en su G4/G7). CERT-F04-03 sigue bloqueado por este gate. Evidencia read-only completa: `~/aranea/work/cert-e04-01/PREFLIGHT-EVIDENCE.md`.

Evidencia de la misión: agent-run `2026-09-20-zcode-glm-5.3-flash-cert-e04-01-blocked-preflight`, change_log `2026-09-20-echo-cert-e04-01-blocked-preflight`, workdir `~/aranea/work/cert-e04-01/`.

### Delta de continuidad Forge F04-03 — 2026-09-20 (misión FORGE-F04-CONTINUITY/F05-NEXT; lane Forge only; sin efectos)

Preparó F04-03 dentro del ownership Forge sin ejecutar el join, sin campañas, sin backtests, sin releases, sin POST y sin tocar producción ni Echo. (1) **Re-verificación del golden:** validador del corpus re-ejecutado PASS (exit 0: 5 miembros, 25 preimages byte-exactos, identidad coherente, evidencia completa); los 10 puntos del handoff re-verificados contra `CORPUS-MANIFEST.json` y la evidencia (5 VersionRefs/5 IdempotencyKeys con `recomputed_match=true`, 5×`HANDOFF_CREATED`/`ingested=false`, 25 preimages, 10 EvaluationRefs contra `seal-request-ev640.json`, DecisionRef `ed703eeb…`, producer `echo-forge-handoff`, allocations `26090011013–018`, provenance 0.2.103/`2993da0` → 0.2.104/`25a5122`). (2) **Gap exacto registrado para F04-03:** bodies canónicos de los 5 manifests y sus `payload_digest` NO disponibles (0 ocurrencias en la evidencia del corpus; viven sólo en PG JSONB `trading_systems_test` por referencia); cuando exista identidad `sqx` RO, la identidad se verifica con SDK S0 + `manifest.Encode()` + `payload_digest` ⇒ estado verificable `PERSISTED_JSONB_CANONICAL_REENCODE_VERIFIED`; `ORIGINAL_TRANSPORT_BYTES_RECOVERED` exige fuente independiente de bytes de transporte que hoy no existe. (3) **Paquete de continuidad Forge:** `~/aranea/work/f04-cert-f04-02/corpus/F04-03-FORGE-CONTINUITY.md` — identidades completas por miembro (tabla regenerada byte-exacto desde el manifest), refs por categoría, contrato `HandoffIngress` + matriz de errores/idempotencia transcripta con cita al contrato F-04, condiciones de no-effects, frontera A (Forge, completada) / B (Echo, owner actions 1/2 intactas) / C (cross-lane, no ejecutable), y matriz de aceptación: `FORGE_PRODUCER_READY=CUMPLE`, `CONSUMER_CERTIFIED=NO`, `CROSS_LANE_QA_READY=NO`, `REAL_JOIN_EXECUTED=NO`, `RECEIPT_VERIFIED=NO`, `F04_03_PASS=NO/OPEN`. (4) **Re-evidencia del productor @ HEAD `25a5122`:** suites herméticas `sqx/core/capabilities`, `sqx/adapters/echo-handoff`, `sqx/adapters/magic-readback` (con fixture auténtico RERUN-5 `9ac377b9…` SHA-verificado desde `~/aranea/work/f04-cert-f04-01-rerun5/`; el gate es FAIL-closed sin fixture, por diseño), `sqx/core/forge`, `sqx/core/releasematrix` PASS + `-race` PASS + vet OK; árbol sin cambios (dirty operacional ajeno `deploy/manifest.json` + `phase4_performance.json` preservado). (5) **Roadmap G4:** sin tarea de ejecución Forge desbloqueada — F-05-I cerrada desde 2026-09-16 (T7-CLOSE `3d0e8c9`; la nota padre fue reconciliada hoy), la preparación de CERT-F05-01 ya fue entregada por F-05-I, y CERT-F05-01…03 permanecen tras CERT-F04-01…03 por orden frozen. **Pendiente de decisión manager (no ejecutado):** refresco de `sqx/core/releasematrix/release-matrix.json` para reflejar CERT-F04-01/02 PASS — exigiría ampliar la allowlist histórica física del guard del artefacto (`FROZEN_CONTRACT_COLLISION` potencial, STOP previsto por la SPEC F-05-I). CERT-F04-03 permanece `DEFERRED` por su dependencia externa CERT-E04-01 (T21, lane Echo). Evidencia: agent-run `2026-09-20-zcode-glm-5.3-flash-forge-f04-continuity-f05-next`, change_log `2026-09-20-echo-forge-f04-03-continuity-frontier`, paquete en el corpus.

### Delta de recertificación CERT-E04-01 + CERT-F04-03 — 2026-09-21 (Daedalus runtime DEV; sin redeploy; veredicto BLOCKED)

Reanudó la certificación sobre el runtime Echo DEV ya desplegado en Daedalus (`5dd998f1`, binarios §5.1 del Environment Contract). **No se cerró ningún gate del backlog** (ningún PASS de producto).

- **G0 PARTIAL:** Core/Gateway `RUNNING` + `/health` 200 reconfirmados; `forge_ingest_misconfigured=true` + POST live 503; keys ETCD `gateway/forge_ingest/*` y `symphony/.../echo/ingest/*` ausentes; mig 061 / tablas `promotion_records`/`strategy_versions` ausentes en `echo-develop`.
- **G1 FAIL (reconciliación):** E-INT-01…08 y F-INT-01…05 **STILL_REPRODUCIBLE** en el mismo SHA; hallazgo nuevo E-INT-09 (Store acepta archivo corrupto). Seed/scratch **no ejecutados**.
- **G3 PARTIAL:** corpus validate + goldenrecompute 5/5 PASS; bodies/payload_digest siguen GOLDEN_AUTHORITY_BLOCKED (owner action 1 intacta).
- **G5 PARTIAL:** Corpus/CertPack HTTP+PG disposable PASS; live matrix NOT_EXECUTED.
- **G6 NOT_EXECUTED.** Veredictos: **`CERT_E04_01_BLOCKED`**, **`CERT_F04_03_BLOCKED`**.
- Evidencia: `~/aranea/work/cert-int-qa-20260921/FINDINGS-CERT-INT-QA-20260921.md`; AS-BUILT [[Echo + Echo Forge — Environment Contract]] §5.2.

### Backlog ordenado; no ejecutar en esta sesión

#### CERT-F04-01 — Forge physical chain / T2.12

- **Prerequisite product SHA/release:** F-04 `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` / `0.2.98`.
- **Prerequisite infrastructure:** Aranea Access Plane operacional y certificado para los targets de ejecución/observación y el control plane usado por el flujo.
- **Required MCP capabilities:** SSH viewer para identificar runtime; SSH operator/run-command en los hosts que ejecutan SQX/MT5; acceso scoped de observación/escritura al PG/Mongo/object-store del flujo; observación/control Temporal o capability equivalente autorizada; secretos permanecen fuera del agente.
- **Physical recipe:** desplegar y probar la release autorizada en los runtimes canónicos; ejecutar un flujo F-04 real con catálogo CC owner, compile MetaEditor/SQX, persistencia de EvaluationRef, readbacks, seal y handoff; no usar mocks como PASS.
- **Durable evidence:** release/deployment proofs, FlowRun/workflow IDs, Decision V2, allocation/readback, compile inputs/outputs/log, EvaluationRefs, StrategyVersion, HandoffManifest y delivery state.
- **PASS criteria:** el mismo SHA corre en los runtimes requeridos; el flujo real completa sin bypass; bytes, refs, allocation, seal y manifest son verificables y cardinality-safe. Hasta entonces: `PLANNED`/`BLOCKED BY INFRA`, no PASS.

#### CERT-F04-02 — Authentic Forge golden / T2.11

- **Prerequisite product SHA/release:** CERT-F04-01 PASS sobre `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` / `0.2.98`.
- **Prerequisite infrastructure:** mismo access plane y almacenamiento durable/exportable para todos los preimages referenciados por el flujo.
- **Required MCP capabilities:** lectura de evidencia Forge, artefactos y metadatos del control plane; transferencia segura de los bytes auténticos; no capability sintética ni fixture manual.
- **Physical recipe:** capturar desde la ejecución física el golden Forge completo: producer F-04 auténtico, StrategyVersion sellada, artefactos compile/readback, EvaluationRefs y manifest; preservar hashes y provenance.
- **Durable evidence:** fixture/preimages byte-exactos, SHA256/size, refs, producer identity, release manifest y enlace al run físico.
- **PASS criteria:** todos los bytes que el manifest/reference declara existen y verifican; producer es `echo-forge-handoff`; corpus no es copia de S0 ni fakeconsumer. ~~No ejecutar ni marcar PASS ahora.~~ **→ EJECUTADO 2026-09-20: `CERT_F04_02_PASS` / T2.11 satisfecho** (corpus `~/aranea/work/f04-cert-f04-02/corpus`, verificación 25/25 + recomputación 5/5 + validador independiente; limitación declarada: bodies de manifests/versions en PG `trading_systems_test` sin canal autorizado, por referencia con garantías verificadas; detalle en el delta CERT-F04-02 de esta nota).

#### CERT-E04-01 — Echo cross-lane golden / T21

- **Prerequisite product SHA/release:** golden auténtico CERT-F04-02 y Echo E-04 integrado en `a99f9a63354bbe72219d1e590bb93757ed08e45e` o release posterior explícitamente verificada.
- **Prerequisite infrastructure:** runtime Echo desplegado y observable; endpoint de promotions alcanzable desde el producer; PG/artifact store/read surface disponibles.
- **Required MCP capabilities:** observación del runtime Echo y HTTP; lectura scoped del resultado/receipt; acceso de evidencia PG/artifacts según el runbook; no admin secret reutilizado.
- **Physical recipe:** enviar el manifest auténtico por `POST /api/v1/forge/promotions`, verificar `INGESTED`, recuperar por GET by-key y repetir el lookup idempotente sin activar/provisionar/capitalizar.
- **Durable evidence:** request/body digest, HTTP status, receipt, mapping/version/promotion rows, artifact hashes y GET by-key outcome.
- **PASS criteria:** Echo acepta exactamente el golden auténtico, persiste una única proyección consistente, devuelve receipt/read-back estable y no produce efectos fuera de E-04. T21/AC-37 permanece OPEN hasta ejecutar esto.
- **Intento 2026-09-20:** `CERT_E04_01_BLOCKED` en preflight (sin POST) por `GOLDEN_MANIFEST_BYTES_BLOCKED` (identidad `sqx` RO sobre `trading_systems_test`, owner action 1) + `ECHO_RUNTIME_BLOCKED` (gateway PROD pre-E-04 sin ruta forge, PG compartido sin migraciones v3, config `forge_ingest` no provisionada, owner action 2). Detalle en el delta de esta fecha.

#### CERT-F04-03 — Real Echo join / T2.13

- **Prerequisite product SHA/release:** CERT-F04-02 PASS, CERT-E04-01 PASS, Forge `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` / `0.2.98` y Echo runtime del E-04 integrado.
- **Prerequisite infrastructure:** ambos runtimes observables/operables por el Access Plane, rutas Forge→Echo habilitadas y control plane con evidencia durable.
- **Required MCP capabilities:** capabilities CERT-F04-01 más observación HTTP/Echo y read-back de receipt/rows; ninguna sustitución por fakeconsumer.
- **Physical recipe:** repetir un run Forge real desde Finalist/StrategyVersion hasta `POST` real y `GET by-key` en Echo, conservando la cadena de identidad y hashes.
- **Durable evidence:** run IDs, release/deployment proofs, manifest/body digest, receipt `INGESTED`, read-backs y no-effects.
- **PASS criteria:** handoff real→receipt real→lookup real converge exactamente una vez y los artefactos se pueden inspeccionar. Hasta entonces, join `DEFERRED`.

#### CERT-F05-01 — Cohesive release/deployment proof

- **Prerequisite product SHA/release:** F-01/F-02/F-03 integrados y F-04 implementación revisada; candidato cohesivo posterior a `b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43`.
- **Prerequisite infrastructure:** release/deployment proof para cada runtime objetivo y observación suficiente para separar código, binario y proceso.
- **Required MCP capabilities:** source/release/deployment read-back; operator sólo donde la receta lo requiera; no cerrar con marcadores legacy.
- **Physical recipe:** publicar desde la rama autorizada, verificar manifest/digest y demostrar que cada runtime ejecuta el mismo release antes de cualquier FULL golden.
- **Durable evidence:** release manifest, SHA/blob equality, deployment proofs, runtime identity y matriz de superficies tocadas.
- **PASS criteria:** release cohesivo trazable a source autorizado y runtime; no mezclar binario viejo con HEAD nuevo.

#### CERT-F05-02 — FULL authentic golden and result surfaces

- **Prerequisite product SHA/release:** CERT-F05-01 PASS y CERT-F04-02/CERT-F04-03 según el alcance del release.
- **Prerequisite infrastructure:** SQX/licencia válida, MetaEditor, runtimes Forge/Echo y acceso durable a result surfaces.
- **Required MCP capabilities:** ejecución/observación de todos los runtimes incluidos, control-plane evidence y lectura de result surfaces; nada sintético para el veredicto físico.
- **Physical recipe:** ejecutar la campaña FULL real con matriz determinística, cómputos largos, cancellation/retry/drain sólo donde el delta lo exige, y golden/result surfaces inspeccionables.
- **Durable evidence:** campaign/run IDs, per-stage evidence, finalistas, artifacts, warnings/funnel, result surfaces, cancellation/recovery proof y golden refs.
- **PASS criteria:** factory usable y explicable; zero finalists honesto sigue siendo resultado válido, pero no se declara PRODUCT CAPABILITY sin la evidencia requerida.

#### CERT-F05-03 — Final Factory V2 closure

- **Prerequisite product SHA/release:** CERT-F05-01 y CERT-F05-02 PASS; cross-lane PASS cuando el release incluya Echo join.
- **Prerequisite infrastructure:** todos los runtimes/capabilities requeridos siguen observables durante la campaña.
- **Required MCP capabilities:** conjunto certificado de F04/F05 y read-back de evidencia final.
- **Physical recipe:** ejecutar la matriz final de release, confirmar no-regresiones del delta y emitir el manifest de certificación de Factory V2.
- **Durable evidence:** cert manifest, release/source/runtime mapping, golden refs, unresolved accepted debt y veredictos por gate.
- **PASS criteria:** sólo entonces F-05 puede marcar `PHYSICALLY CERTIFIED`/`CLOSED`; no recertifica B1A/B1B/B2 sin delta.

### Dependencias y clasificación de trabajo restante

| Trabajo | Clase | Decisión |
|---|---|---|
| F-04 T2.11/T2.12/T2.13 | C — HARD BLOCKED | Requiere ejecución física, golden auténtico y/o runtime cross-lane; queda deferred, no waived. |
| F-05 implementación/pre-cert: matriz release, result/read surface y checklist de conformance | A — CAN CONTINUE NOW | Tiene valor real y usa contratos/fixtures existentes sin afirmar certificación. |
| F-05 release/physical/FULL golden/cierre | B — CAN IMPLEMENT BUT CANNOT CERTIFY | La preparación y el tooling avanzan; el veredicto necesita CERT-F05-01…03. |
| E-04 T01–T20 | A — CAN CONTINUE NOW | Ya está integrado; no se reabre. |
| E-04 T21/AC-37 | C — HARD BLOCKED | Exige golden Forge auténtico y join real; synthetic S0/fakeconsumer no satisfacen el gate. |
| E-02 implementation/verification | B — CAN IMPLEMENT BUT CANNOT CERTIFY | No depende de T2.11–T2.13; conserva su PHYSICAL_PARTIAL y gates propios. |
| E-05 implementation/corrections | B — CAN IMPLEMENT BUT CANNOT CERTIFY | No depende de T2.11–T2.13; su cierre actual depende de S0 erratum/control-plane evidence y su propia verificación. |
| E-06 Reference enrollment and binding | B — CAN IMPLEMENT BUT CANNOT CERTIFY | El diseño/implementación puede avanzar; read-back y primera observación requieren runtime físico. |
| E-07 Raw facts, DEAL and coverage | B — CAN IMPLEMENT BUT CANNOT CERTIFY | Puede avanzar con fixtures; offline/restart/partial/late-cost certification requiere observación real. |
| E-08 Routing, EconomicCommand and risk reservation | B — CAN IMPLEMENT BUT CANNOT CERTIFY | Source/shadow puede avanzar; crash/UNKNOWN/reservation certification requiere runtime. |
| E-09 Execution copy and reconciliation | B — CAN IMPLEMENT BUT CANNOT CERTIFY | Puede implementarse; Execution Fidelity PHYSICAL requiere Reference/Execution real. |
| E-10 Strategy Quality and eligibility | A — CAN CONTINUE NOW | Quality/eligibility puede avanzar con fixtures, coverage explícita y abstención; no depende de golden Forge físico. |
| E-11 PortfolioVersion shadow | A — CAN CONTINUE NOW | Selection/allocation shadow determinista puede avanzar con fixtures y CASH; no toca dinero real. |
| E-12 Apply/rebalance/replacement/retirement | B — CAN IMPLEMENT BUT CANNOT CERTIFY | La implementación DEMO puede avanzar; PHYSICAL DEMO requiere runtime real y ACK/recovery. |
| E-13 Front/read/ops and V1 closure | B — CAN IMPLEMENT BUT CANNOT CERTIFY | Read/ops surfaces pueden avanzar; PRODUCT CAPABILITY/restore requiere evidencia física. |

### Delta de corrección source CERT-F04-01 encoding — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-C3; fix de source; veredicto `SOURCE FIX PASS`)

Corrigió en source el segundo defecto de la release `0.2.99` demostrado por el run físico RERUN (`cert-f04-01-rerun-20260917T211613Z-0997458c`, Campaign `a470e565-…`, FlowRun `efc4ac0c-…`, StrategyRef `4b86cd72-…`, magic `26090011004`): compile child SUCCESS en primer intento (MetaEditor `0 errors, 1 warnings`, EX5 176730 B, log 15984 B durables) pero `mt5_compile_persist_v1` falló `CONTRACT_CONFLICT / artifact_integrity: compile log does not report 0 errors` → Campaign/FlowRun `FAILED`, sin EvaluationRefs/StrategyVersion/HandoffManifest. **Root cause verificada en source @ `49fce32`:** dos gates tomaban decisiones distintas sobre el mismo resultado — `ArtifactCompiler.evaluateCompileArtifacts` matcheaba `compileSuccessPattern` sobre `normalizeCompileLog(data)` (UTF-16LE/BE con BOM decodificado) mientras `VerifyCompiledArtifactForSeal` matcheaba sobre los bytes crudos del log UTF-16LE descargado; los tests Core usaban sólo fixtures UTF-8, invisibilizando el mismatch. **Fix (criterio único compartido, `MIGRATION: NONE`):** nueva función pura `CompileLogReportsZeroErrors(raw []byte) bool` en `sqx/core/capabilities/strategy_version.go` como única autoridad de interpretación de compile logs (UTF-8; UTF-16LE con BOM `FF FE`; UTF-16BE con BOM `FE FF`; patrón contractual intacto `(?i)Result:\s*0 errors`); fail-closed estricto para evidencia inválida — log ausente/vacío, payload UTF-16 impar (sin truncamiento silencioso del byte final) y surrogates sin pareja se rechazan en vez de producir PASS falso (mejora sobre el normalizador previo del adapter, que truncaba); ambos gates (compiler y seal) invocan exactamente esa función y el parser duplicado del adapter (`compileSuccessPattern` local + `normalizeCompileLog` + helpers UTF-16) fue eliminado. Los bytes originales del objeto durable nunca se modifican: la decodificación es sólo interpretación textual, sin reescritura de key/size/SHA256/evidencia; MQ5/EX5/result status/Primary/Evidence/durable refs siguen validándose igual.

- **Branch/base/commit:** `codex/f04-cert-f04-01-encoding-fix` desde `49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1` (== `origin/codex/f05-release-prep`, verificado con fetch antes de modificar). Commit único **`18d09054630f1da0f3c44a13c6132af3e17b0596`** (`fix(forge): unify compile log validation across compiler and seal`), parent exacto `49fce32…`, publicado a origin y read-back remoto completo (HEAD/parent/4 archivos/patch SHA256 idéntico local vs remoto `9fae54f3…`). Sin merge a `codex/f05-release-prep` (no avanzado), sin tag, **release NOT PUBLISHED**, deploy NO, campañas NO, CERT-F04-01 NO re-ejecutado. Dirty foráneo preservado intacto y sin stage: `deploy/manifest.json` + `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json`.
- **Archivos cambiados (sólo allowed files):** `sqx/core/capabilities/strategy_version.go` (función compartida + decodificación estricta + seal delega), `sqx/adapters/mt5/artifact_compiler.go` (parser duplicado eliminado, compiler delega), `sqx/core/capabilities/strategy_version_test.go` y `sqx/adapters/mt5/artifact_compiler_test.go` (regresión T1–T8). Ningún otro archivo requerido.
- **Evidencia física del log (límite de lectura declarado):** comprobación READ-ONLY acotada sin éxito por capabilities autorizadas de la sesión — Temporal RO sólo expone el perfil `aranea` cuyo namespace no contiene el run (el stack `0.2.99` corre en ns `sqx-prop`, confirmado por ETCD `/sqx-mt5-worker/production/temporal/namespace`); MinIO RO preconfigurado expone sólo buckets `deploy`/`examples` (el MinIO de artefactos MT5 es `192.168.31.92:9000`, sin conexión configurada); Postgres RO expone el schema `echo` (Echo Live), no el control plane de Forge; Mongo `forge.strategy_evaluations` sin documento para el FlowRun (consistente: el run murió antes del seal). **PHYSICAL LOG BYTES NOT AVAILABLE:** la regresión usa fixtures UTF-16LE/BE determinísticos (incluye uno representativo del modo de falla RERUN `0 errors, 1 warnings`) que NO sustituyen una futura prueba física con los bytes auténticos.
- **Tests de regresión T1–T8:** T1 UTF-8 `0 errors, 1 warnings` PASS en ambos gates; T2/T3 UTF-16LE/BE con BOM PASS en ambos gates — **los tests seal-level (`TestVerifyCompiledArtifactForSeal_AcceptsUTF16CompileLog`, `TestCertF04_01_CompileLogEncodingRegression`) FALLAN contra `49fce32` con exactamente `compile log does not report 0 errors: artifact_integrity` (demostrado con worktree temporal @ base + tests nuevos, eliminado sin residuo) y PASAN con el fix**; T4 `1 errors` rechazado en los tres encodings en ambos gates; T5 fail-closed (nil/vacío/BOM-only/1 byte `FF`/payload impar que truncado aún contendría `Result: 0 errors`/surrogates huérfanos); T6 integridad intacta con log UTF-16 válido (EX5 digest/size, source MQ5, status); T7 bytes/tamaño/digest originales preservados en memoria y en disco; T8 regresión física representativa con provenance declarado (ver arriba).
- **Suites:** `go test ./sqx/core/capabilities/...` OK, `./sqx/adapters/mt5/` OK (paquete del fix), MT5CompilePersist enfocado 7/7 PASS (incluye `VerificationFailureNeverOpensStage`), workflow `TestGenericWorkflow_WiresCompilePersistAfterDurableSuccess` PASS, vet + gofmt OK. **Fallos preexistentes demostrados idénticos contra baseline `49fce32`** (comparación lista exacta `--- FAIL` base vs fix): `mt5/binding`+`mt5/normalization`+`mt5/report` 55 y `activities/worker` 16 (fixtures físicos ausentes, clase documentada C1) — ningún fallo nuevo atribuible al fix.
- **Estado:** `CERT-F04-01 = BLOCKED`; **blocker source fix encoding = `SOURCE FIX VERIFIED` (gates enfocados PASS; pendiente prueba física con bytes auténticos)**; physical gate remains BLOCKED. Los demás CERT sin cambio. `MIGRATION: NONE`. **Siguiente acción exacta:** `PUBLICATION CONTROLADA DE NUEVA RELEASE (sin asumir número de versión) → ROLLOUT → CERT-F04-01 RERUN FROM ZERO` con identidades todas nuevas (allocations `26090011001–004` write-once permanecen).

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c3-encoding-fix`, change_log `2026-09-17-f05c-cert-f04-01-c3-encoding-fix`.

### Delta de microfix, release y rollout CERT-F04-01 encoding — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-C4; microfix sobre C3 + BUILD/RELEASE → ROLLOUT; ningún gate físico ni campaña ejecutados; veredicto `RELEASE/ROLLOUT PASS`)

Convirtió el fix C3 en release física desplegada en flota completa, previa aplicación del microfix exigido por la revisión manager del source C3: `decodeCompileLogText` validaba estrictamente UTF-16 pero el branch sin BOM retornaba los bytes sin comprobar que fueran UTF-8 válido, contradiciendo el fail-closed ante encoding inválido. Sin ejecutar CERT-F04-01, sin campañas, sin tocar infraestructura, sin ETCD, sin watcher de certificación y sin tocar el proceso histórico `f03cert`. **`CERT-F04-01 = BLOCKED / READY TO RERUN`.**

- **Microfix (commit único sobre C3):** `a440ac4ed730747b1ae6b07557cb6204102f16ac` (`fix(forge): require valid UTF-8 in BOM-less compile logs`), parent exacto `18d09054630f1da0f3c44a13c6132af3e17b0596`, en `codex/f04-cert-f04-01-encoding-fix`; el branch sin BOM de `decodeCompileLogText` exige ahora `utf8.Valid(raw)` y devuelve `false` ante bytes inválidos; UTF-16LE/BE con BOM, patrón `Result: 0 errors` y bytes durables intactos; scope exacto `sqx/core/capabilities/strategy_version.go` + `strategy_version_test.go`; publicado a origin con read-back remoto (`ls-remote` = `a440ac4…`). `MIGRATION: NONE`. Dirty foráneo preservado sin stage (`deploy/manifest.json`, `specs/…/phase4_performance.json`).
- **Regresiones M1–M4:** M1 (`Result: 0 errors` con byte UTF-8 inválido al principio) y M2 (byte inválido al final) rechazados; M3 (UTF-8 válido no ASCII con `0 errors`) aceptado; M4 ambos gates (compiler vía `CompileLogReportsZeroErrors` en `evaluateCompileArtifacts` y seal vía `VerifyCompiledArtifactForSeal`) rechazan los mismos bytes M1/M2 mediante el helper compartido. **Demostración fail-vs-C3:** con los tests nuevos sobre worktree temporal @ `18d0905` (sin el microfix) M1/M2/M4 FALLAN y M3 pasa; con el fix todos pasan; worktree eliminado sin residuo.
- **Source gate:** `FINAL_SOURCE_SHA = a440ac4ed730747b1ae6b07557cb6204102f16ac`; C3 es ancestro inmediato; el segundo commit cambia sólo los 2 archivos permitidos; el diff acumulado contra `49fce32…` se limita a los 4 archivos autorizados de C3. T1–T8 de C3 permanecen PASS. Suites enfocadas: `go test ./sqx/core/capabilities/...` OK; `./sqx/adapters/mt5` raíz OK (C4 ownership, C5 cancellation, durable no-downgrade); MT5CompilePersist enfocado 7/7 PASS (incluye `VerificationFailureNeverOpensStage`) + `TestGenericWorkflow_WiresCompilePersistAfterDurableSuccess` PASS. Fallos de fixtures históricos (`mt5/binding`+`mt5/normalization`+`mt5/report` y `activities/worker`, 75 líneas FAIL) demostrados idénticos al baseline C3 por comparación de listas normalizadas (worktree temporal @ `18d0905`); ninguna suite fallida declarada verde.
- **Integración:** re-fetch de origin; `origin/codex/f05-release-prep == 49fce32…` verificado; fast-forward exacto `49fce32..a440ac4` (sin merge commit, rebase ni squash); push y read-back remoto (`ls-remote` = `a440ac4…`).
- **Versión `0.2.100` resuelta por autoridad remota** (`release-authority` AUTO @ pipeline: `published_version=0.2.99`, `remote_line_max=0.2.99`, `authority_state=CONSISTENT`, candidato `0.2.100`; preflight del target explícito `AVAILABLE`; segundo preflight sin TOCTOU). Sin sobrescribir `0.2.99`; sin reutilizar sus binarios.
- **Build/publicación por el mecanismo canónico** (`./deploy_release.sh --release-only 0.2.100`, deployer screen ENV=production): 6 artifacts desde `a440ac4…` — linux-amd64 `symphony` 40317088 B / sha256 `9fbf73b2a70cbce7f220c4cd50f7b6564dae7c513d336f1ce2721ae682790455`, `sqx-watcher` 32682144 B / `d0dac89c4576678032d6874d7ddef993c91186522f22c5ab03543d636eac312f`, `start-symphony-worker.sh` 310 B / `9f95638441dcb1d60d5c9475f0f95755dd48a09528c326b2ce36063b7ff50849`, `run-symphony` 233 B / `8f240eb20a17d70c22512a6f6f689591f7078c7a8ee23b210aaf766bc459c062`, `promtail-worker.yaml` 355 B / `eaa4e1b21ace4d34202ff6185b9c76710a6ff48274d4d57ba752ea13e532cbd9`; windows-amd64 `sqx-mt5-worker.exe` 37249024 B / `b36216c0facf4aa79454c165ceac1e41202aed39e7b67078ce3e575bc04cc8f3`. SHA256 locales recomputados == manifest. `go version -m` de los 3 binarios: `vcs.revision=a440ac4…`, `vcs.time=2026-09-17T22:38:01Z`, `vcs.modified=true` (dirty operacional no-Go: los únicos archivos modificados del árbol eran `deploy/manifest.json` + fixture JSON; cero cambios Go no autorizados). Deployer publicó manifest `0.2.100` 22:44:45Z (`"release_version":"0.2.100"`); read-back `release-authority --target 0.2.100`: `published_version=0.2.100`, `authority_state=CONSISTENT`, `target_state=EXACT_MATCH`; el manifest git local sigue sin ser autoridad de publicación.
- **Rollout Linux (Stager canónico):** Zeus CURRENT `0.2.100`, worker PID 2742882 (22:46:39Z) ejecutando `/opt/stager/releases/0.2.100/bin/symphony`, SHA256 instalado == artifact, `Started Worker Namespace sqx-prop TaskQueue sqx-main-queue WorkerID 2742882@sqx-ulab-zeus-0` + ESTABLISHED :7233; Hera CURRENT `0.2.100`, PID 1338299 (22:45:51Z, `1338299@sqx-ulab-hera-0`, SHA256 == artifact); Kronos CURRENT `0.2.100`, PID 1302804 (22:47:08Z, `1302804@sqx-ulab-kron-0`, SHA256 == artifact); un worker symphony por host. Zeus y Kronos registraron un arranque previo efímero del stager antes del PID final (comportamiento restart_backoff observado; estado final limpio y singleton). El legado `symphony-f03-382f4ba` de Hera sigue presente, no tocado, no blocker.
- **Rollout Windows `worker-kronos` (Stager, sin `C:\SQX`, sin inspector one-shot ni owner action):** demostrado con la capability recurrente `AraneaEvidencePublish (SYSTEM) → evidence JSON → mt5-kronos-operator read-only`. Reporte 22:48:10Z (`partial=false`, `errors=[]`, publisher `nt authority\system`, freshness <15 min, `inspector_sha256` == canónico): worker `sqx-mt5-worker` PID 5496 desde 22:45:03Z, path `C:\ProgramData\Stager\releases\0.2.100\bin\sqx-mt5-worker.exe`, `exe_sha256` == artifact EXACT (`b36216c0…`), size 37249024 == manifest, padre `stager-runtime` PID 19472 (servicio Running/Automatic, `.\kor`), `CURRENT=0.2.100`/`PENDING=0.2.100`, poller ESTABLISHED PID 5496 → Temporal `192.168.31.46:7233`; singleton (1 worker). Convergencia inmediata; sin intervención manual.
- **Estado:** `CERT-F04-01 = BLOCKED / READY TO RERUN`. El microfix+release+rollout NO constituyen certificación física. Resto de gates sin cambio. `MIGRATION: NONE`. **Siguiente acción exacta:** misión `F05C-CERT-F04-01-RERUN-3` (prompt maestro completo persistido en el workspace de la misión; NO ejecutado en esta misión): re-run físico desde cero sobre `0.2.100`/`a440ac4…` con identidades todas nuevas, allocations `26090011001–004` write-once conservadas, prioridad de observación al compile físico y al log durable auténtico (key, size, SHA256, BOM/encoding) para cerrar la regresión T8.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c4-microfix-release-rollout`, change_log `2026-09-17-f05c-cert-f04-01-c4-microfix-release-rollout`.

### Delta de ejecución CERT-F04-01 RERUN-3 — 2026-09-17/18 (misión NORMAL F05C-CERT-F04-01-RERUN-3; re-run físico desde cero sobre 0.2.100; veredicto `BLOCKED`)

Ejecutó el gate físico CERT-F04-01 desde cero sobre release `0.2.100` / `a440ac4ed730747b1ae6b07557cb6204102f16ac` con UNA campaña real de identidades todas nuevas, observada hasta terminalización. Preflight 10/10 PASS (source HEAD==origin==`a440ac4…` con dirty operacional `deploy/manifest.json`+`phase4_performance.json` preservado; `release-authority --target 0.2.100` `CONSISTENT/EXACT_MATCH` 6/6 objetos 23:11:38Z; Linux Zeus/Hera/Kronos CURRENT `0.2.100` con SHA instalado == manifest y pollers `sqx-main-queue` vivos; Windows evidence publisher SYSTEM 23:13:06Z `partial=false` con worker singleton PID 5496 @ `releases\0.2.100\bin`, SHA256 EXE == artifact `b36216c0…cc8f3` exacto; PENDING.next MISSING; 0 workflows Running). Ingress productivo único en Zeus (watcher release `0.2.100` SHA `d0dac89c…`==manifest, ENV=production, screen, CWD `/tmp/cert-f04-01-rerun3`, dispatch 6/6 pasos en 175 ms a las 23:21:32Z; watcher auto-cerrado 23:21:41Z por guard legacy — sin efecto, dispatch ya durable). Input validado contra structs @ `a440ac4` con validador scratch recompilado contra el checkout vivo (+3 negativos rechazados: `target_finalists=0`, request_id no canónico, unknown field en `forge_campaign`); CFX reutilizados byte-exactos con hashes verificados in-situ. Cadena física demostrada (la más profunda de los tres intentos): funnel completo sin FAILED (overview ×1, retester ×17, optimizer ×12, evaluate_wfm ×12 físico en Zeus) → Decision V2 (`select_robust_run@sqx-robust-selection.v1` COMPLETED, cohort 1) → **allocation durable Magic V1 `26090011005`** (ns `forge-live`, StrategyRef `b94f1400-e495-4636-8587-516db1cb8cb7`, canonical opaco F-01, allocation_ref `sha256:1d44410f5bc88b2da2947c0f9aefc7e19949e2a7c95424c17d4758efb1053f99`, assigned_at 23:36:42.522Z; el allocator continuó desde su estado durable real 001–004, sin preasignar 005) → Apply COMPLETED (read-back contractual fail-closed; read-back byte-level directo NO disponible: MinIO-RO 403 en `sqx-strategies`, Mongo RO `-32003` — limitación registrada) → final reretester → trade list/report/MQ5 export durables (MQ5 282575 B `sha256:f8968dba…`) → **compile físico Windows attempt 1 SUCCESS** (MetaEditor real `0 errors, 1 warnings, 4625 ms` a las 23:38:19.599Z para el MQ5 exacto de este run; job dir con EX5 físico 176426 B) → **`mt5_compile_persist_v1` COMPLETED** (3 objetos verificados, StageExecution `9667014a-4c84-4824-8a42-f62d851b7981`, EvaluationRef inmutable `sha256:6d1a37cef2ad4920c415183f07132b407b9e9a30b9c54ea0319ce259ee852859`; EX5 durable `sha256:6d667d703d252e6f3904d9fce077fb5e46711144c23b15c1f6589a041c14f720` 176426 B; compile log durable key `…/08_mt5_ex5/final-b94f1400….compile.log` 15984 B `bdf5e5b4191f4634052247ce4e47a0255261368fa60c7b7927b9b12a8ebb04d5` — **PHYSICAL LOG BYTES NOT AVAILABLE** por Access Plane vigente; identidad verificada por key/size/SHA256 durables + persist COMPLETED + línea física de `metaeditor.log`; cero `persistence_deadline_required`, cero `artifact_integrity` falso) → **backtest físico real 54 min** (child `MT5BacktestArtifactWorkflow` COMPLETED 00:32:22.504Z; HTM durable 13074872 B `sha256:21917e14…`; input del backtest == EX5 del compile, identidad encadenada exacta) → **fallo terminal**: `mt5_reconcile_v1` en `1302804@sqx-ulab-kron-0@` falló non-retryable `contract_error` **`mt5 report: build not supported: build=6182`** (evento 366, 00:32:23.380Z) → FlowRun/Campaign `FAILED/WAVE_FLOW_RUN_FAILED` 00:32:23.578Z antes de scores/ranking/promotion; StrategyVersion seal NO ocurrió (`strategy_versions: []`), HandoffManifest NO ocurrió (`handoffs: []`), cero finalistas NO por filtros auténticos.

- **Root cause (anclada en source @ `a440ac4`):** `sqx/adapters/mt5/report/types.go:13-21` define la allow-list explícita fail-closed de builds certificados para `mt5-report.v1` = `{6090, 6140, 6180}` ("A build enters the list only after its physical report is certified"); `parse.go:129-130` rechaza cualquier otro build con `BuildNotSupportedError{Build}`; el test `TestIsSupportedBuildAllowList` congela la lista. El terminal MT5 físico de worker-kronos auto-actualizó más allá de 6180 y su reporte declara `Build 6182`. No es defecto de input ni flake: determinístico para todo intento en el entorno actual; el gate actuó según contrato (fail-closed correcto) contra un drift de entorno.
- **Terminalización y drain:** campaign `FAILED` terminal por sí misma (sin cancel externa); `flow_run_seal` COMPLETED selló el estado terminal del FlowRun; children compile y backtest COMPLETED; 0 workflows Running en `sqx-prop` post-run; Windows 00:58:07Z evidence fresco `partial=false` SYSTEM con worker singleton PID 5496 (sin restart) y pollers ESTABLISHED; metatester64/terminal64/metaeditor64 = 0 procesos a las 01:00Z; Zeus sin watchers ni screens residuales; f03cert intacto; `LIVE_PHYSICAL_MT5_EXECUTIONS_ACROSS_FLEET <= 1` respetado (1 backtest físico, terminado).
- **Estado:** `CERT-F04-01 = BLOCKED` (tercer defecto determinístico). Los fixes C1 (deadline), C3 (parser compartido) y C4 (UTF-8 estricto) quedaron **validados físicamente** por este run. Resto de gates sin cambio. `MIGRATION: NONE`. **Siguiente acción exacta:** decisión owner entre (a) certificar build 6182 contra `mt5-report.v1` (verificación física del formato + allow-list + tests + release nueva) o (b) fijar/downgrade del terminal MT5 de worker-kronos al build 6180 certificado; luego CERT-F04-01 re-run #4 desde cero (identidades todas nuevas; allocations `26090011001–005` write-once permanecen).

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-rerun3`, change_log `2026-09-17-f05c-cert-f04-01-rerun3-blocked`, workdir `~/aranea/work/f04-cert-f04-01-rerun3/` (pre-submit, submit y evidence summary completos).

### Delta de certificación y source fix build 6182 — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C6; HTM auténtico entregado a `daedalus` por el owner del Access Plane; veredicto `COMPATIBLE` + `SOURCE_FIX_READY_FOR_REVIEW`)

Ejecutó el mandato de certificación del build 6182 contra `mt5-report.v1` (procedimiento mínimo de SPEC-PARSER §3.3) sobre el HTM físico auténtico de RERUN-3 entregado por el owner del Access Plane en `daedalus` (`/tmp/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`), cerrando por la vía owner el bloqueo de adquisición de C5/C5A/C5B sin modificar IAM, sin credenciales administrativas, sin campañas ni backtests, sin releases y sin RERUN-4. Gate de integridad PASS: tamaño 13074872 exacto, SHA256 `21917e144668cfd014e0d0c6a60d4f70eb3c4a17783bacac02f729eb587e8b5c` (== durable C5), BOM `FF FE` UTF-16LE; copia privada byte-exacta con permisos 700/600 en `~/aranea/work/f04-cert-f04-01-c6/artifact/` (fuera del vault y del repo); el origen quedó en 644 bajo `hermes-ops` (archivo ajeno: se reporta al owner, no se chmoddea). Baseline verificada: `origin/codex/f05-release-prep` = `a440ac4ed730747b1ae6b07557cb6204102f16ac` (== mandato, sin drift material); worktree aislado `~/aranea/work/f04-cert-f04-01-c6/symphony-cert`.

- **Contraste estructural (parser real del baseline, 6182 habilitado SOLO en el worktree de certificación y revertido antes del branch):** encoding, 2 tablas DOM (`Strategy Tester Report` / `Darwinex-Demo (Build 6182)` / `Settings` / `Results`), headers Orders (11) y Deals (13) canónicos, fila de totales colspan, gramáticas numéricas y semántica de campos idénticos a 6090/6140/6180. Parseo completo con `Timezone: GMT`: build 6182, Expert `final-b94f1400-e495-4636-8587-516db1cb8cb7`, XAUUSD H1 `2016.01.04 - 2026.06.05`, deposit 10000, Bars 61675, Ticks 766024381, 44 inputs (`mmLots=0.1`, `MagicNumber=320207229`); NetProfit −6889.82, GP 5699.16, GL −12588.98, PF 0.45 sin flags degenerados, TotalTrades 2117, TotalDeals 4234; orders 19110; deals operativos 4234 (2117 in + 2117 out) + 1 balance deal; totals `C −2073.73 / S 12.21 / P −4828.30 / B 3110.18`; `Margin Level: 142.29%` → **INVALID** `pattern_mismatch` (misma semántica congelada del quirk `414.35%` del 6140; sin reinterpretar).
- **Siete crosschecks: 7/7 PASS** (Expected/Observed/delta, todos < 0.005): `sum_profit_vs_totals` −4828.30/−4828.30/≈0; `sum_commission_vs_totals` −2073.73/−2073.73/≈0; `sum_swap_vs_totals` 12.21/12.21/≈0; `net_profit_vs_total_net_profit` −6889.82/−6889.82/≈0; `final_balance_vs_deposit_plus_net` 3110.18/3110.18/≈0; `operational_deals_vs_total_deals` 4234/4234/≈0; `inout_pairs_vs_total_trades` 2117/2117/≈0. Consistencia in/out verificada de forma independiente (in==out==2117 == TotalTrades). Sin gramáticas ni fórmulas nuevas para Gross Profit, Gross Loss ni Profit Factor. **Veredicto: `COMPATIBLE`** (`parser_version` permanece `mt5-report.v1`).
- **Source fix publicado (tras la demostración, nunca antes):** rama `codex/f05-build6182-parser-cert` desde `a440ac4…`, commit único `bcf67beb9a1c7c542d11d13b4b6f65780f37df58`, push a origin con read-back verificado (`git ls-remote` == HEAD). Diff acotado (5 archivos, +336/−9, todos dentro de la allow-list del mandato): `types.go` (allow-list explícita `6090, 6140, 6180, 6182`; sin rangos, sin fallback de encoding, sin tocar `ParserVersion` ni congeladas), `parse_test.go` (allow-list test con 6182 y rechazo de 6183 agregado), `build6182_test.go` (nuevo), `SPEC-PARSER.md` y `CORPUS.md`. Sin cambios a `parse.go`/`crosscheck.go`/`results.go`/reconcile/scoring/normalización/Temporal/Stager/lifecycle físico; fixtures históricos intactos.
- **Fixture de 13 MB fuera del repo (aplicación literal del mandato §4):** la política del corpus exige checksum y provenance pero no existe autorización explícita para versionar 13 MB de bytes binarios en Git (precedentes trackeados ≤173 KB; mayor blob del repo 3.2 MB; sin LFS) → los bytes NO se incorporaron: quedan durables en su object key ya existente (`sqx-strategies/durable/mt5-export/v1/b6a1edea-…/09_mt5_backtest/final-b94f1400-….htm`) y en la copia restringida del workspace; `CORPUS.md` §5.6 registra provenance/checksum/goldens y el gap de versionado con propuesta concreta para decisión owner (incorporación directa, Git LFS, o fetch de CI vía el `s3:GetObject` puntual ya propuesto en C5A/C5B). Tests reproducibles: `build6182_test.go` resuelve por `SQX_MT5_B6182_FIXTURE` con verificación dura de size/SHA256 antes de parsear y **skip explícito** (nunca PASS) si el artefacto no está disponible; los negativos (vecinos 6181/6183, allow-list, fail-closed de builds arbitrarios) son sintéticos sobre fixtures trackeados y corren siempre.
- **Tests:** suite 6182 10/10 PASS (integridad size+SHA+BOM; settings/results/orders-deals-totals/crosschecks con goldens auténticos congelados; encoding-failure y structural-mutation nunca retornan datos parciales; 6181 y 6183 → `ErrBuildNotSupported` con build observado preservado); paquete completo `./sqx/adapters/mt5/report/` **30/30 PASS, coverage 81.6%, 0 skips** con los bytes auténticos (regresión de builds certificados incluida: 6090 all-loss, zero-trades, 6140 mixed, 6180 zero-trade); skip explícito verificado sin fixture; `gofmt`/`go vet` limpios. Nota ambiental preexistente (ajena a este cambio): la suite de FIX-AL-75 exige `mt5-export.htm` en la raíz (hoy gitignoreado); para la regresión se recuperaron los bytes byte-exactos desde el commit de autoridad `b5c71d5` (SHA `090ca4d1…` == CORPUS) SOLO al worktree como archivo untracked, sin cambio al repo.
- **Review interno:** diff completo revisado; sólo archivos permitidos; semánticas congeladas intactas; sin secretos ni URLs firmadas (la object key ya estaba registrada públicamente en el delta C5). Cosmético preexistente reportado, fuera de scope: `parse.go:158` imprime «allow-list: 6090, 6140» en el error de build row ausente (ya era stale en el baseline por 6180; archivo no permitido por el mandato).
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio; el gate físico requiere release nueva + RERUN-4 — NO declarado PASS). La certificación de compatibilidad 6182 queda **demostrada** y el source fix queda `SOURCE_FIX_READY_FOR_REVIEW` en origin. Adquisición: resuelta según handoff del owner; `OWNER_APPROVAL_REQUIRED` de C5B deja de ser bloqueo activo (queda como historial). `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen; artifacts de RERUN-3 preservados intactos. **Próximo gate único:** source review del manager sobre el diff `a440ac4…bcf67be`; con aprobación → release nueva → RERUN-4 desde cero (identidades todas nuevas).

Registro completo de la misión en `~/aranea/work/f04-cert-f04-01-c6/certification-record.md` (permisos 700; sin secretos ni URLs firmadas).

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c6-build6182-source-fix`, change_log `2026-09-18-f05c-cert-f04-01-c6-build6182-source-fix`.

### Delta de corrección de revisión C6R — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C6R; targeted review fix de los pendientes R1–R4 del manager sobre el source fix C6; ningún gate físico, ninguna release, ninguna campaña; veredicto `SOURCE_READY`)

Ejecutó el mandato de cierre de los cuatro pendientes identificados por la revisión independiente del manager sobre `bcf67be…` (R1 diagnóstico incorrecto, R2 SPEC desactualizada, R3 tests de release, R4 coverage), sin repetir la certificación física, el análisis de compatibilidad ni la adquisición, y sin ejecutar campañas, releases ni deployments. Baseline verificada: HEAD local == `origin/codex/f05-build6182-parser-cert` == `bcf67be…` (parent `a440ac4…` intacto, worktree limpio, dirty foráneo ausente). Fixture auténtico re-verificado en la copia privada de `daedalus` (`~/aranea/work/f04-cert-f04-01-c6/artifact/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`, 13074872 B, SHA256 `21917e14…8b5c` == durable). Coverage medido en condiciones comparables con worktree temporal eliminado sin residuo: **`a440ac4` = 81.5% / `bcf67be` = 81.6%** — el déficit era preexistente del paquete, no del delta C6.

- **R1 — Diagnóstico genérico:** `parseTable0` ya no anuncia una allow-list inline (impresión stale «allow-list: 6090, 6140» que ya era incorrecta por 6180/6182); el `Expected` pasa a `"server row with a build from the certified matrix"`, remitiendo a la matriz certificada cuya única autoridad sigue siendo `isSupportedBuild` en `types.go` (sin duplicar la lista en otra autoridad). Test enfocado nuevo `TestParse_MissingBuildRowFailsClosed`: error tipado `StructureUnknownError` que desenrolla a `ErrStructureUnknown` + `domain.ErrReportParseFailed`, observado `missing build row`, texto con "certified" y **sin ningún número de build inline** (6090/6140/6180/6182 ausentes), cero datos parciales. Política de aceptación sin cambios.
- **R2 — SPEC §3.2:** la descripción estructural incorpora 6182 a la lista de builds certificados («6090, 6140, 6180 y 6182»), alineada con §3.3 y la allow-list; único cambio contractual (CORPUS, semánticas congeladas y otros contratos intactos).
- **R3 — Modo obligatorio de release:** `build6182_test.go` incorpora `SQX_MT5_REQUIRE_B6182_FIXTURE=1` con tabla de decisión congelada por `TestBuild6182_ReleaseModeDecisionTable`: con el gate activo, variable del fixture ausente → FAIL (nunca skip), archivo inexistente → FAIL, tamaño incorrecto → FAIL, SHA256 incorrecto → FAIL (diagnóstico con got/want), y todo test auténtico corre o falla (cero skips posibles); en modo desarrollo el skip explícito se conserva. Sin descargas automáticas, sin credenciales, sin copiar bytes al vault ni al repo: los bytes se consumen sólo de la ruta provista, y el release runner debe ejecutar el paquete completo sin filtros `-run` exigiendo 0 skips (documentado en el comentario del test). El uso en release será sobre la copia privada ya entregada en `daedalus` verificando integridad contra el SHA256 durable.
- **R4 — Coverage:** tras medir baseline y C6 en condiciones comparables, el déficit se cerró con 18 tests de contrato enfocados en `contract_test.go` (nuevo, dentro del allow-list `*_test.go`), cada uno anclado a comportamiento observable de SPEC-PARSER (no tests artificiales): diagnósticos tipados de los 5 errores (formato + unwrap), autoridad pública `IsCertifiedBuild`, MISSING/INVALID por label consumido por `parseResults` (tabla de eliminación sobre mapa dorado), gramáticas degeneradas (Sharpe/PF con flags y Margin Level OBSERVED/MISSING/INVALID), gaps de `applyResultSettings` (history/bars/ticks/symbols MISSING vs PatternMismatch), límites de gramática numérica (miles con espacio, coma decimal rechazada, layouts de tiempo, volume pair) y guards de overflow en los 10 helpers (ParseFloat range → fail-closed sin clamping), pairing DOM con labels adyacentes, helpers de header/celda/totals, errores por celda de order/deal, gaps estructurales de T0/T1 vía llamadas directas deterministas (título/Settings/Results repetidos o ausentes, setting requerido ausente, encabezado sin columna canónica, totals ausente), payload UTF-16 impar y truncamiento de prefijo, fechas imposibles en periodo (inicio y fin), overflow del número de build (`Atoi` range ≠ `ErrBuildNotSupported`), desbalance in/out en ambas direcciones y timezone inválida. Residual (~1%) documentado como ramas defensivas inalcanzables por construcción (error de `html.Parse` sobre strings.Reader, re-chequeos internos de `parseTotalsRow` guardados por `isTotalsRow`, verificación de 13 columnas subsumida por el check de longitud del header) — sin tests artificiales.
- **Validación ejecutada (no sólo reportada):** suite enfocada 6182 con bytes auténticos 11/11 PASS (6182 aceptado con goldens; 6181/6183 → `ErrBuildNotSupported` con build observado; 7/7 crosschecks PASS; encoding/estructura nunca parciales); paquete completo modo release con fixture auténtico **53 PASS / 0 SKIP / 0 FAIL / coverage 99.1%**; modo release sin fixture **exit 1** con 7 FAILs tipados y 0 skips; modo release con SHA incorrecto **exit 1** con diagnóstico de hash; modo dev sin fixture (regresión histórica 6090/6140/6180) 46 PASS + 7 skips explícitos (sólo los auténticos) + 0 FAIL; `gofmt -l` limpio en `sqx/adapters/mt5/`; `go vet ./sqx/adapters/mt5/... ./sqx/core/...` OK; `go build ./sqx/adapters/... ./sqx/core/...` OK — los fallos de `sqx/tools` (main redeclarado) y el gofmt de `sqx/activities/worker/` fueron demostrados preexistentes en HEAD pristine vía stash (fuera del allow-list, no tocados).
- **Commit y push:** commit adicional único `c1d24c1bbfb1819739e173d16593eb15f1afae49` (`fix(mt5): close C6 review findings R1-R4 on build 6182 cert delta`) sobre `bcf67be…` (sin reescritura, sin force-push, sin merge), 5 archivos (+~450/−13: `parse.go` 1 línea, `parse_test.go` +22, `build6182_test.go` modo release, `contract_test.go` nuevo, `SPEC-PARSER.md` §3.2); push a origin con read-back verificado (`git ls-remote` == HEAD local).
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio; el gate físico requiere release nueva + RERUN-4 — NO declarado PASS). R1–R4 cerrados; coverage 81.6% → **99.1%** (≥95% de política, residual defensivo documentado). `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen. **Próximo gate único: Release Authority** — publicar release nueva desde la rama aprobada (con `SQX_MT5_REQUIRE_B6182_FIXTURE=1` + fixture auténtico y 0 skips en el suite del paquete) → RERUN-4 desde cero (identidades todas nuevas).

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c6r-review-fix`, change_log `2026-09-18-f05c-cert-f04-01-c6r-review-fix`.

### Delta de release y rollout CERT-F04-01 build 6182 — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C7; Approved Source → Release → Certified Fleet Rollout; ningún gate físico, ninguna campaña, sin RERUN-4; veredicto `RELEASE/ROLLOUT PASS`)

Publicó la release autorizada siguiente a `0.2.100` desde el source aprobado C6+C6R (`c1d24c1…`) y la desplegó/certificó en la flota completa, sin ejecutar RERUN-4, sin repetir C5/C6/C6R, sin campañas, sin ETCD, sin IAM y sin tocar código de negocio. Baseline verificada: `origin/codex/f05-release-prep` = `a440ac4…` (== HEAD verificado por el manager) y `origin/codex/f05-build6182-parser-cert` = `c1d24c1…`; descendencia exacta demostrada (`bcf67be^` == `a440ac4…`, `c1d24c1^` == `bcf67be…`, dos commits, sin intermedios); dirty operacional (`deploy/manifest.json` + `phase4_performance.json`) preservado sin stage y sin overlap con el diff del delta.

- **Policy de rama resuelta:** el contrato F-05-I fija `codex/f05-release-prep` como la rama de implementación/release de esta serie (creada desde el SHA exacto, nunca desde master); la rama feature `codex/f05-build6182-parser-cert` no obtiene autoridad productiva. Integración por fast-forward puro `a440ac4…`→`c1d24c1…` (sin merge/rebase/squash/cherry-pick), push a origin y read-back (`ls-remote` == `c1d24c1…`). Ninguna release posterior había consumido los commits: `release-authority` preflight `published_version=0.2.100`, `remote_line_max=0.2.100`, `authority_state=CONSISTENT`, candidato `0.2.101`.
- **Fixture auténtico re-verificado:** `~/aranea/work/f04-cert-f04-01-c6/artifact/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`, 13074872 B, SHA256 `21917e14…8b5c` == durable, permisos privados (700/600), bytes intactos.
- **Release test gate (desde el checkout `c1d24c1…` que efectivamente se publicó):** primera corrida con `SQX_MT5_REQUIRE_B6182_FIXTURE=1 SQX_MT5_B6182_FIXTURE=<fixture> go test ./sqx/adapters/mt5/report/ -count=1 -v -cover` terminó exit 1 con 7 FAILs del corpus histórico (`corpus root must contain mt5-export.htm`) — defecto ambiental preexistente documentado en CORPUS `FIX-AL-75` (fixture trackeado históricamente, hoy gitignoreado; bytes de autoridad en git `b5c71d5`). Resolución por la vía ya certificada en C6: bytes recuperados byte-exactos del commit de autoridad (SHA256 `090ca4d1…` == CORPUS, 133664 B) como archivo untracked/gitignoreado, sin cambio al repo. Segunda corrida: **exit 0 — 53 PASS / 0 FAIL / 0 SKIP / coverage 99.1% (≥95%)**, `TestBuild6182_SevenCrosschecks` PASS (7/7 crosschecks con goldens auténticos), `TestBuild6182_AuthenticFixtureIntegrity` PASS, vecinos 6181/6183 rechazados, regresión histórica 6090/6140/6180 PASS. Evidencia `-count=1` (sin caché) en `~/aranea/work/f04-cert-f04-01-c7/test-report-package.log`.
- **Gates estáticos:** `gofmt -l` limpio en `sqx/adapters/mt5/`; `go vet ./sqx/adapters/mt5/... ./sqx/core/...` exit 0; `go build ./sqx/adapters/... ./sqx/core/... ./sqx/workflows/... ./sqx/activities/...` exit 0. Los hallazgos gofmt de `sqx/core` (5+ archivos) y el `main redeclarado` de `sqx/tools` fueron demostrados idénticos en un worktree temporal del baseline `a440ac4` (0 archivos de `sqx/core` en el delta; módulos fuera del allow-list, no tocados), eliminado sin residuo.
- **Build/publicación por el mecanismo canónico** (`./deploy_release.sh --release-only 0.2.101`, deployer-watcher ENV=production en screen): 6 artifacts desde `c1d24c1…` — linux-amd64 `symphony` 40317088 B / sha256 `d795a998340537e95676bd13a0f9a9b6a4e97103c3b6662561d2061d12d59ad3`, `sqx-watcher` 32682144 B / `79dfdf89feff96c711f7164ff67a4b89622278b9383d128663729af8be8c4886`, `start-symphony-worker.sh` 310 B / `9f956384…` (== 0.2.100, plantilla sin cambios), `run-symphony` 233 B / `8f240eb2…` (ídem), `promtail-worker.yaml` 355 B / `eaa4e1b2…` (ídem); windows-amd64 `sqx-mt5-worker.exe` 37249024 B / `f5a8baa1338f01e7260d710f68834a02e5cef25f8937d938691a62815a66f0df`. `go version -m` de los 3 binarios: `vcs.revision=c1d24c1…`, `vcs.time=2026-09-18T21:06:54Z`, `vcs.modified=true` (dirty operacional no-Go, misma clase documentada de 0.2.98–0.2.100). SHA256 locales recomputados == `deploy/manifest.json` (`0.2.101`); MinIO confirmó el manifest; read-back `release-authority --target 0.2.101`: `published_version=0.2.101`, `target_state=EXACT_MATCH`, `authority_state=CONSISTENT`, `remote_line_max=0.2.101`. Sin sobrescribir versiones históricas; sin tags Git (la autoridad es el registry `sqx-release-authority.v1`).
- **Preflight de flota (antes de tocar hosts):** Zeus/Hera/Kronos CURRENT `0.2.100` con SHA instalado `9fbf73b2…` == artifact 0.2.100 y un único worker por host (PIDs 2742882/1338299/1302804, los mismos de C4); Windows evidence 22:08:07Z `partial=false`, worker PID 5496 @ `releases\0.2.100` SHA `b36216c0…` == artifact, CURRENT/PENDING `0.2.100`, singleton; procesos físicos MT5 en worker-kronos = 0 (`LIVE_PHYSICAL_MT5_EXECUTIONS_ACROSS_FLEET = 0`; nada que drenar, sin takeover posible).
- **Rollout Linux (Stager canónico, convergencia automática):** Zeus CURRENT `0.2.101`, worker PID 2769428 (22:09:48Z) ejecutando `/opt/stager/releases/0.2.101/bin/symphony` con SHA256 instalado == artifact `d795a998…`, `Started Worker Namespace sqx-prop TaskQueue sqx-main-queue WorkerID 2769428@sqx-ulab-zeus-0` + ESTABLISHED :7233, cero procesos `0.2.100`; Hera CURRENT `0.2.101`, PID 1365120 (22:11:50Z, `1365120@sqx-ulab-hera-0`, SHA == artifact, ESTABLISHED, sin residuos); Kronos CURRENT `0.2.101`, PID 1328601 (22:10:37Z, `1328601@sqx-ulab-kron-0`, SHA == artifact, ESTABLISHED, sin residuos). El legado `symphony-f03-382f4ba` de Hera sigue presente, no tocado, no blocker. Sin watchers/screens residuales (el watcher de ingress es transitorio por diseño, auto-cerrado).
- **Rollout Windows `worker-kronos` (Stager + `AraneaEvidencePublish (SYSTEM) → evidence JSON → mt5-kronos-operator read-only`):** reporte 22:13:07Z (`partial=false`, `errors=[]`, publisher `nt authority\system`, freshness <15 min, `inspector_sha256` == canónico): worker `sqx-mt5-worker` PID 19436 (nuevo; antes 5496) desde 22:09:02Z, path `C:\ProgramData\Stager\releases\0.2.101\bin\sqx-mt5-worker.exe`, `exe_sha256` == artifact EXACT (`f5a8baa1…`), size 37249024 == manifest, padre `stager-runtime` PID 35900 (servicio Running/Automatic, `.\kor`), `CURRENT=0.2.101`/`PENDING=0.2.101`/`PENDING.next MISSING`, poller ESTABLISHED (state 5) PID 19436 → Temporal `192.168.31.46:7233`; singleton verificado por proceso físico (`COUNT=1`); `MT5_PHYS=0` post-rollout.
- **Estado:** `CERT-F04-01 = BLOCKED / READY TO RERUN-4` (sin cambio de gate; el rollout NO constituye certificación física). Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen; artifacts de RERUN-3 preservados. **Deuda de seguridad registrada (no bloqueante para el rollout): una contraseña SSH aparece literalmente en el `AGENTS.md` del repo symphony (sección Inventario de Workers; valor NO reproducido en logs, vault ni handoffs) — acción owner: rotación de la credencial y saneamiento del documento; el Access Plane opera por identidad/key propia (`echo-dev`), por lo que ningún gate se detuvo.** **Próximo gate único: `CERT-F04-01 RERUN-4`** desde cero sobre `0.2.101`/`c1d24c1…` con identidades todas nuevas (NO ejecutado en esta misión).

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c7-release-rollout`, change_log `2026-09-18-f05c-cert-f04-01-c7-release-rollout`, workdir `~/aranea/work/f04-cert-f04-01-c7/`.

### Delta de ejecución CERT-F04-01 RERUN-4 — 2026-09-18/19 (misión NORMAL F05C-CERT-F04-01-RERUN-4; re-run físico desde cero sobre 0.2.101; veredicto `BLOCKED`)

Ejecutó el gate físico CERT-F04-01 desde cero sobre release `0.2.101` / `c1d24c1bbfb1819739e173d16593eb15f1afae49` con UNA campaña real de identidades todas nuevas, observada hasta terminalización. La cadena física alcanzó la mayor profundidad de la serie y quedó **en verde hasta promotion inclusive** (allocations, apply, compiles físicos, 4 backtests reales, 4 HTM auténticos 6182 byte-verificados, parser 6182 en producción, ranking y promotion), pero terminó **`BLOCKED`** por un **cuarto defecto determinístico (producto)**: `forge_seal_handoff_v1` falló non-retryable `CONTRACT_CONFLICT: member 107f1da5-cf85-443f-8fbf-55ba1dbb551e source Evaluation is project@sqx-final-reretester.v1, apply authority required` (Temporal evento 492, 2026-09-19T03:15:56.217Z) → FlowRun/Campaign `FAILED/WAVE_FLOW_RUN_FAILED` 03:15:56.352Z antes de StrategyVersion seal y HandoffManifest. **CERT-F04-01 = `BLOCKED`**.

- **Identidades del run (todas nuevas):** request_id `cert-f04-01-rerun4-20260918T231115Z-b0e6160f` (preimage SHA256 `ee5f947e360a09e322579169b24600dad289c4d6d64a5706eea6a22cd431ee6b`, 5610 B, VALIDATION_OK contra structs @ `c1d24c1` con negativos 3/3 RECHAZADOS; diff vs receta RERUN-3 limitado a `/strategy`,`/wave`,`/request_id`,`/wave_config/wave_key`; CFX byte-exactos reutilizados con hashes verificados in-situ); ForgeCampaignRef `8a6b7dda-f190-479f-b028-d0b998d8eab3`; CampaignIntentToken `db1bbf4e-e465-4376-88f9-d40136d1ce7d`; WorkflowID/RunID campaña `sqx-forge-campaign-v1-db1bbf4e-…` / `01a0b6d6-2e22-75ef-9a71-667d18633c9e`; WaveRef `forge-8a6b7dda-…-w000001` (1 de 1); FlowRunRef `b98aa99b-f40e-45aa-96c3-f821340e85ba` (`sqx-main-v1-0ce058d1-…` / `01a0b6d6-2f50-7c43-97bb-57d6bf1cc20c`); storage prefix `wave_cert-f04-01-rerun4/xauusd/l_h1/cert-f04-01-rerun4/v1/`.
- **Dispatch único (G2):** watcher release `0.2.101` (`79dfdf89…` == manifest) en Zeus, ENV=production, CWD `/tmp/cert-f04-01-rerun4`, pipeline 6/6 pasos 23:24:44Z. Nota operacional sin efecto durable: el primer arranque (23:22:55Z, input no pre-stageado) fue cerrado por el guard legacy `/opt/symphony/current` a los 10 s ANTES de consumir el archivo; resuelto según contrato (0 campañas, 0 workflows, SHA del input intacto en `input/`) y re-dispatch con la MISMA identidad y archivo pre-stageado (patrón canónico RERUN-3 — el escaneo inicial lo consume en el primer poll).
- **Cadena física lograda:** G0 preflight PASS (release-authority `0.2.101 CONSISTENT/EXACT_MATCH` 23:06:33Z; flota 4/4 SHA instalado == artifact; Windows PID 19436 `f5a8baa1…` EXACT singleton; `MT5_PROCS=0`; 0 Running) → funnel completo sin FAILED (overview ×1, retester ×14, optimizer ×8, evaluate_wfm ×8) → select_robust_run ×4 (cohort: df63bab0, 107f1da5, 49d65359, 912ac5d4) → **allocations durables Magic V1 `26090011006` (912ac5d4, `sha256:37877f33…`), `26090011007` (107f1da5, `sha256:14657baa…`), `26090011008` (df63bab0, `sha256:86196003…`), `26090011009` (49d65359, `sha256:acaf246b…`)** — el allocator continuó desde su estado durable 005, CAS con UNIQUE por (namespace, StrategyRef); 001–005 write-once intactas → apply_selected_run ×4 COMPLETED (read-back fail-closed) → final_reretester ×4 COMPLETED → trade list/report/MQ5 ×4 durables → **compile físico Windows 4/4 SUCCESS attempt 1** (MetaEditor real `0 errors, 1 warnings` ×4, metaeditor.log 23:36:55–23:37:09Z; children `MT5CompileArtifactWorkflow` ×4 COMPLETED 23:36:49Z; EX5 4/4 con identity encadenada; el EX5 de df63bab0 coincide byte-level con la captura física hash del job dir 23:39:11Z) → **backtest físico real 4/4 secuencial en worker-kronos** (children `MT5BacktestArtifactWorkflow` ×4 COMPLETED; df63bab0 23:37→00:24 ≈47 min; metatester64/terminal64 observados vivos; invariante `LIVE_PHYSICAL_MT5_EXECUTIONS <= 1` respetado) → **G4: 4/4 HTM auténticos byte-verificados** (HeadObject/presign GET por identidad autorizada; SHA256 sobre bytes reales == durable: df63bab0 5195458 B `c586785f77d84e84db670dd6425c7a1f5885e1f524c2cf963aa5916bf62f1d14`, 912ac5d4 12460992 B `ead914ab78be98e785e9296a4bd695d5a82897eae9a9709d64556aa9f8ae7a96`, 49d65359 7816270 B `4e69d47073556252991ebc5fee4d04ca2659720976cbe2294eb9424752ef0377`, 107f1da5 3392778 B `f403fc42881cca2c4f498451d5386ae518df28d0684b8d38105893903f54e545`; tester.ini byte-verificado) → **G5: `mt5_reconcile_v1` ×4 COMPLETED en producción** con build 6182 aceptado (`structural_identity: MATCH`, evaluation_refs/trade_set_refs/metric_set_refs durables; cero `StructureUnknown`/`BuildNotSupported`/`CrosscheckFailed`) y parse local certificado de los 4 HTM → 7/7 crosschecks PASS por estrategia → mt5_score_shadow_v1 ×4 + rank_snapshot_v1 COMPLETED → promote_finalists COMPLETED (Decision V2, decision_ref `sha256:df6a355d…`, 4 finalists_v2 con warnings auténticos `PNL_SIGN_FLIP`, rank 1 = 49d65359).
- **Root cause del fallo (anclada en source @ `c1d24c1`):** el assembler `runForgeSealHandoff` (`sqx/workflows/generic_workflow.go:1026`) construye `ApplyEvaluationRef` desde `carrier.EvaluationRef`, pero `validateFinalReretesterFanoutOutput` (`generic_workflow.go:1837`) **exige** que el output del final reretester tenga `EvaluationRef != Apply EvaluationRef` (el carrier queda con la Evaluation del final reretester), mientras el gate (`sqx/activities/worker/forge_seal_handoff.go:251`) exige que esa Evaluation tenga stage `apply_selected_run` — contradicción estructural assembler↔gate, determinística para todo run F-04 que llegue al seal tras un final reretester exitoso; primera vez ejercitada físicamente (EXEC/RERUN/RERUN-3 murieron antes). NO es defecto de input, entorno ni flake.
- **Terminalización y drain:** campaign `FAILED` terminal por sí misma; `flow_run_seal` COMPLETED selló el estado terminal; children compile ×4 y backtest ×4 COMPLETED; 0 workflows Running en `sqx-prop` post-run; `strategy_versions: []` y `handoffs: []` ×4 (read-back flowkit); f03cert intacto; watcher auto-cerrado por guard (sin segundo consumidor). Re-verificación viva de `MT5_PROCS=0` en worker-kronos tras el backtest final quedó como captura pendiente del handoff (la terminalidad dura queda demostrada por children COMPLETED + 0 Running + campaign FAILED terminal).
- **Cambio material del Access Plane (G4):** la capability `aranea-minio-ro` ahora SÍ lee `sqx-strategies` (HeadObject 200 y GET presignado exitosos sobre el HTM de df63bab0; last_modified 2026-09-19T00:24:48Z) — **el blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` de C5A/C5B queda cerrado**; la concesión owner aplicó entre C6 y RERUN-4. GET sin firma sigue denegado correctamente (RequestID `18D691D719C10001`). Nota: los presign de los HTM 3 y 4 se emitieron por error desde la conexión `aranea-minio-rw` (GET-only, sin mutación).
- **Limitación de observación:** Loki NO ingiere al worker symphony Forge (jobs `echo-core`/`echo-bridge`/argus únicamente; >600k líneas escaneadas en las ventanas del run sin menciones forge/sqx/seal) — limitación idéntica a la documentada en RERUN-3; la evidencia autoritativa del fallo es el historial Temporal `sqx-prop` (scratch `/tmp/thist`).
- **Estado:** `CERT-F04-01 = BLOCKED` (cuarto defecto determinístico). Fixes C1 (deadline), C3 (parser compartido), C4 (UTF-8) y la certificación 6182 (allow-list + parser + crosschecks) quedaron **validados físicamente en producción** por este run. Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–009` write-once permanecen; artifacts de RERUN-4 preservados en `sqx-strategies` y copia local auténtica en `~/aranea/work/f04-cert-f04-01-rerun4/artifacts/`. **Próximo gate único:** fix de source del assembler/gate del seal (decisión manager; p.ej. conservar el Apply EvaluationRef original del carrier en un campo que el fan-out del final reretester no toque) → release nueva → RERUN-5 desde cero con identidades todas nuevas.

Evidencia de la misión: agent-run `2026-09-19-zcode-glm-5.3-flash-f05c-cert-f04-01-rerun4-blocked`, change_log `2026-09-19-f05c-cert-f04-01-rerun4-blocked`, workdir `~/aranea/work/f04-cert-f04-01-rerun4/` (pre-submit, submit y evidence summary completos; artifacts con SHA256 verificados).

### Delta de corrección source C9 — 2026-09-19 (misión NORMAL F05C-CERT-F04-01-C9; fix Apply Authority lineage en el assembler del seal; ningún gate físico, ninguna release, ninguna campaña, sin RERUN-5; veredicto `SOURCE_FIX_READY_FOR_REVIEW`)

Corrigió en source el cuarto defecto determinístico demostrado por RERUN-4 ejecutando el contrato C9 del mandato: separar las dos autoridades que compartían el campo `EvaluationRef` del carrier. Baseline verificada sin drift: `origin/codex/f05-release-prep == c1d24c1bbfb1819739e173d16593eb15f1afae49` (fetch + rev-parse; dirty operacional `deploy/manifest.json` intacto en su checkout); worktree aislado `~/aranea/work/f04-cert-f04-01-c9/symphony-c9` con rama nueva `codex/f05-seal-apply-authority-fix` desde el SHA exacto del mandato. RCA anclada en los tres puntos del mandato y confirmada en lectura de source: (A) `runDurableApplySelectedRun` escribía sólo `updated[i].EvaluationRef` (`durable_apply_selected_run_workflow.go:59`), (B) `validateFinalReretesterFanoutOutput` exige `output.EvaluationRef != input.EvaluationRef` (`generic_workflow.go` el reemplazo es contractualmente correcto) y el fan-in reemplaza el carrier completo, (C) `runForgeSealHandoff` tomaba `applyRef := strings.TrimSpace(carrier.EvaluationRef)` (`generic_workflow.go:1026`) entregando la Evaluation del final reretester como Apply; el consumer (`forge_seal_handoff.go:239-255`) exige stage `apply_selected_run` con versión contractual vigente y mismo StrategyRef/FlowRunRef — **el consumer no fue modificado** (actuó correctamente).

- **Contrato del carrier implementado (sin fallback, sin lookups, sin reconstrucción):** nuevo campo `ApplyEvaluationRef string \`json:"apply_evaluation_ref,omitempty"\`` en `runtime.StrategyArtifact` (`sqx/core/runtime/config.go`), aditivo: payloads históricos sin el campo decodifican con autoridad vacía (nunca adquieren autoridad Apply) y el campo vacío se omite del JSON; se establece exclusivamente desde el resultado exitoso de Durable Apply tras las validaciones ya existentes de identidad (StrategyRef/DecisionRef/CanonicalStrategyID) y formato SHA256Ref del EvaluationRef; `EvaluationRef` y `CompileEvaluationRef` conservan su semántica intacta.
- **Archivos cambiados (sólo allowed files):** `sqx/core/runtime/config.go` (campo + delta documental del contrato en el doc comment), `sqx/workflows/durable_apply_selected_run_workflow.go` (+1 asignación `updated[i].ApplyEvaluationRef = string(result.EvaluationRef)` con comentario de autoridad), `sqx/workflows/generic_workflow.go` (assembler: `applyRef := strings.TrimSpace(carrier.ApplyEvaluationRef)`; fail-closed `member %s carries compile lineage without Apply authority` para carrier con lineage de compile sin autoridad Apply — no handoff con lineage incompleto; rechazo `duplicate member %s`; carriers sin compile lineage siguen excluidos del request y la semántica G22 de membresía cero y casos no-F-04 se preserva: zero-supply implica batch vacío), y tests: `sqx/workflows/forge_seal_apply_authority_test.go` (nuevo), `sqx/core/runtime/strategy_artifact_apply_ref_test.go` (nuevo), `sqx/activities/worker/forge_seal_handoff_test.go` (sólo funciones nuevas, diff 99+/0−). **`sqx/activities/worker/steps/steps.go` NO requirió cambio:** el productor del final reretester copia el carrier por valor (`finalCarrier = matched` en la ruta `IsDurableFinalReretesterTask`) y sólo sobrescribe StrategyRef/CanonicalID/Key/Artifact/EvaluationRef/DecisionRef, de modo que el nuevo campo se preserva físicamente (demonstración en source; la única reconstrucción campo-a-campo `rebindExportedObject` no tiene callers en producción).
- **Tests adversariales T1–T7:** T1 pin desde resultado válido (EvaluationRef avanza y `ApplyEvaluationRef == EvaluationRef` nuevo con identidad intacta) + tabla de negativos fail-closed sin salida parcial (StrategyRef/EvaluationRef/DecisionRef de input inválidos, canonical id vacío, activity error, object key mismatch, EvaluationRef de resultado inválido, identity mismatch, FlowRunRef no resoluble); T2/T3 fan-out del final reretester con cohorte de 4: cada output conserva exactamente su `ApplyEvaluationRef` por StrategyRef mientras `EvaluationRef` pasa a la evaluación final (`FINAL != APPLY`), fan-in con orden de entrega permutado sin referencias intercambiadas; T4 intercepción del request real hacia `forge_seal_handoff_v1` desde `GenericSQXWorkflow` completo (promoción V2 mockeada + seal capturado): los 4 miembros llegan con `ApplyEvaluationRef == apply ref` byte-exacto y `!= EvaluationRef` (final reretester) — **RED contra baseline demostrado** (worktree temporal @ `c1d24c1` + sólo el campo inerte para compilar: 4 tests de lógica FALLAN — T1 pin, T4 assembler, T5 autoridad ausente y T5 duplicados — con GREEN en el fix; worktree eliminado sin residuo); T5 consumer: rechazo fail-closed de autoridad Apply ausente/formato inválido, Evaluation de compile usada como Apply (firma exacta del error RERUN-4 `apply authority required`), Evaluation Apply de otra estrategia y de otro FlowRun (`subject/flow mismatch`), sin seal ni POST en todos los casos; T6 exclusión de carriers no-F-04 sin compile lineage (seal con exactamente los miembros de lineage completo), propagación del CONTRACT_CONFLICT del consumer, zero-supply con cero members (G22), suites completas; T7 sin fuentes nuevas de no-determinismo: toda la lógica nueva es función pura del historial (asignación desde resultado de activity y selección por campos del carrier; iteración en orden de slice; mapa sólo para lookup), sin tiempo, sin lecturas externas, sin reordenamiento por mapa; payloads históricos decodifican sin autoridad (tests de round-trip JSON con nombre de campo exacto y omitempty) — sin necesidad de versionado Temporal: para ejecuciones todavía vivas cada punto de decisión nuevo evalúa determinísticamente sobre eventos ya registrados y falla cerrado o procede coherente.
- **Gates:** suites de paquetes modificados con `-count=1` — `sqx/workflows` 21 fallos y `sqx/activities/worker` 16 fallos con **conjuntos idénticos al baseline `c1d24c1`** por comparación normalizada de listas (clases preexistentes documentadas: fixtures sin `flow_run_start` y fixture físico `mt5-export.htm` ausente), `sqx/core/runtime` PASS completo; `gofmt -l` limpio en los 6 archivos tocados; `go vet` limpio en los 3 paquetes; `go build` raíz OK y módulo `sqx` OK salvo `sqx/tools` (main redeclarado, preexistente documentado C6R); coverage función-nivel de los caminos críticos modificados: `runForgeSealHandoff` 100%, `validateApplyCarrier` 100%, `runDurableApplySelectedRun` 95.5% (residual: rama defensiva de output `ValidateDurable` inalcanzable por input); anti-test-masking: cero `t.Skip`, diff de tests 100% aditivo en el archivo existente, sin modificación de tests preexistentes, sin aserciones vacías, sin degradación de cobertura.
- **Commit y publicación:** commit único `87a3ab944c5ce706ac3fe7766fa86af32189cc05` (`fix(forge): pin Apply Evaluation lineage separate from carrier EvaluationRef`), parent exacto `c1d24c1…`, push a origin con read-back (`ls-remote == HEAD`); diff contra baseline = exactamente los 6 archivos del allow-list (646+/9−); escaneo de secretos del diff limpio; `origin/codex/f05-release-prep` intacto en `c1d24c1…`; sin merge, sin tag, sin release, sin rollout, sin RERUN-5.
- **Preflight de seguridad pendiente CERRADO:** la captura viva `MT5_PROCS=0` que RERUN-4 dejó pendiente se ejecutó el 2026-09-19T18:43:13Z vía `mt5-kronos-operator` en modo sólo lectura (`Get-Process -Name metatester64,terminal64,metaeditor64` → 0 procesos; `sqx-mt5-worker` → 1 singleton esperado); sin detener procesos, sin ejecución física, sin credenciales nuevas (nota: el perfil viewer `mt5-kronos` rechaza `read-command` por política; la consulta única usó el perfil operator con operación read-only, patrón canónico C4/C7/RERUN-4).
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio de gate; el fix requiere revisión del manager, release nueva y RERUN-5 — NO declarado PASS). **Fix C9 = `SOURCE_FIX_READY_FOR_REVIEW`** en origin. `MIGRATION: NONE`. Allocations `26090011001–009` write-once permanecen; artifacts de RERUN-4 preservados. **Próximo gate único:** revisión source del manager sobre el diff `c1d24c1…87a3ab9` → con aprobación, release nueva (número por release-authority) → RERUN-5 desde cero con identidades todas nuevas (NO reutilizar `8a6b7dda`/`b98aa99b` ni allocations consumidas).

Evidencia de la misión: agent-run `2026-09-19-zcode-glm-5.3-flash-f05c-cert-f04-01-c9-apply-authority-fix`, change_log `2026-09-19-f05c-cert-f04-01-c9-apply-authority-fix`, workdir `~/aranea/work/f04-cert-f04-01-c9/`.

### Delta de corrección de revisión C9R — 2026-09-19 (misión NORMAL F05C-CERT-F04-01-C9R; targeted review fix de los hallazgos R1–R2 del manager sobre el source fix C9; ningún gate físico, ninguna release, ninguna campaña, sin RERUN-5; veredicto `SOURCE_READY_FOR_MANAGER_REVIEW`)

Cerró los dos hallazgos de revisión del source C9 sin rediseñar el fix: el validador del fan-in del final reretester conservaba `DecisionRef` y los demás carriers pero no comparaba la nueva autoridad `ApplyEvaluationRef`, y no existían pruebas del boundary ante mutaciones de esa autoridad. Baseline inmutable verificada: worktree limpio `~/aranea/work/f04-cert-f04-01-c9/symphony-c9` @ `87a3ab944c5ce706ac3fe7766fa86af32189cc05` en `codex/f05-seal-apply-authority-fix` (HEAD local == origin por fetch + rev-parse, parent exacto `c1d24c1…`, dirty ajeno del checkout principal preservado sin tocarlo).

- **R1 — Invariancia fail-closed del fan-in:** `validateFinalReretesterFanoutOutput` (`sqx/workflows/generic_workflow.go`) agrega la comparación explícita `artifact.ApplyEvaluationRef != input.ApplyEvaluationRef` → error contractual `contract_conflict` (`capabilities.ErrContractConflict`) y ningún resultado parcial aceptado. Referencia Apply válida se conserva byte-exact; input histórico sin Apply sigue fluyendo vacío donde eso es válido; la salida no puede inventar una referencia cuando el input no la tenía, ni borrarla ni sustituirla; `EvaluationRef` continúa reemplazándose por la evaluación del final reretester; el resto de validaciones, cardinalidades, orden y semántica zero-output (G22) intactas; sin fallback, lookup ni recuperación silenciosa.
- **R2 — Boundary RED/GREEN + compuesto:** `TestFinalReretesterFanoutFailsClosedOnApplyAuthorityMutations` con los 3 negativos (drop de la referencia cuando el input la tenía / replace por otro SHA256 sintácticamente válido / fabricar cuando el input estaba vacío): cada caso falla antes de aceptar el fan-in y el error del workflow impide etapas posteriores; RED demostrado contra `87a3ab9` (los 3 FALLAN) y GREEN tras R1 sin otros cambios de producción. `TestSealCompositeApplyFanoutAssemblerHandsExactPerMemberAuthority` — test compuesto integrado en memoria que encadena los runners reales `runDurableApplySelectedRun` → `runFinalReretesterFanout` → `runForgeSealHandoff` con actividades externas simuladas y compile lineage válido por fixture, interceptando el request real a `forge_seal_handoff_v1`: por StrategyRef verifica `ApplyEvaluationRef` == la producida por Apply, `EvaluationRef` final distinto, `CompileEvaluationRef` correcta y ausencia de cruces entre miembros (no simula el fan-in ni fabrica el carrier final). `TestFinalReretesterFanoutRejectsMutatedOutputBoundaries` completa el boundary del validador modificado con 4 tests reales (key inconsistente, StrategyRef ajeno, EvaluationRef no SHA256, carriers alterados).
- **R3 — Fallos preexistentes clasificados:** los 37 reportados por el handoff C9 (21 `sqx/workflows` + 16 `sqx/activities/worker`) se obtuvieron en dos worktrees limpios (`c1d24c1` y C9R final `66faa42`) con `-count=1` y condiciones equivalentes (replace relativo `../../sdk` del módulo `sqx` replicado): conjuntos idénticos por identidad de test (diff de nombres = 0) y por causa normalizada (diff de mensajes = 0) en ambos paquetes. Clasificación: 16 workflows = harness sin activity `flow_run_start` registrada (`ActivityNotRegisteredError`, incluye los 2 DurableApply raíz); 5 workflows = clases contractuales internas de la simulación MT5 backtest/compiler (`durable backtest: carrier ausente para output exact key`, etc.); 16 worker = fixture físico `../../../mt5-export.htm` ausente. Cero fallos nuevos o agravados por el delta C9/C9R; ningún test contractual Apply/final reretester/promotion/seal fallando por el delta; suite completa NO verde y declarada como tal.
- **Gates:** cobertura función-nivel del código modificado — `validateFinalReretesterFanoutOutput` **80% → 100%** (las 4 ramas descubiertas eran brechas preexistentes de la misma función, cubiertas con tests reales del complemento R2, sin inflar números), `runForgeSealHandoff` 100%, `validateApplyCarrier` 100%, `runDurableApplySelectedRun` 95.5% (residual defensivo igual a C9); `sqx/core/runtime` PASS completo; `gofmt -l` limpio en los 3 archivos tocados; `go vet ./sqx/workflows/` limpio; builds con los 2 fallos preexistentes demostrados en `87a3ab9` pristine vía stash (`go.work` incluye `./sqx` → `pebbe/zmq4` sin headers libzmq del entorno; `sqx/tools` main redeclarado C6R) — ajenos al delta; anti-test-masking: guard canónico del repo PASS sobre el delta C9→C9R (cero `t.Skip`/`.only`), diff de tests 100% aditivo (199+/0−), sin tests preexistentes modificados.
- **Commit y publicación:** commit único `66faa42debba18cd0dd09de127ae1374ea2a07f0` (`fix(forge): fail closed when final reretester mutates Apply lineage`) sobre `87a3ab9…` sin rebase ni force-push, push a origin con read-back (`ls-remote == HEAD`); diff contra C9 = exactamente los 3 archivos del allow-list (`generic_workflow.go` +6, `forge_seal_apply_authority_test.go` +115, `durable_final_reretester_fanout_workflow_test.go` +78); worktrees temporales R3 eliminados sin residuo; sin merge, sin release, sin rollout, sin RERUN-5, sin MT5 físico, sin reutilizar allocations.
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio de gate; sigue requerida revisión del manager, release nueva y RERUN-5 — NO declarado PASS). **Fix C9R = `SOURCE_READY_FOR_MANAGER_REVIEW`** en origin. `MIGRATION: NONE`. Allocations `26090011001–009` write-once permanecen; artifacts de RERUN-4 preservados. **Próximo gate único:** revisión source del manager sobre el delta `87a3ab9…66faa42` (rango completo del fix: `c1d24c1…66faa42`) →

Evidencia de la misión: agent-run `2026-09-19-zcode-glm-5.3-flash-f05c-cert-f04-01-c9r-targeted-review-fix`, change_log `2026-09-19-f05c-cert-f04-01-c9r-targeted-review-fix`, workdir `~/aranea/work/f04-cert-f04-01-c9/`.

### Delta de release y rollout CERT-F04-01 fix C9+C9R — 2026-09-19 (misión NORMAL F05C-CERT-F04-01-C10; Approved Source → Release → Certified Fleet Rollout; ningún gate físico, ninguna campaña, sin RERUN-5; veredicto `RELEASE/ROLLOUT PASS`)

Publicó la release autorizada siguiente a `0.2.101` desde el source aprobado C9+C9R (`66faa42debba18cd0dd09de127ae1374ea2a07f0`) y la desplegó/certificó en la flota completa, sin ejecutar RERUN-5, sin campañas, sin backtests y sin tocar código de negocio. Dirty operacional del checkout principal (`deploy/manifest.json` + `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json`) preservado sin stage y sin overlap con el delta.

- **G0 — Integración exacta:** baseline verificada sin drift (`origin/codex/f05-release-prep == c1d24c1…`, `origin/codex/f05-seal-apply-authority-fix == 66faa42…` por fetch+rev-parse); cadena aprobada demostrada por parents (`66faa42^ == 87a3ab9…`, `87a3ab9^ == c1d24c1…`, ancestro FF confirmado, rango de exactamente 2 commits y 7 archivos con 845+/9−); fast-forward puro `c1d24c1..66faa42` en el checkout principal y push a origin aceptado sin force (el propio push valida FF); read-back `ls-remote` == `66faa42…` en ambas ramas.
- **G1 — Verification gate (sobre `66faa42` con `-count=1`, worktree limpio `symphony-c9`):** `sqx/core/runtime` PASS; `sqx/activities/worker` PASS completo — los 16 fallos históricos de la clase fixture eran causados por `mt5-export.htm` ausente en la raíz del repo, y con el fixture auténtico restaurado byte-exacto desde el commit de autoridad `b5c71d5` (SHA256 `090ca4d1…` == CORPUS, 133664 B, untracked/gitignoreado, sin cambio al repo) el paquete queda verde; `sqx/workflows` conserva 21 fallos con conjuntos idénticos al baseline `c1d24c1` reproducido en worktree temporal hermano (eliminado sin residuo) — diff de nombres de test = 0 y diff de causas normalizadas = 0 (normalización: orden no determinista del sufijo `Supported types` por iteración de mapa, UUIDs y punteros); clasificación: 16 tests de harness sin activity `flow_run_start` y 5 tests de clases contractuales internas de la simulación MT5 (`durable success requires Primary`, `carrier ausente para output exact key`, `exactamente un compile log requerido`); suite completa NO verde y declarada como tal. Tests C9/C9R clave PASS por nombre: pin de `ApplyEvaluationRef` desde resultado válido de Durable Apply, conservación byte-exact de la autoridad en el fan-out/fan-in del final reretester con orden permutado, rechazo fail-closed de mutaciones (drop/replace/fabricar) y de outputs mutados, test compuesto real Apply → Reretester → Seal interceptando el request auténtico a `forge_seal_handoff_v1` sin cruces entre miembros, consumer fail-closed (compile como Apply, ref vacía, inválida, otra estrategia, otro FlowRun), G22 zero-supply con cero members, Promotion V2 estructural con warnings, round-trip JSON byte-exact del campo aditivo. Release test gate obligatorio `./adapters/mt5/report/` con `SQX_MT5_REQUIRE_B6182_FIXTURE=1` y fixture auténtico: **53 PASS / 0 FAIL / 0 SKIP / coverage 99.1%**, `TestBuild6182_SevenCrosschecks` y `TestBuild6182_AuthenticFixtureIntegrity` PASS. `gofmt -l` limpio en los 7 archivos del delta; `go vet` limpio en `./workflows/ ./core/runtime/ ./activities/worker/`; `go build` de los paquetes del delta OK con los 2 fallos preexistentes documentados (raíz `pebbe/zmq4` sin libzmq vía `go.work`; `sqx/tools` main redeclarado, C6R). Anti-test-masking: semántica canónica del guard aplicada al rango `c1d24c1..66faa42` PASS — 4 archivos de test 100% aditivos (805+/0−), cero `t.Skip`/`.only`, cero tests preexistentes modificados. Cero regresiones nuevas.
- **G2 — Release `0.2.102`:** preflight `release-authority`: schema `sqx-release-authority.v1`, `published_version=0.2.101`, `remote_line_max=0.2.101`, `authority_state=CONSISTENT`, candidato resuelto por la autoridad `0.2.102`. Construcción/publicación por el mecanismo canónico (`./deploy_release.sh --release-only 0.2.102`, deployer-watcher ENV=production en screen, desde el checkout `66faa42…` que efectivamente se publicó; `vcs.modified=true` por dirty operacional no-Go, misma clase documentada de 0.2.98–0.2.101): 6 artifacts — linux-amd64 `symphony` 40321184 B / sha256 `898630fa9800277da3ca2f2d79f05b811cf8c8df35a990d8a04af4693d09028e`, `sqx-watcher` 32682144 B / `dbb2f2cbab01c104be7839b22d93d840c0f1de9e9bf5f536a696bad4d2ee6b91`, `start-symphony-worker.sh` 310 B / `9f956384…` (== 0.2.101, plantilla sin cambios), `run-symphony` 233 B / `8f240eb2…` (ídem), `promtail-worker.yaml` 355 B / `eaa4e1b2…` (ídem); windows-amd64 `sqx-mt5-worker.exe` 37249024 B / `558d15ccbb2923d0ee7a8ad51372ee9b8c17f2ec8193169b4ed956965ced0429`. `go version -m` de los 3 binarios: `vcs.revision=66faa42…`, `vcs.time=2026-09-19T19:49:29Z` (fecha del commit C9R), `vcs.modified=true`. SHA256 locales recomputados == `deploy/manifest.json` (`0.2.102`); MinIO confirmó el manifest; read-back `release-authority --target 0.2.102`: `published_version=0.2.102`, `target_state=EXACT_MATCH`, `authority_state=CONSISTENT`, `remote_line_max=0.2.102`. Releases históricas intactas (deploy local `0.2.100`/`0.2.101` con hashes de C7 verificados: linux `d795a998…`, windows `f5a8baa1…`); sin tags Git; `--release-only` dejó `input/example/config.json` e `input/` intactos y ningún FlowRun disparado.
- **G3 — Preflight físico (Access Plane exclusivamente; sin credenciales del repo):** Temporal namespace `sqx-prop` consultado por visibilidad gRPC directa (scratch Go `/tmp/thist`, canal autoritativo documentado desde RERUN-4; el perfil RO del MCP no ve `sqx-prop` — limitación conocida): **Running = 0** antes y durante el rollout. Estado previo de flota: Zeus PID 2769428 / Hera PID 1365120 / Kronos PID 1328601 sobre `0.2.101` con SHA instalado `d795a998…` == artifact (los mismos PIDs de C7), Windows PID 19436 @ `0.2.101` `f5a8baa1…` (evidencia C9/RERUN-4), `MT5_PROCS=0` ya capturado por C9 18:43:13Z. Nota de observación sin impacto en gates: la sesión MCP Mongo RO terminó caída (`session not found` persistente); la corroboración de ausencia de nuevas allocations queda anclada en Temporal (cero FlowRuns).
- **G4 — Rollout (Stager canónico, convergencia automática post-manifest):** Zeus CURRENT `0.2.102`, worker PID 2801657 (20:36:33Z) ejecutando `/opt/stager/releases/0.2.102/bin/symphony` con SHA instalado == artifact `898630fa…`, log `Started Worker Namespace sqx-prop TaskQueue sqx-main-queue WorkerID 2801657@sqx-ulab-zeus-0` + ESTABLISHED :7233, watcher `0.2.102` SHA `dbb2f2cb…` == artifact, cero procesos `0.2.101`; Hera CURRENT `0.2.102`, PID 1390690 (20:36:16Z, `1390690@sqx-ulab-hera-0`, SHA == artifact, ESTABLISHED, sin residuos); Kronos CURRENT `0.2.102`, PID 1353062 (20:34:31Z, `1353062@sqx-ulab-kron-0`, SHA == artifact, ESTABLISHED, sin residuos). El legado `symphony-f03-382f4ba` de Hera sigue presente, no tocado, no blocker. Windows `worker-kronos` (Stager + `AraneaEvidencePublish (SYSTEM) → evidence JSON → mt5-kronos-operator read-only`): evidencia fresca en dos ciclos de publicación (20:38:06Z y 20:43:06Z, `partial=false`, `errors=[]`, publisher `nt authority\system`, freshness <15 min, `inspector_sha256` `28d782b7…` == canónico): worker `sqx-mt5-worker` PID 6648 (antes 19436) desde 20:34:03Z, path `C:\ProgramData\Stager\releases\0.2.102\bin\sqx-mt5-worker.exe`, `exe_sha256` == artifact EXACT (`558d15cc…`), size 37249024 == manifest, padre `stager-runtime` PID 32496 (servicio Running/Automatic, `.\kor`), `CURRENT=0.2.102`/`PENDING=0.2.102`/`PENDING.next MISSING`, poller ESTABLISHED (state 5) PID 6648 → Temporal `192.168.31.46:7233`, singleton verificado por conteo físico (`COUNT=1`).
- **G5 — Certificación final del rollout:** comprobación independiente de la cadena completa `SHA source 66faa42 → release 0.2.102 → manifest (6 hashes) → artefactos → ejecutables efectivos` consistente en los 4 hosts; `MT5_PROCS=0` fresco (20:43Z) con singleton físico preservado; cero workflows Running en `sqx-prop` y cero workflows iniciados desde 20:25Z (ninguna ejecución F-04 esperada o inesperada, nada interrumpido por el drenaje); sin campañas, sin backtests, sin smoke físico y sin reutilizar allocations `26090011001–009` (write-once, intactas por ausencia total de FlowRuns nuevos).
- **Estado:** `CERT-F04-01 = BLOCKED / READY TO RERUN-5` (sin cambio de gate; el rollout NO constituye certificación física de la cadena). Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–009` write-once permanecen; artifacts de RERUN-4 preservados. **Próximo gate único: `CERT-F04-01 RERUN-5`** desde cero sobre `0.2.102`/`66faa42…` con identidades todas nuevas (NO reutilizar campañas/FlowRuns/workflow IDs previos).

Evidencia de la misión: agent-run `2026-09-19-zcode-glm-5.3-flash-f05c-cert-f04-01-c10-release-rollout`, change_log `2026-09-19-f05c-cert-f04-01-c10-release-rollout`, workdir `~/aranea/work/f04-cert-f04-01-c10/`.

### Delta de ejecución CERT-F04-01 RERUN-5 — 2026-09-19/20 (misión NORMAL F05C-CERT-F04-01-RERUN-5; re-run físico desde cero sobre 0.2.102; veredicto `BLOCKED`)

Ejecutó el gate físico CERT-F04-01 desde cero sobre release `0.2.102` / `66faa42debba18cd0dd09de127ae1374ea2a07f0` con UNA campaña real de identidades todas nuevas, observada hasta terminalización. La cadena física superó por primera vez el gate de autoridad Apply (el defecto #4 de RERUN-4 quedó corregido y validado en producción) y quedó **en verde hasta promotion inclusive** con una cohorte honesta de 3 miembros, pero terminó **`BLOCKED`** por un **quinto defecto determinístico (producto)**: `forge_seal_handoff_v1` falló non-retryable `CONTRACT_CONFLICT: member 3da55fda-9ba3-488f-8843-a6c82a20a56d sqx readback: sqx xml parse: XML syntax error on line 3: illegal character code U+0003` (Temporal evento 492, 2026-09-20T00:49:08Z, worker Hera `1390690@sqx-ulab-hera-0`) → FlowRun/Campaign `FAILED/WAVE_FLOW_RUN_FAILED` 00:49:08.780Z antes de StrategyVersion seal y HandoffManifest. **CERT-F04-01 = `BLOCKED`**.

- **Identidades del run (todas nuevas):** request_id `cert-f04-01-rerun5-20260919T211749Z-f69f9f3b` (preimage SHA256 `c62e0d925209d3772f80e26db7a1cc2085d9eb2d88fbd510ff96b29d942b2c6e`, 5611 B, VALIDATION_OK contra structs @ `66faa42` con negativos 3/3 RECHAZADOS; diff vs receta RERUN-4 limitado a `/strategy`,`/wave`,`/request_id`,`/wave_config/wave_key`; CFX byte-exactos reutilizados con hashes verificados in-situ); ForgeCampaignRef `c42dd637-3f5a-4702-b9ff-361acededba0`; CampaignIntentToken `3eaff42f-5263-4827-923c-6d8af7f36641`; WorkflowID/RunID campaña `sqx-forge-campaign-v1-3eaff42f-…` / `01a0bb8d-28cf-7b7f-b4e6-69451bd255c2`; WaveRef `forge-c42dd637-…-w000001` (1 de 1); FlowRunRef `fbac1286-23a9-4ecb-b483-d31250dc612a` (`sqx-main-v1-c06b09a9-…` / `01a0bb8d-29d4-…`); storage prefix `wave_cert-f04-01-rerun5/xauusd/l_h1/cert-f04-01-rerun5/v1/`.
- **Dispatch único (G2):** watcher release `0.2.102` (`dbb2f2cb…` == manifest) en Zeus, ENV=production, CWD `/tmp/cert-f04-01-rerun5`, input PRE-stageado (5 archivos con SHA verificados in-situ antes del arranque; JSON `c62e0d92…` == preimage), pipeline 6/6 pasos 21:23:05Z en el primer poll; sin intento previo, sin no-op, sin re-dispatch, sin segundo consumidor.
- **G0 PASS (en vivo):** release-authority `0.2.102 CONSISTENT/EXACT_MATCH` 6/6 objetos; flota 4/4 sobre `releases/0.2.102` con SHA instalado == artifact (Zeus 2801657, Hera 1390690, Kronos 1353062, Windows 6648 `558d15cc…`); evidence Windows fresca `partial=false errors=[]` singleton con padre `stager-runtime`; `MT5_PROCS=0`; `sqx-prop` Running=0 vía gRPC autorizada (`/tmp/thist`).
- **Cadena física lograda (G3–G5):** funnel completo sin FAILED con cohorte de 3 (3da55fda, 847a417b, 8b403f34; el fan-out es honesto: RERUN-3=1, RERUN-4=4, RERUN-5=3) → **allocations durables Magic V1 `26090011010` (3da55fda, `sha256:6a93ce1e…`), `26090011011` (847a417b, `sha256:fe1584f1…`), `26090011012` (8b403f34, `sha256:fa14a899…`)** asignadas por Apply a las 21:36:06Z (el seal sólo replay-ea; `26090011001–009` write-once intactas) → `mt5_compile_persist_v1` ×3 COMPLETED (evals `e775be2f…`, `6d2f0217…`, `d7999185…`; EX5 `977cbc74…`/173872, `bd135290…`/172460, `c7898798…`/173116 con identity encadenada al backtest) → **3 backtests físicos reales secuenciales COMPLETED** (22:44:51Z ~67 min, 23:54:48Z ~70 min, 00:49:05Z ~54 min; `LIVE_PHYSICAL_MT5_EXECUTIONS<=1` demostrado en 4 capturas vivas de metatester64=1 y MT5_PROCS=0 final) → **G4: 3/3 HTM auténticos byte-verificados** (HeadObject + GET presignado + SHA256 local == durable: 3da55fda 6336204 B `19255be8…`, 8b403f34 13966272 B `46a34b31…`, 847a417b 18424420 B `4ba40a75…`; parser productivo `mt5-report.v1` @ `66faa42` build 6182 con 7/7 crosschecks PASS por miembro) → **`mt5_reconcile_v1` ×3 COMPLETED `structural_identity: MATCH`** con evaluation/trade_set/metric_set refs durables → scoring `mt5_score_shadow_v1` ×3 + `rank_snapshot_v1` COMPLETED → **promote_finalists COMPLETED** (DecisionRef `sha256:de5e7accd68e2f2c7f3abb60444c878720ebb941780ca590c11c2bee796d2a9d`; finalists_v2: rank1 847a417b, rank2 8b403f34, rank3 3da55fda; warnings auténticos `PNL_SIGN_FLIP`; score_refs durables).
- **Validación en producción del fix C9/C9R (G6, mitad PASS):** el request real a `forge_seal_handoff_v1` (capturado del evento 490, 1526 B) llegó con EXACTAMENTE 3 miembros y `apply_evaluation_ref != compile_evaluation_ref` por miembro (`faa431ee…` vs `e775be2f…`; `127568c9…` vs `6d2f0217…`; apply del 8b403f34 vs `d7999185…`), FlowRunRef/DecisionRef/namespace `forge-live` correctos y sin cruces; el consumer validó stage `apply_selected_run`, contratos y subject/flow de las Evaluations Apply y Compile antes del fallo — el defecto #4 de RERUN-4 NO se reprodujo.
- **Root cause del fallo (defecto #5, anclada en source @ `66faa42` y caracterizada sobre bytes reales):** `magicreadback.FromSQX` (`sqx/adapters/magic-readback/readback.go:51`) decodifica los bytes del artefacto SQX como documento XML plano (con `decoder.Strict=false`), pero el `strategy.sqx` que el producer Apply (exporter Java de SQX) publica en Durable Apply es un **contenedor ZIP** (`PK\x03\x04`; miembros `META-INF/MANIFEST.MF`, `settings.xml`, `strategy_Portfolio.xml`, resultados binarios): el decoder XML encuentra el byte de control `0x03` del header ZIP y falla con `illegal character code U+0003` → `forgeSealConflict` non-retryable. Evidencia física: `strategy.sqx` del miembro 1 descargado y byte-verificado (`durable/apply-selected-run/v1/fbac1286-…/3da55fda-…/11dc4eba-…/strategy.sqx`, 4506976 B, SHA256 `9ac377b9a2b459b28ecefcf2c8166d8d200b2c47d4a4b2351cb8344d5635120d` == durable del resultado Apply) y repro exacto del mismo mensaje de error invocando el módulo real `magicreadback` compilado @ `66faa42` sobre esos bytes; el magic sellado `26090011010` SÍ está dentro del contenedor (`strategy_Portfolio.xml` → `<variable><name>MagicNumber</name><type>int</type><value>26090011010</value></variable>`, la forma que `findMagicVariable` ya sabe leer), de modo que la semántica del read-back es correcta y el gap es el contenedor. Determinístico para todo run F-04 que llegue al seal con el producer vigente; primera vez ejercitado físicamente (RERUN-4 murió antes, en la validación de autoridad Apply). NO es defecto de input, entorno ni flake.
- **Terminalización, drain e inventario de efectos parciales:** campaign/FlowRun `FAILED` terminal por sí mismos (`flow_run_seal` COMPLETED); 0 workflows Running en `sqx-prop` post-run; `MT5_PROCS=0` capturado vivo 01:07Z con worker Windows singleton 6648; flota re-verificada sobre `0.2.102` post-run (Zeus 2801657 / Hera 1390690, SHA `898630fa…`). Read-back durable ×3 vía flowkit `strategy get`: `strategy_versions: []` y `handoffs: []` → **el seal falló CLOSED antes de escribir cualquier versión, manifest o delivery**; sin POST a Echo (0 keys `echo` en ETCD worker/watcher: el estado terminal contractual habría sido `HANDOFF_CREATED` ×N con 0 POST, conforme F-04 §11). Efectos durables del run: allocations `26090011010–012` (de Apply, write-once, legítimas) y los artifacts del funnel en `sqx-strategies`.
- **Estado:** `CERT-F04-01 = BLOCKED` (quinto defecto determinístico). Fixes C1+C3+C4+6182+C9/C9R validados físicamente en producción por este run. Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–009` write-once permanecen; nuevas `26090011010–012` write-once permanecen (reserva sin uso válida); artifacts de RERUN-5 preservados en `sqx-strategies` y copia local auténtica en `~/aranea/work/f04-cert-f04-01-rerun5/artifacts/`. **Próximo gate único:** decisión manager sobre el fix del read-back SQX (`FromSQX` debe abrir el contenedor ZIP y parsear el XML interno — el magic está en `strategy_Portfolio.xml` — o el exporter debe emitir XML plano; con tests sobre bytes reales) → release nueva → RERUN-6 desde cero con identidades todas nuevas (NO reutilizar campañas/FlowRuns previos; allocations write-once permanecen).

Evidencia de la misión: agent-run `2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-01-rerun5-blocked`, change_log `2026-09-20-f05c-cert-f04-01-rerun5-blocked`, workdir `~/aranea/work/f04-cert-f04-01-rerun5/` (g0–g5 records, evidence-summary, monitor.log y artifacts con SHA256 verificados).

### Delta de corrección source C11 — 2026-09-20 (misión NORMAL F05C-CERT-F04-01-C11; fix mínimo del read-back Magic en .sqx reales contenedor ZIP; ningún gate físico, ninguna release, ninguna campaña, sin RERUN-6; veredicto `SOURCE_FIX_READY_FOR_MANAGER_REVIEW`)

Implementó la corrección mínima, probada y verificable del quinto defecto demostrado por RERUN-5: `FromSQX` (`sqx/adapters/magic-readback/readback.go`) entregaba los bytes del `.sqx` directamente a `xml.Decoder`, pero el `strategy.sqx` que el producer Apply emite es un contenedor ZIP cuyo Magic vive en el XML interno `strategy_Portfolio.xml` → `forge_seal_handoff_v1` fallaba `CONTRACT_CONFLICT sqx xml parse: illegal character code U+0003` en el read-back. Sin ejecutar RERUN-6, sin MT5/SQX físicos, sin CampaignRef/FlowRunRef nuevos, sin reutilizar allocations históricas, sin escribir StrategyVersions/manifests reales, sin POST a Echo, sin release/rollout/stager, sin IAM y sin reescribir evidencias históricas. Baseline verificada antes de modificar: `codex/f05-release-prep` local == `origin` == `66faa42debba18cd0dd09de127ae1374ea2a07f0` (release física vigente `0.2.102`, intacta); trabajo en worktree aislado `~/aranea/work/f04-cert-f04-01-c11/symphony-c11` (branch nuevo `codex/f05-sqx-zip-magic-readback-fix` desde el SHA exacto; el dirty operacional del checkout principal quedó intacto).

- **G0 — artefacto auténtico (copia local existente, sin nuevos fetches):** `~/aranea/work/f04-cert-f04-01-rerun5/artifacts/strategy-3da55fda.sqx` (procedencia RERUN-5, byte-verificado contra el durable de Apply en esa misión); copia de trabajo restringida (600) fuera del repo y del vault en `~/aranea/work/f04-cert-f04-01-c11/fixture/`; tamaño 4506976 exacto y SHA256 `9ac377b9a2b459b28ecefcf2c8166d8d200b2c47d4a4b2351cb8344d5635120d` == registro durable; ZIP estructuralmente válido (`testzip` sin errores, header `PK\x03\x04`); entradas enumeradas — `META-INF/MANIFEST.MF` (25 B, crc `ee027fb2`), `settings.xml` (3897116 B, crc `69af8194`), **`strategy_Portfolio.xml` (32618 B, crc `9ec67337`, ruta y capitalización exactas)**, `lastSettings.xml`, `optimizationProfile.bin`, `orders.bin`, `version.txt` y `Results/*/dailyEquity.bin`; el XML interno contiene `<variable><name>MagicNumber</name><type>int</type><value>26090011010</value></variable>` en `<Variables>` (SHA256 interno `7e1f33fd6164295906efcb16776e4e268c3d2b49146366fe7b782da6af949522`).
- **G1 — corrección mínima (`readback.go` sólo):** `FromSQX` detecta la firma del local header ZIP `PK\x03\x04` → ruta nueva `magicFromSQXZip` (`archive/zip` stdlib, apertura 100% en memoria, sin extracción); resuelve por nombre exacto y case-sensitive la entrada canónica `strategy_Portfolio.xml` determinada en G0 — cero coincidencias → `ErrSQXZipEntryMissing`, duplicadas → `ErrSQXZipEntryAmbiguous`, sin búsqueda alternativa ni por coincidencia parcial; lectura acotada por `MaxSQXZipEntryBytes` (16 MiB documentado vs 32618 B observados; `LimitReader` a límite+1 → `ErrSQXZipEntryLimit` controlado) con errores de apertura, truncamiento y CRC surfaced como `ErrSQXZipContainer`; los bytes XML internos van al parser existente extraído como `magicFromXML` (compartido con la ruta XML plano histórico, que se conserva como ruta explícita y nunca como fallback de un ZIP corrupto); `FromMQ5`, `parseMagicValue`, `findMagicValue`… intactos (sin `parseMagicValue` changes), `VerifyMagicReadback` y las reglas de asignación sin cambios; el magic se devuelve tal como fue observado (decimal string, sin coerción int32 — `26090011010` se conserva exacto); errores tipados nuevos: `ErrSQXZipContainer`, `ErrSQXZipEntryMissing`, `ErrSQXZipEntryAmbiguous`, `ErrSQXZipEntryLimit`. `forge_seal_handoff.go`, exporter, allocator, Apply workflow, Final Reretester, promotion, SDK y contratos Echo sin tocar.
- **G2 — RED contra el baseline `66faa42` (logs y exit codes en `~/aranea/work/f04-cert-f04-01-c11/logs/`, diff RED preservado):** T1 auténtico (`TestFromSQX_AuthenticRERUN5SQX`, fixture vía `SQX_AUTHENTIC_FIXTURE` con verificación dura de size+SHA256 — ausente o incorrecto es FAIL, nunca SKIP) **FALLA contra el baseline con la firma exacta del error de producción `sqx xml parse: XML syntax error on line 3: illegal character code U+0003`** (exit 1); T2 ZIP controlado en memoria con la estructura de entradas de G0 y magic conocido `2609001001` FALLA con el mismo error; T3 seal (`TestForgeSealHandoff_SealsAndDeliversStructuralMember` con el fixture del seal corregido a ZIP real) FALLA con `member 11111111-… sqx readback: sqx xml parse: … U+0003 (CONTRACT_CONFLICT, non-retryable)` — el mismo punto de muerte de RERUN-5; los dos negativos nuevos del seal (mismatch ZIP y ZIP corrupto) pasan también en el baseline (fail-closed por la razón equivocada) y quedan como guardas de regresión. No se mutó ni recomprimió el artefacto auténtico para fabricar tests.
- **G3 — matriz negativa (sintética, `TestFromSQX_ZIPNegativeMatrix` + `TestFromSQX_LegacyXMLVariants`):** ZIP truncado y corrupto (mitad, byte central, byte del central directory) → `ErrSQXZipContainer` sin fallback XML; local header de la entrada canónica corrupto con central directory válido → `ErrSQXZipContainer` (rama `entry.Open`); entrada canónica ausente → `ErrSQXZipEntryMissing` aunque otro XML contenga el Magic (sin búsqueda alternativa); entrada duplicada → `ErrSQXZipEntryAmbiguous`; XML interno malformado → `sqx xml parse` (no `ErrMagicNodeMissing`); Magic ausente o variable vacía → `ErrMagicNodeMissing`; Magic `not-a-number`/`0`/`-5`/`99999999999999999999` → rechazo del dominio; entrada descomprimida sobre el límite → `ErrSQXZipEntryLimit` con límite inclusivo verificado; variantes XML históricas (`<Name>`, `<Value>`, variable sin valor) preservadas en ambas rutas. En el consumer: `TestForgeSealHandoff_ZIPMagicMismatchFailsClosedWithoutAnyEffect` (ZIP con magic distinto → `magic_mismatch` CONTRACT_CONFLICT non-retryable) y `TestForgeSealHandoff_CorruptSQXZipFailsClosedWithoutAnyEffect` → cero StrategyVersions, cero manifests, cero deliveries, cero POST en ambos; el test histórico de mismatch se fortaleció con assertions de manifests/deliveries (sin debilitar nada).
- **G4 — camino auténtico:** con el fix, `FromSQX(bytes auténticos)` → `strategy_Portfolio.xml` exacto → `26090011010` con igualdad byte-exacta de la representación decimal; determinismo demostrado en 2 replays in-test y 2 procesos independientes (exit 0 ×2); no-mutación del ZIP verificado a nivel archivo (size y SHA256 idénticos antes/después). Sólo el `.sqx` del miembro 1 (`26090011010`) está disponible legítimamente en local; los de 847a417b (`…011`) y 8b403f34 (`…012`) NO fueron ejercitados ni declarados (los sintéticos cubren la estructura común, no los sustituyen).
- **G5 — gates de calidad:** `gofmt` limpio en los 3 archivos tocados (el drift de 15 archivos en `worker` demostrado idéntico al baseline); `git diff --check` limpio; `go vet` OK en `magic-readback` y `worker`; `go test -count=1`: `magic-readback` 100% PASS con **coverage 100%**; `TestForgeSealHandoff` 19/19 PASS; suites de `./sqx/activities/worker/... ./sqx/core/domain/... ./sqx/core/capabilities/... ./sqx/core/forge/...` con **conjunto de fallo byte-idéntico al baseline `66faa42` en condiciones idénticas** (16 fallos preexistentes de MT5Reconcile/MT5ScoreShadow, diff vacío de listas `--- FAIL`); anti-test-masking: cero skips nuevos (las únicas menciones `Skip` son el campo de negocio `result.Skipped`), cero assertions debilitadas, cero tests eliminados (0 `func Test` borrados); contaminación del fixture de benchmark preexistente (`phase4_performance.json`, reescrito por corridas de tests históricos) restaurada antes del commit — el commit contiene sólo los 3 archivos autorizados.
- **Estado:** `CERT-F04-01 = BLOCKED / READY TO RERUN-6 (pendiente review manager del fix)`; fix C11 `SOURCE_FIX_READY_FOR_MANAGER_REVIEW`; CERT-F04-02 sin cambio y el join con Echo sin cerrar. `MIGRATION: NONE`. Allocations `26090011001–012` write-once permanecen intactas. **Próximo gate único:** revisión independiente del manager del source C11 (diff `66faa42…2993da0`) → release/rollout C12 desde `codex/f05-release-prep` fast-forward → RERUN-6 desde cero con identidades todas nuevas (NO reutilizar `c42dd637`/`fbac1286` ni ningún workflow ID previo).

Evidencia de la misión: agent-run `2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-01-c11-sqx-zip-magic-readback-fix`, change_log `2026-09-20-f05c-cert-f04-01-c11-sqx-zip-magic-readback-fix`, workdir `~/aranea/work/f04-cert-f04-01-c11/` (fixture restringido 600, logs RED/GREEN con exit codes, RED diff, worktrees fix y baseline).

### Delta de certificación de compatibilidad build 6182 — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C5; camino owner (a): certificar 6182 contra `mt5-report.v1`; veredicto `INSUFFICIENT_EVIDENCE`)

Ejecutó el mandato de certificación de compatibilidad del build 6182 contra el contrato existente `mt5-report.v1` (procedimiento mínimo de SPEC-PARSER §3.3) sin campañas nuevas, sin backtests, sin cambiar el terminal instalado y sin tocar la allow-list vigente `{6090, 6140, 6180}`. Baseline verificada: symphony checkout `/home/kor/go/src/github.com/xKoRx/symphony` en `codex/f05-release-prep` HEAD `a440ac4ed730747b1ae6b07557cb6204102f16ac` (== origin) con dirty operacional `deploy/manifest.json` + `phase4_performance.json` preservado. La provenance física completa del HTM de RERUN-3 fue recuperada del resultado durable del child workflow (lectura read-only autorizada del historial Temporal `sqx-prop` vía el scratch `/tmp/thist/`, canal equivalente al CLI pre-autorizado por R1): bucket `sqx-strategies`; object key completa `durable/mt5-export/v1/b6a1edea-ba20-4a23-8cde-0201ee22e8f0/b94f1400-e495-4636-8587-516db1cb8cb7/sha256:f8968dbaacfd398b97f42ab9eed9b8a6c919ed6c23ad72e936b272f5d2a70e52/09_mt5_backtest/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`; size 13074872; **SHA256 completo `21917e144668cfd014e0d0c6a60d4f70eb3c4a17783bacac02f729eb587e8b5c`**; input encadenado EX5 `…/08_mt5_ex5/final-b94f1400-e495-4636-8587-516db1cb8cb7.ex5` 176426 B `sha256:6d667d703d252e6f3904d9fce077fb5e46711144c23b15c1f6589a041c14f720` (== output del compile físico); evidence durable tester.ini 530 B `sha256:151f23c8…`, terminal.log 10618 B `sha256:156e5150…`, tester.log 52997660 B `sha256:cba437c3…`, agent.log 62255130 B `sha256:1158fe35…`; MT5 child WorkflowID `mt5-backtest-forge-campaign-1fc62ab5-c859-4b46-ac7f-4e0f0375e74f-wave-000001-fe48d1183e928349` RunID `01a0b1bc-4165-7a9f-bab9-847cc9b88a6c`; timezone de ejecución `server_timezone: GMT` (declarada en el input de campaña e inyectada al parseo por configuración, nunca inferida del HTM); producer identity MT5 build 6182 en `worker-kronos` (192.168.31.128) bajo `sqx-mt5-worker` 0.2.100 (singleton PID 5496); fecha UTC: backtest 2026-09-17T23:38:19Z → 2026-09-18T00:32:22.504Z, fallo reconcile 2026-09-18T00:32:23.380Z (la ejecución cruza la medianoche UTC); FlowRunRef `b6a1edea-…`, StrategyRef `b94f1400-…`; sin StageExecutionRef del stage reconcile (el parse precede a toda escritura de stage, fail-closed) — el StageExecution durable de este run es el de compile `9667014a-4c84-4824-8a42-f62d851b7981`.

- **Las cuatro vías de lectura autorizadas quedaron agotadas sin bytes:** (1) ArtifactRef persistida — recuperada exacta (arriba), pero es referencia, no bytes; (2) CLI de artifacts — `sqx-flowkit` expone sólo read models (`campaign`, `run`, `strategy`, `release-matrix`, `push-output`) y ningún binario del repo (`sqx-worker`, `sqx-mt5-worker`, `sqx-watcher`) tiene capacidad de lectura de artifacts; (3) MinIO RO — 403 estructural: el perfil sólo ve los buckets `deploy` y `examples`, `sqx-strategies` no es listable (Access Plane vigente, no ampliado); (4) copia física en Windows — el job dir `C:\MT5\test\EchoForgeJobs\forge-campaign-1fc62ab5-c859-4b46-ac7f-4e0f0375e74f-wave-000001` está VACÍO (limpieza post subida durable, verificado con `dir /a /s`); inventario completo de `*.htm` bajo `C:\MT5` (16 archivos) y data folders MetaQuotes de AppData sin ninguna coincidencia de SHA256 contra el durable — los candidatos más próximos `e03_verify_6182_report.htm` (`44f10572…`) y `e03_finalverify_fac480_report.htm` (`637935a7…`) son smoke de misiones E03 previas, NO el objeto de RERUN-3. El fetch del reconcile es in-memory (`FetchDurable → []byte`, `mt5_reconcile_activity.go:152`) y el parse falló antes de persistir: sin copia residual en el worker Linux y sin documento parsed en Mongo/PG.
- **Veredicto `INSUFFICIENT_EVIDENCE`:** sin los bytes auténticos no es ejecutable la certificación (preservación, parseo, goldens, siete crosschecks, comparación con 6090/6140/6180); la compatibilidad de 6182 queda NO demostrada ni refutada. NO se agregó 6182 a la allow-list, NO se creó branch `codex/f04-cert-f04-01-build6182` (sin diff que publicar), NO hubo commit, tests ni cambios a CORPUS/SPEC-PARSER. Prohibiciones respetadas: sin fixture sintético, sin HTM de otro run, sin credenciales privilegiadas, sin ampliar Access Plane, sin backtest nuevo.
- **Blocker único y preciso: `READ_ONLY_ARTIFACT_FETCH_REQUIRED`** — operación mínima necesaria: GET de lectura del objeto exacto `s3://sqx-strategies/durable/mt5-export/v1/b6a1edea-ba20-4a23-8cde-0201ee22e8f0/b94f1400-e495-4636-8587-516db1cb8cb7/sha256:f8968dbaacfd398b97f42ab9eed9b8a6c919ed6c23ad72e936b272f5d2a70e52/09_mt5_backtest/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm` (SHA256 `21917e14…8b5c`, 13074872 B) para el agente de certificación; basta la lectura de ese objeto puntual, no acceso general al bucket. Alternativa equivalente: copia física byte-exacta aportada por owner, verificable contra el SHA256 durable antes de cualquier uso.
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio; la causa primaria sigue siendo el defecto entorno×contrato del build 6182 y ahora además la certificación de compatibilidad está bloqueada por lectura). Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen; artifacts de RERUN-3 preservados intactos. **Siguiente acción exacta:** owner/Access Plane habilita la lectura del objeto exacto (o aporta copia byte-exacta verificada por SHA256) → re-ejecutar esta certificación (preservación → parser/golden/crosschecks → decisión COMPATIBLE/INCOMPATIBLE) → si COMPATIBLE: allow-list + tests + corpus + release → RERUN-4 desde cero; si INCOMPATIBLE o el owner elige (b): fijar/downgrade del terminal a 6180 y RERUN-4 desde cero.

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c5-build6182-compat`, change_log `2026-09-18-f05c-cert-f04-01-c5-build6182-compat`.

### Delta de verificación del gate de autorización C5B — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C5B; continuación exacta del bloqueo C5A; ningún cambio IAM, ningún byte obtenido; veredicto `OWNER_APPROVAL_REQUIRED`)

Ejecutó el gate A0 del mandato de re-adquisición: identificar si existe una autorización expresa y vigente del owner del Access Plane para la operación exacta (`s3:GetObject` del HTM de RERUN-3) antes de cualquier acción privilegiada, continuando desde el bloqueo demostrado por C5A sin reiniciar C5 ni C5A, sin campañas, sin backtests, sin tocar Windows, sin releases y sin RERUN-4.

- **Estado de la concesión verificado con sondas vivas read-only (`aranea-minio-ro`, conexión preconfigurada):** (1) `s3_get_object_metadata` (HeadObject) sobre bucket+key exacta → **HTTP 403 `api error Forbidden: Forbidden`, RequestID `18D66E30A94AC147`**; (2) `s3_get_object` sobre la misma key → **HTTP 403, RequestID `18D66E3697C8719F`**; (3) `s3_list_buckets` → exactamente `deploy` y `examples` (sin bucket nuevo ni canal de entrega owner visible; LIST sobre `sqx-strategies` sigue denegado). La concesión `s3:GetObject` solicitada en C5A NO fue aplicada: RequestIDs nuevos sobre la misma identidad y el mismo recurso, sin contradicción material con el bloqueo C5A.
- **Búsqueda de autorización expresa posterior a C5A: sin resultado.** El último delta de la serie es C5A; ningún registro posterior (deltas del backlog, agent-runs, change_logs del 2026-09-18) contiene aprobación owner, statement aplicada ni copia entregada. El contrato del Access Plane (`AGENT-PLATFORM - MCP Access Plane`) confirma que el cambio IAM de MinIO es owner action (`mcp` sin autoridad admin — 403 en Admin API v3; prohibido root y credenciales dev de etcd), de modo que el agente no posee capability administrativa legítima para aplicar el cambio y no la buscó (sin ETCD, sin procesos, sin credenciales ajenas). La alternativa aceptada (copia byte-exacta del owner por canal autorizado) no apareció en ningún canal visible. El mandato recibido NO constituye autorización para modificar IAM (explícito en su propio gate A0).
- **Solicitud mínima re-entregada por el canal canónico (delta + handoff al manager/owner):** principal = service account de `aranea-minio-ro` (endpoint `mcps:3011/mcp`, upstream MinIO `192.168.31.92:9000`); operación = `s3:GetObject` puntual (cubre HeadObject en MinIO; sin `s3:PutObject`, sin `s3:DeleteObject`, sin `s3:ListBucket`, sin acceso general al bucket, conector sin cambios, preservando todos los statements existentes); recurso = `arn:aws:s3:::sqx-strategies/durable/mt5-export/v1/b6a1edea-ba20-4a23-8cde-0201ee22e8f0/b94f1400-e495-4636-8587-516db1cb8cb7/sha256:f8968dbaacfd398b97f42ab9eed9b8a6c919ed6c23ad72e936b272f5d2a70e52/09_mt5_backtest/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`; evidencia de denegación vigente = 403 con RequestIDs `18D66E30A94AC147`/`18D66E3697C8719F` (precedentes C5A `18D66C70BAF0077A`/`18D66C9D9B5C9BAD`); alternativa equivalente = copia byte-exacta verificable contra SHA256 `21917e14…8b5c` (13074872 B) antes de cualquier uso; responsable = owner del Access Plane.
- **Fase B (certificación 6182) NO ejecutada:** sin bytes no hay preservación, inspección física, parser/golden ni siete crosschecks; compatibilidad NO demostrada NI refutada. Sin fixture sintético, sin HTM de otro run, sin branch `codex/f05-build6182-parser-cert`, sin commit, sin cambios a allow-list/CORPUS/SPEC-PARSER/tests. `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen; artifacts de RERUN-3 preservados intactos.
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio; la causa primaria sigue siendo el defecto entorno×contrato del build 6182 y la certificación de compatibilidad sigue bloqueada por lectura). Resto de gates sin cambio. **Siguiente acción exacta:** owner del Access Plane concede el `s3:GetObject` puntual (statement en la policy embedded del principal RO, recurso exacto, preservando statements existentes) o aporta copia byte-exacta verificada por SHA256 → con bytes disponibles re-ejecutar la certificación (preservación → parser/golden/crosschecks → decisión) → COMPATIBLE: allow-list `6182` + tests T1–T10 + corpus + source fix para revisión del manager; INCOMPATIBLE o camino (b): fijar/downgrade del terminal de worker-kronos a 6180 y RERUN-4 desde cero.

Registro completo de la verificación en `~/aranea/work/f04-cert-f04-01-c5b/authorization-gate-record.md` (permisos 700; sin secretos ni URLs firmadas).

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c5b-authorization-gate`, change_log `2026-09-18-f05c-cert-f04-01-c5b-authorization-gate`.

### Delta de re-adquisición del HTM vía MinIO RO — 2026-09-18 (misión NORMAL F05C-CERT-F04-01-C5A; continuación acotada de C5; veredicto `ACQUISITION BLOCKED`)

Intentó recuperar exactamente UN HTM auténtico del RERUN-3 usando los valores de la provenance durable C5 sin re-descubrimiento (bucket `sqx-strategies`, key completa `durable/mt5-export/v1/b6a1edea-ba20-4a23-8cde-0201ee22e8f0/b94f1400-e495-4636-8587-516db1cb8cb7/sha256:f8968dbaacfd398b97f42ab9eed9b8a6c919ed6c23ad72e936b272f5d2a70e52/09_mt5_backtest/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`, size 13074872, SHA256 `21917e14…8b5c`), con orden de vías A (GET exacto por la capability MinIO RO) → B (capability existente que autorice la lectura) → C (concesión mínima del owner). Sin reiniciar C5, sin campañas, sin backtests, sin tocar Windows, sin releases, sin RERUN-4, sin otra auditoría del homelab.

- **Distinción GET vs LIST cerrada con sondas vivas sobre `aranea-minio-ro` (conexión preconfigurada):** (1) `s3_list_connections` → sólo la preconfigurada; (2) `s3_list_buckets` → exactamente `deploy` y `examples` (LIST sobre `sqx-strategies` denegado por IAM); (3) `s3_get_object_metadata` (HeadObject) sobre bucket+key exacta → **HTTP 403 `api error Forbidden: Forbidden`, RequestID `18D66C70BAF0077A`**; (4) `s3_get_object` sobre la misma key → **HTTP 403, RequestID `18D66C9D9B5C9BAD`**; (5) `s3_presign_url` GET 120 s firmado por la misma identidad + curl local → **HTTP 403** (URL efímera expirada, no registrada en vault ni handoff).
- **Clasificación de la denegación (rectifica la lectura estructural previa):** endpoint accesible (respuestas S3 firmadas con RequestID), credenciales válidas (ListBuckets OK en la misma conexión) y **conector correctamente configurado** — el S3 target de la capability es `http://192.168.31.92:9000` (runbook `aranea-minio-mcp`), el mismo MinIO que sirve `sqx-strategies`, descartando el escenario «conector que sólo conoce el bucket deploy». La denegación es exclusivamente de **política IAM del principal RO**: la service account (hija de la access key owner, policy embedded) tiene sólo `deploy` List + `deploy/worker/sqx/*` Get + `examples` List/Get, DENY `*backup*`, resto denegado. La inexistencia del objeto no es demostrable vía esta identidad (MinIO deniega antes de resolver existencia); la existencia sigue establecida por la provenance durable del child workflow (C5).
- **Vía B agotada:** la matriz certificada `aranea-mcps-expert` no tiene ninguna otra capability de object-store («no lectura global de Forge»); Mongo/PG/Temporal guardan referencias y read models, no bytes del artifact; cualquier camino SSH exigiría credenciales de servicio desde ETCD/config ajeno (prohibido por mandato y preferencias Aranea). No se ejecutó ningún cambio IAM por iniciativa propia (vía C es owner action).
- **Veredicto `ACQUISITION BLOCKED`:** sin bytes la certificación de compatibilidad 6182 no es ejecutable; compatibilidad NO demostrada NI refutada. Sin fixture sintético, sin HTM de otro run, sin branch `codex/f04-cert-f04-01-build6182`, sin commit, sin cambios a allow-list/CORPUS/SPEC-PARSER. Registro de adquisición completo en `~/aranea/work/f04-cert-f04-01-c5a/acquisition-record.md` (permisos 700; sin secretos ni URLs firmadas).
- **Blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` refinado y entregado:** principal RO afectado = service account de `aranea-minio-ro` (endpoint `mcps:3011/mcp`, upstream MinIO `192.168.31.92:9000`); operación denegada = `s3:GetObject` (y `s3:ListBucket`) sobre `sqx-strategies`, demostrada en HeadObject/GetObject/presign; recurso S3 completo = `arn:aws:s3:::sqx-strategies/durable/mt5-export/v1/b6a1edea-ba20-4a23-8cde-0201ee22e8f0/b94f1400-e495-4636-8587-516db1cb8cb7/sha256:f8968dbaacfd398b97f42ab9eed9b8a6c919ed6c23ad72e936b272f5d2a70e52/09_mt5_backtest/final-b94f1400-e495-4636-8587-516db1cb8cb7.htm`; error = HTTP 403 con los dos RequestIDs de arriba; cambio mínimo requerido = statement `s3:GetObject` para ese recurso exacto en la policy embedded del principal, sin `PutObject`/`DeleteObject`/`ListBucket` ni acceso general al bucket, **sin cambios de conector**; responsable de aprobación = owner del Access Plane (acción owner sobre la identidad upstream; nunca root ni credenciales dev de etcd). Alternativa equivalente: copia byte-exacta del owner verificable contra el SHA256 durable antes de cualquier uso.
- **Estado:** `CERT-F04-01 = BLOCKED` (sin cambio). Resto de gates sin cambio. `MIGRATION: NONE`. Allocations `26090011001–005` write-once permanecen; artifacts de RERUN-3 preservados intactos. **Siguiente acción exacta:** owner concede el GetObject puntual (o aporta copia verificada) → re-ejecutar la certificación C5 (preservación → parser/golden/crosschecks → decisión) → si COMPATIBLE: allow-list `6182` + tests T1–T10 + corpus + release → RERUN-4 desde cero; si INCOMPATIBLE o camino (b): fijar/downgrade del terminal a 6180 y RERUN-4 desde cero.

Evidencia de la misión: agent-run `2026-09-18-zcode-glm-5.3-flash-f05c-cert-f04-01-c5a-acquisition`, change_log `2026-09-18-f05c-cert-f04-01-c5a-acquisition-blocked`.

### Delta de ejecución CERT-F04-01 RERUN — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-RERUN; re-run físico desde cero sobre 0.2.99; veredicto `BLOCKED`)

Re-ejecutó el gate físico desde cero bajo release `0.2.99` / `49fce32` con identidades todas nuevas por el ingress productivo canónico (watcher release `0.2.99` en Zeus, ENV=production, ETCD `watch_dir=./input` resuelto contra CWD real, queue `sqx-main-queue`, ns `sqx-prop`). La cadena física avanzó ~2 stages más allá del intento EXEC y **demostró físicamente la regresión del fix C1** (descarga durable de source OK, `persistence_deadline_required` ausente, MetaEditor ejecutado de verdad, EX5 176730 B + compile log durables), pero el run falló determinístico en `mt5_compile_persist_v1` por un **segundo defecto de la release en el gate de seal**: `CERT-F04-01 = BLOCKED`. Sin fix de source + release nueva el gate no puede completar backtest/seal/handoff.

- **Identidades del run (todas nuevas):** request_id `cert-f04-01-rerun-20260917T211613Z-0997458c` (preimage JSON SHA256 `807c4d71f3301c1b8a4f268fb90a40c8cf8cc248e162ae78da1d641f124f7031`, 5607 B, VALIDATION_OK contra structs @ `49fce32` con validador scratch + negativos; CFX byte-idénticos reutilizados con hashes verificados); ForgeCampaignRef `a470e565-5fbc-45b3-908d-f55c3cb4b70e`; CampaignIntentToken `5c16ab31-85df-48a6-9b5d-3891e8113a2f`; WorkflowID/RunID campaña `sqx-forge-campaign-v1-5c16ab31-85df-48a6-9b5d-3891e8113a2f` / `01a0b13f-cdbd-7a98-afa7-8296e5c471db`; WaveRef `forge-a470e565-…-w000001` (1 de 1); FlowRunRef `efc4ac0c-545b-47bd-8750-79d9abf1e4a3` (`sqx-main-v1-c62c36f9-…` / `01a0b13f-cf28-…`); intake durable cfg `b6b70b3e-5c05-4136-810c-955289ecd708`.
- **Cadena física lograda:** overview ×1 → retester ×16 → optimizer ×12 → evaluate_wfm ×12 (todos COMPLETED, cero FAILED en funnel) → select_robust_run ×1 COMPLETED → **apply_selected_run ×1 COMPLETED con la allocation durable Magic V1 #4** → final_reretester ×1 COMPLETED → trade list durable (13 trades, StrategyRef `4b86cd72-cc1a-41a3-a3c2-8e22d7974f7f`) + MQ5 export durable (source key sha256 `86eca9dd…`) → **compile físico Windows attempt 1 SUCCESS** (`MT5CompileArtifactWorkflow` `mt5-compile-…-10d8e5b1fe541893` 21:35:26→21:35:31Z; MetaEditor corrió 4707 ms: `0 errors, 1 warnings` en `metaeditor.log`; EX5 `c698331c…`/176730 B; compile log `96360c81…`/15984 B durables) → **mt5_compile_persist_v1 FAIL CONTRACT_CONFLICT** → FlowRun `FAILED`, Campaign `FAILED/WAVE_FLOW_RUN_FAILED` 21:35:32.263Z, terminal por sí mismo (non-retryable, sin cancelación necesaria, flota drenada).
- **Magic allocation (read-back durable):** namespace `forge-live`; StrategyRef `4b86cd72-…` → magic_decimal `26090011004`, allocation_ref `sha256:ff4ecf07…`, assigned_at `2026-09-17T21:33:57.166363Z`. El allocator **continuó desde su estado durable real** (001–003 reservados por el run CANCELLED + 004 nueva; sin liberar, sin reset, sin presuponer números). Stamp efectivo demostrado por Apply COMPLETED fail-closed + MQ5 stamped consumido por compile.
- **Defecto que bloquea (causa primaria única):** `mt5_compile_persist_v1` → `artifact_integrity (CONTRACT_CONFLICT): compile log does not report 0 errors` aunque el compile fue limpio. Root cause: `VerifyCompiledArtifactForSeal` (`sqx/core/capabilities/strategy_version.go:62` @ `49fce32`) matchea `compileSuccessPattern` (`(?i)Result:\s*0 errors`) contra los bytes crudos del log descargado, pero MetaEditor64 escribe UTF-16LE con BOM; el gate local del compile activity SÍ normaliza (`evaluateCompileArtifacts` → `normalizeCompileLog`, `sqx/adapters/mt5/artifact_compiler.go:346-352`) y por eso reporta success — dos gates, mismos bytes, criterios distintos. Invisible en suite CONTRACT (fixtures UTF-8). NO es defecto de input, entorno ni del MQ5 generado; el defecto C1 quedó corregido y demostrado. Fix esperado: normalizar encoding en el gate de seal (o al publicar el log), un archivo, `MIGRATION: NONE` (decisión manager).
- **Regresión del fix C1 (§9):** 1) source durable MQ5 descarga OK (compile attempt 1, sin loop infinito); 2) cero `persistence_deadline_required` en todo el run; 3) MetaEditor real (job dir `forge-campaign-a470e565-…` creado 21:35:26Z + `metaeditor.log` 21:35:31.568Z); 4) compile log durable existe; 5) EX5 no vacío con SHA/size declarados durables. Backtest/`persistence_deadline_required`-en-backtest: NO EJERCITADOS (el run murió antes).
- **Terminación limpia:** sin reintentos (CONTRACT_CONFLICT non-retryable), 0 workflows Running en sqx-prop, worker Windows PID 20044 único y drenado (poller vivo, sin touches post-21:35:31), watcher ingress auto-cerrado 21:22:33Z por el check de upgrade contra el marcador LEGACY `/opt/symphony/current` (0.2.40; comportamiento conocido, sin efecto sobre el despacho ya completado, sin segundo consumidor en ningún momento), f03cert intacto, allocations 26090011001–004 write-once permanecen.
- **Evidencia persistida:** `~/aranea/work/f04-cert-f04-01-rerun/` (`pre-submit-record.md`, `submit-record.md`, `evidence-summary.md`, input JSON + `preimage.sha256`); remoto `sqx-zeus:/tmp/cert-f04-01-rerun/` (input processed + `watcher.screen.log`); durable del flujo en Temporal/PG/MinIO/`metaeditor.log`.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-rerun`, change_log `2026-09-17-f05c-cert-f04-01-rerun-blocked`.

### Delta de corrección source CERT-F04-01 — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-C1; fix de source; veredicto `SOURCE FIX PASS`)

Corrigió en source el defecto demostrado por el run físico EXEC (`cert-f04-01-20260917T181526Z-275d2be3`, FlowRunRef `794b0e02-aae5-4ccb-820b-17294ddb7af8`): los compile children morían en Windows con `persistence_deadline_required` antes de ejecutar MetaEditor. **Root cause:** bajo B2 el `physicalCtx` se deriva de `context.WithoutCancel(activityCtx)` y viaja **sin deadline**; `DownloadArtifactTaskSource` (boundary compartido de source durable, usado por `ArtifactCompiler.Compile` y `ArtifactRunner.Backtest`) pasaba ese ctx tal cual a `FetchDurableToPath`, cuyo guard fail-closed `capabilities.ValidatePersistenceContext` exige deadline (`ErrDeadlineRequired`). **Fix (diseño frozen en el boundary, `MIGRATION: NONE`):** `DownloadArtifactTaskSource` auto-acota SOLO el fetch durable cuando el caller no trae deadline — constante package-local `artifactSourcePersistenceTimeout = 2 * time.Minute` (`context.WithTimeout` + `defer cancel`); deadline entrante se preserva exacto; cancelación del caller permanece observable (sin `Background`/`WithoutCancel`); source legacy key-only sin cambios; sin tocar B2/C4/C5, workflows, ActivityOptions, ETCD, MinIO adapter ni release config. El timeout de persistencia no se convierte en timeout del proceso físico MT5 (Compile crea su propio `execCtx` de MetaEditor después de la descarga).

- **Branch/base/commit:** `codex/f04-cert-f04-01-deadline-fix` desde `3d0e8c958765da23840e00bf1a0ac017fc47597a` (== `origin/codex/f05-release-prep`; `b57bfb2` ancestro verificado; locus del defecto idéntico entre `b57bfb2` y base). Commit único **`49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1`** (`fix(mt5): bound durable source fetch context`). Sin merge a master, sin tag/release.
- **Archivos cambiados (sólo allowed files):** `sqx/adapters/mt5/artifact_compiler.go` (producción, +constante +bounded ctx en `DownloadArtifactTaskSource`); `sqx/adapters/mt5/artifact_compiler_test.go` y `sqx/adapters/mt5/artifact_runner_test.go` (regresión). `artifact_runner.go` no requirió cambio (consume el boundary compartido). Dirty foráneo `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado intacto.
- **Tests de regresión T1–T6 (todos habrían fallado en 0.2.98):** T1 durable+sin deadline recibe ctx bounded (~2m, futuro); T2 deadline del caller preservado exacto (no reemplazado por +2m); T3 caller cancelado permanece `context.Canceled` observable; T4 legacy key-only sigue por `DownloadArtifactToPath` sin `FetchDurableToPath`; T5 `Compile` durable con caller sin deadline supera la descarga (mock rechaza ctx sin deadline) con `PrimaryDurable` intacto; T6 `Backtest` durable EX5 con caller sin deadline descarga con deadline y completa success (mock rechaza ctx sin deadline). **Repro del defecto contra el source 0.2.98** (worktree temporal @ base + guard real): `DownloadArtifactTaskSource(context.Background(), …)` produce exactamente `persistence_deadline_required` — defecto reproducible, no STOP condition.
- **Suites:** gates obligatorios — `adapters/mt5` OK (incluye C4 ownership + C5 cancellation verdes, durable no-downgrade intacto), `core/capabilities` OK, `adapters/storage-minio` OK, `activities/worker/{pipeline,hooks,steps}` OK; suite amplia `./sqx/...` verde en todo lo demás (cmd workers, echo-handoff, metadata-mongo, core/*, registry-postgres/migrations). **Fallos preexistentes aislados contra baseline limpio (idénticos sin el fix, ajenos):** `activities/worker` 16 + `mt5/report` + `mt5/binding`+`mt5/normalization` 37 (fixture físico `mt5-export.htm` ausente en el working copy), `registry-postgres` 4 (estado de integration PG), `workflows` 22 (fixtures sin `flow_run_start`), `tools` (múltiples `main`, histórico). Efecto lateral de suite (`core/evaluation/warnings/artifacts/f5_warning_example.json` re-escrito por su propio test) restaurado a HEAD.
- **Estado:** `CERT-F04-01 = BLOCKED`; **blocker source fix = `SOURCE VERIFIED`**; **physical gate remains BLOCKED**. `MIGRATION: NONE`. **Siguiente acción exacta:** `BUILD/RELEASE → ROLLOUT → CERT-F04-01 RERUN FROM ZERO` (nuevo request_id/run; las allocations `26090011001–003` permanecen reservadas).

### Delta de release y rollout CERT-F04-01 — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-C2; BUILD/RELEASE → ROLLOUT; ningún gate físico ejecutado; veredicto `RELEASE/ROLLOUT PASS`)

Convirtió el fix C1 en una release física desplegada y verificable, sin ejecutar `CERT-F04-01`, sin modificar el fix, sin tocar ETCD, sin campañas y sin lanzar watcher de certificación. **`CERT-F04-01` permanece `BLOCKED`; pasa a `READY TO RERUN`.**

- **Source authority:** gate local PASS (`codex/f04-cert-f04-01-deadline-fix` @ `49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1`, parent `3d0e8c958765da23840e00bf1a0ac017fc47597a`, diff exacto de los 3 archivos permitidos, dirty foráneo `phase4_performance.json` fuera del commit); fix branch publicado a `origin` y demostrado por `ls-remote` + fetch + API GitHub (SHA/parent/message/files); `origin/codex/f05-release-prep` re-verificado en `3d0e8c9…` y fast-forward puro a `49fce32…` (sin merge/rebase/squash/cherry-pick), read-back remoto exacto. Ese SHA es el source authority de la release.
- **Test gate pre-release desde `49fce32…`:** `sqx/adapters/mt5` (raíz: C4 ownership, C5 cancellation, durable no-downgrade, regresión T1–T6), `sqx/core/capabilities` y `sqx/adapters/storage-minio` OK; fallos `mt5/report`/`mt5/binding`/`mt5/normalization` = misma clase baseline de C1 (fixture físico `mt5-export.htm` ausente en el working copy, causa confirmada), ningún fallo nuevo atribuible al fix. Sin STOP.
- **Versión `0.2.99` resuelta por autoridad remota** (`release-authority` AUTO @ pipeline: `published_version=0.2.98`, `remote_line_max=0.2.98`, `authority_state=CONSISTENT`, candidato `0.2.99`); sin sobrescribir versiones existentes.
- **Build/publicación por el mecanismo canónico** (`./deploy_release.sh --release-only 0.2.99`, deployer-watcher ENV=production): 6 artifacts desde source `49fce32…` — linux-amd64 `symphony` 40312992 B / sha256 `21ad38a16168c84f13babd7c8f4b5a49d5c085b585955a0199005cff9f42a502`, `sqx-watcher` 32682144 B / `f08d1684d01c38d4925ff1718ab8ac84e61b6efa76d916b0a05c3402102d06de`, `start-symphony-worker.sh` 310 B, `run-symphony` 233 B, `promtail-worker.yaml` 355 B; windows-amd64 `sqx-mt5-worker.exe` 37248512 B / `f9af5c7b325001129b5751abeeb26555ea85cf02791181495d4eb6753a47800a`. `go version -m` de los 3 binarios: `vcs.revision=49fce32…`, `vcs.time=2026-09-17T20:24:13Z`, `vcs.modified=true` (dirty operacional no-Go, misma clase explicada de 0.2.98). MinIO confirmó manifest `0.2.99` 20:43:07Z; read-back `release-authority --target 0.2.99`: `published_version=0.2.99`, `authority_state=CONSISTENT`, `target_state=EXACT_MATCH` (bytes verificados); manifest remoto capturado (etag `12830e1f8e556d2b73b2c0c76bc73a51`) con 6/6 keys/sizes/sha256 == locales; el `deploy/manifest.json` git sigue sin ser autoridad de publicación.
- **Rollout Linux (Stager canónico):** Zeus CURRENT `0.2.99`, worker PID 2730741 (20:43:17Z) ejecutando `/opt/stager/releases/0.2.99/bin/symphony` con SHA256 instalado == artifact, poller `Started Worker Namespace sqx-prop TaskQueue sqx-main-queue WorkerID 2730741@sqx-ulab-zeus-0` + ESTABLISHED :7233; Hera PID 1330991 (20:45:30Z, `1330991@sqx-ulab-hera-0`); Kronos PID 1294710 (20:45:17Z, `1294710@sqx-ulab-kron-0`); exactamente un worker por host, sin residuos `0.2.98`. El legado `symphony-f03-382f4ba` de Hera sigue presente (desde sep10, ambiente separado) — documentado, no tocado, no blocker.
- **Rollout Windows `worker-kronos` (modelo Stager vigente, sin `C:\SQX`, sin inspector one-shot):** demostrado con la capability recurrente `AraneaEvidencePublish (SYSTEM) → evidence JSON → mt5-kronos-operator read-only`. Reporte 20:48:07Z (`partial=false`, `errors=[]`, publisher SYSTEM, freshness 3 min < stale 15): worker `sqx-mt5-worker` PID 20044 (nuevo; antes 1700) desde 20:44:02Z, path `C:\ProgramData\Stager\releases\0.2.99\bin\sqx-mt5-worker.exe`, SHA256 == artifact EXACT MATCH, size == manifest, padre `stager-runtime` PID 8520 (relación supervisada), `CURRENT=0.2.99`/`PENDING=0.2.99`, poller ESTABLISHED 20044 → Temporal `192.168.31.46:7233`; singleton (1 worker).
- **Estado:** `CERT-F04-01 = BLOCKED / READY TO RERUN`. El rollout NO equivale a physical certification. `MIGRATION: NONE`. **Siguiente acción exacta:** misión `F05C-CERT-F04-01-RERUN` — re-ejecutar el gate desde cero con identidades todas nuevas (reglas frozen en `Próxima tarea única recomendada para NORMAL`).

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c2-release-rollout`, change_log `2026-09-17-f05c-cert-f04-01-c2-release-rollout`.

### Delta de ejecución CERT-F04-01 — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-EXEC; primer gate físico del orden frozen; veredicto `BLOCKED`)

Ejecutó exactamente UN flujo F-04 físico real bajo release `0.2.98` / `b57bfb2` por el ingress productivo canónico (watcher release 0.2.98 en Zeus, ENV=production, watch_dir ETCD `./input`, queue `sqx-main-queue`, ns `sqx-prop`; sin segundo consumidor — f03cert legacy despacha a `f03-cert-queue`, sin interferencia). El run alcanzó estado terminal y produjo evidencia física durable hasta el stage `apply_selected_run` inclusive; falló determinístico en `mt5_compiler`; el run propio fue cancelado de forma segura (autorización §6, precedente 0.2.97). **CERT-F04-01 = `BLOCKED`** — causa primaria única: defecto de la release en el compile path durable (abajo). No PASS; no FAIL transitorio. Exige fix de source + release nueva + rollout antes de reintentar el gate desde cero.

- **Identidades del run:** request_id `cert-f04-01-20260917T181526Z-275d2be3` (preimage JSON SHA256 `d955bf193b339fb8ead86781fd4935c0c758354c3bd085848e3869d3c6afeeca`, 5583 B, validado contra structs vigentes `b57bfb2` con validador scratch: WorkflowSpec + ValidateWorkflowSpec + ForgeCampaign.Validate + reglas intake → VALIDATION_OK); ForgeCampaignRef `d400dba0-114b-423f-b891-c34a914ce235`; CampaignIntentToken `b41bd7fc-d6ac-4b7b-a828-3888a17d3f75`; WorkflowID/RunID campaña `sqx-forge-campaign-v1-b41bd7fc-d6ac-4b7b-a828-3888a17d3f75` / `01a0b09d-4b12-7451-9fb1-9b94c3fd1288`; WaveRef `forge-d400dba0-114b-423f-b891-c34a914ce235-w000001` (wave 1 de 1, target_finalists=1, max_waves=1 frozen en la campaña); FlowRunRef `794b0e02-aae5-4ccb-820b-17294ddb7af8` (GenericSQXWorkflow `sqx-main-v1-da7fe5d1-3154-4944-8174-efe657a351d8`); intake durable `91bcd00b-8df8-401f-a612-73489feacf82`; valores frozen del input: XAUUSD/L/H1, promotion `finalist_promotion@2.0.0`, `magic_number: 0` (nunca leído en path durable), `forge_campaign` v2, sin `mt5.timeout`; .cfx byte-idénticos al run físico 0.2.97 (builder `fd5ffebe…`, optimizer `121ec05e…`, retester/reretester `1a993957…`).
- **Cadena física lograda (primera parcial F-04):** overview ×1 COMPLETED (builder generó 3 candidatos) → retester ×15 COMPLETED → optimizer ×14 COMPLETED → evaluate_wfm ×14 COMPLETED → select_robust_run ×3 COMPLETED → **apply_selected_run ×3 COMPLETED con las primeras allocations durables Magic V1 de F-04** → final_reretester ×3 COMPLETED → trade list + report + MQ5 export publicados durables (etags `d061f0bd…`/`235a96c2…`, prefix `durable/mt5-export/v1/794b0e02-…`). Stamped artifact por estrategia en `durable/apply-selected-run/v1/794b0e02-…` (etag `7fe1fe6e…`, 18:40:22Z) y carrier exacto consumido por final reretester (log 18:40:38Z).
- **Magic — evidencia obligatoria (§8):** namespace `forge-live`; 3 allocations write-once en `sqx.strategy_magic` vía flowkit `strategy get`: StrategyRef `53373932-9c2e-4c41-b36d-cb839025f3a1` → magic `26090011001`, allocation_ref `sha256:b431260e7c6ae2dd7697889cc2ba1fd699b25ab36a3442010806c18ef6c06e2a`, assigned_at 18:40:09.732Z; `d907183f-43be-4f93-b54a-8276195d3f2e` → `26090011002`, `sha256:59a5a6bd9a0bf937b04dab534cd79a1320a3f8534e798909b6d83a3405fc3fce`, 18:40:09.749Z; `5e16acb6-aade-4268-a0cf-7cecf35d494d` → `26090011003`, `sha256:4d3e827545f3a63b9fc78758bcf612b1b4b6125ebc161ff36ad329e55ce697c9`, 18:40:09.757Z. Layout Magic V1 verificado (`2609`=YYMM sep-2026, `001`=XAUUSD, `1`=LONG desde `L`, secuencia mensual compartida sin partición por dirección, nunca reservados 888111/11111); stamp/readback demostrado por Apply COMPLETED bajo contrato fail-closed (mismatch ⇒ sin completion) + input con `magic_number: 0` separa allocated de cualquier requested. Las 3 filas PERMANECEN (reserva sin uso válida; no liberar).
- **Defecto que bloquea (causa primaria):** `mt5_compile_artifact` falla 100% determinístico en la descarga de source durable con `infrastructure: mt5 artifact compile: download: persistence_deadline_required` — guard fail-closed `capabilities.ValidatePersistenceContext` (`sqx/core/capabilities/persistence.go:618` @ `b57bfb2`) exige ctx con deadline y la actividad MT5 no se auto-envuelve una antes de `DownloadArtifactTaskSource` → `FetchDurableToPath` (`sqx/adapters/storage-minio/durable_artifacts.go:147`, `sqx/adapters/mt5/artifact_compiler.go:186`; la convención vigente del codebase es el self-wrap, p.ej. `durable_select_robust_run.go:60-64`). Reproducido en 3/3 compile children (`…wave-000001-{060f2647,8f3971eb,cedb9f7b}…`), attempts 99→110+ a ~30s, siempre `lastWorkerIdentity 1700@worker-kronos@` (worker Windows 0.2.98 SHA-verified); el job dir `C:\MT5\test\EchoForgeJobs\forge-campaign-d400dba0-114b-423f-b891-c34a914ce235-wave-000001\compile` se ejercitó por attempt sin llegar jamás a MetaEditor64. Este path nunca se había ejercitado físicamente (0.2.92 compilaba desde source no-durable; 0.2.97 murió antes en Apply). Retry policy `MaximumAttempts=0` ⇒ loop infinito sin intervención. No existe key de config que inyecte el deadline ⇒ el fix es de source (`MIGRATION: NONE` esperado; decisión manager) + release nueva + rollout, y recién entonces re-ejecutar CERT-F04-01 desde cero (nuevo request_id/run; las allocations actuales quedan reservadas y el re-run generará StrategyRefs nuevos con allocations nuevas).
- **Terminación limpia:** cancel del run propio 19:35:36Z vía CLI Temporal local read-only-install en daedalus (pre-autorizado por el delta R1; cancel puntual del campaign parent, autorización §6 de la misión); propagación verificada 5/5 workflows CANCELED (campaña, wave, 3 compile children); read model final: campaign `CANCELLED`, `cancel_reason PARENT_CANCELLED`, `finished_at 2026-09-17T19:35:45.763391Z`, flow_run `CANCELLED`; flota drenada (worker Windows sin conexión MinIO y sin touches post-cancel, poller único vivo; cero watchers/screens residuales; f03cert intacto).
- **Evidencia persistida:** resumen completo + preimage + hashes en workspace local de la misión (`~/aranea/work/f04-cert-f04-01-exec/`: `evidence-summary.md`, `pre-submit-record.md`, `cert-f04-01-20260917T181526Z-275d2be3.json`, `preimage.sha256`); staging remoto preservado en `sqx-zeus:/tmp/cert-f04-01-exec/input/processed/` (5 archivos con hashes verificados); logs del watcher/worker referenciados por timestamp.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-exec`, change_log `2026-09-17-f05c-cert-f04-01-exec-blocked`.

### Delta de cierre de evidencia runtime — 2026-09-17 (misión NORMAL F05C-F04-01-RUNTIME-EVIDENCE-CLOSE; read-only; ningún gate ejecutado, ningún estado de gate cambiado)

Cierra el deployment/runtime evidence set previo a CERT-F04-01-EXEC. Persiste el estado **WINDOWS CONVERGED PASS** cerrado por el owner (paso 0 elevado sobre `worker-kronos`: worker PID 1700, PPID 30700, ejecutable real `C:\ProgramData\Stager\releases\0.2.98\bin\sqx-mt5-worker.exe`, SHA256 `0bceda4badd982b25b01f13ec40a23eb95c8179e28d8a6d955036b30fc7aa474` == artefacto publicado `worker/sqx/0.2.98/windows-amd64/sqx-mt5-worker.exe` MATCH EXACTO; `StagerRuntime` Running PID 30700, StartName `.\kor`; owner acepta el modelo stager) y lo re-verifica vivo a 2026-09-17T17:12Z sin contradicción material. Veredicto: **`READY FOR CERT-F04-01-EXEC`** — A PASS, B PASS, C PASS, D PASS, E PASS; prompt maestro `F05C-CERT-F04-01-EXEC` redactado y entregado en el handoff, NO ejecutado.

- **1. Parent/child identity:** vivo confirma exactamente un `sqx-mt5-worker` PID 1700 y un `stager-runtime` PID 30700, mismos PIDs del cierre owner (estabilidad ⇒ sin restart intermedio ⇒ el padre precede al hijo y ambos preceden esta sesión; el hijo mantiene conexiones ESTABLISHED long-lived). CreationDate/ExecutablePath/CommandLine por canal `echo-dev`: INDETERMINABLE — boundaries re-confirmados en vivo: `Get-CimInstance Win32_Process` → Access denied 0x80041003; `wmic` ausente del build; `Get-Process` StartTime/Path ocultos (identidad cruzada `.\kor`); `wevtutil qe System` → Access denied (eventos SCM 7036 no disponibles); `tasklist /v` y Prefetch denegados (previo). La relación PPID 30700→1700 y el hash del ejecutable permanecen con autoridad del cierre owner elevado; sin contradicción viva.
- **2. Stager release state:** `C:\ProgramData\Stager` sigue ACL-bloqueado por diseño (`icacls /inheritance:r` Administrators+SYSTEM; re-confirmado en vivo: `Get-Content CURRENT` → Access is denied, `Test-Path PENDING`/`releases\0.2.98\...` → UnauthorizedAccessException con `False` de ACL, no de ausencia). CURRENT/PENDING/target.yaml/manifest/hashes: autoridad = cierre owner elevado (CURRENT==0.2.98, dir de release real, hash MATCH). Corroboración viva sin leer el dir: el worker stager-managed corre desde `releases\0.2.98\bin` (stager sólo lanza el entrypoint de su release activa) con SHA del artefacto publicado. `C:\stager` sólo staging agosto (f33/f36), nada 0.2.98.
- **3. StagerReconcile: UNKNOWN (no convertido a ABSENT).** `schtasks /Query /TN StagerReconcile /V` → "ERROR: The system cannot find the path specified." (API legada, ambigua ante tarea oculta); CIM `Get-ScheduledTask` denegado (previo); query completo previo exit 0 no la listó; el instalador `install-stager.ps1` la declara (cada 1 min, SYSTEM, RunLevel Highest). Sin mutar; presencia/estado exigible sólo con elevación.
- **4. Poller Windows:** `netstat -ano` PID 1700 con exactamente 2 ESTABLISHED — Temporal `192.168.31.46:7233` (poller vivo) + OTel `192.168.31.60:14317`; barrido `Get-Process` global (`*sqx*|*stager*|*mt5*|*worker*`) → 1 solo `sqx-mt5-worker` (PID 1700) + 1 `stager-runtime` (PID 30700), cero duplicados. Identidad TaskQueue `sqx-mt5-queue`: NO demostrable por canal disponible (Loki `aranea-observability-ro` no ingiere el worker Windows — labels 24h: sólo echo-*/argus; stdout del worker corre bajo identidad `.\kor` sin archivo legible) — soportada por el contrato del servicio `docs/services/sqx-mt5-worker-windows.md` @ baseline + binario SHA-verificado 0.2.98 + conexión al Temporal correcto. Drain: no declarado (fuera de alcance).
- **5. Linux snapshot (sin repetir auditorías):** flota completa en 0.2.98 a 2026-09-17T17:12Z — Zeus `cat /opt/stager/CURRENT` → `0.2.98`, worker PID 2633855 `/opt/stager/releases/0.2.98/bin/symphony` (ps, mismo PID que R1, estable desde sep13); Hera CURRENT `0.2.98`, worker PID 1231842 con cmdline `/opt/stager/releases/0.2.98/bin/symphony`; Kronos CURRENT `0.2.98`, worker PID 1203622 con cmdline `/opt/stager/releases/0.2.98/bin/symphony`. Mtimes de PID Hera/Kronos sep13 00:21 local (03:21Z ≈ publicación 03:20:41Z); stager-runtime+tee locales restartearon sep16 (Hera 06:51, Kronos 06:32) sin respawn del worker. `/proc/*/exe` cross-user no resuelve target (ptrace boundary); cmdline world-readable usado como prueba.
- **Observación (pre-existente, no blocker):** Hera mantiene el artefacto de certificación F-03 vivo desde sep10 (`/tmp/f03-cert`: `symphony-f03-382f4ba`, `sqx-watcher-f03-382f4ba`, ENV=`f03cert` HOST_KEY=`hcert`) y `ss -tnp` muestra 2 ESTABLISHED a `:7233` (worker 0.2.98 + legacy f03cert; `ss` sin columna Process sin root). No es `sqx-mt5-worker`, no apareció en esta ventana (precede convergencia/readiness/preexec y la campaña física 09-12), y corre ambiente separado por ENV/ETCD root; identidad de TaskQueue/namespace del f03cert UNKNOWN por canal. Higiene opcional pre-EXEC: owner puede drenar `/tmp/f03-cert` para eliminar cualquier competencia residual de pollers; el gate no lo exige.
- **E sin nuevos blockers:** watcher de producción no corre actualmente en la flota (esperado: mecanismo campaign-scoped frozen en C); licencia SQX sigue a verificación en ejecución dentro del propio gate (condición STOP de la receta); `C:\ProgramData\Stager` y `StagerReconcile` exigen elevación sólo si el owner quiere lectura directa adicional — no requerida para el gate.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-runtime-evidence-close`, change_log `2026-09-17-f05c-cert-f04-01-runtime-evidence-close`.

### Delta de convergencia Windows owner — 2026-09-17 (misión NORMAL F05C-F04-01-WINDOWS-CONVERGENCE con autoridad owner explícita; ningún gate ejecutado, ningún estado de gate cambiado)

Misión de ejecución de la receta owner del preexec lock contra el artefacto publicado `worker/sqx/0.2.98/windows-amd64/sqx-mt5-worker.exe` (SHA256 `0bceda4badd982b25b01f13ec40a23eb95c8179e28d8a6d955036b30fc7aa474`, size 37250048). Veredicto: **`WINDOWS CONVERGENCE BLOCKED`** — la precondición B sigue FAIL (`BLOCKED — OWNER ACTION REQUIRED`) y la receta queda refinada con un descubrimiento material: la autorización owner de la misión no crea capability; el plano MCP certificado sólo entra a `worker-kronos` como `echo-dev` sin membresía `BUILTIN\Administrators`, y el stop del incumbent es cross-identity.

- **Paso 0 — identity read-back: INDETERMINABLE (boundary de diseño, no fallo transitorio).** `whoami /groups` sin `S-1-5-32-544` (sólo Users; Mandatory Level Medium); `Get-Process -Id 1700` sin Path/StartTime; `tasklist /v /fi "PID eq 1700"` Access denied; `(Get-Process -Id 1700).Modules` RuntimeException (sin `PROCESS_QUERY`, por DACL también sin `PROCESS_TERMINATE`); `sc.exe queryex` OpenSCManager FAILED 5; `Get-ScheduledTask` CIM Access denied; `C:\Windows\Prefetch\SQX-MT5-WORKER*` UnauthorizedAccessException; `C:\ProgramData\Stager` ACL-bloqueado. El SHA256 del ejecutable real de PID 1700 no es obtenible por canal certificado ⇒ la comparación con `0bceda4b…` (y por tanto el paso 2 y la condición del paso 3) no es ejecutable por el agente.
- **Descubrimiento: el worker Windows es stager-managed.** Registro HKLM legible: servicio `StagerRuntime` START=2 TYPE=16, ImagePath `C:\ProgramData\Stager\bin\stager-runtime.exe service --target-config C:\ProgramData\Stager\target.yaml`; `C:\stager\install-stager.ps1` (legible) registra además la tarea `StagerReconcile` cada 1 minuto como SYSTEM RunLevel Highest (reconcile de releases contra manifest) y aplica `icacls /inheritance:r` con grant sólo Administrators+SYSTEM — el ACL-block de `C:\ProgramData\Stager` es by design; `stager-target.yaml.example` declara driver `windows-scm`, entrypoint `bin/worker.exe`, restart_backoff 5s. PID 1700 (`sqx-mt5-worker`) es el entrypoint supervisado por ese servicio. **Tensión de modelo de deployment:** el contrato canónico Windows (`deploy/windows/sqx-mt5-worker/README.md`: owner-operated foreground, "deliberately does not install a Windows service") convive con este stager-managed activo; decidir es owner (aceptar stager como modelo Windows, o converger a C:\SQX deshabilitando `StagerReconcile`+`StagerRuntime` — si no, el reconcile re-activa el servicio en ≤1 min y produce doble poller).
- **Idle/drain: PASS.** `C:\MT5\test\EchoForgeJobs` sin actividad desde 2026-09-05 21:18 (último `echo-forge-full-golden-20260905T221730Z-…`); netstat PID 1700 con sólo 2 ESTABLISHED (Temporal `192.168.31.46:7233` poller vivo + OTel `192.168.31.60:14317`); sin transferencias activas ⇒ el stop del incumbent es seguro desde drain, cuando exista canal con elevación.
- **Backup y scripts canónicos: verificados en source @ `3d0e8c9`.** `Install-EchoForgeMT5Worker.ps1` crea `releases\<ver>\` + backup automático del binario previo en `backups\<ts>\`; `Rollback-EchoForgeMT5Worker.ps1` restaura sólo desde `backups\`; `Start-EchoForgeMT5Worker.ps1` exige `ENV` (bootstrap ETCD existente) y valida paths portable. Artefacto de staging byte-exacto verificado local: worktree cert `symphony-f04-cert-20260913` @ `b57bfb2` `deploy/0.2.98/windows-amd64/sqx-mt5-worker.exe` == digest publicado == read-back MinIO del preexec lock.
- **Capacidad de instalación: factible; stop: imposible; segundo poller: rechazado.** echo-dev puede crear `C:\SQX` (probe creado y eliminado, footprint cero) y `sftp-upload` está certificado en `mt5-kronos-operator`; pero sin elevación no se puede detener PID 1700 ni el par servicio/tarea stager. Arrancar el worker canónico con el incumbent vivo fue descartado: consumidores competentes sobre `sqx-mt5-queue` contaminarían el determinismo de CERT-F04-01.
- **Acción exacta pendiente owner (sin cambios de gates):** (1) paso 0 elevado en `worker-kronos`: `Get-Process -Id 1700 | Select Path` + `Get-FileHash <path> -Algorithm SHA256` + inventario con hashes de `C:\ProgramData\Stager\target.yaml` y `releases\*`; (2) si hash == `0bceda4b…` y el owner acepta el modelo stager ⇒ Windows converged sin instalar (registrar identidad+poller como deployment proof); (3) si prevalece el modelo canónico C:\SQX ⇒ `Disable-ScheduledTask -TaskName StagerReconcile` + `sc.exe stop StagerRuntime` + `sc.exe config StagerRuntime start= disabled` + confirmar terminación de PID 1700, staging byte-exacto en `C:\staging\`, `Install-EchoForgeMT5Worker.ps1 -ArtifactPath C:\staging\sqx-mt5-worker.exe -InstallDirectory C:\SQX\sqx-mt5-worker -Version 0.2.98` (WhatIf → real), `Start-EchoForgeMT5Worker.ps1` con `ENV` autorizado (WorkerPath `C:\SQX\sqx-mt5-worker\sqx-mt5-worker.exe`, MetaEditor `C:\MT5\test\MetaEditor64.exe`, Terminal `C:\MT5\test\terminal64.exe`, InstallationRoot `C:\MT5\test`, JobsRoot `C:\MT5\test\EchoForgeJobs`), proofs (hash == `0bceda4b…`, proceso nuevo con Path legible, poller `sqx-mt5-queue`), rollback canónico ante fallo.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-windows-convergence`, change_log `2026-09-17-f05c-cert-f04-01-windows-convergence`.

### Delta de desbloqueo Windows access owner — 2026-09-17 (misión ACCESS-RECOVERY del Access Plane; read-only sobre worker-kronos; ningún gate ejecutado, ningún estado de gate cambiado)

Misión del responsable del Access Plane: restaurar la capability mínima para identificar y converger el worker Windows (`worker-kronos`, qemu/135, hoy `192.168.31.128`) contra el artefacto publicado `worker/sqx/0.2.98/windows-amd64/sqx-mt5-worker.exe` (SHA256 `0bceda4badd982b25b01f13ec40a23eb95c8179e28d8a6d955036b30fc7aa474`). Veredicto: **CAPABILITY RESTAURADA PARCIALMENTE — convergencia 0.2.98 UNKNOWN, paso 0 owner-gated** (la elevación es un límite de identidad Windows, no del plane; la autorización no crea capability).

- **Topología demostrada por canal certificado (consumer-side, SDK+plane, bearer por stdin):** identidad `worker-kronos\echo-dev` (sin `S-1-5-32-544`, Medium integrity — re-verificado); worker `sqx-mt5-worker` sigue siendo **PID 1700** (`Get-Process`; Path/StartTime ocultos), poller vivo (`netstat :7233` ESTABLISHED 1700→`192.168.31.46`) + OTel `192.168.31.60:14317`; supervisor `stager-runtime` PID 30700; servicio `StagerRuntime` START=2 AUTO, ImagePath `C:\ProgramData\Stager\bin\stager-runtime.exe service --target-config C:\ProgramData\Stager\target.yaml`, **ObjectName `.\kor`** (corrección material al handoff previo: el servicio NO corre como SYSTEM); tarea `StagerReconcile`: NO aparece en `schtasks /Query` completo (API legada, exit 0) y CIM la deniega a esta identidad ⇒ **presencia no demostrable por canal certificado** (el dato "cada 1 min SYSTEM" proviene del instalador, no de observación de la tarea viva).
- **Boundary ratificado en vivo (fail-closed, sin bypass):** `tasklist /FI` Access denied; `sc.exe query` OpenSCManager FAILED 5; `Get-ScheduledTask` CIM Access denied; `C:\ProgramData\Stager` (dir, `CURRENT`, `PENDING`, `releases\`) denegados por ACL (`icacls /inheritance:r`, grant sólo Administrators+SYSTEM según `C:\stager\install-stager.ps1`); TaskCache denegado. Gotcha: `Get-ChildItem C:\stager` oculta por atributo Hidden archivos legibles por path directo (instalador y `stager-target.yaml.example` leídos OK). `C:\SQX` no existe; `C:\stager_old` existe; jobs root sin actividad desde 2026-09-05 21:18 (drain PASS re-confirmado).
- **Resolución de acceso: A (viewer) y B (operator) insuficientes para el paso 0; C implementado como INSPECTOR stageado, no como perfil nuevo.** Cero cambios de configuración del plane (sin perfiles, keys ni permisos nuevos; sin ampliar `echo-dev`). Recovery operacional único: `docker restart ssh-mcp` por pool de 64 sesiones agotado (503 con `/status` healthy y `connections:[]`; diagnóstico previo exigido). Inspector read-only `stager-identity-inspect.ps1` (write-surface SOLO stdout; target.yaml con líneas sensibles redactadas; re-ejecutable) stageado vía `sftp-upload` certificado a `C:\Windows\Temp\stager-identity-inspect.ps1`, sha256 verificado byte-exacto local/remote `4f6712bc159ea15545f6f90996f57794cf0a30db317d177f355464ed7f9f2818` (3373 B). Una sola ejecución elevada del owner cierra hash del running, padre CIM, CURRENT/PENDING, hashes de `releases\*` y presencia/estado de `StagerReconcile`.
- **Certificación de capability: PASS acotado.** Prueba consumer-side real: PID 1700 + poller ESTABLISHED + ImagePath + ObjectName leídos en vivo; negativo H2 viewer (`run-command` POLICY_DENIED) vigente; boundaries documentados con error exacto. SHA256 del ejecutable en ejecución: INDETERMINADO (único gap; blocker = elevación).
- **Receta de convergencia refinada (OWNER ACTION BUNDLE en `~/aranea/work/f04-window/`):** paso 1 = inspector elevado (única acción requerida); paso 2 = decisión de modelo: (A) stager aceptado ⇒ hash coincidente ⇒ WINDOWS CONVERGED sin instalar; hash distinto ⇒ convergencia POR STAGER (target/manifest 0.2.98 ya publicado en MinIO `deploy`; Stager priorizado por el brief); (B) modelo canónico `C:\SQX` ⇒ `Disable-ScheduledTask StagerReconcile` (si existe), `sc.exe stop StagerRuntime` + `sc.exe config StagerRuntime start= disabled` (como `.\kor`, el stop/disable no requiere su contraseña), staging byte-exacto `C:\staging\`, `Install-EchoForgeMT5Worker.ps1 -Version 0.2.98` (WhatIf → real), `Start-EchoForgeMT5Worker.ps1` con `ENV` owner (WorkerPath `C:\SQX\sqx-mt5-worker\sqx-mt5-worker.exe`, MT5 `C:\MT5\test\…`, JobsRoot `C:\MT5\test\EchoForgeJobs`), proofs (hash == `0bceda4b…`, proceso único con Path legible, poller `sqx-mt5-queue`), rollback canónico `Rollback-EchoForgeMT5Worker.ps1`. Sin segundo poller; sin modificar ETCD; sin campañas.

Evidencia de la misión: agent-run `2026-09-17-hermes-ariadna-f05c-cert-f04-01-access-recovery`, change_log `2026-09-17-f05c-cert-f04-01-access-recovery`.

### Delta de readiness R1 — 2026-09-17 (misión NORMAL F05C-CERT-F04-01-READINESS-R1; corrección manager del preflight aceptada; ningún gate ejecutado, ningún estado de gate cambiado)

El manager corrigió el preflight 2026-09-16: CERT-F04-01 NO está demostrado como BLOCKED por infraestructura; la ausencia de un MCP Temporal dedicado no es un blocker demostrado; el Access Plane congeló política REUSE-FIRST (SSH/logs + read models PostgreSQL antes de crear/extender Temporal); `mt5-kronos-operator` ya existe y no se crean MCPs preventivamente. Misión ejecutada read-only para decidir si CERT-F04-01 puede correr con capabilities existentes. Veredicto: **`CERT-F04-01 READY TO EXECUTE`** (receta exacta emitida; prompt siguiente `F05C-CERT-F04-01-EXEC` producido, NO ejecutado, pendiente autorización manager). Los cuatro blockers del preflight quedan resueltos o reclasificados con evidencia:

- **Temporal — no requerido (T5 permanece DEFERRED).** Ningún dato exigido por la autoridad de CERT-F04-01 requiere MCP Temporal ni history/replay. Mapeo por dato: WorkflowID/RunID/status/TaskQueue/Namespace → worker log `/var/log/symphony/symphony-worker.log` (legible por SSH; líneas `ExecuteActivity` con WorkflowType/WorkflowID/RunID/Attempt; el sanitizador de entropía oculta parcialmente UUIDs) + read models PG vía `sqx-flowkit` (`run stages`); ParentWorkflowID/RunID → estructura logs+refs (no exigido por el gate); history/replay → no exigido por CERT-F04-01 (pertenece a un Temporal dedicado si algún gate futuro lo demuestra). Canal demostrado en vivo: flota Linux `CURRENT=0.2.98` (Zeus/Hera/Kronos), Zeus `stager-runtime` active con worker `/opt/stager/releases/0.2.98/bin/symphony` PID 2633855, `ACTIVATION.json` committed `fedae05f…` 0.2.97→0.2.98, `Started Worker Namespace sqx-prop TaskQueue sqx-main-queue`.
- **Read models PostgreSQL — canal existente demostrado.** El control plane Forge real es `192.168.31.220:5432/trading_systems_test` schema `sqx` (server compartido con `temporal`/`temporal_visibility`/`echo`; los perfiles MCP PG cubren sólo `echo`/`echo-develop`). El canal autorizado es `sqx-flowkit` (CLI read-only entregado por F-05-I; boot ETCD+PG `/sqx-flowkit/production/` provisionado): demostrado en vivo desde Daedalus con `campaign list` exit 0 sobre campañas reales (`a9e73e66` COMPLETED del recert `0.2.92`). Cobertura: `campaign get/list` (ForgeCampaign), `run get/stages` (FlowRun, funnel, timeline de stages), `strategy get` (identidad, participations, magic `{registry_namespace, magic_decimal, allocation_ref, assigned_at}`, `strategy_versions {version_ref, payload_digest, sealed_at}`, `handoffs {idempotency_key, payload_digest, wave_key, version_ref, decision_ref, delivery.state}`). EvaluationRefs/compile evidence por Mongo `forge` RO (certificado). Limitación documentada: los documentos flowkit no emiten las columnas `temporal_workflow_id/temporal_run_id` de `sqx.stage_executions`; su captura en EXEC va por worker log (RunID visible) o CLI Temporal local read-only instalado en Daedalus si el sanitizador impidiera la evidencia (instalar un CLI local no es crear MCP).
- **Gate owner del catálogo CC — STALE, ya resuelto.** Owner decision 2026-09-11 (Magic Number V1 frozen `YYMMIIIDSSS`, XAUUSD=001) materializada en migration `016_magic_number_v1_allocator` (catálogo immutable `sqx.magic_instruments` XAUUSD=1…USDJPY=8 + contadores mensuales) presente en el SHA de la release `b57bfb2`; `CC_MISSING_OWNER_GATE` tiene 0 ocurrencias en producción @ `b57bfb2` y @ `3d0e8c9`; `UseDurableMagicAllocation: true` cableado en el caller productivo; namespace `forge-live`. No existe decisión owner pendiente; no bloquea.
- **Windows/MT5 — capability existe; observación ejercitada vía operator.** Viewer `mt5-kronos` re-demostrado POLICY_DENIED para las lecturas del gate (H2 enforcement intacto). `mt5-kronos-operator` ejecutó lecturas read-only: proceso `sqx-mt5-worker` PID 1700 RUNNING; `C:\MT5\test` con `MetaEditor64.exe`/`terminal64.exe`/`metatester64.exe` (build 2026-09-05), root `EchoForgeJobs`, `MQL5/` y `Tester/`; worker MT5 es proceso foreground owner-operated (no servicio Windows, README de deploy). UNKNOWNs acotados que la propia campaña resuelve como deployment proof del gate: identidad build/path del worker Windows en ejecución no legible con autoridad no-admin (Path/StartTime ocultos, WMI y `tasklist /V` denegados; path canónico `C:\SQX` del README inexistente; exes viejos `f33/f36` de agosto en `C:\stager`), y materialización del binario `0.2.98` Windows en `worker-kronos` no demostrada (rollout 2026-09-13 INCONCLUSIVE vigente). No son gaps de capability: son pasos internos del gate.
- **MinIO/etcd — sin requerimiento de evidencia para este gate.** MinIO (bucket `sqx-strategies`, uploads con etag/size en worker log): los bytes se verifican in-flow (`VerifyCompiledArtifactForSeal`, readbacks) y sus digests quedan en evidencia durable (flowkit + Mongo); la autoridad de CERT-F04-01 no exige export agent-side de preimages — ese requerimiento pertenece a CERT-F04-02 y se resuelve ahí (hoy sin canal dedicado, registrado como REQUIRED_LATER del gate siguiente). etcd: sólo config (postgres/telemetry/temporal/echo ingest); ninguna evidencia del gate vive en etcd; la ausencia de `echo/ingest/base_url`+bearer deja la entrega de handoffs en fail-closed (0 POST) por diseño — es el asunto de CERT-F04-03 (join real), no de este gate. Evidencia startup Zeus: `Echo E-04 ingest no configurado: entrega de handoffs en fail-closed (0 POST)`.
- **Separación explícita de lanes:** la identidad de build del runtime Echo PROD vs E-04 (UNKNOWN del preflight) pertenece a CERT-E04-01, no a este gate. Loki no ingiere logs de `sqx-worker` (canal = archivo por SSH, demostrado). Licencia SQX: se verifica en ejecución (receta PHYSICAL F-04: si reporta expired → STOP owner).

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-readiness-r1`, change_log `2026-09-17-f05c-cert-f04-01-readiness-r1`.

### Delta de preexec lock — 2026-09-17 (misión NORMAL F05C-F04-01-PREEXEC-LOCK; corrección manager del handoff R1 aceptada; ningún gate ejecutado, ningún estado de gate cambiado)

El manager corrigió el handoff R1: `deploy/manifest.json` Git @ `3d0e8c9` declara `0.2.96` y el repositorio no contiene `deploy/0.2.98`, por lo que esas rutas no sustentan la publicación de 0.2.98; los UNKNOWNs Windows que R1 clasificó como pasos internos de la campaña sí gatean la autorización de EXEC y deben cerrarse antes de la campaña. Misión read-only que cerró las tres precondiciones del EXEC. Veredicto: **`NO READY TO EXECUTE`** — A PASS, B FAIL (`BLOCKED — OWNER ACTION REQUIRED`), C PASS, D PASS, E FAIL por B; el prompt maestro `F05C-CERT-F04-01-EXEC` no se redacta hasta cerrar B.

- **A — Autoridad remota 0.2.98: PASS.** Read-back con el CLI `release-authority` @ `3d0e8c9` (`--target 0.2.98`, sólo ListObjects/GetObject sobre MinIO producción): `published_version=0.2.98`, `authority_state=CONSISTENT`, `target_state=EXACT_MATCH` — los 6 objetos `worker/sqx/0.2.98/` (5 linux-amd64 + 1 windows-amd64) verifican byte-exacto (SHA256+tamaño) contra el manifest publicado `worker/sqx/manifest.json` (bucket `deploy`). Artefacto Windows: `worker/sqx/0.2.98/windows-amd64/sqx-mt5-worker.exe`, size 37250048, sha256 `0bceda4badd982b25b01f13ec40a23eb95c8179e28d8a6d955036b30fc7aa474`. Source: `vcs.revision=b57bfb2c3d2c4e0a96d2b3fa654cea41e1a64f43` (buildinfo del exe Windows; `vcs.modified=true` ya explicado por artefactos operacionales ignorados). Publicación: `deployer_screen.log` del worktree de certificación registra 6 uploads + "Manifest publicado `release_version=0.2.98`" entre 2026-09-13T03:20:38Z y 03:20:41Z.
- **B — Windows identidad/convergencia: FAIL.** `worker-kronos`: `sqx-mt5-worker` PID 1700 RUNNING con conexión ESTABLISHED a Temporal `192.168.31.46:7233` (poller vivo), pero identidad de build NO atribuible con autoridad echo-dev no elevada (`Get-Process` Path/StartTime/FileVersion ocultos por integridad; WMI `Win32_Process` Access denied 0x80041003; `C:\ProgramData\Stager` ACL-bloqueado). Inventario legible sin coincidencias: `C:\SQX` inexistente (la instalación canónica nunca se ejecutó), `C:\stager` sólo exes f33/f36 (2026-08-13; SHA256 `F7BBFE65…`/`FC9895B6…`; tamaños 52007936/52034048 ≠ 37250048), `C:\stager_old` binarios era 0.2.40. WINDOWS NOT CONVERGED. Receta owner emitida con scripts canónicos `deploy/windows/sqx-mt5-worker/` @ baseline: paso 0 identity read-back elevado (`Get-Process … Path` + `Get-FileHash` del ejecutable en ejecución; si hash == `0bceda4b…` → converged sin instalar); si no converge → transferir el artefacto verificado a `C:\staging\`, preflight `Install-EchoForgeMT5Worker.ps1 -WhatIf` (instalación versionada `releases\0.2.98\` + backup automático a `backups\<ts>\`), stop/drain del worker foreground actual (sólo owner, ventana dedicada, sin kill indiscriminado, sin sustituir binario bajo proceso activo), install real, `Start-EchoForgeMT5Worker.ps1` (WorkerPath `C:\SQX\sqx-mt5-worker\sqx-mt5-worker.exe`, MetaEditor `C:\MT5\test\MetaEditor64.exe`, terminal `C:\MT5\test\terminal64.exe`, InstallationRoot `C:\MT5\test`, JobsRoot `C:\MT5\test\EchoForgeJobs`), read-back de convergencia (hash `C:\SQX\…\sqx-mt5-worker.exe` == `0bceda4b…` + poll `sqx-mt5-queue` confirmado), rollback con `Rollback-EchoForgeMT5Worker.ps1` desde `backups\<ts>\` + restart; STOP conditions: hash mismatch post-instalación, poller ausente, validación MetaEditor fallida, licencia SQX expired.
- **C — Input canónico CERT-F04-01: PASS.** Mecanismo de arranque: escribir el input JSON en el `watch_dir` del servicio `sqx-watcher` (ETCD key `watch_dir`, default `./input`; mecanismo canónico input/watcher del run físico 2026-09-12). Provenance congelado vía `sqx-flowkit campaign get a9e73e66-5062-44d7-b665-22740383c676` (recert 0.2.92: COMPLETED TARGET_REACHED, `target_finalists=1`, 1 wave, flow_run `4f135030-ad0c-4b41-9e32-f2f8c466c50b`, duración ~9 min) — provenance, NO autorización: el input de EXEC se deriva de esa estructura con los deltas 0.2.98/F-04. Deltas obligatorios del contrato §PHYSICAL: XAUUSD, dirección única `L`, promotion `2.0.0` (`finalist_promotion`), robust selection = 1, sin requested-magic 888111, `UseDurableMagicAllocation: true` productivo @ release. Identidad: `request_id` nuevo por run con patrón `<nombre>-<UTC yyyyMMddTHHMMSSZ>-<hex8>`; routing RequestID+SourceKey y durable FlowIntentToken/FlowRunRef. Namespace Temporal `sqx-prop`; queues `sqx-main-queue` (Linux) / `sqx-mt5-queue` (Windows); namespace Forge `forge-live`. Límites: `target_finalists=1`, `max_waves=1`; timeout/cancel: Temporal nativo (`mt5.timeout` → StartToClose +2 min) y cancel del run propio. Observación: worker log por SSH + `sqx-flowkit` campaign/run/strategy + Mongo `forge` RO. Evidencia esperada: release/deployment proofs, FlowRun/workflow IDs, Decision V2, allocation/readback, compile inputs/outputs/log, EvaluationRefs, StrategyVersion, HandoffManifest y delivery state.
- **Delivery fail-closed: CLÁUSULA CITADA.** Contrato F-04 §PHYSICAL: la cadena física termina en `HandoffManifest` y "E-04 runtime/deploy/join es one-shot posterior (T2.13)"; "No mezclar E-04 runtime config/ETCD/gateway con C4". Delivery matrix: `Echo unavailable → HANDOFF_CREATED` (retry, non-terminal) y "Si E-04 aún no tiene read: no fingir INTEGRATION PASS". El fail-closed (0 POST, entregables en `HANDOFF_CREATED` por falta de `echo/ingest/base_url`) es por tanto resultado compatible con CERT-F04-01; la entrega `INGESTED`/join real pertenece a CERT-F04-03. El delivery state del run es evidencia durable exigida, no gate de PASS.
- **D — Observación: PASS** (canales re-demostrados hoy: SSH worker log, `sqx-flowkit` exit 0 contra producción, Mongo `forge` RO certificado, operator Windows para lecturas autorizadas). **E — Blockers: FAIL por B**; sin otros blockers concretos.
- **Único siguiente paso:** owner ejecuta el paso 0 (identity read-back elevado en `worker-kronos`) y, si hash ≠ `0bceda4b…`, la receta de convergencia canónica; con Windows converged el manager re-evalúa A–E y autoriza la redacción y despacho de `F05C-CERT-F04-01-EXEC`.

Evidencia de la misión: agent-run `2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-preexec-lock`, change_log `2026-09-17-f05c-cert-f04-01-preexec-lock`.

### Preflight de entrada a F-05-C — 2026-09-16 (misión NORMAL F05C-ENTRY-PREFLIGHT, read-only; ningún gate ejecutado, ningún estado cambiado)

F-05-I está CLOSED (`IMPLEMENTED / SOURCE VERIFIED`, cierre declarativo commit `3d0e8c958765da23840e00bf1a0ac017fc47597a`, HEAD == `origin/codex/f05-release-prep` verificado; handoff F-05-C presente en repo: read-surface + conformance checklist + manifest template con los 7 gates OPEN). El orden frozen de ejecución permanece intacto: **CERT-F04-01 → CERT-F04-02 → CERT-E04-01 → CERT-F04-03 → CERT-F05-01 → CERT-F05-02 → CERT-F05-03**. Veredicto del preflight: **ningún gate es ejecutable hoy**; el primer gate del orden está `BLOCKED BY INFRA`. Las certificaciones históricas B1A/B1B/B2/F-03 conservan su alcance original y no se extrapolan a la release actual (matriz @ `3d0e8c9` verificada: 7 gates DEFERRED, allowlist histórica intacta).

- **CERT-F04-01 — BLOCKED BY INFRA.** Released `0.2.98` desde `b57bfb2` ✓ (as_of 2026-09-13); Linux Zeus/Hera/Kronos OBSERVED/RUNNING vía `stager-runtime.service` ✓; observación runtime Echo ✓ (viewer `echo-runtime-prod` + observability, GAP-ECHO-004 CLOSED). Faltan: (a) capability Temporal de observación/control — sigue `REQUIRED_LATER` deferred por decisión owner en [[ACCESS-CERTIFICATION]], y `etcd` sin capability (`UNKNOWN_NEEDS_SOURCE_PROOF`); (b) gate owner del catálogo CC (allocation real); (c) canal de evidencia Windows: el viewer `mt5-kronos` sigue con policy gap (sólo identidad), el perfil `mt5-kronos-operator` está certificado (H2 smoke) pero la observación de servicio/release/path **no fue ejercitada** — última campaña física 2026-09-13 quedó rollout INCONCLUSIVE con Windows sin evidencia; (d) MinIO/object-store sin capability MCP en el plane (lectura de artefactos del flujo quedaría sin canal dedicado). Ningún flujo F-04 real ejecutado bajo `0.2.98`: no existen FlowRun/workflow IDs, Decision V2, allocation/readback, compile logs, EvaluationRefs, StrategyVersion ni HandoffManifest reales que certificar.
- **CERT-F04-02 — BLOCKED** (depende de CERT-F04-01 PASS; además el almacenamiento durable/exportable de preimages necesita canal — MinIO fuera del plane). No existe golden auténtico: `FORGE_GOLDEN_FIXTURE_PENDING` sigue vigente; S0/fakeconsumer/sintéticos no califican.
- **CERT-E04-01 — BLOCKED** (clase C HARD; depende de CERT-F04-02). E-04 integrado: `a99f9a63` es ancestro de `origin/master` echo @ `5dd998f1` ✓ source-side. **UNKNOWN material nuevo:** la identidad de build del runtime Echo PROD (`.71`) vs E-04 no está demostrada — logs vivos exponen `service.version=2.0.0` sin SHA, los PIDs observados (core desde ~ago20, gateway desde ~sep09) preceden al merge E-04 (2026-09-12) y el endpoint `POST /api/v1/forge/promotions` no fue verificado en runtime; falta deployment proof o verificación de identidad de build antes de considerar ese prerequisto satisfecho. T21/AC-37 permanece OPEN.
- **CERT-F04-03 — BLOCKED** (cadena completa: CERT-F04-02 + CERT-E04-01 PASS).
- **CERT-F05-01 — BLOCKED POR ORDEN FROZEN.** Su prerequisto de producto quedó **recién cumplido** con el cierre F-05-I: existe candidato cohesivo posterior a `b57bfb2` (`codex/f05-release-prep` @ `3d0e8c9`, F-01…F-04 integrados y F-05-I implementado/source verified). No ejecutar antes de tiempo: el trigger exige la cadena completa previa. Al momento de ejecutar requerirá publicar release cohesiva desde rama autorizada y deployment proofs por runtime (incluido el canal Windows, hoy sin evidencia).
- **CERT-F05-02 / CERT-F05-03 — BLOCKED** (cadena; CERT-F05-03 es el único que puede declarar F-05 `PHYSICALLY CERTIFIED`/`CLOSED`). Estado de licencia SQX vigente no verificado en este preflight.
- **Dependencia dominante a resolver primero (decisión owner/manager):** habilitar observación/control Temporal (o equivalente autorizado — re-visitar la decisión T5 deferred del Access Plane); en paralelo: gate owner del catálogo CC, canal de evidencia Windows vía `mt5-kronos-operator` (sólo lectura, policy acotada) y deployment proof de identidad Echo vs E-04. Sin esos cuatro ítems, la cadena CERT no arranca.
- **Misión NORMAL de readiness propuesta (pendiente autorización manager; NO ejecuta ningún gate):** (1) demostrar identidad de build del runtime Echo PROD vs E-04 (deployment proof / read-back de identidad vía viewer u observability, read-only); (2) ejercitar el canal de observación Windows `mt5-kronos-operator` para servicio/release/path/poller (sólo lectura, con justificación de autoridad mínima); (3) producir el diseño de la capability Temporal read-only para el Access Plane (insumo para la decisión owner sobre T5). Evidencia del preflight: agent-run `2026-09-16-zcode-glm-5.3-flash-f05c-entry-preflight`, change_log `2026-09-16-f05c-entry-preflight`.

### Delta de readiness por certificación de accesos — 2026-09-15b (GAP-ECHO-004 CLOSED)

Actualiza el delta anterior tras instalar el owner seed en `.71` y recertificar el viewer `echo-runtime-prod` end-to-end. **No cambia ninguna clase A/B/C ni ejecuta gates productivos.**

- **GAP-ECHO-004 → CLOSED:** owner seed instalado (identidad dedicada `echo-dev` uid/gid 1001, sudo DENIED; key `echo-dev.pub` del plane en `authorized_keys`) y viewer `echo-runtime-prod` certificado desde Daedalus real: identity PASS (`echo-dev@echo`, sin sudo), initialize/tools-list PASS, listeners 80/9080/9090/8080/8090, negative `run-command` → POLICY_DENIED MUST DENY, leak CLEAN. Runtime observado: `echo-gateway` (PID 713) y `echo-core` (PID 110701) RUNNING como `kor`, `echo-functions` (StateFun) RUNNING; **Bridge NOT_DEPLOYED** (registrado con evidencia; no se levanta).
- **E-02:** el gate físico Gateway pasa de BLOCKED-pending a **observación SSH directa PASS** (procesos/listeners/identity); sigue sin control verbs (restart = PROD, fuera de scope). `E02_PHYSICAL_CERTIFICATION_BLOCKED` se mantiene por sus gates propios (Hasura DEV roles/hook, Kafka PublishSync/redelivery, Flink restart/recovery), todos con capability certificada disponible para ejecutarse.
- **CERT-F04-01/CERT-F04-02 (F-04 T2.12/T2.11):** el prerrequisito de observación del runtime Echo queda **completo por vía directa** (viewer SSH) además de la observabilidad. Sigue `BLOCKED BY INFRA` por Temporal/equivalente.
- **CERT-E04-01 (E-04 T21/AC-37):** sigue `HARD BLOCKED` (clase C) por golden Forge auténtico + join real; el prerrequisito de observación runtime ya no es gap.
- **Condición de acceso vigente:** todas las capabilities requeridas del Access Plane están operacionales y certificadas (viewer runtime incluido). El trigger de reactivación de este backlog queda **abierto**; la limitación conocida del viewer es de cobertura (sin systemctl/docker/curl productivo, journal propio únicamente) — los logs productivos siguen por `aranea-observability-ro`.
- **Deuda separada no bloqueante:** host key ED25519 de `.71` idéntica a `sqx-zeus` (clon sin regenerar); rotación owner-side opcional con actualización de `trustedHostKey` del perfil en el mismo cambio.

### Delta de readiness por certificación de accesos — 2026-09-15 (gaps stale reconciliados)

Actualiza el delta 2026-09-14 tras el cierre H1/H2, la certificación `aranea-observability-ro` (:3009, PASS 12/12 + consumer PASS) y la resolución del target runtime Echo. **No cambia ninguna clase A/B/C ni ejecuta gates productivos.**

- **Gaps stale eliminados:** Hasura DEV/PROD (`aranea-hasura-dev-admin`/`aranea-hasura-prod-ro`), Kafka DEV (`aranea-kafka-dev-admin`) y Flink DEV (`aranea-flink-dev-admin` + `docker-echo-dev-operator`) están **certificados y operativos** desde 2026-09-12/13 — los GAP-ECHO-001/002/003 de la matriz de 2026-09-14 eran históricamente stale. Observabilidad PROD (GAP-ECHO-007) queda cubierta por `aranea-observability-ro`.
- **E-02:** sus verificaciones físicas pendientes contra Hasura-DEV/Kafka/Flink **ya tienen capability certificada para ejecutarse** (siguen sin ejecutarse: no se corrieron en esta sesión). `etcd`/tokens reales siguen `NOT_OBSERVED` (sin capability). Clase B se mantiene; `E02_PHYSICAL_CERTIFICATION_BLOCKED` ahora depende sólo de ejecutar esas verificaciones y del target Gateway.
- **E-02 Gateway physical:** target real resuelto — `.211` muerto; runtime vivo = PROD `192.168.31.71` (`prod.echo.gateway.lab.aranea`, `/health` 200). Viewer SSH `echo-runtime-prod` staged; verificación SSH pending owner key install. Observación vía `aranea-observability-ro` operativa (logs `service=echo-core` `env=production` en vivo).
- **CERT-F04-01/CERT-F04-02 (F-04 T2.12/T2.11):** el prerrequisito de observación del runtime Echo queda **parcialmente cubierto por observabilidad** (logs/metrics PROD en vivo); la observación directa SSH queda pendiente de la única owner action (instalar `echo-dev.pub` del plane en `.71`). Sigue `BLOCKED BY INFRA` por Temporal/equivalente.
- **CERT-E04-01 (E-04 T21/AC-37):** sigue `HARD BLOCKED` (clase C); el prerrequisito de observación runtime queda parcial (observabilidad sí, SSH viewer pending).
- **CERT-F04-03 / CERT-F05-01…03:** sin cambio; dependen de los gates anteriores.
- **Condición de acceso vigente:** H1/H2 RESOLVED; el trigger de reactivación de este backlog queda **abierto sólo para la certificación SSH viewer sobre `.71`** (post owner action) — el resto de capabilities requeridas ya está operacional.

### Delta de readiness por certificación de accesos — 2026-09-14

Fuente: [[ACCESS-CERTIFICATION]] (run 2026-09-14, veredicto `ACCESS_CERTIFICATION_PARTIAL`). Este delta **no cambia ninguna clase A/B/C ni cierra carriles**; registra qué superficies de acceso quedaron verificadas físicamente y qué gaps siguen bloqueando cada gate.

- **Verificado y disponible para gates físicos:** PostgreSQL PROD-RO y DEV-RW (DML sin DDL en DEV), MongoDB forge RO/RW, Kafka DEV admin (con `delete_topic` eventual — post-condición obligatoria de cleanup), Flink DEV control-plane read, SSH operator sobre SQX/MT5/docker-echo-dev (identidades re-verificadas).
- **E-02:** sus verificaciones físicas pendientes contra `psql`/Kafka/Flink/Hasura-DEV ya tienen capability certificada para ejecutarse; `etcd`/tokens reales siguen `NOT_OBSERVED` (sin capability). Clase B se mantiene.
- **CERT-F04-01/CERT-F04-02 (F-04 T2.12/T2.11):** bases de acceso re-verificadas; siguen `BLOCKED BY INFRA` por la ausencia de observación del runtime Echo y de capability Temporal/equivalente autorizada. Prerrequisito nuevo explícito: capability de observación del host Echo runtime (192.168.31.71:8090, visto en webhooks Hasura).
- **CERT-E04-01 (E-04 T21/AC-37):** sigue `HARD BLOCKED`; el mismo prerrequisito de observación del runtime Echo es el gap dominante. Clase C se mantiene.
- **CERT-F04-03 / CERT-F05-01…03:** sin cambio; dependen de los gates anteriores.
- **E-05…E-13:** sin cambio de clase; para sus futuros gates físicos queda disponible la superficie DEV certificada arriba, y quedan nombrados como `REQUIRED_LATER`: observación runtime Echo, etcd, observabilidad (OBS3), Temporal (T5), Kafka/Flink PROD.
- **Condición de acceso vigente:** el trigger de reactivación de este backlog **sigue cerrado** hasta resolver H1 (credenciales expuestas por `export_metadata`), H2 (boundary viewer SSH no aplicado) y la capability de observación del runtime Echo.

### Próxima tarea única recomendada para NORMAL

**Consumidas:** la corrección source C1 (`SOURCE FIX PASS`), la release C2 (`RELEASE/ROLLOUT PASS`), el re-run físico RERUN (`BLOCKED`, defecto encoding), la corrección source C3 (`SOURCE FIX PASS`), la misión C4 (microfix UTF-8 + release `0.2.100` + rollout, `RELEASE/ROLLOUT PASS`), el re-run físico RERUN-3 (`BLOCKED`, build 6182), la certificación de compatibilidad C5 (`INSUFFICIENT_EVIDENCE`, `READ_ONLY_ARTIFACT_FETCH_REQUIRED`), su re-adquisición C5A (`ACQUISITION BLOCKED`: 403 de política IAM demostrado en HeadObject, GetObject y GET presignado sobre el objeto exacto, con conector correcto e identidad RO válida), la verificación de autorización C5B (`OWNER_APPROVAL_REQUIRED`), **la certificación + source fix C6 (`COMPATIBLE` + `SOURCE_FIX_READY_FOR_REVIEW`)** **la corrección de revisión C6R (`SOURCE_READY`: R1–R4 cerrados en el commit adicional `c1d24c1…` sobre `bcf67be…`, publicado en origin con read-back) y la release + rollout C7 (`RELEASE/ROLLOUT PASS`: `0.2.101` publicada desde `c1d24c1…` integrado por fast-forward en `codex/f05-release-prep` y desplegada/certificada en flota completa 4/4) y la corrección source C9 (`SOURCE_FIX_READY_FOR_REVIEW`: fix Apply Authority en el assembler del seal — campo `ApplyEvaluationRef` fijado exclusivamente desde Durable Apply, assembler fail-closed, consumer intacto — commit único `87a3ab944c5ce706ac3fe7766fa86af32189cc05` en `codex/f05-seal-apply-authority-fix` desde `c1d24c1…`, publicado en origin con read-back), **la corrección de revisión C9R (`SOURCE_READY_FOR_MANAGER_REVIEW` → aprobada por el manager: fail-closed byte-exact de `ApplyEvaluationRef` en el fan-in + tests RED/GREEN del boundary + test compuesto real, commit `66faa42debba18cd0dd09de127ae1374ea2a07f0` sobre `87a3ab9…`)** **y la release + rollout C10 (`RELEASE/ROLLOUT PASS`: `0.2.102` publicada desde `66faa42…` integrado por fast-forward en `codex/f05-release-prep` con verification gate PASS — 21 fallos de workflows idénticos al baseline, worker/runtime PASS con fixture auténtico, release test gate 53/53 coverage 99.1% — y flota 4/4 convergida con SHA instalado == artifact, `MT5_PROCS=0` y 0 Running en `sqx-prop`)**: el owner del Access Plane resolvió la adquisición fuera del carril MCP (HTM auténtico de RERUN-3 entregado físicamente a `daedalus`, integridad verificada contra el SHA256 durable `21917e14…8b5c`, 13074872 B), C6 demostró la compatibilidad del build 6182 con `mt5-report.v1` (7/7 crosschecks PASS con deltas < 0.005) y publicó el source fix, y C6R cerró los cuatro pendientes de la revisión del manager: R1 diagnóstico genérico («server row with a build from the certified matrix», sin lista inline duplicada, con test que congela el error tipado y la ausencia de matriz falsa), R2 SPEC §3.2 con 6182, R3 modo obligatorio de release `SQX_MT5_REQUIRE_B6182_FIXTURE=1` (variable ausente/archivo inexistente/size incorrecto/SHA256 incorrecto/test omitido → FAIL, skip explícito sólo en desarrollo, sin descargas ni credenciales, tabla de decisión congelada por test) y R4 coverage **81.6% → 99.1%** (baseline `a440ac4` = 81.5% y C6 = 81.6% medidos en condiciones comparables; 18 tests de contrato enfocados en `contract_test.go` sobre caminos críticos; residual ~1% documentado como ramas defensivas inalcanzables). Validación ejecutada de C6R: suite enfocada 6182 11/11 PASS con bytes auténticos (6182 aceptado, 6181/6183 rechazados, 7/7 crosschecks PASS); paquete completo modo release con fixture auténtico 53 PASS / 0 skips / coverage 99.1%; modo release sin fixture exit 1 (7 FAILs tipados, 0 skips); modo release con SHA incorrecto exit 1; modo dev sin fixture 46 PASS + 7 skips explícitos + 0 FAIL; gofmt/vet/build del scope limpios (fallos preexistentes de `sqx/tools` y gofmt de `sqx/activities/worker/` demostrados en HEAD pristine vía stash). **Estado vigente (tras C10):** `CERT-F04-01 = BLOCKED / READY TO RERUN-5`; el cuarto defecto determinístico quedó **corregido en source y aprobado** (fix C9+C9R = `66faa42debba18cd0dd09de127ae1374ea2a07f0`): `StrategyArtifact.ApplyEvaluationRef` (`apply_evaluation_ref,omitempty`) fijado exclusivamente desde el resultado exitoso de Durable Apply, assembler del seal consumiendo el campo fijado con fail-closed ante autoridad ausente y miembros duplicados, fan-in del final reretester exigiendo la autoridad byte-exact (drop/replace/fabricar → `contract_conflict`), productor del final reretester intacto, consumer `forge_seal_handoff.go` sin cambios; release `0.2.102` publicada con read-back `EXACT_MATCH/CONSISTENT` y desplegada en flota 4/4 (Zeus PID 2801657 / Hera 1390690 / Kronos 1353062 / Windows PID 6648, SHA instalado == artifact, poller ESTABLISHED, singleton, `MT5_PROCS=0`, 0 Running en `sqx-prop`, releases históricas intactas). El re-run #4 había demostrado físicamente en verde todo el tramo previo: funnel completo, allocations durables `26090011006–009` (write-once, el allocator continuó desde 005), 4 compiles físicos Windows `0 errors`, 4 backtests reales ≈47–55 min, 4 HTM auténticos build 6182 byte-verificados (SHA256 local sobre bytes reales == durable vía lectura MinIO ya autorizada — el blocker `READ_ONLY_ARTIFACT_FETCH_REQUIRED` de C5A/C5B quedó cerrado), `mt5_reconcile_v1` ×4 COMPLETED en producción con 6182 aceptado y 7/7 crosschecks, ranking y promotion con 4 finalists_v2 (warnings auténticos `PNL_SIGN_FLIP`). **Próximo paso único (RERUN-5):** re-run físico del gate desde cero sobre `0.2.102`/`66faa42…` con identidades todas nuevas (NO reutilizar `d400dba0`/`794b0e02`/`b41bd7fc`/`a470e565`/`efc4ac0c`/`5c16ab31`/`1fc62ab5`/`b6a1edea`/`76aefcec`/`8a6b7dda`/`b98aa99b` ni workflow IDs previos; allocations `26090011001–009` write-once permanecen). Notas vigentes: Loki no ingiere al worker symphony Forge (evidencia del run vía historial Temporal `sqx-prop` con el scratch `/tmp/thist/`); la re-verificación viva `MT5_PROCS=0` quedó cerrada por la captura C9 del 2026-09-19 y re-capturada fresca por C10; los registros C5A/C5B/C6/C7/RERUN-4/C9/C10 viven en `~/aranea/work/f04-cert-f04-01-{c5a,c5b,c6,c7,rerun4,c9,c10}/`; la sesión MCP Mongo RO quedó caída el 2026-09-19 (`session not found`) — canal no disponible hasta recover del conector. Al PASS continuar el orden frozen (CERT-F04-02 → CERT-E04-01 → CERT-F04-03 → CERT-F05-01/02/03); al BLOCKED, una sola causa primaria y STOP. **El re-run físico RERUN-5 (`BLOCKED`, defecto #5) consumió ese próximo paso y actualiza el estado: `CERT-F04-01 = BLOCKED / READY TO RERUN-6` tras demostrar en verde sobre `0.2.102` todo el tramo funnel→promotion con cohorte de 3 (campaña `c42dd637…`, FlowRun `fbac1286…`, allocations `26090011010–012` write-once) y validar en producción el fix C9/C9R (request del seal con `ApplyEvaluationRef != CompileEvaluationRef` ×3); el fallo único es el read-back de magic en el seal — `magicreadback.FromSQX` parsea como XML plano el `strategy.sqx` contenedor ZIP del producer Apply (`readback.go:51` @ `66faa42`; byte-verificado `9ac377b9…` con `26090011010` dentro de `strategy_Portfolio.xml`; repro exacto del error). Próximo paso único: decisión manager del fix (unzip+parse interno vs exporter XML plano) con tests sobre bytes reales → release nueva → RERUN-6 desde cero con identidades todas nuevas (NO reutilizar `d400dba0`/`794b0e02`/`a470e565`/`efc4ac0c`/`1fc62ab5`/`b6a1edea`/`8a6b7dda`/`b98aa99b`/`c42dd637`/`fbac1286` ni workflow IDs previos; allocations `26090011001–012` write-once permanecen).** **La corrección source C11 (`SOURCE_FIX_READY_FOR_MANAGER_REVIEW`, commit `2993da0…` en `codex/f05-sqx-zip-magic-readback-fix` desde `66faa42…`) consumió la mitad source de ese paso — `FromSQX` lee ahora el contenedor ZIP con la entrada canónica exacta, gate auténtico `26090011010` sobre los bytes reales de RERUN-5, RED/GREEN demostrado, cobertura 100% y conjunto de fallo idéntico al baseline — y actualiza el estado: `CERT-F04-01 = BLOCKED / READY TO RERUN-6` pendiente la revisión del manager del diff `66faa42…2993da0`; el resto del paso (release/rollout C12 → RERUN-6 desde cero con identidades todas nuevas) sigue la misma secuencia congelada.** **La misión C13 (`CERT_F04_01_PASS`, 2026-09-20) consumió el ciclo completo restante — manager review aprobada del fix `25a5122…`, release `0.2.104` publicada y desplegada 4/4, recuperación natural del attempt 17 sobre los workers nuevos con 5/5 `HANDOFF_CREATED` y campaña `f50e5cdb…` `COMPLETED/TARGET_REACHED`, veredicto de certificación por lectura literal de la SPEC con atribución por etapa (`2993da0` productoras / `25a5122` seal+delivery) y `RERUN-7 NOT_REQUIRED` — y actualiza el estado: `CERT-F04-01 = PHYSICALLY CERTIFIED (PASS)`. Próximo paso único (nuevo): **CERT-F04-02 — golden auténtico / T2.11**, ahora desbloqueado: capturar desde la ejecución física `c71687ca…` el golden Forge completo (5 StrategyVersions selladas `6d3cab1a/04ab1176/d41f3468/491c2094/eefefdbd`, 5 manifests write-once con sus IdempotencyKeys, 5 effective-inputs, 20 artefactos compile/readback SHA-anclados, 10 EvaluationRefs, DecisionRef `sha256:ed703eeb…`), preservando hashes y provenance byte-exacto (los 20 artefactos ya están re-verificados en `~/aranea/work/f04-cert-f04-01-rerun6/g1-stage/`); no ejecutar CERT-E04-01 ni CERT-F04-03 antes.**

### Trigger de reactivación

Activar este backlog cuando las capabilities requeridas del Aranea MCP Access Plane estén operacionales y certificadas, y los runtimes objetivo sean observables y operables por la policy correspondiente. En ese momento ejecutar estrictamente CERT-F04-01 → CERT-F04-02 → CERT-E04-01 → CERT-F04-03 → CERT-F05-01 → CERT-F05-02 → CERT-F05-03; no usar una fecha calendario ni saltar gates.

## Fuentes

- [[Echo Forge — Factory V2 Completion]] y [[Echo Forge — F-04 Magic allocation, version seal and handoff]]: F-04/F-05 scope, SHA, release, T2.11–T2.13 y evidencia histórica.
- [[Echo — Producto Integrado]] y [[Echo — Live Platform V1]]: DAG E-01…E-13, E-04 T21/AC-37 y reglas de paralelismo.
- [[Echo — E-04 Forge Ingestion E1]]: E-04 INTEGRATED, T21 POST-INTEGRATION, synthetic ≠ cross-lane golden.
- [[AGENT-PLATFORM - MCP Access Plane]] y [[aranea-agent-dev]]: capabilities/policy de acceso Aranea y límite de no inventar autoridad.
- Decisión explícita del owner en esta sesión: desarrollo continúa; certificación física se difiere, no se waiva.
