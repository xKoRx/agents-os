# Echo — E-10 Manager Erratum M1 · 2026-09-21

**Authority:** manager único de Echo. **Scope:** corrección acotada a la planificación `[[Echo — E-10 Strategy Quality and Eligibility]]` v1.0.0, NO otra arquitectura. **Decision:** `PLANNING_REVIEWED_WITH_M1 · CLASS_A_T00_T03_CONDITIONALLY_AUTHORIZED · E10_IMPLEMENTATION_NOT_YET_STARTED · E09_COVERAGE_PENDING · E10_PHYSICAL_PENDING · FINAL_CLOSED=NO`. Este erratum PREVALECE sobre toda instrucción contradictoria de la nota E-10 v1.0.0; el primer ejecutor debe corregirla antes de iniciar source y registrar enlace/delta. La etiqueta `PLANNING_FROZEN` de la nota original significa propuesta TOP, NO aceptación sin M1.

## Git y ownership — M1-A (invariante duro)

Repositorio `xKoRx/echo`. `master` estable `5dd998f16aea7b2821f460188718d7a6d279829c`. Única feature remota de desarrollo `feature/e09-execution-copy-reconciliation-fidelity` @ `d69e1ee35d95f84ddaec812a956f5837d799fbe3`, verificada por GitHub el 2026-09-21. Dos `rescue/*` son refs de preservación, no carriles de desarrollo. Queda **revocada** en TODA la nota E-10 la instrucción de crear `feature/e10-strategy-quality-eligibility` o basarse en `master`/SHA antiguo. E-10 se integra secuencialmente sobre la MISMA feature e09 @ d69e1ee3 o su HEAD remoto avanzado verificable; un único ejecutor con ownership, FF-only, sin force/rebase/reset destructivos; si hubo avance concurrente, STOP y delta antes de escribir. El nombre e09 es histórico, no una limitación de ownership de producto. NO branch remota adicional. Ningún merge a master en este mandato.

## Alcance real de escrituras — M1-B

La nota S4/T05 exige `CanonicalWriter.Write` de E-05; código verificado `v3/sdk/postgres/canonical_writer.go`: inserta Scope→TradeSet→MetricSet transaccionalmente. Migración `063_analytics_convergence_a0.up.sql` crea `echo.canonical_scopes`, `echo.canonical_trade_sets`, `echo.canonical_metric_sets`, write-once. Por tanto la frase S11/G5 «cero escrituras a 001–068» es contradictoria y se reemplaza por: E-10 es read-only en **001–062 y 064–068**; en 063 sólo INSERT write-once mediante `CanonicalWriter.Write` en esas tres tablas para nuevos scopes forward de la versión; replay idempotente, ningún UPDATE/DELETE, sin alterar migración 063 ni tocar otros objetos. En 069 se autorizan escrituras exclusivamente a objetos E-10. Validar fingerprint completo del resto, preexistentes intactos en 063, provenance/digests de los nuevos y cero publicaciones/órdenes. Toda técnica de test que temporalmente altera schema requiere PG 17.x propia, preflight de identidad, transacción/restauración y preimage/postimage; jamás DEV compartido ni PROD.

## Política, abstención y estado de datos — M1-C

S8 es autoridad: `EligibilityDecision` SOLO puede emitirse con una política owner-ratificada cuyo ref y digest resuelvan y coincidan. `POLICY_UNAVAILABLE`, digest mismatch, Expectation ausente o sin evidencia suficiente NO pueden producir una `EligibilityDecision` válida ni un ref que E-11 interprete como autorización. Se permite diagnóstico de assessment/no-evaluabilidad con razón explícita y sin autoridad; si el esquema 069 exige expectation_ref presente, NO fabricar ref/assessment para Expectation ausente: devolver resultado tipado no-autorizante, persistir únicamente lo que permita el contrato sin inventar identidad. Abstención consumible por E-11 como `EligibilityDecision` requiere política válida y decisión de abstención sellada; ausencia de decisión obliga a E-11 a CASH por su propio gate fail-closed, sin fabricar decisión E-10. Corregir matriz E10-02/08/09/16 y tests.

El vector de **conocimiento de datos** distingue `UNKNOWN` (coverage/basis/integridad no demostrables), `KNOWN` y cuando proceda evidencia conflictiva; el **juicio frente a policy** distingue `INSUFFICIENT` (información válida pero menos muestra/calendario que lo exigido), `DEGRADED` (degradación económica positivamente demostrada), `QUARANTINED` (violación DQ material), además de otros estados frozen. No usar `INSUFFICIENT` como comodín semántico para coverage UNKNOWN o política ausente; razón tipada explícita. No asignar score/0/ELIGIBLE a UNKNOWN. Reconciliar S5/S6 y matriz preservando taxonomía del master y sin añadir política inventada.

## Sellado temporal verificable — M1-D

`EXPECTATION_SEALED_BEFORE_FORWARD` no puede demostrarse sólo con `sealed_at` suministrado por un caller: para certificar pre-forward, exigir timestamp/provenance de sellado registrado por la base en la inserción write-once, existencia verificable de refs Forge/E-04/E-05 al sellado y confrontación con admission barrier y hechos forward. Una Expectation redactada después de comenzar la observación no adquiere precedencia por backdating de un campo. Sin evidencia histórica suficiente: no evaluable/UNKNOWN, jamás elegibilidad. Durante clase A se demuestra la regla con fixtures etiquetados como tales; clase B/C exige evidencia real. No modificar E-04/06/07 ni catálogo S0 para reparar vacíos.

## Orden autorizado y bloqueos

La planificación A/B/C y Reference-only se mantienen. Migración `069` es la reserva propuesta del TOP: reconfirmar que esté libre en HEAD real antes de escribir; no cambiar numeración unilateralmente. Primera entrega autorizada NORMAL es **T00–T03**, no T00–T10: (T00) corregir nota Agents-OS y materializar SPEC/PLAN/TASKS/VERIFICATION con M1 explícito, remover nombres de branch obsoletos, definir contrato determinista exacto para identidad/digest (reusar canon S0, no inventar hash ambiguo), failing-set baseline; (T01) tipos/validaciones/estados puros sin policy thresholds; (T02) migración 069 con constraints e interlocks sin tocar 001–068, harness E-10 con gate propio antes de DROP/DDL, test colisión; (T03) stores 069 idempotentes/conflict fail-closed, timestamps DB y pruebas de replay/temporalidad. Detener tras T03, commits atómicos y push FF a única feature, evidencia y handoff Manager. T04–T10 quedan EN COLA, NO autorizadas por este erratum: evitar una corrida gigante de diez WPs sin gate intermedio.

STOP antes de implementación si la nota no puede reconciliarse, el formato canónico del digest no está determinable por autoridad existente, identidad temporal no es implementable bajo contrato, requeriría nuevo tipo S0, mutation fuera de 069/exception 063, o existe avance remoto concurrente. En STOP entregar defecto exacto y propuesta acotada; no rediseñar fase.

## Evidencia y límites

Review GitHub en esta sesión: nota E-10 master `main/10-projects/Echo/agentes/Echo — E-10 Strategy Quality and Eligibility.md` (S4/S5/S8/S11/S12, PLAN, NORMAL-PROMPT); `canonical_writer.go` y `063_analytics_convergence_a0.up.sql` en baseline d69e1ee3; refs remotos `master`, feature/e09 y dos rescates. El manager no ejecutó tests PG ni validó ambiente DEV en vivo. El handoff TOP declara documentación de proyecto materializada y sin código: el manager verificó la nota en repo Agents-OS, no supone que el cambio se refleje ya en cada artefacto padre.

**Siguiente paso:** NORMAL T00–T03 en única feature con M1 como autoridad superior. Tras T03, review manager para decidir T04–T10. E-09 coverage 88.6% sigue PENDING, ninguna certificación física/producción es inferida por iniciar E-10.