---
agent: forge-functional-cartographer
role: Forge Functional Cartography
task_id: KBC-C
status: COMPLETE
baseline: symphony 9fad768ccd1f9d25ebb535a2d26edb3d74556c10 (feature/f04-magic-version-handoff) · origin/master 0b9742b09019526a8119f086199d15d1f0d42cb1 · dirty_files: 1
inputs: ~/go/src/github.com/xKoRx/symphony (sqx/ module, internal/, cmd/, specs/); echo SDK pinned module github.com/xKoRx/echo/v3/sdk/contracts v0.0.0-20260910031519 (leído vía imports/código, no checkout)
scope: cartografía funcional Echo Forge read-only
started_at: 2026-09-13T00:05:16-03:00
updated_at: 2026-09-13T01:22:57-03:00
---

## Assignment

Task KBC-C (Fase C, reintento único tras fallo de infraestructura): mapear READ-ONLY el comportamiento implementado HOY de Echo Forge en el repo `symphony` al baseline `9fad768c` (branch `feature/f04-magic-version-handoff`), con evidence packs claim → file → símbolo → test/config → confidence, en este único artifact. Distinguir branch F-04 vs `origin/master` cuando difieran materialmente. Crítico: verificar desde el lado Forge qué emite Forge hacia Echo hoy (¿HTTP? ¿fakeconsumer?).

## Baseline

- HEAD verificado: `git rev-parse HEAD` = `9fad768ccd1f9d25ebb535a2d26edb3d74556c10`, branch `feature/f04-magic-version-handoff` — coincide con el baseline de campaña declarado. `origin/master` = `0b9742b09019526a8119f086199d15d1f0d42cb1` — coincide.
- Working tree DIRTY (1 archivo): `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` (16 líneas: 8+ / 8-, diffs de valores numéricos del fixture). Sólo se ejecutó `git diff --stat` sobre él; ningún test fue corrido por mí. Consumidor del fixture: `sqx/activities/worker/phase4_performance_test.go`. No se leyó ningún archivo vía `git show` porque la única divergencia es ese fixture de test; todo el código fuente está limpio y coincide con HEAD.
- Diff `origin/master...HEAD` = commits de F-04 (merge `9fad768`, `ea8be76 feat(sqx): Magic Number V1 owner allocator replaces CC gate (F-04)`): ~6119 líneas nuevas, todas bajo `sqx/` (magic V1, StrategyVersion seal, handoff, migraciones 015/016). El resto de la cartografía es común a ambos branches salvo lo marcado [F-04].
- Estructura del repo: el root module `github.com/xKoRx/symphony` (internal/, cmd/, go.mod con Zeebe/goka/Temporal) es el pipeline de datos/orquestación legacy (feeds, candles, Zeebe/Camunda); Echo Forge vive en el sub-module `sqx/` (go.mod propio, Go 1.24, depende de `github.com/xKoRx/echo/v3/sdk/contracts` y `github.com/xKoRx/sdk`). `go.work` agrupa root, `./deployer` y `./sqx`. Todo claim de Forge de este artifact refiere al módulo `sqx/` salvo indicación contraria.

## Findings

### 1. Pipeline general (orquestación)
- Pipeline principal = Temporal Workflow `GenericSQXWorkflow` (`sqx/workflows/generic_workflow.go`), que ejecuta secuencialmente `req.Spec.Tasks` (WorkflowSpec declarativo) mutando un `StratBatch` (Keys, StrategyArtifacts, snapshots de ranking/clasificación, bindings). Confidence HIGH (código + tests `project_stages_test.go`, `generic_*_test.go`).
- Dispatch de task por `Type` (normalizado lowercase): `project`/`overview_exporter`, `group` (child `GroupSQXWorkflow`), `metadata_import`/`extractor`, `evaluate_wfm`, `evaluate_strategy`, `verify_wfm_extracted`, `verify_wfm_evaluated`, `select_robust_run`, `verify_robust_run_selected`, `apply_selected_run`, `legacy_apply_selected_run_disabled`, `wfm_exporter` → error "contrato público legado cerrado". Gates `verify_*` son activities read-only que fallan si la evidencia durable previa no existe. Confidence HIGH.
- Stages internos del task `project` (Builder/Retester/Optimizer/FinalReretester comparten el tipo) se activan por predicados exclusivos de `Stage` (`runtime.IsDurableFinalReretesterTask`: Type=project && Stage=final_reretester; `IsDurableOptimizerTask` similar), nunca por folder/posición. Evidence: `sqx/core/runtime/config.go` líneas 220-232. Confidence HIGH.
- El task `project` ejecuta una pipeline de steps (`sqx/activities/worker/steps/`): `prepare_input` (con variantes `prepareDurableRetesterInput`, `prepareDurableFinalReretesterInput`, `prepareDurableOptimizerInput`, `prepareDurableBuilderTemplateInput`), `download_strategies`, `download_config` (CFX + inyección de CustomAnalysis XML), `resolve_stage_execution`, `claim_output_namespace` (ownership write-once), luego ejecuta SQX. Confidence HIGH.
- FlowRun lifecycle: activities `flow_run_start`/`flow_run_seal` (`worker.FlowRunStartActivityName`, `FlowRunSealActivityName`) con correlación temporal (`flowRunTemporalCorrelation`); seal corre en contexto disconnected en error. Confidence HIGH.
- Regla legacy de terminación: activity `project` que empieza con keys y termina vacía corta el flujo salvo exporters o cuando `spec.Promotion != nil` (zero-supply closure pasa a ser propiedad de FinalistPromotion). Confidence HIGH.
- Builder template mode: si `task.SourceFolder` != "", activity `ResolveBuilderTemplateCohortActivityName` resuelve la cohorte histórica; exige batch actual vacío y cohorte no-vacía (fail-closed). Confidence HIGH.
- MT5 dentro del pipeline: tasks MT5 compilación/backtesting/artefactos se ejecutan como child workflows dedicados (`MT5CompileArtifactWorkflow`, `MT5BacktestArtifactWorkflow`, `mt5ArtifactExecution` en `generic_workflow.go` 2552-3006) con IDs deterministas (`mt5_identity.go`); fuentes durables resueltas por `resolveMT5ArtifactSources` / `durableMT5ArtifactSources`. Confidence HIGH.

### 2. ForgeCampaign (campaña multi-wave)
- `ForgeCampaignWorkflow` (`sqx/workflows/forge_campaign_workflow.go`, 146 líneas): valida input schema `sqx-forge-campaign-workflow-input.v1` + `ForgeCampaignRef` + `ForgeCampaignIntentToken`; activity `Start` devuelve StopPolicy (MaxWaves); loop `for waveNumber := 1..MaxWaves`: `ResolveWave` → child `GenericSQXWorkflow` con WorkflowID determinista `binding.WorkflowIDV1(FlowIntentToken)` y RetryPolicy MaximumAttempts=1 → `FinalizeWave(ChildSucceeded)`; si `FINALIST/COMPLETED` con StopEvaluation termina; al agotar waves ejecuta `FailContract` y falla non-retryable `CAMPAIGN_CONTRACT_CONFLICT`. Cancel handling via disconnected context + `ForgeCampaignCancelActivity`. Confidence HIGH (tests `forge_campaign_workflow_test.go`).
- Actividades de campaña (`sqx/activities/worker/forge_campaign_activity.go`) delegan en `capabilities.ForgeCampaignOrchestrationStore` (persistido por Postgres `registry-postgres/forge_campaign.go`); Start registra correlación temporal (Namespace, WorkflowID, FirstRunID, CurrentRunID). Confidence HIGH.
- Dispatcher: `TemporalDispatcher.StartForgeCampaign` (`sqx/adapters/dispatcher-temporal/temporal_dispatcher.go:379`) inicia el workflow; `ConvergeAlreadyStarted` maneja start duplicado. Confidence HIGH.

### 3. WFM (Walk-Forward Manager)
- WFM durable = task `evaluate_wfm` → `runDurableEvaluateWFM` (`sqx/workflows/wfm_durable.go`): por cada StrategyArtifact lanza `WFMDurableExportActivity` (fan-out paralelo) y luego `WFMDurableSealActivity` (fan-out seal); valida portadores (`validateDurableWFMCarrier`), exige FlowRunRef, valida mismatch exacto de inputs (StrategyRef/CanonicalStrategyID/EvaluationRef/ObjectKey); supervivientes pasan al batch. Activities: `sqx/activities/worker/wfm_durable.go`, `wfm_durable_export_activity.go`, `wfm_durable_seal_activity.go`, físicos en `wfm_durable_physical.go` (+ lock). Confidence HIGH.
- Motor de evaluación WFM puro en `sqx/core/wfm/evaluator.go`: `EvaluateNeighborhood(matrix, params)` sobre grid (matrix 6x9, vecindario 3x3 por defecto en claves del workflow), candidatos por celdas que pasan, scoring por `scoreDispersionCoV`, `scoreRunConsistency`, `scoreWeightedPerformance` (selector por params), ranking determinista `rankPicks` con orden por keys; `rules.go:EvaluateRules` aplica ruleset (DefaultRulesParams). Tests: `evaluator_test.go`, `rules_test.go`, `wfm_n5_audit_test.go`. Confidence HIGH.
- Claves de identidad: `domain.GenerateWFMConfigKey(runsCount, oosPercent, matrixDims, neighborDims, optName, optChecksum, metricProfile)`, `GenerateWFMGroupRunKey`, `GenerateWFMRunKey`, `GenerateEvaluatorConfigKey`, `GenerateWFMEvaluationKey` — defaults hard-coded en workflow cuando no hay WaveConfig (5 runs, 20% OOS, 6x9, 3x3, ret_dd). Confidence HIGH.
- `verify_wfm_extracted` / `verify_wfm_evaluated` son gates read-only (`verify_wfm.go` en activities/worker). Confidence HIGH.

### 4. Ranking / Selection / Finalist Promotion
- Ranking snapshot global: `RankingSnapshotActivity` (`sqx/activities/worker/rank_snapshot_activity.go`) persiste snapshot inmutable vía `capabilities.ImmutableEvidenceStore` + `RankingSnapshotStore`; algoritmo congelado `score_descending.v1` (`domain.RankingAlgorithmID`, version 1.0.0, params `score-descending-params.v1`); ref computada como `HashIdentity("ranking-snapshot.v1", flowRun, configDigest, ...)`. Confidence HIGH.
- Ranking temprano per-logical-type tras Builder: `weighted_combination_minmax.v1` (`sqx/core/domain/ranking_snapshot_per_type.go`), activities `early_ranking_activity.go` + `early_ranking_group_activity.go` con resolver de grupo; clasificación previa vía `classification_snapshot_activity.go` y `classify_and_rank.go` (adaptive). Confidence HIGH.
- Selección robusta: task `select_robust_run` → `runDurableSelectRobustRun` → `DurableSelectRobustRunActivity` (`durable_select_robust_run.go`), con config keys `GenerateRobustSelectionConfigKey(selectionRules="stage4.v1", warningPenalties="stage4_default", "ret_dd", "stage4.v1")` → `SelectedRobustRunKey`. Decision `OPTIMIZER_SELECTION` en modo ENFORCE con evidencia exacta (wfm_aggregate, selected_cell, selected_metric_set) validada en Apply. Confidence HIGH.
- Apply: task `apply_selected_run` → `DurableApplySelectedRunWorkflow` (`sqx/workflows/durable_apply_selected_run_workflow.go`, durable con retry/lock propio) → `DurableApplySelectedRunActivity` (`durable_apply_selected_run.go` + lock + physical): carga Decision exacta, lineage, fetch de bytes del artefacto durable, resuelve config efectiva, aplica. Variante `legacy_apply_selected_run_disabled` conserva el camino viejo. Confidence HIGH.
- FinalistPromotion: `runFinalistPromotion` (generic_workflow.go:966) exige exactamente UNA binding GLOBAL del ranking fuente; activity `FinalistPromotionActivity` (rank_snapshot_activity.go:163-544) construye Decision `FINALIST_PROMOTION` v1 o **v2** (schema `FinalistPromotionOutputSchemaV2`, migración `014_finalist_promotion_v2.up.sql`), con `promotionFinalistsV2` que copia ScoreEvidence; soporta zero-supply (G22) sin ranking. Confidence HIGH.

### 5. Reretest / Final Reretester
- Retester = stage del task `project` (pipeline steps con `retesterbinding`); Final Reretester = `IsDurableFinalReretesterTask` → `runFinalReretesterFanout` (generic_workflow.go:1632): valida inputs (sólo artefactos duros exactos), lanza child workflows por artefacto y mergea outputs con validación `validateFinalReretesterFanoutOutput` (1:1 input→output). Tests: `durable_final_reretester_fanout_workflow_test.go`, `final_reretester_carrier_test.go`. Confidence HIGH.
- Los directorios `cmd/symphony/02_retester_hp` y `cmd/symphony/03_retester_full` contienen sólo binarios `.sqx` de datos (fixtures de estrategias XAUUSD), sin código Go — no son servicios retesteador. Confidence HIGH.
- Baseline de reretester en Mongo: `metadata-mongo/reretester_baseline.go` + `reretester_baseline_derive.go`. Confidence MEDIUM (no leído en detalle, sólo presencia).

### 6. Integración SQX (motor Strategy Quant X)
- Ejecución SQX: `sqx/adapters/executor-sqx/sqx_executor.go` `SQXExecutor.ExecuteAndWait(projectName)` coordina vía etcd + `capabilities.CommandExecutor` y valida markers de fin; preparación de config CFX con plugin CustomAnalysis inyectado en `steps.go` (`ensureProjectOverviewProducer`, `overviewCustomAnalysisTaskXML`). Confidence HIGH.
- Plugin Java dentro de SQX: `sqx/exporter-plugin/src/SQ/CustomAnalysis/` — `EchoForgeOverviewExporter.java`, `EchoForgeWFMExporter.java`, `EchoForgeRobustRunExporter.java`, `EchoForgeMT5Exporter.java`, `EchoForgeTradeListExporter.java` + infra de trades (`trades/*.java`, producción desde SQX real via `ProductionSQXTradeSource`). Lifecycle: los exporters corren dentro de SQX al cerrar la CustomAnalysis y escriben JSON/XML que el watcher/activities suben a MinIO. Confidence HIGH para existencia y nombres; MEDIUM para lifecycle interno Java (no leído línea a línea).
- Workers: `sqx/cmd/sqx-worker/main.go` (worker Temporal principal, registra 5 workflows + ~40 activities, líneas 329-390), `sqx/cmd/sqx-mt5-worker/main.go` (worker MT5 dedicado con `lifecycle` + singleton Windows), `sqx/cmd/sqx-watcher/main.go` (intake fsnotify de outputs → upload MinIO → dispatch Temporal), `sqx/cmd/sqx-flowkit`. Confidence HIGH.

### 7. MT5 (reconciliación / scoring)
- Workflows MT5: `MT5CompileArtifactWorkflow`, `MT5BacktestArtifactWorkflow`, `MT5ArtifactWorkflow` (`sqx/workflows/mt5_compile_workflow.go`, `mt5_backtest_workflow.go`, `mt5_artifact_workflow.go`, `durable_mt5_exporter.go`) con `mt5_identity.go` para IDs deterministas; e2e en `mt5_pipeline_e2e_test.go`. Confidence HIGH.
- Runner real: `sqx/adapters/mt5/runner.go` `Runner.Backtest` — copia EA a la carpeta Experts del terminal MT5, escribe `tester.ini` (`writeTesterINI`), ejecuta el terminal vía CommandExecutor, localiza el reporte (`findReportFile`); `slot_allocator.go`/`slot.go` para slots de terminal; `compiler.go`/`artifact_compiler.go` para compilación. Confidence HIGH.
- Parseo de reportes: `sqx/adapters/mt5/report/` (decode/parse/results/crosscheck). Reconciliación: `MT5ReconcileActivity` (`mt5_reconcile_activity.go`) compara identidad estructural requested vs observed y métricas SQX vs MT5 (`core/evaluation/reconcile.go:BuildReconciliation` con tolerancia); persiste `persistMT5ReconcileV1` (generic_workflow.go:3006). Scoring/shadow: `MT5ScoreShadowActivity` (`mt5_score_shadow_activity.go`), fidelidad en `core/evaluation/mt5_fidelity.go` + matriz (`mt5_fidelity_matrix_test.go`), scoring puro en `sqx/adapters/mt5/scoring/score.go` con golden tests. Confidence HIGH.

### 8. Temporal (confirmación)
- Temporal CONFIRMADO como motor de workflows de Forge: `go.temporal.io/sdk v1.35.0` en `sqx/go.mod:24` (v1.44.1 existe sólo en el go.mod root `github.com/xKoRx/symphony`, módulo legacy de feeds, no Forge); 5 workflows registrados (`GenericSQXWorkflow`, `GroupSQXWorkflow`, `MT5CompileArtifactWorkflow`, `MT5BacktestArtifactWorkflow`, `ForgeCampaignWorkflow`) + workflows durable auxiliares (`durable_select_workflow.go`, `durable_apply_selected_run_workflow.go`, `wfm_durable.go` como funciones, `durable_trade_list_workflow.go`, `durable_mt5_exporter.go`, `adaptive_workflow.go`); ~40 activities en `sqx-worker`. Zeebe/Camunda (`zeebe clients v8`, `internal/services/camunda`, `internal/tasks/*`) pertenece SOLO al módulo root legacy (feeds/symphony), no a Forge. Confidence HIGH.

### 9. Artefactos y materialización
- Artefactos producidos: overview metadata (builder durable), WFM runs/evaluations exportadas y selladas, robust selection outputs, MT5 reports parseados, trade lists (JSON con manifest, plugin Java), clasificaciones, ranking snapshots, decisiones (Decisions), estrategias `.sqx` y EAs MT5 compilados. Almacenamiento: MinIO bucket `sqx-strategies` (`sqx/adapters/uploader-minio/minio_uploader.go:39`, `minio.BucketStrategies`) vía SDK `xKoRx/sdk`; metadata/evidencia en MongoDB; durabilidad/autoridad en PostgreSQL. Confidence HIGH.
- Refs durables: artefactos referenciados por `DurableArtifactRef` sha256 (`steps.go:exactDurableArtifactRefs`), `stage_producer_outputs` (migración 008) y `output_namespace_ownership` (007, claim write-once en `claim_output_namespace`). Confidence HIGH.
- Readback de bytes: `sqx/adapters/magic-readback/readback.go` — única autoridad de readback F-04: extrae `MagicNumber` efectivo del XML `.sqx` producido (falla cerrada `ErrMagicNodeMissing`, nunca sustituye el valor pedido). Confidence HIGH.

### 10. Identidad / Version / Magic (F-04) [F-04: sólo en HEAD]
- StrategyVersion identity (`sqx/core/domain/strategy_version.go`): `StrategyVersionIdentity{TargetPlatform, ExecutableSha256, EffectiveInputsSha256, RuntimeContextSha256, DependenciesSha256}`; `Ref()` = sha256 canónico; `EffectiveInputs.Digest()`, `RuntimeContext.Digest()`, `ExecutionManifest.Digest()`, `DependenciesDigest` (deps ordenadas por `SortDependencies`); `StrategyVersionRecord.Payload()`/`ComputePayloadDigest()`. Confidence HIGH (tests domain + adapter).
- Seal durable: `ControlPlane.SealStrategyVersion` (`registry-postgres/strategy_version.go:25`, write-once, tabla `sqx.strategy_versions` con UNIQUE natural key redundante) + `LoadStrategyVersion`. **NO tiene ningún caller de producción** (grep: sólo tests). Implementado a nivel capability/adapter, no cableado en workflows. Confidence HIGH.
- Magic Number V1 (`sqx/core/domain/magic_v1.go`): formato congelado `YYMMIIIDSSS` (11 dígitos: periodo YYMM UTC, código de instrumento 3 dígitos 001-999, dígito de dirección, secuencia 3 dígitos); `EncodeMagicV1`/`ValidateMagicV1`/`DecodeMagicV1`; dirección LONG/SHORT/BOTH; identidad desde CanonicalStrategyID vía `ParseMagicV1AllocationIdentity`. Migración `016_magic_number_v1_allocator.up.sql`: catálogo inmutable `sqx.magic_instruments` (XAUUSD=001, luego AUDUSD, EURUSD, GBPUSD, NZDUSD, USDCAD, USDCHF, USDJPY 002-008 lexicográfico) con trigger forbid-mutation, y `sqx.magic_monthly_counters` (counter por (period, code), techo 999/mes, compartido por dirección). Confidence HIGH.
- Allocator: `ControlPlane.AllocateMagicV1` (`registry-postgres/magic_v1.go`) + `AllocateMagic` genérico (`magic_allocation.go`, tabla `sqx.strategy_magic` migración 015: 1:1 StrategyRef↔magic por namespace, sin reciclaje ni update); capa de capability en `sqx/core/capabilities/magic_allocation.go` con `MagicCandidateSource` y fallos cerrados INSTRUMENT_CODE_MISSING / MAGIC_MONTHLY_CAPACITY_EXHAUSTED. **Único punto cableado en producción**: `DurableApplySelectedRunActivity.resolveEffectiveConfig` (`durable_apply_selected_run.go:217`) llama `AllocateMagicV1(namespace LIVE, StrategyRef, CanonicalStrategyID)` SOLO si `req.UseDurableMagicAllocation` (opt-in por flow), y estampa el magic en la config efectiva antes de Apply. Confidence HIGH.
- Migración 015 también crea `sqx.handoff_manifests` (write-once, UNIQUE(decision_ref, version_ref) = G24) y `sqx.handoff_deliveries` (estados CHECK). Migraciones con tests de integración (`015_..._test.go`, `016_..._test.go`). Confidence HIGH.

### 11. Persistencia
- PostgreSQL (autoridad durable): `sqx/adapters/registry-postgres/` — `postgres_registry.go`, `control_plane.go`, `decision_store.go`, `flow_run.go`, `stage_execution.go`, `stage_producer_output.go`, `strategy_identity.go`, `forge_campaign.go`, migraciones 001-016 con runner propio (`migrations/runner.go`). Tablas clave: flow_runs, decisions, stage executions, stage_producer_outputs, strategies (identity v1/v2, migraciones 005/006), finalist promotion (009/010/014), forge campaign (011/012/013), F-04 (015/016). Confidence HIGH.
- MongoDB (evidencia/metadata): `sqx/adapters/metadata-mongo/` — evidence_store (+indexes), ranking_snapshot_store/query, classification_snapshot_store, robust, evaluations, trade_lists, reretester_baseline. Confidence HIGH (presencia + tests por archivo).
- etcd (coordenación de campaña/ejecución): `sqx/adapters/campaign-etcd/` — StateStore (estado VM por machineID, Summary por requestID), ControlWatcher (watch/poll de campaigns), MaintenanceExecutor (comandos validados); etcd también en `SQXExecutor`. Confidence HIGH.

### 12. Fallos / retries / recovery
- RetryPolicy por activity: ForgeCampaign activities con `MaximumAttempts: 0` (sin retry temporal, retries gestionados por contrato propio, StartToCloseTimeout 30s) y fallos non-retryable `CAMPAIGN_CONTRACT_CONFLICT`; child waves con `MaximumAttempts: 1` y `PARENT_CLOSE_POLICY_REQUEST_CANCEL`. Error helpers clasifican persistencia (unknown commit vs conflict) en ranking/promotion (`rankingUnknownCommit`, `classifyReconcileError`). Confidence HIGH.
- Cancel/drain del worker: `sqx/core/lifecycle/controller.go` (estados accepting/admit/release/drain/stopped) + `temporal.go` ActivityGate interceptor; `sqx/cmd/sqx-mt5-worker/main.go` con lifecycle + singleton; adapter `quiesce-file`. Confidence HIGH.
- Reintentos de handoff: NO hay re-POST ciego — estado `UNKNOWN_RECEIPT` parquea la delivery y `ErrDeliveryAwaitingReceipt` exige reconciliación vía read idempotente futuro (E-04). Confidence HIGH (código).

### 13. Observability
- OpenTelemetry: `go.opentelemetry.io/otel` (metrics + trace) en go.mod; `sqx/core/instrumentation/` (metrics.go watcher, worker_metrics.go, heartbeat.go); spans via `xKoRx/sdk` `telemetry.StartSpan` (uploader, activities); spans de workflow (`begin_workflow_span`/`begin_group_span` activities) con TraceID/SpanID en telemetry.Context propagado en requests; atributos otel en logs del workflow. Specs: `specs/FEAT-SQX-OBSERVABILITY`. Confidence HIGH.

### 14. Config real
- `sqx/core/config/config.go` + `runtime.WorkflowSpec`/`WaveConfig` (WFMParams.Algorithm/MinPassCells, Ranking.Algorithm, Promotion); env/.env via `godotenv` (worker); ejemplos declarativos en `examples/config-*.json`. Defaults hard-coded en workflow para claves WFM si no hay WaveConfig (ver §3). Confidence HIGH.

### 15. Tests como evidencia
- 290 archivos `*_test.go` en `sqx/` (325 en repo). Cobertura fuerte: identity/ref hashing, migraciones (integración con postgrestest), WFM evaluator/rules (golden), MT5 report parse/scoring (golden), reconcile, ranking/promotion, apply-selected-run (contrato, lock, físico, magic), handoff (fakeconsumer 201/200/409), campaign workflow (testmode). Cobertura débil/ausente: no hay test E2E que conecte FinalistPromotion → BuildHandoffManifest → DeliverHandoff dentro de un workflow real (porque el cable no existe, ver §Handoff); lifecycle Java del plugin cubierto sólo por fixtures (`exporter-plugin/test-*`). Confidence HIGH.

## Evidence

| Claim | File (rel. repo) | Símbolo | Test/Config | Conf. |
|---|---|---|---|---|
| Pipeline principal es GenericSQXWorkflow secuencial por tasks | sqx/workflows/generic_workflow.go | runGenericSQXWorkflow (L165) | project_stages_test.go, generic_workflow_options_test.go | HIGH |
| Stage final_reretester por predicado Type/Stage exclusivo | sqx/core/runtime/config.go | IsDurableFinalReretesterTask (L222) | runtime/final_reretester_test.go | HIGH |
| Builder template exige cohorte vacía→resuelta no-vacía | sqx/workflows/generic_workflow.go | bloque SourceFolder (L253-270) | builder_template_activity_test.go | HIGH |
| wfm_exporter legado cerrado con error | sqx/workflows/generic_workflow.go | case "wfm_exporter" (L355) | — (error explícito) | HIGH |
| WFM durable = export + seal fan-out | sqx/workflows/wfm_durable.go | runDurableEvaluateWFM | wfm_durable_workflow_test.go | HIGH |
| Evaluador WFM 6x9/3x3 con scoring CoV/consistencia/ponderado | sqx/core/wfm/evaluator.go | EvaluateNeighborhood, scoreDispersionCoV | evaluator_test.go, wfm_n5_audit_test.go | HIGH |
| Ranking global congelado score_descending.v1 | sqx/core/domain/ranking_snapshot.go | RankingAlgorithmID (L16) | rank_snapshot_activity_test.go | HIGH |
| Ranking per-type weighted_combination_minmax.v1 | sqx/core/domain/ranking_snapshot_per_type.go | PerLogicalTypeRankingAlgorithmID | early_ranking_workflow_test.go | HIGH |
| FinalistPromotion V2 con ScoreEvidence y zero-supply G22 | sqx/activities/worker/rank_snapshot_activity.go | buildFinalistPromotionDecisionV2, promotionFinalistsV2 | rank_snapshot_activity_test.go | HIGH |
| Selección robusta stage4.v1 con claves derivadas | sqx/workflows/generic_workflow.go | case select_robust_run + GenerateRobustSelectionConfigKey | durable_select_robust_run_test.go | HIGH |
| Apply validado contra Decision OPTIMIZER_SELECTION ENFORCE y evidencia exacta | sqx/activities/worker/durable_apply_selected_run.go | loadDecision, validateDecisionEvidence | durable_apply_selected_run_contract_test.go | HIGH |
| Magic V1 cableado SÓLO en Apply con opt-in UseDurableMagicAllocation | sqx/activities/worker/durable_apply_selected_run.go | resolveEffectiveConfig (L214-233), AllocateMagicV1 call (L217) | durable_apply_selected_run_magic_test.go | HIGH |
| Magic formato YYMMIIIDSSS 11 dígitos, techo 999/mes | sqx/core/domain/magic_v1.go | EncodeMagicV1, MagicV1Digits | magic_v1_test.go, 016_magic_number_v1_allocator_test.go | HIGH |
| Catálogo instrumentos inmutable XAUUSD=001 | sqx/adapters/registry-postgres/migrations/016_magic_number_v1_allocator.up.sql | magic_instruments + trigger | 016_..._test.go | HIGH |
| SealStrategyVersion write-once existe pero SIN caller de producción | sqx/adapters/registry-postgres/strategy_version.go | SealStrategyVersion (L25) | strategy_version_test.go (sólo tests) | HIGH |
| Tablas F-04: strategy_magic, strategy_versions, handoff_manifests, handoff_deliveries | sqx/adapters/registry-postgres/migrations/015_strategy_magic_version_seal_handoff.up.sql | DDL | 015_..._test.go | HIGH |
| Readback de MagicNumber desde bytes .sqx, fail-closed | sqx/adapters/magic-readback/readback.go | FromSQX, ErrMagicNodeMissing | readback_test.go | HIGH |
| MT5 ejecución real: tester.ini + Experts + reporte | sqx/adapters/mt5/runner.go | Runner.Backtest, writeTesterINI | runner_test.go, mt5_backtesting_integration_test.go | HIGH |
| Reconciliación SQX↔MT5 con tolerancia y persist V1 | sqx/activities/worker/mt5_reconcile_activity.go + core/evaluation/reconcile.go | MT5ReconcileActivity.Execute, BuildReconciliation | mt5_reconcile_activity_test.go | HIGH |
| Scoring MT5 con golden tests | sqx/adapters/mt5/scoring/score.go | — | score_golden_test.go | HIGH |
| Plugin Java exporters dentro de SQX | sqx/exporter-plugin/src/SQ/CustomAnalysis/ | EchoForge*Exporter.java | exporter-plugin/test-* | MEDIUM (nombres/existencia HIGH, lifecycle MEDIUM) |
| SQX se ejecuta vía etcd+CommandExecutor con markers | sqx/adapters/executor-sqx/sqx_executor.go | SQXExecutor.ExecuteAndWait, validateMarkers | sqx_executor_test.go | HIGH |
| Worker Temporal registra 5 workflows + ~40 activities | sqx/cmd/sqx-worker/main.go | L329-390 | lifecycle_test.go, proactiva_e2e_test.go | HIGH |
| Temporal SDK v1.35.0 en sqx module (go.mod:24) | sqx/go.mod | require temporal | — | HIGH |
| Campaña: waves con child ID determinista y stop policy | sqx/workflows/forge_campaign_workflow.go | ForgeCampaignWorkflow (L51) | forge_campaign_workflow_test.go | HIGH |
| Dispatcher Temporal con convergencia de starts duplicados | sqx/adapters/dispatcher-temporal/temporal_dispatcher.go | StartV1, StartForgeCampaign, ConvergeAlreadyStarted | temporal_dispatcher_v1_test.go, forge_campaign_dispatch_test.go | HIGH |
| Watcher fsnotify → MinIO → dispatch Temporal | sqx/cmd/sqx-watcher/main.go | runWatcherLoop, processFileEvent | watcher tests (intake_test.go) | HIGH |
| MinIO bucket sqx-strategies para artefactos | sqx/adapters/uploader-minio/minio_uploader.go | EnsureBuckets (L34-39) | — | HIGH |
| Metadata/evidencia en MongoDB | sqx/adapters/metadata-mongo/ | evidence_store, ranking_snapshot_store | adapter_test.go, evidence_store_test.go | HIGH |
| Coordinación etcd de campaña y SQX | sqx/adapters/campaign-etcd/state_store.go | StateStore, ControlWatcher | state_store_test.go | HIGH |
| Worker lifecycle drain/admit | sqx/core/lifecycle/controller.go | Controller.TryAdmit, BeginDrain | controller_test.go, temporal_test.go | HIGH |
| Forge→Echo: NO hay cliente HTTP; única ingress = fakeconsumer in-process | sqx/adapters/echo-handoff/fake_ingress.go | FakeConsumerIngress.Deliver | delivery_test.go | HIGH |
| Handoff manifest V1 construido y validado contra contrato congelado Echo SDK | sqx/core/forge/handoff_producer.go | BuildHandoffManifest (L69) | handoff_producer_test.go | HIGH |
| State machine de delivery sin re-POST ciego (UNKNOWN_RECEIPT park) | sqx/core/capabilities/handoff.go | DeliverHandoff (L87) | handoff tests + strategy_version_test.go | HIGH |
| DeliverHandoff/BuildHandoffManifest SIN caller de producción (no cableado en workflow/activity) | grep repo completo | — | sólo *_test.go los referencian | HIGH |
| Idempotency key = sha256(namespace, waveKey, canonicalStrategyID, versionRef) | sqx/core/capabilities/handoff.go + contracts SDK | contracts.IdempotencyKey | delivery_test.go | HIGH |

## Implemented vs Spec-Only

- IMPLEMENTADO (código + tests, cableado en producción): pipeline GenericSQX/Group, stages Builder/Retester/Optimizer/FinalReretester, WFM durable (export+seal), ranking global/per-type + clasificación, selección robusta, Apply Selected Run, FinalistPromotion V1/V2, ForgeCampaign multi-wave, MT5 compile/backtest/reconcile/score-shadow, plugin Java exporters, MinIO/Mongo/Postgres/etcd, watcher, workers Temporal, Magic V1 allocation (opt-in en Apply).
- IMPLEMENTADO A NIVEL LIBRERÍA pero SIN CABLEAR (F-04): `SealStrategyVersion` (Postgres, write-once), `BuildHandoffManifest`, `DeliverHandoff` + stores de handoff en Postgres, `FakeConsumerIngress`. Ningún workflow/activity de producción invoca el handoff hoy; el pipeline termina en FinalistPromotion decision + Apply con magic. Migraciones 015/016 sí están en el runner.
- SOLO-SPEC / INTENCIÓN (docs en `specs/FEAT-SQX-ECHO-INGESTION` y comentarios de código): cliente real E-04 (`HandoffIngress` real, con read idempotente para reconciliar UNKNOWN_RECEIPT), provisioning/activación en Echo, handoff automático post-promoción. Los comentarios de `handoff.go`/`fake_ingress.go` son evidencia de intención, no de comportamiento.
- LEGADO CERRADO: `wfm_exporter` task (error explícito), `legacy_apply_selected_run_disabled`, `overview_exporter` legacy skip cuando Builder durable produce metadata. Módulo root (internal/ con Zeebe/goka) es el sistema legacy de feeds, NO Forge.

## Handoff to Echo (implemented truth)

- VERDAD IMPLEMENTADA: Forge NO emite nada hacia Echo hoy por red. No existe ningún cliente HTTP, producer Kafka ni transporte de red hacia Echo en el repo `symphony` (grep de `DeliverHandoff`/`BuildHandoffManifest`/`FakeConsumer`: cero callers de producción). Esto CONFIRMA la nota de campaña desde el lado Forge.
- Lo que existe es la capacidad de emisión implementada a nivel librería (F-04, branch `feature/f04-magic-version-handoff`, no en origin/master): (1) `sqx/core/forge/handoff_producer.go:BuildHandoffManifest` construye un `contracts.HandoffManifestV1` del SDK congelado `github.com/xKoRx/echo/v3/sdk/contracts v0.0.0-20260910031519` (pin en `sqx/go.mod`), lo valida contra `manifest.Validate(IngestionContractVersion)` y produce body canónico (`manifest.Encode()`), payload digest y idempotency key `sha256(registryNamespace, waveKey, canonicalStrategyID, versionRef)`; (2) `sqx/core/capabilities/handoff.go:DeliverHandoff` ejecuta la máquina de estados con persistencia write-once primero en Postgres (`sqx.handoff_manifests`/`sqx.handoff_deliveries`, migración 015), gate de byte-equality del body canónico, convergencia de replay (INGESTED = no-op), G24 SOURCE_BINDING_CONFLICT vía UNIQUE(decision_ref, version_ref), y estados HANDOFF_CREATED/INGESTED/CONTRACT_CONFLICT/SOURCE_BINDING_CONFLICT/UNAVAILABLE/UNKNOWN_RECEIPT; nunca re-POSTea tras UNKNOWN_RECEIPT (reconciliación futura por read idempotente E-04); (3) la única implementación del puerto `HandoffIngress` es `sqx/adapters/echo-handoff/fake_ingress.go:FakeConsumerIngress`, un wrapper in-process del `fakeconsumer` del propio SDK Echo (semántica 201 primer ingest / 200 replay / 409 conflicto), usada sólo en tests de certificación CONTRACT.
- Contrato del payload (lo que Forge emitiría): HandoffManifestV1 con ContractVersion/SchemaVersion/RequiredCapabilities, Producer ID congelado `echo-forge-handoff` versión `1.0.0`, bloque Strategy (CanonicalStrategyID, StrategyRef, InstrumentID, Direction wire, Timeframe, MagicAllocation {magic decimal, allocation_ref sha256, assigned_at RFC3339}), bloque Version (version_ref sha256, target platform, 4 sha256 de identidad, execution_manifest, artefactos con refs verificados por readback, BuildLineage con decision ref y source artifact), bloque Promotion (DecisionRef + content digest, PolicyID/Version, OutputSchema FinalistPromotion V2, FlowRunRef, WaveKey, MemberProof con evidencia estructural requested/observed — rank y score excluidos por diseño G02/F-02). Membership exclusivamente estructural desde Decision FINALIST_PROMOTION V2 completada; cero finalistas ⇒ cero manifests y cero deliveries (G22).
- Conclusión para Fase D: el handoff Forge→Echo es un contrato de librería certificado contra fakeconsumer + esquema Postgres, PENDIENTE de cableado en el pipeline (no hay activity/workflow que lo invoque) y pendiente del cliente E-04 real; cualquier statement de que Forge "publica" a Echo vía HTTP/Kafka es hoy falso.

## Conflicts / Unknowns

- UNKNOWN: quién (futuro flujo/workflow) invocará SealStrategyVersion + BuildHandoffManifest + DeliverHandoff — no hay cable ni TODO en código; el orden natural (post-FinalistPromotion/Apply) es sólo inferencia de diseño, no evidencia.
- UNKNOWN: disponibilidad operativa real del entorno (Temporal namespace, DSN Postgres, MinIO, MT5 terminal) — sólo se mapeó config de código, no entorno vivo.
- LOW AMBIGUITY: el fixture dirty `phase4_performance.json` (working tree vs HEAD) podría cambiar resultados de `phase4_performance_test.go` si se corrieran tests en el working tree; todo lo demás está limpio.
- MEDIUM: lifecycle exacto interno de los exporters Java (orden de escritura de archivos, naming exacto de outputs) quedó a nivel de presencia/nombres; no se leyó cada .java línea a línea.
- Sin conflictos de evidencia que bloqueen: código, tests, migraciones y configs son consistentes entre sí.

## Handoff

- Para Fase D (integración Echo): partir de `## Handoff to Echo (implemented truth)` — el único punto de integración real hoy es el pin del SDK congelado `github.com/xKoRx/echo/v3/sdk/contracts` + puerto `capabilities.HandoffIngress`; Echo debe tratar la emisión de Forge como no-cableada y no asumir feeds en vivo.
- Artefactos hermanos: `10-projects/Echo/agentes/kb-consolidation/artifacts/02-echo-cartography.md` (lado Echo) — contrastar su observación de "no existe cliente HTTP emisor en Symphony / F-04 usa FakeConsumer": CONFIRMADA por este cartografiado con evidencia file/símbolo.
- Gaps recomendados para decisiones del planner (no ejecutados por mí): decidir owner del cableado del handoff (dónde en el pipeline), y definir la reconciliación UNKNOWN_RECEIPT con el read idempotente E-04.
- Lectura de repo estrictamente read-only; ningún archivo fuera de este artifact fue modificado; sin checkout/reset/stash; el dirty file quedó intacto.

## Corrections

- Corrección (KBC-G fix F-1, 2026-09-13T01:22:57-03:00): el claim "Temporal SDK v1.44.1 en sqx/go.mod" era erróneo. Evidencia verificada: `sqx/go.mod:24` declara `go.temporal.io/sdk v1.35.0`; `go.temporal.io/sdk v1.44.1` está sólo en el go.mod root (`github.com/xKoRx/symphony`, módulo legacy de feeds, línea 24), que no es Forge. Líneas corregidas: sección "### 8. Temporal (confirmación)" y fila correspondiente de la tabla Evidence. Spot-check de demás versiones citadas (echo contracts pin v0.0.0-20260910031519, xKoRx/sdk, otel, zeebe v8 sólo root): coinciden con los go.mod reales; sin más errores adyacentes de este tipo. Baseline: 9fad768ccd1f9d25ebb535a2d26edb3d74556c10.
