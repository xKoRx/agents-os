---
agent: echo-forge-integration-cartographer
role: Echo ↔ Forge Integration Boundary
task_id: KBC-D
status: COMPLETE
baseline: echo f7ddea18cab51db72c9765aa74381328134d7ce7 (feature/e02-control-safety-journal-recovery) · symphony 9fad768ccd1f9d25ebb535a2d26edb3d74556c10 (feature/f04-magic-version-handoff) · vault 9d4b31119615992ca3150b63efb6cb3ecd38ac2b al empezar
inputs: 02-echo-cartography.md, 03-forge-cartography.md, ambos repos READ-ONLY, contratos frozen v3/sdk/contracts + specs E-01/E-04 y F-04
scope: frontera funcional/contractual Echo Forge → Echo, read-only, tres categorías separadas (CONTRACTUAL / IMPLEMENTED / GAP)
started_at: 2026-09-12T23:55:00-03:00
updated_at: 2026-09-13T00:30:00-03:00
---

# KBC-D — Echo Forge → Echo Integration Boundary

## Assignment

Reconstruir la frontera Forge → Echo end-to-end (generación/finalist/seal → manifest V1 → transporte → ingestion → persistencia → downstream inmediato) separando CONTRACTUAL TRUTH (contratos frozen + specs vigentes), IMPLEMENTED TRUTH (source/tests/config de ambos baselines) y GAP (diferencias demostrables). Verifiqué ambos lados en primera mano; los artifacts B/C son input, no autoridad. READ-ONLY total: única escritura es este archivo.

## Baseline

- Echo verificado con `git rev-parse HEAD` = `f7ddea18cab51db72c9765aa74381328134d7ce7` en `feature/e02-control-safety-journal-recovery`, working tree clean, `origin/master` = `a99f9a63354bbe72219d1e590bb93757ed08e45e` — coincide con baseline de campaña.
- Symphony verificado = `9fad768ccd1f9d25ebb535a2d26edb3d74556c10` en `feature/f04-magic-version-handoff`, `origin/master` = `0b9742b09019526a8119f086199d15d1f0d42cb1` — coincide; working tree con exactamente 1 dirty file declarado (`specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json`), untouched, irrelevante para la frontera (fixture numérico de test).
- Vault Agents-OS al empezar: `9d4b31119615992ca3150b63efb6cb3ecd38ac2b` (el planner declara `9a5299f1...` al inicio de campaña; drift esperado por bitácora de Fase C).
- Divergencia branch/master de comportamientos cargantes, verificada con `git cat-file -e`: receptor Echo E-01/E-04 (`v3/gateway/internal/forge_ingest_handler.go`, `forge_ingest_auth.go`, migración `061_identity_bwc_foundation.up.sql`) PRESENT en `origin/master` a99f9a63 (E-04 INTEGRATED per `specs/FEAT-FORGE-INGESTION-E1/VERIFICATION.md`); E-02 (`auth_middleware.go`) ABSENT en master (sólo branch). Lado Forge: `sqx/core/forge/handoff_producer.go` y migración `015_strategy_magic_version_seal_handoff.up.sql` ABSENT en symphony master 0b9742b0 — F-04 completo es branch-only.
- Pin de contrato: `symphony/sqx/go.mod` pina `github.com/xKoRx/echo/v3/sdk/contracts v0.0.0-20260910031519-91671f6f46ff`; diff `v3/sdk/contracts` entre `91671f6f46ff` y baseline Echo `f7ddea18` = 1 línea en 1 archivo de test (`wire/canonicalize_test.go`) — superficie contractual idéntica.

## Contractual Truth

### CT-1 Contrato frozen SDK compartido (autoridad S0, paquete `xKoRx/echo/v3/sdk/contracts`)

- `promotion.go`: `IngestionContractVersion = "forge-echo-ingestion.v1"` (frozen) y `IdentityModelVersion = 2`; `IdempotencyKey(ns, wave_key, canonical_strategy_id, strategy_version_ref)` = `H("forge-echo-ingest.v1", [...])` — igualdad semántica de ingestión; `payload_digest` modela igualdad de contenido (`promotion.go` L12-36).
- `HandoffManifestV1` (envelope Forge→Echo): bloques obligatorios `contract_version`, `schema_version`, `required_capabilities`, `producer`, `strategy`, `version`, `promotion`; opcionales `evidence` (refs analíticas, nunca inline) y `historical_adapter` (adapter V1 explícito; nunca flow nuevo disfrazado); `Encode()` produce bytes canónicos y `PayloadDigest()` el digest frozen sobre esos bytes (`promotion.go` L277-325).
- `HandoffManifestV1.Validate` impone cross-checks frozen: G08 (member_proof.strategy_ref == strategy.strategy_ref), G09 (version_ref recompute + tested digests == sealed digests), G10 (forma de integrity de artefactos), G11/G12 (requested/observed instrument/timeframe iguales entre sí y con Strategy); dirección wire sólo `LONG`/`SHORT`; magic decimal string int64 positivo con `allocation_ref` y `assigned_at` obligatorios (`promotion.go` L393-517).
- `PromotionRecord` (receipt) con único status válido `PromotionRecordStatusIngested = "INGESTED"`; el contrato declara que INGESTED "is never an activation" (`promotion.go` L519-566).
- `HandoffStrategy`/`HandoffVersion`/`BuildLineage`/`MemberProof`/`HandoffPromotion` definen exactamente qué viaja: identidad V2, 4 sha256 de identidad de versión, execution_manifest, artefactos con refs verificados por readback, lineage de apply/compile/decisión, prueba estructural de membership sin score/rank (`promotion.go` L66-221).

### CT-2 Contrato receptor Echo vigente (`specs/FEAT-FORGE-INGESTION-E1/SPEC.md` v1.0.2, Spec-Active, authorities: SDK certified S0 @ 91671f6f46ff, Live Authority V1 §4, F-04 Contract)

- Endpoints frozen (§6.2): `POST /api/v1/forge/promotions` (auth servicio `forge:ingest`), `GET /api/v1/forge/promotions/by-key/{idempotency_key}` y `GET /api/v1/forge/promotions/{promotion_record_id}`; síncrono, sin batch ni 202.
- Namespace autenticado `forge-live` viene de la credencial/config, nunca del body (§3, §5.3); header `Idempotency-Key` MUST igualar la receta S0, discrepancia → 400 `INVALID_INPUT` (§5.4).
- Orden obligatorio §9: auth → límite de body → decode estricto → `HandoffManifestV1.Validate([forge-echo-ingestion.v1])` → derivar key y comparar header → computar digest → replay lookup SIN artifact I/O → fetch+verify+copy de artefactos fuera de tx → una única transacción PG (advisory lock, verify/create mapping V2, verify/insert Version, insert PromotionRecord) → COMMIT → receipt.
- Matriz de errores §10: 201 primer accept; 200 exact replay (mismo receipt_id/accepted_at, cero relectura de artefactos); 409 `CONTRACT_CONFLICT` (key+digest distinto) y (version misma ref otro paquete); 409 `SOURCE_BINDING_CONFLICT` (mismo (ns, decision_ref, version_ref) otra key, G24); 409 `IDENTITY_CONFLICT` (mapping con magic/ref/canonical distintos); 422 `VERSION_DIGEST_MISMATCH`/`IDENTITY_MISMATCH`/`STRUCTURAL_MISMATCH`/`UNSUPPORTED_CONTRACT_VERSION`/`UNSUPPORTED_CAPABILITY`/`UNSUPPORTED_AUTHORITY_VERSION`/`ARTIFACT_INTEGRITY`; 403 store fuera de allowlist; 413 `PAYLOAD_TOO_LARGE`; 503 `UNAVAILABLE` retryable (PG down, timeout pre-commit, artifact transiente, misconfig fail-closed); 404 `NOT_FOUND` retryable en GET-by-key desconocida (permite re-POST idéntico); retryable=true sólo 503/429.
- §6.8: 409 es terminal (Forge no re-POSTea esa key/digest); timeout → Forge hace GET-by-key: 200 = tratar como INGESTED sin re-POST, 404 = re-POST idéntico; `UNKNOWN_RECEIPT` es estado exclusivo de Forge (`sqx.handoff_deliveries`), Echo nunca lo persiste.
- §8: Echo retiene copia operativa verificada (size+sha256+kind/platform) de executable, effective inputs, dependencies y manifest canónico ANTES del commit; store físico V1 = filesystem `artifact_root` keyed `sha256/<64hex>`; allowlist por `store_id`; no hay MinIO productivo en Echo V1.
- §4.3 ownership sin autoridad dual y §3 separaciones frozen: `INGESTED ≠ PROVISIONED ≠ ACTIVE`; `INGESTION ≠ PROVISIONING ≠ OBSERVATION ≠ ELIGIBILITY ≠ CAPITAL ≠ ACTIVATION`; INGESTED significa receipt durable con FKs satisfechas y artefactos copiados, NO attach de EA, NO portfolio, NO riesgo, NO activación, NO avance de `current_version`, NO magic asignado por Echo.
- §11 non-effects SOURCE: cero escrituras a `accounts`/`account_strategy_risk_policy`/`active_positions`/`trade_journal`/`raw_trade_events`/`reference_bindings`, cero publish Kafka desde ingestion, cero símbolos `AllocateMagic`/`NextMagic`, flags Provisioning/Activation/CapitalAllocation en false.
- §6.0/§16: `FORGE_GOLDEN_FIXTURE_PENDING` (inspección física 2026-09-11) — el corpus S0 y el fakeconsumer son SYNTHETIC y NO satisfacen CROSS_LANE GOLDEN; T21/AC-37 es gate POST-INTEGRATION que bloquea E-04 FINAL CLOSED pero no el merge; corpus G01 usa `producer.id = "forge"`, el producer F-04 real es `echo-forge-handoff`.
- Estado contractual declarado por `VERIFICATION.md` de E-04: E-04 INTEGRATED = YES (controlled integration complete 2026-09-12), T01–T20 PASS, T21/AC-37 PENDING, `FORGE_GOLDEN_FIXTURE_PENDING` = YES, E-04 FINAL CLOSED = NO.

### CT-3 Contrato productor Forge vigente (código frozen F-04 en branch `feature/f04-magic-version-handoff` + puerto)

- `sqx/core/forge/handoff_producer.go`: producer identity frozen `HandoffProducerId = "echo-forge-handoff"`, `HandoffProducerVersion = "1.0.0"`; `BuildHandoffManifest` exige una Decision `FINALIST_PROMOTION` v2 COMPLETED con schema `FinalistPromotionOutputSchemaV2`, membership estructural (rank/score nunca leídos, F-02), version ref recompute exacto, valida el manifest contra `Validate([IngestionContractVersion])` y devuelve (manifest, body canónico, payload digest, idempotency key); manifest inválido nunca se persiste ni entrega; membership vacía → cero manifests y cero deliveries (G22, no delegable).
- `sqx/core/capabilities/handoff.go`: puerto frozen `HandoffIngress.Deliver(ctx, ns, *HandoffManifestV1, payloadDigest) (PromotionRecord, httpStatus, error)` — sin paths HTTP definidos, sin acceso a DB Echo; `DeliverHandoff` es máquina de estados write-once: persiste manifest ANTES de entregar (gate de byte-equality del body canónico contra `manifest.Encode()`, sin equivalencia JSON), converge replay INGESTED como no-op, parquea `UNKNOWN_RECEIPT` y rechaza re-POST ciego (`ErrDeliveryAwaitingReceipt`), clasifica `CONTRACT_CONFLICT`/`SOURCE_BINDING_CONFLICT`/`UNAVAILABLE` preservando la evidencia durable existente.
- Estados de delivery Forge frozen (comentario y DDL): `HANDOFF_CREATED ≠ INGESTED ≠ PROVISIONED ≠ ACTIVE`; "provisioning and activation are Echo concerns Forge never asserts".
- Magic V1 frozen (`sqx/core/domain/magic_v1.go`): formato `YYMMIIIDSSS` (11 dígitos: periodo YYMM UTC, código de instrumento 3 dígitos, dígito de dirección 1=LONG/2=SHORT/3=BOTH, secuencia); `ParseMagicV1AllocationIdentity` sólo acepta direcciones L/S desde la identidad canónica (BOTH existe en el codec pero ningún flow lo produce — comentado en código L184-186); `AllocateMagicV1` en namespace `forge-live` (`MagicRegistryNamespaceLive`), 1:1 StrategyRef↔magic sin reciclaje (migración 015: PK (namespace, strategy_ref), UNIQUE valor y UNIQUE allocation_ref).

### CT-4 Specs conceptuales Forge (Spec-Active, PRE-frozen; precedencia menor que CT-1/CT-2/CT-3)

- `specs/FEAT-SQX-ECHO-INGESTION/SPEC.md` (Forge): conceptual `IngestFinalist`/`IngestTradeHistory` con idempotencia `wave_key + strategy_id + version`, payload con métricas/warnings/`future_reoptimization_date`/trade history y `EchoStrategyRef` persistido en `strategy_lineage`; estado "bloqueada para PLAN por NEED-INFO de contrato API Echo" (NI-EI-1/2/3). Este contrato conceptual es anterior y DIVERGE del frozen que luego materializó el código F-04 y del receptor E-04 (ver GAP G6).
- `specs/FEAT-SQX-ECHO-DEPLOYMENT-LINK/SPEC.md` (Forge): conceptual `LinkDemoDeployment` vía el mismo adapter `echo-api`; bloqueado por NEED-INFO NI-DL-1; no existe endpoint receptor en Echo ni contrato frozen que lo cubra.

## Implemented Truth

### Lado Forge (symphony `sqx/` @ 9fad768c, branch F-04; TODO lo de F-04 ausente en origin/master 0b9742b0)

- Pipeline de producción TERMINA en FinalistPromotion V2 (`rank_snapshot_activity.go` `FinalistPromotionActivity`) + Apply Selected Run (`durable_apply_selected_run.go`); ningún workflow ni activity de producción invoca `SealStrategyVersion`, `BuildHandoffManifest` ni `DeliverHandoff` (grep repo-wide: sólo archivos definitorios y `*_test.go` los referencian) — el seal→manifest→deliver NO está cableado y no hay TODO en código.
- `SealStrategyVersion` (`registry-postgres/strategy_version.go` L25): INSERT write-once `ON CONFLICT (version_ref) DO NOTHING` sobre `sqx.strategy_versions`; implementado a nivel capability/adapter, sin caller de producción.
- `DeliverHandoff` + stores PG (`sqx.handoff_manifests` write-once con `UNIQUE(decision_ref, version_ref)` = G24, y `sqx.handoff_deliveries` con CHECK de 6 estados, migración `015_strategy_magic_version_seal_handoff.up.sql`): implementados y con tests (`delivery_test.go`: primer ingest/replay/conflicto/G24, retry pre-commit y park UNKNOWN_RECEIPT, gate de byte-equality, conflicto preserva delivery durable) pero sin caller de producción.
- Única implementación del puerto `HandoffIngress`: `sqx/adapters/echo-handoff/fake_ingress.go` `FakeConsumerIngress`, wrapper in-process del `fakeconsumer` del SDK Echo pinned (201 primer ingest / 200 replay / 409 conflicto); carpeta `adapters/echo-handoff/` contiene sólo `fake_ingress.go` + `delivery_test.go` + testdata; NO existe cliente HTTP, producer Kafka ni ningún transporte de red hacia Echo (grep `forge/promotions` en sqx = 0 resultados; dependencia del módulo sqx hacia echo = únicamente `sdk/contracts`).
- `BuildHandoffManifest` valida contra el contrato congelado y produce body/digest/key; los tests ensamblan input in-memory; NO existe output committed auténtico del producer (no `encode.json`/digest propio; testdata de `echo-handoff` es copia del corpus S0: `testdata/v1/G06/g06-original.json` sha256 `c50358c3...e22` byte-equal al corpus Echo — verificado por sha256sum), lo que confirma `FORGE_GOLDEN_FIXTURE_PENDING` también desde el lado Forge.
- Magic V1 cableado en EXACTAMENTE un punto de producción: `DurableApplySelectedRunActivity.resolveEffectiveConfig` (L210-233) llama `AllocateMagicV1(MagicRegistryNamespaceLive, StrategyRef, CanonicalStrategyID)` SOLO si `req.UseDurableMagicAllocation` (opt-in por flow) y estampa el magic en la config efectiva antes de Apply; el magic resultante vive en la config del EA aplicado, NO en ningún handoff emitido.
- Persistencia del lado Forge: PG autoridad (migraciones 001-016 con runner propio; 015 = magic/versions/handoff, 016 = catálogo inmutable de instrumentos con trigger forbid-mutation y counters mensuales techo 999), Mongo evidencia, MinIO `sqx-strategies` para artefactos, etcd coordinación; readback único de magic desde bytes `.sqx` fail-closed (`magic-readback/readback.go`).

### Lado Echo (xKoRx/echo @ f7ddea18; receptor también integrado en origin/master a99f9a63)

- Rutas montadas exactas al spec (`v3/gateway/internal/server.go` L166-168): POST collection + GET by-key + GET by receipt_id, todas vía `ForgeIngestHandler`; misconfig (token vacío o service nil) → 503 `UNAVAILABLE` fail-closed; auth Bearer constant-time (`forge_ingest_auth.go`, `crypto/subtle.ConstantTimeCompare`), 401 `UNAUTHENTICATED`; deadline por request.
- `ForgeIngestHandler.handlePost` (`forge_ingest_handler.go`): límite `max_body_bytes` (413 `PAYLOAD_TOO_LARGE`), delega en `IngestionService.Ingest(ctx, namespace, body, headerIdempotencyKey)`, 201 vs 200 (`Replay`), header `Location` by-key; proyección receipt `forgeIngestReceipt` con nombres Live Authority `receipt_id`/`state` y `state` siempre `contracts.PromotionRecordStatusIngested`; GET por key o receipt_id con 404 `NOT_FOUND` retryable=true (permite re-POST idéntico tras timeout, SPEC §12).
- `IngestionService.Ingest` (`v3/sdk/postgres/ingestion_service.go` L92-156) implementa el orden §9 completo: namespace semántico → `DecodeHandoffManifestV1` → `Validate([forge-echo-ingestion.v1])` → `checkAuthorityVersion` (rechaza flow nuevo sin `historical_adapter` que declare policy/output V1 o mixta → 422 `UNSUPPORTED_AUTHORITY_VERSION`) → derivar key → comparar header (400 `INVALID_INPUT` field `Idempotency-Key`) → digest canónico → replay lookup POR ANTES de artifact I/O (exact replay converge incluso con ArtifactSource caído) → `DetectSealedConflict` para key+digest distinto → `copyOperationalArtifacts` fuera de tx → `commitAcceptance`.
- `commitAcceptance` (L212-381): una sola transacción — advisory lock `pg_advisory_xact_lock(hashtext(ns), hashtext(canonical))`; parse magic string→int64 y assigned_at RFC3339 (400 si inválido); mapping V2 con clasificación sealed-equal (conflicto por strategy_ref ajeno o magic atado a otra strategy → `IDENTITY_CONFLICT`); Version verify/insert (mismo (ns, version_ref) con paquete distinto → `CONTRACT_CONFLICT`); pre-lookup de binding secundario `(ns, decision_ref, strategy_version_ref)` → `SOURCE_BINDING_CONFLICT` si otra key; INSERT promotion con `status='INGESTED'` implícito y `accepted_body_canonical`; COMMIT; receipt después del commit; rollback en cualquier fallo (partial persistence imposible por tx única).
- Copia de artefactos (`copyOperationalArtifacts` L162-188): executable + effective inputs + dependencies; allowlist fail-closed (`RefAllowed`), plataforma consistente (`CheckArtifactPlatform`), fetch vía puerto `ArtifactSource`, store en `FilesystemStore` bajo `artifact_root`; fallo definitivo 422, transiente 503, cero commit en ambos casos.
- Fuente de artefactos de PRODUCCIÓN = `unavailableArtifactSource` (`forge_ingest_handler.go` L255-265, cableado en `server.go` `newForgeIngestService` cuando source nil): todo fetch retorna `ErrArtifactSourceUnavailable` → 503 retryable con cero commit; "Echo aún no opera stores remotos"; `FixtureArtifactSource` existe sólo para tests; NO hay integración MinIO en Echo V1.
- Persistencia (`migrations/061_identity_bwc_foundation.up.sql` L394-431): `echo.promotion_records` con CHECK `status = 'INGESTED'` (único valor posible en DB), `UNIQUE(registry_namespace, idempotency_key)`, `UNIQUE(registry_namespace, decision_ref, strategy_version_ref)` (G24 a nivel DB), FKs compuestas a `strategy_versions` y a `strategy_identity_mappings`; `strategy_identity_mappings.magic bigint CHECK >= 1` + `magic_allocation_ref` + `magic_assigned_at`; REVOKE UPDATE/DELETE de las 4 tablas de identidad a `echo_user` (write-once protegido en DB, AC-32).
- Forbidden writes verificados por tests: `ingestion_noneffects_test.go` (`TestIngest_NoRuntimeTables` — rowcount invariante en accounts/risk_policy/active_positions/trade_journal/strategy_definitions; `TestIngest_StatusOnlyIngested`; `TestSOURCE_NoAllocatorInIngestion`; `TestSOURCE_NoContractsDelta`; `TestSOURCE_NoMigrationDelta`; `TestSOURCE_AllowedFilesOnly`), `identity_bwc_gate_test.go::TestNoEchoMagicAllocator` (grep SOURCE sobre sdk/postgres, sdk/eapersist, pipe_handler y EAs MQL: Echo no tiene allocator de magic en ningún árbol productivo), 30+ tests de ingestion en `ingestion_service_test.go` (`TestIngest_G01_201`, `TestIngest_ExactReplay_200_NoArtifact`, `TestIngest_G07_409`, `TestIngest_IdentityConflict`, `TestIngest_MappingConflictMagic`, `TestIngest_NoEchoAllocator`, `TestCorpus_ValidationMatrix`, rollback en failure de promotion, recuperación de timeouts pre-commit y commit-then-lost-response vía GET/replay) y `forge_ingest_handler_test.go` + `TestServer_MountsForgeIngest`.
- Downstream de INGESTED en Echo HOY: NINGÚN consumidor de `echo.promotion_records`/`echo.strategy_versions` fuera del boundary (grep v3: sólo contracts, sdk/postgres y el gateway del boundary); no hay attach de EA, portfolio, riesgo, activación ni Kafka post-ingestión; el único consumo posible es la lectura del receipt por GET (reconciliación Forge). E-06+ (RuntimeBinding/activación) es la frontera futura y está fuera de este boundary.
- Echo NO importa symphony (grep `xKoRx/symphony` en go.work y contracts go.mod = 0); la única dependencia cruzada de código es sqx → `xKoRx/echo/v3/sdk/contracts` (1 dependencia en `sqx/go.mod`), que es el canal contractual diseñado.
- Observability del boundary: handler Echo emite warn OTel `forge ingestion rejected` con atributo `code`; no hay counter dedicado del boundary en `sdk/telemetry`; lado Forge usa OTel spans de activities; no hay propagación de trace cross-boundary definida (ver Unknowns).

## Boundary Matrix

| Tema | Contract (referencia) | Implemented (evidencia) | Gap | Estado |
|---|---|---|---|---|
| Envelope HandoffManifestV1 | contracts/promotion.go (frozen) | Forge construye/valida; Echo decodifica/valida igual módulo | Ninguno | OK |
| Transporte Forge→Echo | SPEC E-04 §6.2 (HTTP POST/GET) | Forge: sólo FakeConsumerIngress in-process, sin cliente HTTP; Echo: rutas montadas sin emisor real | G1 | GAP_REAL |
| Producer identity | F-04: `echo-forge-handoff` v1.0.0 | Implementado en producer; corpus S0 G01 usa `forge` | G5 (golden) | GAP_REAL (fixture) |
| Idempotency key | Receta S0 `H(forge-echo-ingest.v1, ...)` | Ambos lados computan idéntica; Echo compara header (400) | Header no viaja en puerto Forge (cliente real debe derivarlo) | GAP_BY_DESIGN (nota de diseño) |
| Payload digest / seals | `PayloadDigest` sobre `Encode()` | Ambos lados; Forge gate byte-equality antes de persistir | Ninguno | OK |
| Identity: canonical/wide ID ≤1024 | identity.go + Validate | Echo CheckCanonicalStrategyID; Forge parse instrument/dirección L/S | Ninguno | OK |
| StrategyVersion ref recompute | SPEC §6.5 receta S0 | Forge `Identity.Ref()` delega en `contracts.StrategyVersionRef`; Echo recompute en Validate | Ninguno | OK |
| Magic allocation | Forge owner (SPEC §4.3) | `AllocateMagicV1` cableado en Apply opt-in (Forge); Echo verifica/persiste, `TestNoEchoMagicAllocator` | Ninguno | OK |
| Magic propagation | magic_decimal string int64 en manifest | Forge estampa en config efectiva; viaje vía manifest al activarse handoff; Echo parse→bigint | Viaje real bloqueado por G1 | GAP_REAL (vía G1) |
| Strategy seal | Forge seal write-once | `SealStrategyVersion` implementado sin caller | G2 | GAP_REAL (sin cable) |
| Artefactos: refs verificados | SPEC §8 | Forge: readback+refs; Echo: copy+verify+FilesystemStore | Backend remoto no configurado en Echo V1 | GAP_REAL (G3) |
| Artifact source Echo | Puerto `ArtifactSource` | Producción = `unavailableArtifactSource` 503 fail-closed | G3 | GAP_BY_DESIGN V1 / bloqueante E2E |
| Ingestion write-once | SPEC §5.6-5.10, §9 | Implementado + tests 30+ + constraints DB | Ninguno | OK |
| Duplicates / replay | 200 exact replay sin artifact reads | `TestIngest_ExactReplay_200_NoArtifact`; fakeconsumer G06 | Ninguno | OK |
| Conflicting replay / G24 | 409 ambos lados | Echo `DetectSealedConflict`+UNIQUE; Forge `ErrSourceBindingConflict`+UNIQUE(decision,version) | Ninguno | OK |
| UNKNOWN_RECEIPT / ACK failure | SPEC §6.8, Forge estados | Forge park + no re-POST; Echo GET 200/404 retryable | Reconciliación GET no implementada en Forge | GAP_REAL (G4) |
| Partial failure | tx única Echo; manifest-first Forge | commitAcceptance atómico; DeliverHandoff persiste evidencia pre-delivery | Ninguno | OK |
| Persistencia boundary | echo.promotion_records / sqx.handoff_* | Ambas migraciones (061 / 015) presentes en sus runners | Ninguno | OK |
| Estados | HANDOFF_CREATED≠INGESTED≠PROVISIONED≠ACTIVE | Forge 6 estados CHECK; Echo INGESTED único CHECK | Ninguno | OK |
| Non-effects / forbidden writes | SPEC §11, AC-07..AC-09, AC-32 | Tests SOURCE + rowcount + REVOKE DB + sin imports cruzados | Ninguno | OK |
| Observability | Warn/attrs OTel | Echo warn con code; sin trace propagation cross-boundary | G7 | UNKNOWN |
| Backwards compatibility | HistoricalAdapter V1 explícito | `checkAuthorityVersion` admite adapter; 061 BWC backup | Ninguno | OK |
| Version compatibility | Pin 91671f6f46ff ≡ baseline (1 test-line diff) | sqx go.mod; diff git verificado | Ninguno | OK |
| Golden fixture CROSS_LANE | FORGE_GOLDEN_FIXTURE_PENDING | Sin output auténtico committed en ninguno de los 2 repos | G5 | GAP_REAL (gate T21) |
| IngestTradeHistory / métricas inline | CT-4 spec conceptual Forge | No existe en contrato frozen ni receptor | G6 | GAP_REAL (spec desactualizado) |
| LinkDemoDeployment | CT-4 spec conceptual Forge | Sin endpoint receptor ni contrato | G6b | GAP_REAL (solo-spec) |

## Gaps

- **G1 — GAP_REAL (central): no existe transporte real Forge→Echo.** Lado emisor: `BuildHandoffManifest`/`DeliverHandoff`/`SealStrategyVersion` sin ningún caller de producción (sólo tests) y sin cliente HTTP (`grep forge/promotions` en sqx = 0; única ingress = FakeConsumerIngress in-process de certificación CONTRACT). Lado receptor: endpoint completo y certificado, pero inalcanzable desde Forge. Cierre requiere: (a) decidir y cablear el punto del pipeline Forge que invoque seal→manifest→deliver post-FinalistPromotion/Apply, (b) implementar el cliente E-04 real que implemente `capabilities.HandoffIngress` con el mismo pin, (c) merge de F-04 a symphony master. Cualquier afirmación de que Forge "publica/envía" a Echo vía HTTP/Kafka es hoy FALSA.
- **G2 — GAP_REAL: `SealStrategyVersion` sin caller** (branch F-04): la versión sellada que el manifest declara como autoridad (`version.ref`, 4 sha256) no se produce aún en el flujo productivo; el manifest no puede construirse con datos sellados reales hasta cablear seal antes de build-manifest.
- **G3 — GAP_REAL (bloqueante E2E aunque G1 se cierre): Echo V1 no tiene backend de artefactos.** `unavailableArtifactSource` fail-closed 503: todo primer accept (que exige fetch+copy verificado antes del commit) fallaría; sólo el replay de registros ya commiteados funcionaría. Los artefactos de Forge viven en MinIO `sqx-strategies`; Echo no integra MinIO en V1 (SPEC §8 declara que la integración real usará el puerto contra stores allowlisted). Cierre: implementar ArtifactSource contra store allowlisted + configurar `gateway/forge_ingest/store_allowlist`.
- **G4 — GAP_REAL: reconciliación UNKNOWN_RECEIPT no implementada en Forge.** El receptor Echo provee el read idempotente (GET-by-key 200/404-retryable, SPEC §6.8/§12 y AC-11/AC-17), pero Forge no tiene el consumidor de ese read: `ErrUnknownOutcome`/`ErrDeliveryAwaitingReceipt` park la delivery sin camino automatizado de salida (comentario: "reconciliación futura por read idempotente E-04").
- **G5 — GAP_REAL (gate): `FORGE_GOLDEN_FIXTURE_PENDING`.** Ningún repo tiene output auténtico y versionado del producer F-04 (testdata Forge = copia byte-equal del corpus S0, verificado por sha256; corpus G01 usa producer `forge` ≠ `echo-forge-handoff`); T21/AC-37 CROSS_LANE GOLDEN no puede declararse PASS; E-04 queda INTEGRATED pero FINAL CLOSED = NO. No es bloqueante de merge (decisión 1.0.2 del spec).
- **G6 — GAP_REAL (documental, lado Forge): SPEC conceptual desactualizado.** `FEAT-SQX-ECHO-INGESTION/SPEC.md` describe un contrato API conceptual (idempotencia `wave_key+strategy_id+version`, payload con métricas/warnings/trade history inline, `EchoStrategyRef`, operación separada `IngestTradeHistory`) que difiere del frozen implementado (idempotencia S0 de 4 componentes, `HandoffEvidence` sólo por refs, sin trade-history endpoint, sin concepto EchoStrategyRef). El NEED-INFO NI-EI-1 que lo bloqueaba quedó parcialmente respondido por el contrato frozen E-04, pero el spec no fue superseded ni reconciliado. G6b: `FEAT-SQX-ECHO-DEPLOYMENT-LINK` (LinkDemoDeployment) no tiene receptor ni contrato en Echo.
- **G7 — UNKNOWN: observability cross-boundary.** No hay correlación de traces ni métricas compartidas definidas entre la delivery Forge y la aceptación Echo; cada lado observa por su cuenta.
- **No-gap verificado (compatibilidad latente cerrada por diseño):** el codec magic V1 admite dirección BOTH (dígito 3) pero `ParseMagicV1AllocationIdentity` sólo parsea L/S y el wire sólo acepta LONG/SHORT — ninguna estrategia BOTH puede producir magic ni manifest en V1 (comentario explícito en `magic_v1.go` L184-186).
- **Nota de diseño (no gap):** el puerto frozen `HandoffIngress.Deliver` no recibe el idempotency key como parámetro; el header `Idempotency-Key` exigido por Echo (400 si difiere) deberá derivarlo el cliente HTTP real vía `contracts.IdempotencyKey` — el fakeconsumer no lo necesita.

## End-to-End Flow (as implemented today)

- Paso 0-5 (Forge, cableado y operativo): GenericSQXWorkflow/ForgeCampaignWorkflow → Builder/Retester/Optimizer/FinalReretester → WFM durable → ranking/clasificación → selección robusta → FinalistPromotion V2 (membership estructural, zero-supply permitido) → Apply Selected Run con magic V1 opt-in (`UseDurableMagicAllocation` → `AllocateMagicV1` namespace `forge-live`, estampado en config efectiva del EA).
- Paso 6 (Forge, ROTURA 1 — no cableado): `SealStrategyVersion` (sellado write-once de la versión) NO es invocado por ningún flujo; la cadena seal→manifest no arranca.
- Paso 7 (Forge, capacidad sin emisor): `BuildHandoffManifest` puede construir y validar un `HandoffManifestV1` contra el contrato congelado (produciría body canónico, payload digest e idempotency key S0), pero nadie lo llama en producción; no existe fixture auténtica committed de ese output.
- Paso 8 (transporte, ROTURA 2 — inexistente): no hay cliente HTTP ni Kafka ni ningún transporte de red en sqx hacia Echo; la única `HandoffIngress` es `FakeConsumerIngress` in-process usada en tests CONTRACT (semántica 201/200/409 del fakeconsumer). `DeliverHandoff` con stores PG escribiría `sqx.handoff_manifests` (write-once, G24) y `sqx.handoff_deliveries` (HANDOFF_CREATED → INGESTED/CONFLICT/UNKNOWN_RECEIPT), pero no se ejecuta en producción.
- Paso 9 (Echo receptor, implementado y esperando): `POST /api/v1/forge/promotions` con Bearer `forge:ingest` + header `Idempotency-Key` → validación S0 + authority V2 → replay lookup → fetch+verify+copy de artefactos (aquí operaría ROTURA 3: `unavailableArtifactSource` 503 en V1 sin backend configurado) → transacción única mapping/version/promotion → COMMIT → 201 con receipt `state=INGESTED` (`receipt_id`, `payload_digest`, `strategy_version_ref`, `accepted_at`); replay 200 sin re-lectura de artefactos; 409 CONTRACT_CONFLICT / SOURCE_BINDING_CONFLICT / IDENTITY_CONFLICT; GET-by-key/GET-receipt para reconciliación.
- Paso 10 (Echo post-INGESTED, implementado): NADA más. Cero provisioning, activación, capital, portfolio, attach, current_version, Kafka, cuentas (impuesto por tests de non-effects, REVOKE DB y CHECK status='INGESTED'); el registro queda inspeccionable sólo vía GET del boundary; consumidores downstream (E-06+) no existen aún en código.
- Estado neto de la cadena: Forge termina en FinalistPromotion V2 + Apply (con magic asignado y estampado localmente); Echo expone un receptor certificado ocioso; la frontera real implementada HOY es únicamente el pin del módulo `contracts` (identidad de tipos y recetas compartidas) — ningún byte de handoff cruza la frontera en producción.

## Ownership

- Tabla de ownership en la frontera (autoridad única, sin dual): GeneratedStrategy/canonical/StrategyRef = Forge crea, Echo verifica y persiste mapping declarado; Magic allocation = Forge registry (recatalogado e immutable), Echo nunca asigna/recicla/infiere (AC-09, TestNoEchoMagicAllocator); StrategyVersion seal = Forge (receta S0), Echo recomputea ref e inserta write-once sin sellar paquetes distintos; FINALIST_PROMOTION membership = Forge Decision, Echo valida MemberProof sin recalcular rank/score; HandoffManifest bytes = Forge producer write-once, Echo persiste el body canónico aceptado sin regenerarlo; Acceptance/receipt = Echo ingestion + Echo DB (UNIQUE/FK/tx como prueba durable, sin segunda tabla de receipts); Transporte = hoy sin owner efectivo (capacidad en ambos lados, cable en ninguno); Runtime activation = Echo E-06+ (futuro), Forge nunca la afirma.
- Identity Propagation por identificador (creator / owner / transported as / validated by / persisted by / mutability): `canonical_strategy_id` (Forge pipeline / Forge / manifest.strategy.canonical_strategy_id / CheckCanonicalStrategyID ≤1024 bytes ambos lados / echo.strategy_identity_mappings PK con ns / write-once immutable); `strategy_ref` UUID (Forge / Forge / manifest.strategy.strategy_ref, igualado a member_proof por G08 / CheckUUIDLower / mapping + FK promotion / immutable); `version_ref` (Forge seal / Forge / manifest.version.version_ref / recompute receta `echo-strategy-version.v1` en Validate + versions store / echo.strategy_versions y sqx.strategy_versions / write-once immutable); `magic` (Forge AllocateMagicV1 / Forge registry / magic_decimal string int64 + allocation_ref sha256 + assigned_at RFC3339 / int64 positivo + parse + conflicto de mapping / mapping.magic bigint / 1:1 sin reciclaje); `decision_ref` (Forge Decision store / Forge / promotion.decision_ref / CheckOpaqueRef + binding secundario / promotion_records + handoff_manifests / immutable por versión); `wave_key` (Forge campaign / Forge / promotion.wave_key / CheckSemanticKey / parte de la idempotency key / immutable por binding); `idempotency_key` (ambos computan la receta S0 / Echo es autoridad en el accept / header + derivación interna / comparación estricta header==derivado / UNIQUE(ns,key) / immutable); `payload_digest` (ambos computan sobre Encode() / Echo autoridad en el accept / digest interno + receipt / DetectSealedConflict / promotion_records.payload_digest / immutable); `registry_namespace` (credencial Echo config / Echo / nunca viaja en body / CheckSemanticKey / scope de todas las filas / configuración fija `forge-live` en V1 ambos lados).

## Conflicts / Unknowns

- Sin contradicciones de evidencia entre artifacts B/C y el source verificado: la afirmación "no existe transporte real" quedó CONFIRMADA independiente en ambos repos; el pin de contracts y la byte-equality del fixture G06 también.
- UNKNOWN 1: dónde se cableará seal→manifest→deliver dentro del pipeline Forge (post-FinalistPromotion vs post-Apply es sólo inferencia de diseño; no hay TODO ni contrato de wiring en código).
- UNKNOWN 2: si la coincidencia de namespace (`forge-live` constante Forge vs namespace configurado Echo) se mantiene cuando Echo rote tokens/namespaces (spec: "Rotar el token no cambia namespace ni keys históricas"; no hay mecanismo de negociación de namespace).
- UNKNOWN 3: propagación de observability cross-boundary (trace/métrica compartida) — no definida en contrato ni código.
- UNKNOWN 4: comportamiento operativo real del entorno (DSN PG, etcd, MinIO, token ingest configurado) — se cartografió código/config, no runtime vivo.
- LOW: fixture dirty de symphony (`phase4_performance.json`) puede alterar resultados de ese test en working tree; no toca la frontera.
- Nota de divergencia baselines: Echo master ya contiene el receptor (E-04 INTEGRATED), pero los docs de master declaran T21 pendiente y F-04 sigue branch-only en symphony — la wiki NO debe simetrizar el estado de los dos repos.

## Evidence

| Claim | Repo/file | Símbolo / sección | Test/config | Conf. |
|---|---|---|---|---|
| Contrato frozen V1 + IdentityModelVersion 2 + receta idempotencia | echo v3/sdk/contracts/promotion.go | IngestionContractVersion, IdempotencyKey, PromotionRecordStatusIngested | promotion_test.go; corpus testdata/v1 | HIGH |
| Cross-checks G08/G09/G10/G11/G12 en Validate | echo v3/sdk/contracts/promotion.go | HandoffManifestV1.Validate | corpus_test.go, gaps_test.go | HIGH |
| Receptor: 3 rutas frozen + misconfig 503 + 401 | echo v3/gateway/internal/server.go, forge_ingest_handler.go | ServeHTTP, misconfigured | forge_ingest_handler_test.go, TestServer_MountsForgeIngest | HIGH |
| Orden §9 + replay antes de artefactos + tx única + 409/G24/IDENTITY | echo v3/sdk/postgres/ingestion_service.go | Ingest, commitAcceptance, checkAuthorityVersion | ingestion_service_test.go (30+), ingestion_noneffects_test.go | HIGH |
| Artifact source producción fail-closed 503 | echo v3/gateway/internal/forge_ingest_handler.go; server.go | unavailableArtifactSource.Fetch, newForgeIngestService | — (código de producción) | HIGH |
| Copy verificado + allowlist + FilesystemStore | echo v3/sdk/postgres/ingestion_artifact.go | copyOperationalArtifacts, RefAllowed, NewFilesystemStore | ingestion_artifact_test.go | HIGH |
| Receipt proyección receipt_id/state INGESTED | echo forge_ingest_handler.go | forgeIngestReceipt, projectReceipt | handler tests | HIGH |
| DB write-once: CHECK INGESTED, UNIQUEs G24, FKs compuestas, REVOKE | echo migrations/061_identity_bwc_foundation.up.sql L394-431, L500-503 | DDL | strategy_identity_repository_test.go | HIGH |
| Echo sin magic allocator | echo v3/sdk/postgres/identity_bwc_gate_test.go | TestNoEchoMagicAllocator (grep SOURCE sobre productivo) | PASS declarado en VERIFICATION E-04 | HIGH |
| Non-effects: cero filas runtime, status único, sin delta contratos/migraciones | echo v3/sdk/postgres/ingestion_noneffects_test.go | TestIngest_NoRuntimeTables, TestIngest_StatusOnlyIngested, TestSOURCE_* | suite completa | HIGH |
| Producer frozen echo-forge-handoff 1.0.0 + G22 + membership estructural | symphony sqx/core/forge/handoff_producer.go | BuildHandoffManifest, finalistMembership, HandoffMembersForDecision | handoff_producer_test.go | HIGH |
| Delivery write-once, byte-equality gate, UNKNOWN_RECEIPT no re-POST | symphony sqx/core/capabilities/handoff.go | DeliverHandoff, ErrDeliveryAwaitingReceipt | delivery_test.go (6 tests) | HIGH |
| Stores PG handoff write-once + G24 UNIQUE + 6 estados CHECK | symphony sqx/adapters/registry-postgres/strategy_version.go; migrations/015_...up.sql L37-59 | PersistHandoffManifest, SaveHandoffDelivery | 015_..._test.go | HIGH |
| F-04 sin callers de producción y sin transporte | symphony (grep repo) | BuildHandoffManifest/DeliverHandoff/SealStrategyVersion sólo en definición+tests; `forge/promotions` = 0 hits; adapters/echo-handoff = fake + tests | — | HIGH |
| Única ingress = fakeconsumer in-process 201/200/409 | symphony sqx/adapters/echo-handoff/fake_ingress.go; echo v3/sdk/contracts/fakeconsumer/fakeconsumer.go | FakeConsumerIngress.Deliver, Consumer.Ingest | delivery_test.go, fakeconsumer_test.go | HIGH |
| Magic cableado sólo en Apply opt-in, namespace forge-live | symphony sqx/activities/worker/durable_apply_selected_run.go L210-233; sqx/core/capabilities/magic_allocation.go L14 | resolveEffectiveConfig, AllocateMagicV1, MagicRegistryNamespaceLive | durable_apply_selected_run_magic_test.go | HIGH |
| Magic V1 L/S-only desde identidad (BOTH codec sin flow) | symphony sqx/core/domain/magic_v1.go | ParseMagicV1AllocationIdentity L184-204 | magic_v1_test.go | HIGH |
| Version ref receta compartida (delegación a contracts) | symphony sqx/core/domain/strategy_version.go L143-151 | StrategyVersionIdentity.Ref → contracts.StrategyVersionRef | domain tests | HIGH |
| Pin contracts 91671f6f46ff ≡ baseline echo (1 test-line) | symphony sqx/go.mod; git diff en repo echo | require contracts | git diff --stat verificado | HIGH |
| Fixture Forge = copia S0, no output auténtico | sha256 G06 g06-original.json ambos repos = c50358c3...e22; testdata = {G06,G07,G20,G21,G22,manifest.json} | — | sha256sum verificado | HIGH |
| Corpus G01 producer `forge` ≠ producer F-04 | echo v3/sdk/contracts/testdata/v1/G01/g01-valid.json | producer.id | — | HIGH |
| E-04 INTEGRATED en echo master; T21 pending | echo specs/FEAT-FORGE-INGESTION-E1/VERIFICATION.md L3-15; git cat-file master | — | declaración del spec + cat-file | HIGH |
| F-04 ausente en symphony master | git cat-file -e 0b9742b0:sqx/core/forge/handoff_producer.go y migrations/015 | ABSENT | verificación directa | HIGH |
| Specs conceptuales Forge desactualizados/bloqueados | symphony specs/FEAT-SQX-ECHO-INGESTION/SPEC.md §2/§3/§6; specs/FEAT-SQX-ECHO-DEPLOYMENT-LINK/SPEC.md | NEED-INFO NI-EI-1..3, NI-DL-1 | — | HIGH |

## Handoff

- Documentable como CONTRATO VIGENTE (para la wiki, página frontera `Echo — Forge Integration Boundary`): el contrato frozen S0 (`xKoRx/echo/v3/sdk/contracts`, `IngestionContractVersion = forge-echo-ingestion.v1`, `IdentityModelVersion = 2`, recetas IdempotencyKey/PayloadDigest/StrategyVersionRef, MemberProof estructural, receipt INGESTED único) es la autoridad compartida por pin explícito (`sqx/go.mod` → 91671f6f46ff) con superficie idéntica al baseline Echo; el SPEC E-04 1.0.2 define el boundary receptor completo (endpoints, auth forge:ingest + namespace forge-live, matriz de errores, orden §9, non-effects, ownership sin dual authority); el puerto `HandoffIngress` y los estados de delivery Forge están frozen en código F-04; las separaciones INGESTED ≠ PROVISIONED ≠ ACTIVE y INGESTION ≠ PROVISIONING ≠ OBSERVATION ≠ ELIGIBILITY ≠ CAPITAL ≠ ACTIVATION sostienen el source actual en ambos lados.
- Documentable como ESTADO IMPLEMENTADO (siempre con baseline y fecha): receptor Echo operable en reposo (rutas + validación + tx + idempotencia + errores, integrado también en echo master a99f9a63, con E-04 INTEGRATED y T21/AC-37 pending); Forge con capacidad productor completa a nivel librería SÓLO en `feature/f04-magic-version-handoff` (9fad768c, no en master 0b9742b0): BuildHandoffManifest, DeliverHandoff, stores PG 015, FakeConsumerIngress, SealStrategyVersion; magic V1 asignado y estampado en Apply opt-in; pipeline Forge termina en FinalistPromotion V2 + Apply.
- Documentable como GAP CONOCIDO (estado y qué falta para cerrar): G1 sin transporte real (emisor sin cablear y sin cliente HTTP; receptor ocioso); G2 seal sin caller; G3 Echo sin backend de artefactos en V1 (unavailableArtifactSource 503 fail-closed → ningún primer accept posible hoy); G4 reconciliación UNKNOWN_RECEIPT sin consumidor en Forge (el GET idempotente Echo ya existe); G5 FORGE_GOLDEN_FIXTURE_PENDING (corpus S0 es synthetic, producer del corpus `forge` ≠ `echo-forge-handoff`, E-04 no FINAL CLOSED); G6 spec conceptual Forge (`FEAT-SQX-ECHO-INGESTION`) divergente del frozen y con NEED-INFO obsoleto, más LinkDemoDeployment sin receptor.
- Lo que la wiki NO debe afirmar: que Forge publica/envía estrategias a Echo (falso hoy); que existe un flujo automático post-FinalistPromotion (no cableado, ubicación futura desconocida); que Echo puede ingerir end-to-end (fallaría en artifact fetch en V1); que CROSS_LANE/GOLDEN o el join Forge está certificado (T21/AC-37 PENDING, fixture pendiente); que el SPEC conceptual Forge describe el contrato real (está superseded de facto por el frozen; citarlo sólo como historia); que magic es asignado o validado por Echo (Forge-only); simetría de integración entre repos (receptor en master Echo, productor branch-only en symphony); que INGESTED implica activación, provisioning, capital o eligibility en cualquier grado.
- Puentes útiles para Fase F: los tests `ingestion_service_test.go`/`ingestion_noneffects_test.go` (Echo) y `delivery_test.go`/`handoff_producer_test.go` (Forge) son el corpus de gates citable; la tabla Boundary Matrix de este artifact mapea 1:1 los temas a documentar; el pin de contracts y la byte-equality del fixture G06 son la evidencia de compatibilidad de versión a citar; toda cita debe llevar baseline (`echo f7ddea18` / `symphony 9fad768c`) y reconciliarse contra HEAD al publicar (fase H).
