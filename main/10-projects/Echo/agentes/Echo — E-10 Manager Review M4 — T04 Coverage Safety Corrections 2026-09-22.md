# Echo — E-10 Manager Review M4 · 2026-09-22

**Autoridad:** manager técnico de Echo. **Repo:** `xKoRx/echo`, única feature `feature/e09-execution-copy-reconciliation-fidelity`. **Baseline revisado:** `d27544153d795dbd041125d6312c0fcdc1481a69`. **HEAD remoto verificado:** `bea4c099f75c601bb51d5ede16a03db98b118f3e`; `master` permanece `5dd998f16aea7b2821f460188718d7a6d279829c`, rescates preservados. **Decisión:** `M3_POLICY_PIN_SOURCE_ACCEPTED · T04_SOURCE_PRESENT_CORRECTION_REQUIRED · T05_T10_NOT_AUTHORIZED · POLICY_RATIFICATION_UNACCREDITED · PHYSICAL_PENDING · INTEGRATION_PENDING · FINAL_CLOSED=NO`. M4 prevalece sobre cualquier handoff que declare T04 listo para consumirse en T05.

## Contraste Git y alcance

GitHub compare `d2754415...bea4c099`: tres commits FF, behind=0, nueve rutas: `strategy_quality_stores.go` y su test, cuatro archivos nuevos de ensamblador domain/postgres y tests, SPEC/TASKS/VERIFICATION. Ningún cambio en migraciones 001–069 ni en source E-08/E-09, S0, Forge o master. Los commits son `6cad71ca` (policy pin), `508c3e57` (T04) y `bea4c099` (docs). `expectationPolicyPin` lee los campos de la Expectation durable re-verificada; `PutAssessment` y `PutDecision` exigen ref/digest igual al sello previo, con `ErrPolicyPinMismatch` antes de insertar; corrección de source aceptada. El ejecutor reporta PG17 E-10 18/18 -race, harness E08/E09 PASS, failing set 121/121 idéntico en PostgreSQL contra base de E10, build/vet PASS. Estos resultados NO fueron ejecutados de manera independiente por el manager; no se equiparan a certificación física.

## M4-R1 — conflicto VALID_NO_SIGNAL conserva exposición positiva

**Source:** `v3/sdk/domain/strategy_quality_coverage.go`, bucle `for _, c := range components` posterior a creación de `components` y cálculo de `asm.Totals`. Cuando `verifiedOpens` cae dentro de un `CoverageValidNoSignal`, sólo se agrega `CoverageConflictNoSignalTrade` a `asm.Conflicts`; el componente `CoverageValidNoSignal` y `asm.Totals.ValidNoSignal` ya calculados no se invalidan. `TestE10CoverageAssembly_TradeInsideValidNoSignalIsConflict` comprueba la presencia del conflicto, NO el estado del vector ni los denominadores. La prueba física `TestE10CoverageAssemblerPG_ShadowForeignVersionWrongBinding` además siembra un trade CANONICAL a las 09:30 dentro de VNS [09:00,10:00) y espera erróneamente `ValidNoSignal == 1h`. Esto es fuente concreta de denominador contradictorio para T05. **Corrección:** identificar el sub-intervalo VNS contradicho, preservar conflicto tipado y provenance, degradar esa exposición a `UNKNOWN` sin límites (o devolver error tipado fail-closed sin ningún resultado consumible), y recalcular duración sin contabilizar el tramo contradicho; otros tramos probados permanecen. Caso exacto [09,11) VNS + trade 09:30 => ninguna hora positiva de VNS en tramo contradicho; segmentación y límites half-open comprobados. Dos o más trades en mismo intervalo no cuentan dos veces. Tests rojo→verde tanto puro como lector PG, ajustar expectativa previa equivocada; T05 nunca debe obtener un vector/total contradictorio.

## M4-R2 — basis temporal NULL rompe scan en lector READ ONLY

**Source:** `v3/sdk/postgres/strategy_quality_coverage.go`, `loadLifecycleTrades`: `open_event_time_basis` se escanea directamente sobre `t.OpenTimeBasis string`. Migración 065 `echo.trade_lifecycle.open_event_time_basis text CHECK (IS NULL OR IN ('UTC','OFFSET','BROKER_LOCAL','UNKNOWN'))` permite NULL; query incluye explícitamente `open_event_time_basis IS DISTINCT FROM 'UTC'`, por tanto selecciona esos NULL. `database/sql` no escanea SQL NULL a Go string. Resultado: error de Scan en lugar de `TradesUnverifiableTime`/evidencia UNKNOWN. **Corrección:** usar `sql.NullString` (o equivalente nullable), representar ausencia semánticamente sin inventar UTC y contabilizarla en el ensamblador como tiempo no verificable; conservar distinción de NULL en digest de inputs o documentar y demostrar una representación canónica inequívoca. Tests PG rojo→verde con fila cuyo basis es NULL, `open_event_time_utc` NULL y/o presente, verificando ausencia de conversión, conteos, digest reproducible y cero observación falsamente válida. No modificar DDL 065.

## Única autorización de implementación

Un solo agente NORMAL, worktree aislado, baseline exacto `bea4c099f75c601bb51d5ede16a03db98b118f3e`, FF-only; STOP ante avance concurrente o dirty ajeno, sin reset/rebase/force. Corregir exclusivamente M4-R1 y M4-R2, actualizar tests físicos/herméticos previamente incorrectos y SPEC/VERIFICATION/TASKS por delta. No implementar T05 ni otros T06–T10. PG17 propia descartable con marcador E-10; no reutilizar puertos/datadirs ajenos; no correr suites destructivas en base compartida. Gates: rojo→verde por defecto, consistencia vector↔totals↔conflicts, null basis cubierto, reproducibilidad del digest, E10 harness, build/vet/race en scope, regresiones E08/E09 sólo en sus bases exclusivas si se ejecutan, failing sets apples-to-apples; ningún cambio migrations 001–069 ni S0 ni fuera del alcance sin STOP. Commits atómicos, FF push/read-back, estado Agents-OS + agent_run/change_log/session-close. Entregar SHA, errores/fixtures R1/R2, tests y gate restante. Ratificación owner queda UNACCREDITED y ninguna `EligibilityDecision` real autorizada; E09 coverage 88,6 % con su deuda pendiente. **STOP tras R2 y review manager; T05 no autorizado todavía.**