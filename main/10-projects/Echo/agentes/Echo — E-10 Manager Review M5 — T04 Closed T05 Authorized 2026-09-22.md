# Echo — E-10 Manager Review M5 · T04 CLOSED / T05 AUTHORIZED

**Fecha:** 2026-09-22. **Rol:** manager técnico de Echo. **Autoridad:** este mandato sobre el alcance/orden T05; prevalece sobre el estado anterior «T05–T10 NOT AUTHORIZED» exclusivamente para T05. No reemplaza S0, Live Authority, M1–M4 ni SPEC E-10 v1.4.0.

## Estado contrastado

Repo `xKoRx/echo`; única branch `feature/e09-execution-copy-reconciliation-fidelity`. Baseline revisado = HEAD remoto `a861e73b6c11a87f2e13b97f5c7598de9c406e68`; compare `bea4c099...feature/e09`: ahead=3, behind=0, siete rutas acotadas: cuatro archivos `v3/sdk/{domain,postgres}/strategy_quality_coverage{,_test}.go` y `specs/FEAT-STRATEGY-QUALITY-ELIGIBILITY-E10/{SPEC,TASKS,VERIFICATION}.md`. Commits `bf243307` R1 → `9c3f347f` R2 → `a861e73b` docs. `origin/master` esperado `5dd998f16aea7b2821f460188718d7a6d279829c` (confirmar de nuevo al preflight). Ninguna migración tocada en el delta Git.

**M4-R1 SOURCE ACCEPTED:** `AssembleObservationCoverage` marca por claim `VALID_NO_SIGNAL` un trade CANONICAL UTC dentro de intervalo half-open antes de resolver spans; `vnsTradeConflict` fuerza estado desconocido sin denominador antes de sumar `Totals.ValidNoSignal`, genera conflicto tipado, preserva otros claims y evita duplicación por múltiples trades dentro del mismo claim. Tests herméticos específicos de bounds, claims intactos y trade múltiple + PG test con expectativa anterior corregida materializados. No se repite auditoría general.

**M4-R2 SOURCE ACCEPTED:** lector `loadLifecycleTrades` escanea `open_event_time_basis` con `sql.NullString`, lo proyecta como `*string` nil para SQL NULL; dominio clasifica nil/non-UTC como `TradesUnverifiableTime`, no como `TradesObserved`, y digest diferencia null vs string. Test físico PostgreSQL NULL con/sin instante y digest reproducible materializado.

**Evidencia declarada por NORMAL, NO reejecutada por manager:** E-10 PG17 descartable exclusivo :15467; run.sh PASS; domain -race 230/230 PASS y tests E10 PG -race PASS; failing set postgres 121/121 por nombre idéntico a baseline; M3-P1 sin drift, build/vet/gofmt delta PASS; marcadores de instancia y cierre físico reportados. No convertir esa evidencia de clase A en PHYSICAL/INTEGRATION PASS.

**DECISIÓN:** `M4-R1=ACCEPTED · M4-R2=ACCEPTED · T04=SOURCE/CONTRACT CLOSED · T05=CONDITIONALLY AUTHORIZED (CLASS A ONLY) · T06–T10=NOT AUTHORIZED · POLICY_RATIFICATION=UNACCREDITED · PHYSICAL/INTEGRATION=PENDING · COVERAGE_GATE=T09 PENDING · FINAL_CLOSED=NO`.

## Mandato de implementación T05 — UN solo agente NORMAL

**Preflight:** bootstrap Agents-OS y carga de esta nota + E-10 SPEC v1.4.0/S4/S6/S11, PLAN/TASKS, E-05 writer/calculator/catalog S0 y Live Authority. Verificar SHA actual de `origin/feature/e09` = `a861e73b6c11a87f2e13b97f5c7598de9c406e68`, `origin/master` = `5dd998f16aea7b2821f460188718d7a6d279829c`, worktree ownership, dirty ajeno y rescue refs antes de escribir. STOP si cambia origin o existe conflicto; no reset/rebase/force. Worktree sesión aislado, PostgreSQL17 descartable identidad exclusiva E-10 para tests.

**Scope T05, sin T06:** implementar gates DQ estructurales S6 en orden estricto con short-circuit (1) identidad estrategia/versión/binding/Expectation y sellado físico pre-forward cuando se tenga evidencia; (2) liveness/coverage del vector T04, UNKNOWN y conflictos con reason tipada sin números de política inventados; (3) atribución canónica version-proven, riesgo inicial KNOWN para R, economics_completeness y bases comparables; (4) SÓLO cuando pasan anteriores, comparación económica contra baseline E-04/E-05 ya ingerida por la Expectation. Si depende de mínimos cuantitativos de política aún no ratificada, devolver diagnóstico no autorizante y dejar su evaluación a T06. DQ fail-closed: UNKNOWN no es cero, no score ni comparaciones económicas ante bloqueo, conflicto material ≠ muestra insuficiente, `VALID_NO_SIGNAL` sólo como exposición coherente, `MARKET_CLOSED`/`STRATEGY_DISABLED` reportadas separadas, PF indefinido no 0, NULL fees/fills no 0, DD closed trades ≠ equity intradía.

**Forward sets:** derivar operaciones Reference observadas únicamente desde `echo.trade_lifecycle` y `echo.trade_deals` versión y binding CANONICAL verificados de 065, bases comparables declaradas en `allowed_comparisons`; baseline sólo refs Forge E-04/E-05 ingeridos y ya existentes, sin recrearla; persistir scopes/tradesets/metricsets forward NUEVOS solamente mediante `postgres.CanonicalWriter.Write` y `analytics/calculator.Compute` frozen; validar `contracts.Catalog` S0 v1.0.0 y par clave/basis en allowlist, sin fork calculator ni claves inventadas. Writes exclusivamente 063 `CanonicalWriter.Write` INSERT write-once y 069 objetos propios cuando el WP lo requiera; 001–062 y 064–068 READ ONLY, SQL fixtures externos sólo sobre PG exclusivo. No escribir `economic_copy_authorizations`, no leer 066/067 en calidad, no Kafka, órdenes ni publicación. Probar replay exacto cero filas extra y fallo transaccional ante conflicto; demostrar preimage/postimage ajeno.

**Guardas antes de scoring:** identidad y provenance durable, snapshot consistente entre coverage/trades/deals/E-05 para no mezclar hechos de momentos distintos, baseline digest realmente verificable; si falta un contrato para transformar 065 → OperationsNDJSON S0 sin inventar semántica (fees/fills/risk/units), STOP con punto exacto y opción mínima acotada, NO completar con ceros/sintéticos disfrazados. No modificar S0 ni E-04–E-09 ni migraciones; no relajar gates para pasar tests.

**Tests/gates:** rojo→verde dominio y PG17 exclusiva por cada clase DQ (identidad ajena, conflicto VNS, UNKNOWN, tiempo sin basis, riesgo R desconocido, economics NULL, keys/basis no permitidas, PF indefinido, no baseline/Expectation, replay y versión nueva); assertions cero writer si DQ falla y sólo scope nuevo si pasa; build/vet/gofmt delta, -race, arnés E-10, failing set baseline apples-to-apples por nombres, no-effects fingerprint para 001–068 y migraciones byte-intactas. Coverage por archivo informativa, G4 formal reservado T09. No certificar resultados físicos reales con fixtures.

**Archivos autorizados:** crear archivos `v3/sdk/domain/strategy_quality_dq{,_test}.go`, `v3/sdk/postgres/strategy_quality_forward{,_test}.go` o equivalentes de nombre E-10 dentro de `v3/sdk/domain/` y `v3/sdk/postgres/` exclusivamente; extensión mínima de archivos `strategy_quality*.go` si es indispensable, nuevos fixtures/tests E-10 dentro de arnés propio y docs `specs/FEAT-STRATEGY-QUALITY-ELIGIBILITY-E10/{SPEC,TASKS,VERIFICATION}.md` por delta. Si requiere editar otro archivo, STOP con ruta y razón. `go.mod`/`go.work`, SDK contracts S0, migrations, core/gateway, E-08/E-09, Forge/E-11/E-12 no autorizados.

**Cierre:** commits atómicos en única feature con FF + read-back, docs y Agents-OS agent_run/change_log/session-close. Handoff corto RESULT, source/contract, SHA, artefactos y evidencia PG, failing set, DQ cases, writers 063 effects exactos, residual, gates; STOP tras T05. T06/T07 owner-ratification write path UNRESOLVED; no emitir ni certificar EligibilityDecision real.