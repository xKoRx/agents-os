---
type: doc
schema_version: 1
status: active
scope: project
project: "[[AGENTS OS]]"
area: "[[Personal]]"
created: 2026-09-13
updated: 2026-09-13
description: P3-C Canonical Linter Adversarial Verifier — verificación adversarial e independiente de la tool canonical-linter (motor, selftest, README, spec/modelo y runs reales). Clasificación D/N/S/U de los 14 ataques obligatorios y ampliaciones, muestreo estadístico de los FAIL masivos y veredicto global.
aliases:
  - p3-adversarial-verification
tags:
  - kind/doc
  - project/agentsos
  - tech/agents-os
---

# P3-C — Verificación Adversarial del Canonical Linter

- Rol: verificación adversarial (P3-C), sin capacidad de lanzar subagents ni modificar nada salvo este artifact; mutaciones sólo en tempdirs del sistema. Fecha: 2026-09-13. VAULT_ROOT = `/home/kor/secondbrain/main` (marker verificado). Objetivo: DEMOSTRAR que la tool está mal; donde no lo logré, queda PASS con la evidencia de lo atacado.
- Objeto bajo verificación: `80-agents/tools/canonical-linter/` (`canonical_linter.py` 1719 líneas con CL-01..CL-20, `selftest.py`, `README.md`, `artifacts/p3-canonical-model.md`, `artifacts/p3-canonical-linter-spec.md` con la enmienda CL-01, `artifacts/p3-implementation-notes.md`, `results/run-*.json`). El harness, `validate_schema_contract.py`, `lint.py`, `doctor.py` y context-budget NO están bajo verificación (sus defectos serían hallazgos de sistema S<n>).

## 1. Alcance y método

- Reproducción base: ejecuté la suite completa dos veces (auto y `--vault-root` absoluto) y comparé con los dos runs previos (`run-20260913-071448.json` git `c94d994`, `run-20260913-072436.json` git `12ab1fd`): en los tres casos PASS 6 · FAIL 5 (CL-06 22, CL-11 337, CL-14 3, CL-15 4, CL-18 1) · WARN 9 · SKIP 0 · 1003 findings, exit 1 — el estado declarado es reproducible y estable entre 3 git heads.
- Método por ataque: (a) análisis forense de los findings del run real contra el disco (mapas vault-wide de basenames y aliases, incluyendo zonas excluidas); (b) reproducción de la lógica importando `canonical_linter` como librería (mismo código, sin fork); (c) mutaciones adversariales en tempdirs (vaults sintéticos con copia de infraestructura, mismo método del selftest) para falsos negativos y edge cases; (d) verificación de determinismo, CLI, exit codes y SKIP/malformado; (e) lectura del código y del spec para contrastar veredicto/autoridad/dedup línea por línea.
- Ataques ejecutados: los 14 obligatorios más ampliaciones propias (clasificación exhaustiva 100% de CL-11, hashes de contenido en CL-06, enumeración de todos los 00-index bajo 30-resources, recomputación del set M16, hueco de multi-link en filas de índice, colisiones sólo-por-acento, links en campos no-routing, colisión de nombres de results en el mismo segundo).
- selftest.py ejecutado por mí: 8/8 PASS; su cobertura declarada (positivos/negativos por check, A8, determinismo, exit 2, malformado, schema del record) es real, no ficticia.

## 2. Tabla de findings

Convención: D<n> = defecto MATERIAL de la tool (expected vs observed + repro); N<n> = nota menor (limitación declarada, ruido documental o imprecisión sin impacto en veredictos); S<n> = hallazgo de SISTEMA (defecto real del vault que la tool confirma; se registra, no se corrige); U<n> = unknown. Severidad/confidence según el vocabulario del spec.

- **D1 — CL-14: filas de índice con varios wikilinks: sólo el PRIMER link de cada fila se verifica (`_index_rows` usa `WIKILINK_RE.search`, no `finditer`).** Ataque 3 (ampliación propia: hueco multi-link). Expected: el spec dice "Fila **o link** de un 00-index ... apuntando a archivo inexistente" → cada link de la fila debe verificarse. Observed: `30-resources/aranea/00-index.md` línea 125 contiene DOS links rotos (`[[../../10-projects/Aranea/BACKUP-DR-OWNER-PROJECT/agentes/agent-project-00]]` y `.../agent-project-08`); el run reporta sólo `agent-project-00` (3 findings en total) y `agent-project-08` queda sin hallazgo (verificado: `resolve("...agent-project-08")` = missing, sin asset en disco). Repro: enumerar las filas de los 13 índices en scope: 22 filas con ≥2 wikilinks, 1 link roto no cubierto. Severidad: menor (no invierte ningún veredicto; el mismo archivo ya está en FAIL), confidence EXACT. Fix trivial para P3-D: `finditer` + dedup por target por fila + caso positivo en selftest.

- **N1 — CL-06: el veredicto FAIL incluye 6 grupos estructurales mandados por autoridad.** Los 22 findings = 16 de identidad + 6 estructurales (`skill` ×48, `readme` ×12, `log` ×11, `00-index` ×13, `index` ×3, `runbook` ×2 — filenames por-directorio exigidos por doctor/00-RESOURCE-WIKI). La decisión "sin whitelist, con anotación en evidencia" está declarada en `THRESHOLDS` y README (limitación 2); el veredicto FAIL se mantiene igualmente por los 16 de identidad, así que no hay inversión; pero el conteo de "22 colisiones" infla el señalado al operador. La decisión de whitelistrar es del owner (S6).
- **N2 — CL-11: redacción imprecisa en los 27 findings hacia journal.** Esos destinos SÍ existen en disco (`80-agents/journal/{logs,agent-runs,sessions,feedback}/...`) y Obsidian los resolvería; el `observed` dice "no resuelve a ninguna nota" cuando lo correcto es "existe en zona no canónica (journal)". El veredicto FAIL es correcto según la autoridad ratificada (relation-maintenance: no enlazar sessions/logs; agents-os.md: journal es auditoría; THRESHOLDS declara el scope de resolución), pero la evidencia debería distinguir inexistencia de exclusión por política.
- **N3 — CL-11: los campos de routing ratificados dejan 176 wikilinks de frontmatter sin verificar; 27 están rotos.** Campos fuera de ROUTING_FIELDS con wikilinks en el corpus: `sources` 112, `source_session` 14, `sprint` 12, `banner` 11, `children` 9, otros 18; rotos: `banner` 11, `source_session` 14, `sources` 1, `owner` 1. Es la limitación declarada del README (limitación 1) y ROUTING_FIELDS es la lista ratificada del modelo; cuantificado aquí para el parent.
- **N4 — CL-15/CL-18: destinos con estado "en retiro" FUERA del enum del tipo no se marcan (delegación A3).** Verificado por mutación: una fila de 00-index hacia una nota `type: doc` con `status: deprecated` (fuera del enum de doc: active/archived) NO produce finding (CL-15 ni CL-18), porque `life_state` sólo clasifica en-enum y delega el valor a `lint.py:bad-status`. Con tipo in-enum (`application`) sí marca FAIL (mutación verificada). Es la decisión A3 declarada en `ambiguities`; el residuo es que lint.py marca el status pero NADIE marca la fila del índice que apunta a esa página. Hoy sin instancia real detectada en los índices en scope.
- **N5 — CL-13: links de sucesión canónica producen WARN sistemático.** 36 de los 108 findings vienen de campos de frontmatter e incluyen pares legítimos de sucesión (p.ej. `30-resources/agents/skills/pr-description/SKILL.md` y la continuidad global enlazando sus predecesores archived vía `supersedes`). La cláusula "unless intentional" está respetada (108/108 WARN, cero FAIL), pero los enlaces de sucesión son intencionales por definición (00-RESOURCE-WIKI) y podrían eximirse (SUCCESSION_FIELDS) o etiquetarse en evidencia.
- **N6 — CL-19 marca la plantilla `70-templates/resource.md`** (status active sin last_verified): el template entra al corpus por definición de `iter_vault_md` y técnicamente cumple el tipo; es ruido WARN sin consecuencia.
- **N7 — CL-06 no cubre colisiones sólo-por-acento** (la autoridad `90-system/convenciones.md` menciona "mayúsculas, acentos o singular/plural"; el check ratificado es casefold). Sonda con NFD-fold sobre los 853 basenames del corpus: 0 grupos que colisionen sólo por acento hoy — gap teórico, sin impacto actual.
- **U1 — Precedencia real de resolución de Obsidian vs A9 (path-relativo primero) no es observable sin Obsidian.** El algoritmo implementado es exactamente el ratificado (A9) y sus 5 pasos funcionan (verificado por mutación); si Obsidian priorizara otro candidato en algún caso de colisión, el destino reportado podría diferir. UNKNOWN por diseño, ya declarado en A9/README; no imputable a la tool.

## 3. Hallazgos de SISTEMA (S<n>; confirmados por la tool y por mi auditoría; no se corrigen)

- **S1 — Identidad sin página y entidades inexistentes en campos de routing (CL-11, núcleo del FAIL):** `[[Symphony]]`/`[[symphony]]` (~95 referencias en `entities/project/application/area`) y `[[Symphony Portal]]` no existen como nota ni como alias en NINGUNA zona del vault; `[[xKoRx/symphony]]` (75, campo `application`) referencia un repo externo como si fuera entidad; ~131 referencias usan identificadores técnicos como entidades (`sqx-watcher`, `sqx-worker`, `EchoForgeTradeListExporter`, `TradeListArtifactWriter`, `BuildMinIOPath`, `ExportTradeListActivity`). Autoridad: `90-system/convenciones.md` L77-78 ("area, project, application, entities y related deben usar links canónicos cuando referencian entidades"). Concentrado en memorias `known-error/decision/learning` de symphony y contratos de Echo Forge.
- **S2 — Réplicas de directorio no declaradas (A1):** `archive/projects/meli/` replica `10-projects/Meli/` (y `40-archive/projects/meli/` conserva una tercera copia): 9 pares de CL-06 son byte-idénticos, 7 difieren (drift real). Genera las 25 ambigüedades WARN de CL-11 (`[[Onboarding Signals]]`, `[[Playmaker — Doble dispatch...]]` con 3 candidatos), parte de CL-08 (42), CL-10 (5 directorios) y CL-12/CL-13. Decisión del owner: migrar, declarar o excluir la raíz `archive/`.
- **S3 — Duplicación real de memorias con misma identidad y contenido distinto:** `known-error/` vs `known-errors/` (`durable-retester-contract-conflict-on-reset`, `qkvs-save-version-zero-no-garantiza-create-only`) y `memory/internal/agent-memory/` vs `memory/public/decision/symphony/` (`2026-09-06-echo-forge-finalist-model-v2`); además `20-areas/Trading.md` vs `30-resources/aranea/02-servicios/trading.md` y `Echo Forge WFM Dashboard` en dos raíces con drift.
- **S4 — El router meli enruta un runbook superseded:** `30-resources/agents/skills/meli-agent-dev/SKILL.md` L58 y L85 enlazan `[[signals-code-review]]` (estado `superseded`, `indexable: true`, `index_priority: high`) en vez de `[[signals-code-review-runbook]]`. Es el FAIL de CL-18; el bootstrap Hard Rule "Never load superseded..." lo convierte en contaminación real del hot path. (A3: el status en tipo s1 lo cubre lint.py; la sucesión de índice del dominio sí está correcta.)
- **S5 — Deuda de índices wiki:** `30-resources/aranea/00-index.md` con ≥4 links rotos (agent-project-00 y agent-project-08 en L125; backup-policy L128; backup-inventory-template L129 — verificado: sin ningún archivo/asset con esos nombres en todo el vault) y filas hacia páginas retiradas en `applications/00-index.md` (rio-frontend, rio-materializer), `knowledges/00-index.md` (signals-knowledge) y `methodologies/sdd/00-index.md` (fuente superseded) — CL-14/CL-15.
- **S6 — Colisiones estructurales sin decisión:** los 6 grupos (SKILL ×48, etc.) requieren que el owner decida si se whitelistran como filenames por-directorio (hoy el linter los anota pero los cuenta en el FAIL; ver N1).
- **S7 — Links rotos fuera del alcance ratificado:** 27 links en campos no-routing (banner 11, source_session 14, sources 1, owner 1) no son verificados por ningún check (ver N3); el parent podría ampliar ROUTING_FIELDS o excluir formalmente esos campos.

## 4. Muestreo estadístico de los FAIL masivos (reales vs falsos positivos)

- **CL-11 (337): clasificación EXHAUSTIVA (100%), no muestreo.** 312 FAIL + 25 WARN. Contra mapa vault-wide de basenames y aliases (incluye journal, packaging, inbox, 40-archive): 78 = referencia a repo/path inexistente (`xKoRx/symphony`), 131 = identificador técnico sin página, 76 = concepto sin página (`Symphony`), 27 = nota de journal existente (destino no canónico DECLARADO; veredicto correcto por autoridad, redacción imprecisa N2), 25 = ambiguo por réplicas con candidatos listados (WARN). **Falsos positivos de resolución (alias/basename/casefold existente marcado FAIL): 0/337.** Reales según la autoridad ratificada: 337/337 (100%); de ellos 310 (92%) no existen en ninguna zona del vault y 27 (8%) son violaciones reales de "no enlazar sessions/logs". Muestreo adicional de 14 archivos origen con la línea citada: los campos y valores existen textualmente en frontmatter (no son artefactos del parser).
- **CL-06 (22): clasificación completa.** 16 identidad (9 pares byte-idénticos por réplicas S2, 7 con contenido distinto) + 6 estructurales (N1). 0 falsos positivos mecánicos; los 6 estructurales son hallazgos-spec legales pero de decisión de owner. Archivos idénticos que viven SOLO en 40-archive quedan fuera del check por scope (correcto: el corpus vivo es la definición ratificada).
- **CL-14 (3): verificación 1 a 1.** Los 3 targets no existen como archivo de NINGUNA extensión en todo el vault (búsqueda por substring case-insensitive: 0 matches). 3/3 reales. Selección de índices correcta: existen 14 `00-index.md` con `type: index` + `status: active` bajo 30-resources; 13 en scope + 1 copia de packaging (`30-resources/agents-os/...`) correctamente excluida del corpus. Falso negativo propio de la tool detectado aparte (D1).
- **CL-15 (4): verificación 1 a 1.** rio-frontend (deprecated), rio-materializer (deprecated), signals-knowledge (deprecated), symphony-sdd-adoption-plan (superseded, resuelta por basename). 4/4 reales; el índice raíz lista páginas retiradas (00-RESOURCE-WIKI: "el índice raíz deja una sola entrada vigente"). Desduplicación contra REGISTRY-DISK-PARITY correcta (ese L0 cubre sólo INDEX.md de skills).
- **CL-18 (1): verificación completa con recomputación de destinos.** El set M16 real (14 archivos: constitución, 3+1 perfiles, continuidad, INDEX, 2 routers, aranea-mcps-expert, 4 especialistas meli) fue reconstruido y los 8 destinos omitidos son exactamente miembros de M16 (0 re-barridos, 0 omitidos indebidos); `signals-code-review.md` (runbook) NO está en M16 (M16 contiene la SKILL homónima, archivo distinto) y su estado `superseded` está verificado. 1/1 real, dedup CTX-12/M16 correcta. Los links no-resolventes de los routers (`[[meli-agent-dev]]` ×4, `[[aranea-agent-dev]]` ×2, `[[fury-lib-consumer-deploy]]`, `[[sync-local-branch]]`) van correctamente a CL-12 (WARN), no a CL-18.
- **Balance de los FAIL masivos: reales.** De 367 findings FAIL (CL-06+CL-11+CL-14+CL-15+CL-18), 0 son falsos positivos de resolución; 27 (7.4%) son hallazgos por política-de-journal con redacción mejorable (N2) y 6 (1.6%) son estructurales con decisión de owner pendiente (N1). La tool NO infla: el corpus real tiene esa deuda.

## 5. PASS con evidencia (ataques que no lograron demostrar defecto)

- **Falsos negativos por mutación (16/16 verificados tras corregir 3 fixtures míos — el fallo inicial fue de mis fixtures, no de la tool; documentado por honestidad):** CL-01 con `memory_state: active` + `superseded_by` → FAIL; CL-02 con `superseded_by:` vacío y con `""` → FAIL (= ausente, como exige el spec) y con puntero válido → sin finding; CL-04 con `superseded_by` y con `supersedes` rotos → FAIL cada uno; CL-07 con slug duplicado (incl. quoting) → FAIL único con ambos paths; CL-11 con `area: "[[Nota Inexistente]]"` → FAIL y con `[[Mi Alias]]` (alias existente) → sin finding; CL-15 con filas hacia nota archived-por-estado, superseded-por-memory_state, deprecated in-enum, nota bajo 40-archive y nota resuelta SOLO por alias → FAIL en los 5 casos; CL-16 con skill cuyo SKILL.md declara `status: deprecated` (fuera de contrato s1) → WARN (nunca FAIL, como ratifica el spec); CL-17 con path inexistente citado por el bootstrap → FAIL; CL-18 con destinos deprecated / memory_state-superseded / 40-archive desde un router → FAIL cada uno y destino vigente → sin finding.
- **Resolución A9 (ataque 6):** `[[Destino#Sección]]`, `[[Destino#^abc123]]`, `[[^bloque-suelto]]`, `[[sub/Nota Con Espacios|label]]`, `[[sub/Nota Con Espacios.md]]`, `[[Mi Alias]]` (alias multilínea con quoting) → todos resuelven sin finding, en frontmatter y cuerpo; casefold ambiguo (`FOO.md` + `foo.md` + `[[Foo]]`) → WARN único con ambos candidatos, sin crash ni FAIL falso; fenced code blocks e inline code spans excluidos del cuerpo (verificado por mutación: sólo el link fuera de código se reporta).
- **Frontmatter (ataque 8):** quoting simple/doble en `status` y campos de routing, lista inline `entities: "[[A]], [[B]]"`, `related: []` vacío, listas multilínea: el parser del harness + `as_list` + `fm_field_links` los maneja sin romper; `status: "superseded"` quoted se detecta (CL-02 WARN demo en selftest).
- **Archived vs referenced (ataque 7):** CL-13 emite 108/108 WARN (cero FAIL — la cláusula "unless intentional" está respetada estructuralmente); los links históricos intencionales (sucesión, S5/N5) aparecen como WARN documental, nunca como veredicto.
- **Exclusiones y corpus (ataque 9):** 0 findings con path bajo `80-agents/journal/`, `30-resources/agents-os/`, `_shared/fixtures/` o fixtures del entity-lifecycle en TODO el run (fuera de CL-09, cuyo scope ES 40-archive por diseño, 1 finding A7 legítimo); la copia packaging del 00-index de applications queda fuera del scope de CL-14/15; los archivos vivos no quedan fuera (853 notas de corpus verificadas en evidencia de cada check).
- **Desduplicación (ataque 10):** `dedup_cites` presentes y correctos en 16/20 checks (CL-02 omite el slice memory/internal con conteo; CL-03 cita lint.py:forbidden-field para el caso A3; CL-09 cita L0:CLOSED-CLUB-ALWAYS + doctor:check_always; CL-16 registra "47 destinos únicos" y NO re-emite existencia (0 missing en INDEX hoy); CL-18 omite los 8 de M16 con conteo; CL-11/CL-04 se parten los campos de sucesión sin solape; CL-17 no re-escanea AGENTS.md). Sin doble emisión del mismo link entre CL-11 y CL-12 (el frontmatter se excluye del escaneo de cuerpo). Muestreo CL-03/CL-09/CL-16/CL-18: sin solape no citado.
- **Determinismo y estabilidad (ataque 11):** dos `run_suite(write=False)` sobre el vault real → JSON canónicamente IDÉNTICOS; runs bajo 3 git heads distintos (c94d994/12ab1fd/76ddc6c) → mismos counts y mismos findings por check; corrida desde cwd distinto (`/tmp`, `--vault-root` absoluto) → mismos resultados; colisión de timestamp en el mismo segundo resuelta con sufijo `-2` sin sobrescribir.
- **SKIP/malformado (ataque 12):** frontmatter sin cerrar, `.md` binario, frontmatter con líneas basura, wikilink sin cerrar, archivo con permisos 000 → veredictos válidos sin traceback ( PermissionError capturado como OSError en `ctx.text`); vault sin harness → 20/20 checks SKIP con motivo ("harness no importable..."), exit 0. Nunca un crash observado en ninguna prueba.
- **Schema del record (ataque 13):** `model` (A5+A9), `checks`, `counts`, `observations`, `ambiguities`, `thresholds`, `git_head` presentes; severidades por check exactas a la tabla ratificada (CL-01/02/04/06/07/11/14/15/16/17/18 FAIL; resto WARN); FAIL sólo donde el spec lo permite (verificado: CL-05/08/10/13/19/20 nunca emitieron FAIL ni en mutaciones ni en el corpus); enmienda CL-01 aplicada (sólo `superseded_by`+active; `supersedes`+active contado en evidencia sin hallazgo; 3 casos reales en el corpus sin finding).
- **Exit codes y CLI (ataque 14):** suite completa → 1; categoría con sólo WARN (METADATA) → 0; `--check CL-01` → 0; `--check CL-14` → 1; `--check CL-99` → 2; `--vault-root /tmp` (sin marker) → 2; run dirigido consistente con el run completo (CL-14: FAIL 3 en ambos); `--category HOT-PATH` → 1 (correcto: CL-18 es HOT-PATH y está en FAIL); escritura de results también en runs dirigidos (aceptable: results/ es la única escritura del motor, se reporta).

## 6. Veredicto global

- **TOOL WITH MATERIAL DEFECTS (1 defecto material, impacto menor).** El único defecto demostrado es D1 (CL-14 verifica sólo el primer wikilink de cada fila de índice; deja sin reportar el link roto real `agent-project-08` en `30-resources/aranea/00-index.md` L125). No invierte ningún veredicto del run (el archivo ya está en FAIL), pero es una desviación real del texto del spec ("fila o link") con falso negativo vivo en el corpus.
- **Los 5 FAIL del run son REALES:** el corpus tiene la deuda que la tool señala (muestreo exhaustivo 100% en CL-11/CL-06 y 1-a-1 en CL-14/15/18, con 0 falsos positivos de resolución); los WARN son veredictos correctos con ruido documental declarado (N1/N2/N5/N6).
- **La arquitectura cumple el contrato de la misión:** read-only (git limpio salvo results/, luego sincronizado por el orquestador), findings sin auto-corrección, dedup con citas, determinismo, SKIP motivado, schema conforme, enmienda CL-01 aplicada. El selftest declarado (8/8) es real y lo reproduje.
- **Confianza:** EXACT en todos los defectos demostrados (repro programático); las clasificaciones de CL-11/CL-06 son censos completos, no muestras.

## 7. Recomendaciones para P3-D (fixer; separadas de hallazgos de sistema)

- **R1 (fix D1, prioritario):** en `_index_rows`, extraer TODOS los wikilinks de cada fila (`WIKILINK_RE.finditer`) y verificar cada target distinto en CL-14 (y consistentemente en CL-15); añadir al selftest una fila con dos links rotos donde el primero resuelva y el segundo no.
- **R2 (N2):** en CL-11/CL-12, distinguir en `observed/evidence` los destinos que existen en zona no canónica (journal/packaging/derivados) de los inexistentes: "existe en <zona excluida>; no es destino canónico (agents-os.md/relation-maintenance)".
- **R3 (N5):** evaluar eximir SUCCESSION_FIELDS de CL-13 (o etiquetar el finding como "link de sucesión canónica, intencional por definición") para reducir el ruido de los 36 WARN de frontmatter.
- **R4 (N1):** proponer al parent (no decidir en la tool) una clase de veredicto o whitelist declarada para los grupos estructurales de CL-06; mientras tanto mantener la anotación actual.
- **R5 (N7):** proponer al parent ratificar (o rechazar) el fold de acentos (NFD) en CL-06, citando convenciones.md; hoy sin instancias reales.
- **R6 (N6):** evaluar excluir `70-templates/` del alcance de CL-19 o anotar que el hit es una plantilla.
- **NO ampliar ROUTING_FIELDS sin ratificación del parent** (N3/S7 queda registrado como observación con sus 27 links rotos como dato).
- Ver tras el fix: re-run completo debe mantener PASS 6 · FAIL 5 y pasar de 1003 a ≥1004 findings (el link de D1 entra; nada más debe moverse).

## 8. Hallazgos de sistema para el owner (resumen decisorio; ver sección 3)

- S1: crear la página canónica de `[[Symphony]]` (y decidir si repos/clases técnicos se referencian como entidades o en texto plano); 337 hallazgos de CL-11 dependen mayoritariamente de estas 3 decisiones de identidad.
- S2/S3: resolver las réplicas `archive/projects/meli` (A1) y los duplicados `known-error(s)`/memorias internas-vs-públicas; eliminan a la vez colisiones CL-06, ambigüedades CL-11/CL-12 y WARN de CL-08/CL-10/CL-13.
- S4: reenrutar el Minimal Read del router meli de `[[signals-code-review]]` a `[[signals-code-review-runbook]]` y decidir A3 (status en s1 federado).
- S5: reparar las filas de `30-resources/aranea/00-index.md` (4 links rotos) y retirar las filas de páginas retiradas en applications/knowledges/sdd (CL-14/CL-15).
- S6/S7: decidir whitelist estructural de CL-06 y el alcance formal de campos con wikilink fuera de ROUTING_FIELDS.
