---
type: change_log
schema_version: 1
scope: session
created: "2026-09-18"
updated: "2026-09-18"
area: "[[Aranea]]"
project: "[[BACKUP-DR-OWNER-PROJECT]]"
application:
entities:
  - "[[agent-project-02-pbs-on-backup-node]]"
  - "[[BACKUP-DR-OWNER-PROJECT]]"
related:
  - "[[BACKUP-DR-CONTRACT]]"
aliases:
  - "R2 cierre piloto 2026-09-18"
  - "R2 closeout bundle v3"
confidence: verified
source_session:
source_feedbacks: []
share_scope: local
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/change-log
  - scope/session
  - area/aranea
---

# 2026-09-18-r2-closeout

%% Routing: area/project/application/entities/related usan links canónicos. Aliases son variantes humanas; tags/paths usan slugs. %%

## Cambio

- **Tipo:** updated
- **Archivo(s):**
  - `10-projects/Aranea/BACKUP-DR-OWNER-PROJECT/agentes/agent-project-02-pbs-on-backup-node.md` (bitácora R2 closeout; tareas nuevas AGENT-TASK-02-8/02-9/02-10; frontmatter `status_detail`/`updated`)
  - `10-projects/Aranea/BACKUP-DR-OWNER-PROJECT/BACKUP-DR-OWNER-PROJECT.md` (bitácora + línea R2 closeout; frontmatter `updated`)
  - `30-resources/aranea/03-storage/backup-dr/00-index.md` ("De un vistazo" actualizado: hallazgos corregidos + bundle v3)
  - `80-agents/journal/logs/2026-09-18-r2-closeout.md` (este log)
  - `~/aranea/work/r2-pbs-20260918/r2-closeout-analysis.md` (FUERA del vault: análisis completo del cierre)
  - `~/aranea/work/r2-pbs-20260918/owner-action-bundle-v3.md` (FUERA del vault: bundle ÚNICO de autorización, 3 acciones independientes)
- **Cero mutaciones en infra**: toda la evidencia del día es read-only o local-scratch; PBS/PVE intactos; sin reboot; sin prune/GC; sin schedules.

## Motivo

- Mandato one-shot owner 2026-09-18: "R2 cierre del piloto y preparación operativa" — resolver hallazgos del piloto, entregar un único paquete ejecutable, no repetir discovery/provisión/piloto, no reabrir D0, sin session close.

## Fuentes usadas

- **OBSERVADO live (read-only, 18sep 20:17–20:45 -03):** CT 155 config (`rootfs` sin flag incluido; `mp0` sin flag → excluido), `pct exec` df/du (datos etcd 144.856.711 B; rootfs 2.0G/9.8G), task log vzdump (línea de exclusión mp0); configs de 101/115/136/138/144/147/148/152/153/154/156 en su nodo residente (todas las exclusiones backup=0 son líneas explícitas SOLO en kafka/PG/Mongo; los etcd tienen mp0 SIN flag = default); PBS .123: fstab UUID `f7726067…` + findmnt + unit fstab-generator + `systemctl cat/show` (servicios SIN dependencia del mount, sin drop-ins) + `ls -ld /mnt/pbs-data` = **755 backup:backup** (dir 000 NO aplicada) + `datastore show/list` (verify-new true) + df/du (733M/295G, 429 chunks) + servicios active + uptime 40d; kronos: VG local-kronos 931.51G/267.51G libres (6 LVs, delta = vm-180-disk-1), `ceph -s` HEALTH_WARN 2 OSD nearfull + pool1 87.16% (raw 64.18%), thin pool hera `pve/data` 27.96%; storage.cfg sha256 `87482f91…` + jobs.cfg AUSENTE + huellas md5 de `qm config 180`/`lvs`/fstab capturadas para el baseline del bundle v3.
- **Local (hermes):** artefacto R1 etcd `20260918-050018` sha256 == manifest (`f2ea…a611`), `etcdutl snapshot status` (rev 56625, 985 keys, 5.1 MB), **restore offline a scratch exit=0** (estructura member/snap+wal, 66M, NUNCA arrancado, scratch borrado); `systemd-escape --path /mnt/pbs-data` = `mnt-pbs\x2ddata`; drop-in propuesto parseado INI OK; `etcdutl` 3.6.4.
- **DOCUMENTADO:** log piloto (`2026-09-18-r2-pbs-pilot-execution.md`), bundle v2, log discovery R2, contrato §2 (23 Tier 0 congelado), R0 roadmap, workflow ap-02.

## Resolución aplicada

- **Hallazgo etcd resuelto (diagnóstico definitivo):** mp0 `/var/lib/etcd` está excluido por el **default de PVE 8.4.20** (mount points no-root no entran sin flag explícito), NO por una línea del CT; familia completa afectada (101/147/148/154/155/156 comparten patrón rootfs+mp0 sin flags). rootfs SÍ entra (default de rootfs = incluido). Decisión operativa provisional aplicada: MANTENER exclusión de facto (cero ediciones a CTs), protección de datos = cadena R1 (timer 05:00 → snapshot cluster → verify → manifest; run del día rev 56625/985 keys; restore offline re-demostrado hoy). Procedimiento de recuperación combinada (R1 estructural PBS → R2 datos snapshot lógico → R3 servicio → R4 quorum) documentado en el análisis con estado de demostración explícito (R1/R2 demostrados, R2 sólo offline, R3/R4 NO demostrados).
- **Seguridad del montaje diagnosticada:** con `nofail` y sin dependencia del mount, un arranque sin el disco deja PBS (root) escribiendo chunks sobre el rootfs (38G libres). La mitigación "dir 000" del piloto **NO quedó aplicada** (755 real) — corrección registrada. Propuesta fail-closed: drop-in `/etc/systemd/system/proxmox-backup.service.d/10-require-pbs-data.conf` con `Requires/After=mnt-pbs\x2ddata.mount` (+`RequiredBy` simétrico); independiente de chmod y nofail; nombre de unit validado con `systemd-escape`; INI validado; diff mínimo + prueba positiva + negativa segura + rollback en bundle v3 Acción 1. Prueba negativa física (disco ausente) diferida a ventana, NO ejecutada.
- **Línea base de capacidad registrada** (análisis §3): 733M/295G (1%), 429 chunks, snapshot única verified ok, VG soporte 267.5G libres, Ceph pool1 87.16% nearfull (pre-existente, carril Infrastructure Ops), thin hera 27.96%. Umbrales: adv 70% / ABORT 85% / mínima 30G libres. Retención lógica (no configurada) ≠ liberación física (GC no configurado → sólo crece) ≠ crecimiento observado. No extrapolación del ratio de 155 a Tier 0.
- **Plan de medición 7 días (NO activado):** set activo = 7 CTs ligeros unprivileged (155/156/154/101/147/148/115), raíces en local-lvm (NINGUNO toca pool1 Ceph), ventana 03:05–04:30, serie, driver local con log JSONL, cero estado persistente en PVE/PBS; métricas, ABORT, recuperación, criterio de éxito y método de desactivación definidos. Exclusiones con causa explícita (datos etcd mp0; 152/153 iscsi backup=0 → R3; kafka pool1 GATED; mt4 cache=unsafe diferido; truenas passthrough → ZFS). Separación A/B/C/D documentada.
- **Riesgo storage:** Ceph nearfull NO se corrige desde este proyecto; no afecta al set activo del piloto (local-lvm); kafka marcado GATED mientras pool1 esté nearfull.
- **Bundle ÚNICO entregado** (`owner-action-bundle-v3.md`): Acción 1 fail-closed (aplicable hoy, sin reboot), Acción 2 medición 7d, Acción 3 reboot controlado en ventana futura (+ prueba negativa física opcional). Baseline criptográfico pre capturado (storage.cfg sha256, md5 qm config 180/lvs/fstab, jobs.cfg ausente, snapshot única). 3 aprobaciones independientes; fuera de alcance explícito: retención/prune/GC, jobs.cfg, ampliación de datastore, CTs, Ceph, D0, R1.6.

## Validación

- Read-only verificado: sin comandos de escritura en infra en toda la sesión; huellas capturadas post-sesión coinciden con el estado esperado (storage.cfg sha256 estable desde el piloto; jobs.cfg ausente; snapshot única 22:15:24Z).
- Restore lógico etcd: exit=0 + estructura esperada + sha256 del artefacto == manifest; scratch eliminado (verificado `ls`).
- Drop-in: `systemd-escape` correcto + parse INI correcto (validación definitiva de carga queda en PBS, paso P4 del bundle; no se ejecutó en remoto para no tocar servicios antes de aprobación).
- Correcciones al registro del piloto incluidas en este log (dir 000 no aplicada; origen de la exclusión mp0 = default PVE).
- CT 155 y todos los guests: intactos (configs leídas, uptime continuo).

## Compartibilidad

- **Scope:** local
- **Redacción revisada:** sin identidad, paths locales, memoria interna ni secretos (ningún token/credencial impreso; huellas criptográficas son hashes de configuración, no secretos)

## Rollback

- Documental: revertir bitácoras/frontmatter en ap-02, proyecto padre e índice; borrar este log; borrar los 2 archivos de `~/aranea/work/r2-pbs-20260918/` de esta fase (análisis + bundle v3). Infra: nada que revertir (cero mutaciones); el bundle v3 trae rollback propio por acción.
