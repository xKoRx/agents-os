# change_log 2026-09-16 — E-02 CLOSED (AC-11/AC-12/AC-01 + verifier)

Sesión Ariadna (Hermes, TUI), continuación directa de la physical certification. Objetivo owner: completar ACs pendientes y veredicto final del lane.

## Resultado

`E02 CLOSED` (software) — AC-11 PASS, AC-12 PASS, AC-01 PASS, verifier independiente PASS, sobre la physical certification PASS previa (3/3 gates). Queda pendiente para closure formal: CONTROLLED INTEGRATION a `master` + AC-18 (rotación secret prod, gate ops del owner). **E-06 queda desbloqueado** (no iniciado).

## Decisión arquitectónica owner (FROZEN, autoridad durable)

- **Daedalus `192.168.31.161`** = runtime temporal/dev de Echo y Echo Forge; builds y fixtures de certificación.
- **`.75` (docker-echo-dev)** = infraestructura DEV compartida (Hasura, Flink/StateFun, Portainer). NUNCA runtime Echo: sin echo-core/gateway/Forge, sin systemd services, sin containers persistentes, sin autostart de componentes Echo.
- **`.211`** = antiguo ubuntu-dev/Daedalus, retirado y offline; referencias históricas stale.
- **DNS canónico (Pi-hole `192.168.31.31`, actualizado por owner):** `dev.echo.core.lab.aranea → 192.168.31.161`. DNS actual manda sobre documentación histórica.
- **`.71`** = Echo PROD, observation-only en este carril. **Windows test VM** = sólo gates que materialmente requieren Windows (MT4/MT5, named pipes, EA, terminal demo).
- Regla durable: **"Application runtime follows declared topology; infrastructure proximity is never authority to colocate product components."**

## Evidencia física (todos los AC contra contrato SPEC v1.0.2 @ `f7ddea18`)

- **Setup:** `echo-core` compilado en Daedalus desde repo `xKoRx/echo` HEAD `f7ddea18` (sha256 `00240dd1d37384d22f21f1707f9c4ac09e5d9d6ae8819b98fe3f9e67693ed6a9`); servido `:9090` (`/health` 200); Flink StateFun `.75` resuelve canónico `.161`; Kafka DEV `.44`; PG DEV `.220/echo-develop`. Sin `extra_hosts`, sin tocar `urlPathTemplate`, sin Echo en `.75` (un intento previo en `.75` fue completamente revertido antes de la decisión owner: proceso kill -9, hosts byte-identical, binarios borrados).
- **AC-12 (kill -9 del core Go, compose StateFun develop + PG real) PASS:** Window A = kill -9 tras persistir OPEN (fila 1/1 id) → core DOWN ~11s → restart → redelivery StateFun → **1 fila/1 id, sin doble fila**. Window B = kill -9 antes de persistir el close → close publicado con core DOWN (~30s; job Flink restart ×2 por restart-strategy) → restart → redelivery → **close aplicado exactamente 1 vez** (fila CLOSED, close_price 2405.75), cero pérdida. Replay tardío del OPEN → `OPEN_AFTER_CLOSED` → cuarentena durable 1 fila (unique index `journal_quarantine_pending_fact_reason_uidx` verifica dedupe) → ACK → loop converge (105 conflictos antes=después). 0 publishes a `echo.commands.*` desde journal.
- **AC-11 (outage real PG 17.6) PASS:** `LOCK TABLE echo.trade_journal IN ACCESS EXCLUSIVE MODE` sostenido ~100s (sesión fixture, propio PG 17.6 de DEV, blast radius = 1 tabla) → fact publicado durante outage → INSERTs bloquean → `canceling statement due to … timeout` → handler clasifica **TRANSIENT** (5× `open failed`/`persistence_error`, error retornado a StateFun para retry; **0 inserts en quarantine, 0 filas parciales**); lock liberado → redelivery → **exactamente 1 fila/1 id**, sin duplicado, 0 quarantine para ese trade.
- **AC-01 (bundle) PASS:** `vite build` real sobre árbol @ `f7ddea18` (43 assets; dist agregado sha16 `2747ba2985042409`) → `check-frontend-bundle.sh` exit 0 → 4 greps independientes (`x-hasura-admin-secret`, `VITE_HASURA_ADMIN_SECRET`, literal `Bearer <token>`, hex-48) limpios → **doble-check con el valor REAL del admin secret DEV Hasura** (sha16 del valor `8a36217243c11629` como evidencia; valor jamás impreso; transferido stdin-only mcps→Daedalus, nunca en argv/env-file persistente).
- **Verifier independiente PASS (V1–V5):** superficie separada de la ejecución, read-only, sin secretos, salida bounded. V1 identidad source (repo HEAD `f7ddea18`, branch limpio) + binario sha; V2 evidencia PG (filas fixture exactas, cuarentena taxonómica, universo fixture = 2 filas, sin quarantine para caso transient); V3 bundle re-scan; V4 sha binario; V5 drift `.75` (hosts sha `fc197382…` == baseline, 0 procesos Echo, `/root` limpio, Flink job RUNNING 28/28) + Kafka (topics fixture ausentes). Hallazgos del verifier y correcciones: (a) 2 topics fixture residuales `e02cert-gate2{,b}-20260915` de la sesión anterior → borrados (REMOVE), list-topics limpio; (b) bug SQL del propio verifier (cast `::text` inflaba un count) → corregido y re-ejecutado; documentado, no maquillado.

## Cambios de infraestructura

- **KEEP:** migración **062 `echo.journal_quarantine`** aplicada a `.220/echo-develop` como `echo_user` (idempotente `IF NOT EXISTS`, additive, down-migrable; el handler del branch la exige en runtime; rollback = `062 down` — ahora vacía, drop posible). Nota: 062 NO existe aún en PROD PG (152); se crea con el deploy del branch (PLAN §3).
- **REMOVE:** filas fixture de journal y quarantine (DELETE; post: 0 filas `e02ac1%` y quarantine total 0); 2 topics Kafka fixture; core fixture detenido (port 9090 libre); intento temprano en `.75` revertido byte-identical; bearers MCP locales en `/tmp` shredded.
- **Persistente en Daedalus (por decisión owner):** fixture de build `/tmp/e02ac12` (~1.3G: toolchain go1.25.5, árbol v3 @ `f7ddea18`, binario `echo-core` sha `00240dd1…`). Sin procesos Echo corriendo.
- **No tocado:** PROD, trading, MT4/MT5, etcd, firewall, branches ajenas, `master`, `.71` (solo viewer reads), Hasura PROD.

## Hallazgos

- El resolver embebido de Docker (127.0.0.11) en `.75` NO consulta `/etc/hosts` del host: un hosts-fixture en `.75` no alcanza a los contenedores de Flink; la autoridad real es el DNS del lab (Pi-hole `192.168.31.31`). Lección: verificar el resolver efectivo del consumidor antes de elegir el mecanismo de targeting.
- `echo_user` (owner del schema `echo` en `.220`) no tiene `rolcreatedb`; la migración 062 se aplicó in-process leyendo credenciales de etcd (secreto nunca impreso).
- Los ingresses journal de StateFun tienen `startupPosition: earliest`; tras restart completo del job, un fact conflictuado sin tabla de cuarentena genera retry-loop infinito (el ACK depende de InsertQuarantine). Con 062 aplicada converge inmediato. Riesgo operacional si PROD deploya el core sin 062 previa — cubierto por el orden del runbook PLAN §3 (migrar 062 primero).

## Follow-ups

1. CONTROLLED INTEGRATION de `feature/e02-control-safety-journal-recovery` a `master` (closure formal; ancestry demostrado, sin force-push).
2. E-06 desbloqueado (no iniciado en esta sesión).
3. AC-18: rotación del admin secret en prod = gate ops del owner (antes de uso con capital).
4. GAP-ECHO-010 (P1, Access Plane async-202): permanece paralelo, no tocado.
5. Limpieza opcional del fixture de build en Daedalus cuando E-06 no lo necesite (~1.3G en `/tmp`).
