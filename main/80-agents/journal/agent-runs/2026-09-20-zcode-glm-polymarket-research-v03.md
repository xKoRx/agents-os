# Agent Run — 2026-09-20-zcode-glm-polymarket-research-v03

## Trabajo

- **Objetivo:** mandato POLYMARKET RESEARCH v0.3 — NegRisk Validation + Sports Continuous Capture sobre baseline `7bd264d` (branch `feature/research-strategies-v01`): resolver los bloqueos restantes de datos de POC-S01 (NegRisk/PE-002) y POC-S02 (Sports/PE-005-R1) sin modificar hipótesis, con lectura on-chain pública autorizada (sólo lectura, sin wallet/signing) y captura continua deportiva read-only. Sin nuevas estrategias, sin rediseño, LIVE_DISABLED, sin push.
- **Fase A (`ea1e3c8`, gate `V03_PREFLIGHT_PASS`):** preflight limpio (SHA, árbol, sin writers). Flake de catalog reproducido (2/4 rondas del suite completo con race) y corregido por causa raíz: `TestScanContextCancelInsideLoop` cancelaba en carrera contra el chequeo temprano de `RunScan`; el fixture ahora notifica cada request servido (canal `served`) y la cancelación es causalmente posterior (3/3 rondas verdes). Flake adicional descubierto en supervisor (`TestShutdownDrainDeadlineExceeded`, TOCTOU entre la observación de lag y Shutdown con el writer libre): seam aditivo `supervisor.Config.CaptureCfg` (nil-preserving) + coreografía determinista con fault hook que estaciona al writer (10/10 con race). Cobertura `cmd/engine` 94.4%→98.24% (1478 stmts, 26 sin cubrir): 57 statements cubiertas con tests de fallo deterministas (`cmd/engine/defensive_paths_test.go`: esquemas hostiles de store, contenido proyectado corrupto, universo corrupto, bundles manipulados, agotamiento de presupuesto de frames con 3400 cortes, skip por identidad ausente); residuo clasificado con inaccesibilidad demostrada por bloque en `testdata/research-v03/COVERAGE.md` — la excepción `DEFENSIVE_FAULT_GUARD` de v0.2 queda resuelta sin autoaprobación y se corrige su subestimación (el residuo real en `7bd264d` eran 83 statements, no ~14). Erratum `manifest_id` de v0.1 verificado (evidencia histórica intacta; erratum vigente en bundles). Journal v0.2 preservado en `polymarket-engine-datasets/rs-v02/` (47 archivos, SHA-256, frontier 1429, replay desde el destino: digest `b24982b0…` idéntico en {1} y {32,7,1}, `diff -r` originales intactos).
- **Fase B (`cc57e8d`):** B1 — walk read-only de 2000 eventos activos de Gamma: 788 `negRisk=true`, 685 aumentados (un miembro con `negRiskOther=true` — patrón 30829 generalizado: 87% de los NegRisk vivos), 103 estándar, 82 elegibles tras el tamiz (≥2 miembros, binarios YES/NO, tokens, activos, order book, coherencia de contexto, sin placeholders); hallazgo de wire: `negRiskOther` no existe a nivel evento — el tamiz por miembro es el único válido. B2 — lectura pública block-pinned: chainId `0x89` en dos RPCs independientes, bloque `0x59bede5` con hash cruzado entre RPCs, direcciones desde `docs.polymarket.com/resources/contracts`, `getQuestionCount(ctx)=15` == 15 miembros en el adaptador legado `0xd91E80cF…` (ONCHAIN_MEMBERSHIP_MATCH VERIFIED), CTF `getOutcomeSlotCount=2` ×15, `payoutDenominator=0` ×15; `isQuestionKnown` no resolvió (selector no verificado contra el ABI real) — límite declarado en B3. B3 — `PAYOUT_EXHAUSTIVENESS=UNKNOWN` honesto (resolución futura + semántica mutable), separado del match; re-check post-captura en `0x59bf457` mantiene 15. B4 — captura real 1800 s del evento 159954 (3764 frames in, 3585 admitidos, 0 rechazados; frontier 3591): SCREEN 30 cortes → 21 oportunidades, 21 INCONCLUSIVE, 0 rechazos estructurales (el bloqueo v0.2 desaparece sobre evento estándar); REPLAY determinista {1}=={32,7,1} digest `33cbaffe…` 3647 records; SHADOW 923 frames, 663 conjuntos, 663 INCONCLUSIVE, 0 rejected, 0 fills, outcome INCONCLUSIVE.
- **Fase C (`4189a9b`):** partido ancla 494880 Rays vs Yankees, moneyline 2284198, kickoff verificado `gameStartTime == event.startTime = 2026-09-22T17:05:00Z`; libro vivo 0.42/0.43 (~$112k mejor bid). Captura fase 1 21600 s (T-64.9h→T-58.9h): 8844 frames in, 6685 admitidos, 0 rechazados, ambos lados OBSERVED_USABLE (46 niveles), frontier 6748, sin gaps. Pipeline congelado (widen 50 bps intacto): SCREEN 56 ventanas → 0 señales (`NO_SIGNAL_OBSERVED_IN_WINDOW`); REPLAY determinista digest `110381d7…`; SHADOW 923 frames, 0 oportunidades, INCONCLUSIVE (min_samples 100 no alcanzado). Ventana pre-match completa (T-8.5h→kickoff) preregistrada como runbook mecánico (`SPORTS-CAPTURE-RUNBOOK.md`) pendiente de ejecución.
- **Fase D (`eaf2d94`, pin `f070496`):** regresión completa verde (build/vet/gofmt; suite 28/28 con `-race`); cobertura de paquetes modificados ≥95% (cmd 98.2, catalog 95.1, supervisor 95.9); certificado `M4_CERTIFIED_NON_LIVE` (27 PASS / 0 FAIL / 0 in-scope NOT_RUN / 5 diferidos live) emitido con `--baseline eaf2d94` y re-pineado en `f070496`; prueba negativa del harness (baseline ajeno ⇒ `M4_STALE_CERTIFICATION`); prueba física `vcs.revision` (binario sella `commit=f070496…` == HEAD). Journals v0.3 preservados en `polymarket-engine-datasets/rs-v03/` (41 archivos, verificación desde destino, digests reproducidos, originales intactos); `.rs-v03-*` des-trackeados según convención v0.1/v0.2; gofmt de higiene en 2 archivos preexistentes ya desalineados en el baseline.

## Verificación

- Regresión: `go build ./...`, `go vet ./...`, `go test -race -count=1 ./...` → 28/28 paquetes ok (incluidos catalog y supervisor con los flakes corregidos). gofmt limpio en el árbol final.
- Cobertura (atomic): `cmd/engine` 98.24%, `internal/catalog` 95.1%, `internal/supervisor` 95.9% — piso 95% sin excepciones pendientes para los paquetes tocados.
- Certificación: `engine experiment certify --repo-root . --baseline eaf2d94…` → `M4_CERTIFIED_NON_LIVE` 27 PASS / 0 FAIL / 0 in-scope NOT_RUN / 5 diferidos live; negativa: baseline ajeno ⇒ `M4_STALE_CERTIFICATION`. LIVE_DISABLED intacto.
- Datos reales: syncs y records contra Gamma/CLOB WS read-only; lecturas eth_call block-pinned con procedencia cruzada; outputs commiteados en `testdata/research-v03/`; bundles verificados por `journal verify` + replay desde el destino persistente.

## Rework

- Intra-sesión: ajuste de cortes de screen (6→30 NegRisk, 56 Sports) por desbordes del inbox del owner (128) con journals grandes — knob de reporte, no parámetro estadístico; dos iteraciones de tests propios (FK entera de `catalog_revisions`, re-proyección de regimes desde el journal, eventos `tick_size_change` como los únicos proyectables por regimes en marketws); double-close del canal de release en el test de supervisor (sync.Once). Sin rework del owner.

## Estado

- Resultado global **`RS_V03_PARTIAL_DATA`**: POC-S01 NegRisk línea COMPLETA (validada de punta a punta sobre evento estándar con membresía on-chain VERIFIED; outcome estadístico INCONCLUSIVE por muestra y por payout-exhaustiveness honesto UNKNOWN); POC-S02 Sports PARCIAL (fase 1 de captura completa ejecutada con 0 señales honestas; ventana pre-match completa preregistrada y pendiente). Certificado `M4_CERTIFIED_NON_LIVE` en el árbol final; LIVE_DISABLED intacto; sin órdenes; sin push (un actor externo del checkout compartido pusheó el checkpoint `25f578a` al remoto durante la captura — esta ejecución no lo hizo; local ahead 5). Próximos experimentos falsables: (1) fase 2 deportiva según runbook el 2026-09-22 antes del kickoff; (2) re-run NegRisk 159954 post-resolución (2026-12-09) para convertir el payout-exhaustiveness.
