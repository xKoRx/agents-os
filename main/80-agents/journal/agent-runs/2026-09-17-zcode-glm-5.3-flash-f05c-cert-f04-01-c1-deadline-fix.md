---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-17"
updated: "2026-09-17"
area: "[[Echo]]"
project: "[[Echo + Echo Forge — Deferred Certification Backlog]]"
application: "[[xKoRx/symphony]]"
entities:
  - "[[Echo Forge]]"
related:
  - "[[Echo Forge — F-04 Magic allocation, version seal and handoff]]"
  - "[[Echo Forge — F-01 Canonical Generation Concurrency Contract]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: coding
task_complexity: medium
outcome: success
verification: passed
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c1-deadline-fix

## Trabajo

- **Objetivo:** F05C-CERT-F04-01-C1 — implementar exclusivamente la corrección source del defecto demostrado por el run físico EXEC (`persistence_deadline_required` en `mt5_compile_artifact` @ release `0.2.98` / `b57bfb2`). Sin re-ejecutar el gate, sin release, sin deploy, sin infraestructura. Fin en `SOURCE VERIFIED` + commit.
- **Alcance atribuible a esta combinación superficie×modelo:** bootstrap Agents OS (constitución, perfil, continuidad, router dominio aranea, backlog, F-04, decisiones B1A/B1B/B2); verificación de baseline (`HEAD == origin/codex/f05-release-prep == 3d0e8c9…`, `b57bfb2` ancestro); branch de trabajo; lectura del source afectado (boundary `DownloadArtifactTaskSource`, guard `ValidatePersistenceContext`, `FetchDurableToPath`, Compile/Backtest, convención self-wrap `durable_select_robust_run.go`); implementación del fix; tests de regresión T1–T6; repro del defecto contra source 0.2.98 en worktree temporal; gates de test y aislamiento de fallos preexistentes contra baseline; review local; commit único; registro documental.
- **Artefactos afectados:** repo `xKoRx/symphony` — commit `49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1` en branch `codex/f04-cert-f04-01-deadline-fix` (3 archivos: `sqx/adapters/mt5/artifact_compiler.go` +13/−1, `artifact_compiler_test.go` +110, `artifact_runner_test.go` +50). Vault — delta fechado + `Estado de entrada` + `Próxima tarea` en [[Echo + Echo Forge — Deferred Certification Backlog]], este agent-run, change_log `2026-09-17-f05c-cert-f04-01-c1-source-fix`. Worktree temporal de repro creado y eliminado (sin residuo). Dirty foráneo `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado intacto y sin stage.

## Evidencia

- **Root cause confirmado en source:** B2 deriva `physicalCtx` desde `context.WithoutCancel(activityCtx)` (sin deadline por diseño); `DownloadArtifactTaskSource` (`artifact_compiler.go:72` @ base) pasaba el ctx tal cual a `FetchDurableToPath`, cuyo guard `capabilities.ValidatePersistenceContext` (`persistence.go`) exige deadline (`ErrDeadlineRequired`). Usado por `Compile` (línea 184 @ base) y `Backtest` (línea 93 @ base).
- **Fix:** en el boundary compartido, si el ctx entrante no tiene deadline se deriva un hijo bounded con `context.WithTimeout(ctx, artifactSourcePersistenceTimeout)` (constante package-local `2 * time.Minute`) + `defer cancel`, sólo para el fetch durable; deadline entrante se preserva exacto; cancelación permanece observable; legacy key-only sin cambios; sin `Background`/`WithoutCancel`; sin tocar B2/C4/C5, workflows, ActivityOptions, ETCD, MinIO adapter global, worker boot ni release config. `MIGRATION: NONE`.
- **T1–T6 PASS:** T1 durable+`context.Background()` recibe ctx bounded con deadline futuro ≈2m; T2 deadline del caller (30s) preservado idéntico (tolerancia reloj 2s), no reemplazado por +2m; T3 caller cancelado sigue `context.Canceled` observable en el ctx entregado y en el retorno; T4 legacy llama `DownloadArtifactToPath` y nunca `FetchDurableToPath`; T5 `Compile` durable con caller sin deadline supera la descarga (mock falla el test si `FetchDurableToPath` no recibe deadline) con `PrimaryDurable` y sin downgrade; T6 `Backtest` durable EX5 (SHA/size exactos) con caller sin deadline descarga con deadline y completa success — demuestra que el rerun no cae en el mismo defecto en Backtest tras superar Compile.
- **Repro del defecto contra 0.2.98:** worktree temporal @ `3d0e8c9` (locus idéntico a `b57bfb2`, verificado por diff vacío en los 3 archivos del locus) + test standalone con guard real `ValidatePersistenceContext`: `DownloadArtifactTaskSource(context.Background(), …)` devuelve exactamente `persistence_deadline_required`. Defecto reproducible; sin STOP conditions (no se requirió tocar `MT5OwnershipCoordinator`, B2/C4/C5, workflows ni migrations).
- **Gates:** `go test ./sqx/adapters/mt5/` OK (incluye suites C4 ownership y C5 cancellation verdes), `./sqx/core/capabilities/` OK, `./sqx/adapters/storage-minio/` OK, `./sqx/activities/worker/{pipeline,hooks,steps}` OK; suite amplia `./sqx/...` verde en todo lo demás. Fallos preexistentes demostrados idénticos contra baseline limpio (stash de los 3 archivos del fix) y ajenos al fix: `activities/worker` 16, `mt5/report` corpus, `mt5/binding`+`mt5/normalization` 37 (los tres por fixture físico `mt5-export.htm` ausente en el working copy), `registry-postgres` 4 (mismos tests, estado integration PG), `workflows` 22 (fixtures sin `flow_run_start`, histórico), `tools` (múltiples `main`, histórico). Efecto lateral de suite detectado y revertido (`core/evaluation/warnings/artifacts/f5_warning_example.json` re-escrito por su propio test — restaurado a HEAD; estaba limpio al inicio).
- **Review local:** `git diff --check` limpio; sólo los 3 allowed files stageados; sin config/ownership/workflow/migration/release artifacts; dirty foráneo intacto.

## Evaluación

- **Correctness:** la semántica implementada coincide 1:1 con la guía contractual frozen (bounded sólo si falta deadline, preservación exacta del deadline entrante, cancelación observable, legacy intacto); los tests T2/T3 demuestran los dos riesgos opuestos del fix (no alargar deadlines ajenos, no revivir cancelaciones).
- **Autonomy:** sin STOP; el único hallazgo fuera de lo esperado (fixture mutado por suite) se resolvió restaurándolo a HEAD sin expandir scope.
- **Efficiency:** repro en worktree temporal hermano del repo (resolución `../sdk` del `go.work`) en vez de mutar el árbol principal; aislamiento de preexistentes por conteo/lista exacta con stash, sin re-ejecutar suites completas de más.
- **Tool use:** git/go toolchain locales; sin MCP aranea, sin infraestructura física; worktree temporal eliminado.
- **Overall:** misión completa en un commit único con regresión que demuestra el defecto (falla en 0.2.98, pasa con fix).

## Resultado

- **Outcome:** success — veredicto **`SOURCE FIX PASS`**; blocker source fix = `SOURCE VERIFIED`; `CERT-F04-01 = BLOCKED` (physical gate remains BLOCKED).
- **Siguiente acción exacta:** `BUILD/RELEASE → ROLLOUT → CERT-F04-01 RERUN FROM ZERO` (pendiente autorización manager; no merge a master, no tag/release en esta misión).
- **Aprendizaje para comparar herramientas:** los fixtures físicos ausentes (`mt5-export.htm`) explican un set estable de fallos preexistentes en 4 paquetes; comparar conteo exacto de `--- FAIL` con y sin el cambio fue el mecanismo más barato y riguroso para aislar regresión real de ruido ambiental.
