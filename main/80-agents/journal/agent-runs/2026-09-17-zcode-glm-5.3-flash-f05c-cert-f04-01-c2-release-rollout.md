---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-17"
updated: "2026-09-17"
area: "[[Echo]]"
project: "[[Echo + Echo Forge — Deferred Certification Backlog]]"
application: "[[xKoRx/symphony]]"
entities:
  - "[[Echo Forge]]"
related:
  - "[[Echo Forge — F-04 Magic allocation, version seal and handoff]]"
  - "[[Echo Forge — F-05-I Release Matrix and Read Surface Contract]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: coding
task_complexity: medium
outcome: success
verification: passed
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c2-release-rollout

## Trabajo

- **Objetivo:** F05C-CERT-F04-01-C2 — convertir el fix `49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1` en una release física desplegada y verificable (`BUILD/RELEASE → ROLLOUT`), preparada para re-ejecutar `CERT-F04-01`. Sin ejecutar el gate, sin modificar el fix, sin ETCD, sin campañas, sin watcher de certificación.
- **Alcance atribuible a esta combinación superficie×modelo:** bootstrap Agents OS (constitución, perfil, continuidad global, router aranea, backlog, runbook release-certification, decisiones version-authority y deploy-release-only, contrato evidence publisher); source authority gate local; publicación del fix branch y read-back GitHub; fast-forward de `codex/f05-release-prep` con read-back; test gate pre-release; resolución de versión por autoridad remota; build/publicación con `deploy_release.sh --release-only`; captura de buildinfo/hashes/manifest remoto; verificación de rollout Linux por SSH; verificación de rollout Windows por evidence publisher; registro documental.
- **Artefactos afectados:** repo `xKoRx/symphony` — refs remotas `codex/f04-cert-f04-01-deadline-fix` y `codex/f05-release-prep` ambas en `49fce32…` (fast-forward puro); release `0.2.99` publicada en MinIO bucket `deploy` (6 objetos + manifest). Vault — delta fechado + `Estado de entrada` + `Próxima tarea` en [[Echo + Echo Forge — Deferred Certification Backlog]], este agent-run, change_log `2026-09-17-f05c-cert-f04-01-c2-release-rollout`. Sin cambios de source (el fix no se tocó); dirty foráneo `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` preservado intacto; `deploy/manifest.json` local actualizado por el pipeline (efecto operativo esperado).

## Evidencia

- **Source authority:** local `HEAD == 49fce32…`, `HEAD^ == 3d0e8c9…`, diff exacto de 3 archivos permitidos; push y read-back desde origin/GitHub API (`sha`/`parent`/`message`/files); `origin/codex/f05-release-prep` verificado en `3d0e8c9…` antes del fast-forward y en `49fce32…` después (ls-remote + fetch).
- **Tests:** `go test ./sqx/adapters/mt5/` (paquete raíz: C4 ownership, C5 cancellation, durable no-downgrade, regresión T1–T6) OK; `./sqx/core/capabilities/` OK; `./sqx/adapters/storage-minio/` OK. Fallos en `mt5/report`/`mt5/binding`/`mt5/normalization` por fixture físico ausente — clase baseline C1, causa confirmada en `binding_test.go` (`repo root must contain the tracked all-loss fixture`), ningún fallo nuevo atribuible al fix.
- **Versión:** `release-authority` AUTO → `published=0.2.98`, `remote_line_max=0.2.98`, `authority_state=CONSISTENT`, candidato `0.2.99`; `NEW_RELEASE=0.2.99`.
- **Build:** 6 artifacts linux-amd64 (symphony 40312992 B sha256 `21ad38a1…`, sqx-watcher 32682144 B `f08d1684…`, start-symphony-worker.sh 310 B, run-symphony 233 B, promtail-worker.yaml 355 B) + windows-amd64 (sqx-mt5-worker.exe 37248512 B sha256 `f9af5c7b325001129b5751abeeb26555ea85cf02791181495d4eb6753a47800a`); `go version -m` de los 3 binarios: `vcs.revision=49fce32eb9eb6d0a4144891700fb6e5ccb36c2d1`, `vcs.time=2026-09-17T20:24:13Z`, `vcs.modified=true` (dirty operacional no-Go, misma clase que 0.2.98).
- **Publicación:** MinIO confirmó manifest `0.2.99` (20:43:07Z); read-back `release-authority --target 0.2.99` → `published_version=0.2.99`, `authority_state=CONSISTENT`, `target_state=EXACT_MATCH`; manifest remoto leído byte-exacto (6/6 keys/sizes/sha256 == locales, etag `12830e1f…`).
- **Rollout Linux:** Zeus CURRENT `0.2.99`, worker PID 2730741 (20:43:17Z) `releases/0.2.99/bin/symphony`, SHA256 instalado == artifact, `Started Worker … sqx-main-queue WorkerID 2730741@sqx-ulab-zeus-0`, ESTABLISHED :7233; Hera PID 1330991 (20:45:30Z) WorkerID `1330991@sqx-ulab-hera-0`; Kronos PID 1294710 (20:45:17Z) WorkerID `1294710@sqx-ulab-kron-0`; un solo worker por host (sin residuos 0.2.98). Legado `symphony-f03-382f4ba` en Hera presente desde sep10 — documentado, no tocado (no blocker).
- **Rollout Windows:** evidence publisher recurrente, reporte 20:48:07Z (`partial=false`, `errors=[]`, identidad `nt authority\system`, freshness 3 min < stale 15): worker `sqx-mt5-worker` PID 20044 (nuevo; antes 1700) desde 20:44:02Z en `C:\ProgramData\Stager\releases\0.2.99\bin\sqx-mt5-worker.exe`, `exe_sha256` == artifact EXACT MATCH, size == manifest, padre `stager-runtime` PID 8520, `CURRENT=0.2.99`/`PENDING=0.2.99`, poller ESTABLISHED PID 20044 → `192.168.31.46:7233`; singleton (1 worker).
- **Limitaciones de la evidencia:** `vcs.modified=true` no desglosado por archivo (clase ya explicada en 0.2.98: dirty operacional no-Go); convergencia Windows observada sólo vía evidence publisher (canal certificado vigente); identidad TaskQueue Windows apoyada por contrato del servicio + binario SHA-verificado + conexión al Temporal correcto (límites del canal ya documentados en el Access Plane).

## Evaluación

- **Correctness:** cada gate de la misión demostrado en la capa que posee la semántica (GitHub API para refs, release-authority para bytes, evidence publisher para runtime Windows, ps/log/ss para Linux); sin saltarse ningún STOP condition del mandato.
- **Autonomy:** cero STOP; único hallazgo fuera del happy path fue el retraso natural de convergencia Windows (~1 min tras la primera publicación de evidencia), resuelto esperando el ciclo del publisher.
- **Efficiency:** un solo ciclo de build/publicación (sin reintentos); verificación de flota en paralelo por perfiles SSH existentes; versión resuelta por autoridad en vez de asumirse.
- **Tool use:** git/go/gh locales, pipeline canónico `deploy_release.sh`, MCP `aranea-ssh` (perfiles operator certificados) y `aranea-minio-ro` para el manifest remoto; sin edición manual de objetos MinIO.
- **Overall:** release trazable de punta a punta: source SHA → buildinfo → manifest remoto → binarios instalados → procesos vivos, con un mecanismo de evidencia por capa.

## Resultado

- **Outcome:** success — veredicto **`RELEASE/ROLLOUT PASS`**; `CERT-F04-01 = BLOCKED / READY TO RERUN` (el rollout no equivale a certificación física).
- **Rework posterior:** ninguno conocido. Siguiente mandato preparado: `F05C-CERT-F04-01-RERUN` (nuevo RequestID/CampaignRef/WaveRef/FlowRunRef/workflow-run IDs; NO reutilizar `d400dba0…`; allocations `26090011001–003` write-once conservadas; allocator continúa desde su estado durable real; el input validado de EXEC puede reutilizarse como provenance/template si sigue compatible con 0.2.99).
- **Aprendizaje para comparar herramientas:** la cadena release→runtime se puede certificar sin tocar infraestructura si cada capa tiene un lector autorizado distinto (git remoto, release-authority CLI, SSH operator Linux, evidence publisher SYSTEM Windows); el coste real estuvo en respetar la frescura del publisher (ciclo 5 min / stale 15 min) en vez de leer procesos por canales no certificados.

