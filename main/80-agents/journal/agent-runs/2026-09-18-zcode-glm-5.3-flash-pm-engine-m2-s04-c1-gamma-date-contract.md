---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-18"
updated: "2026-09-18"
area: "[[Personal]]"
project: "[[Polymarket Engine — MVP]]"
application:
entities:
  - "[[Polymarket Engine — MVP]]"
related:
  - "[[Polymarket — Technical Platform Map — synced 2026-09-17]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: coding
task_complexity: high
outcome: success
verification: passed
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-18-zcode-glm-5.3-flash-pm-engine-m2-s04-c1-gamma-date-contract

## Trabajo

- **Objetivo:** M2-S04-C1 — mandato correctivo owner en dos fases estrictamente secuenciales: (A) corrección upstream S02 del parser temporal Gamma (`internal/protocol`) para las variantes demostradas por el servicio real, con fixtures, manifest y G-03 re-emitidos vía el mecanismo existente; (B) cierre de S04 — regresión de ingesta, sync real read-only con la implementación corregida, cuarentenas, idempotencia, known-at/recovery, coverage atribuible, regresión global y actualización documental.
- **Alcance atribuible a esta combinación superficie×modelo:** bootstrap Agents OS (cold start, entidad Polymarket Engine); preflight (HEAD `fc26c35`, árbol limpio, sin otros writers, archivos `.tmp.*` ajenos del vault intactos). FASE A: gramática cerrada Gamma-scoped en `internal/protocol/timeunits.go` (branch exclusivo de `TimeFieldGammaDate`; layouts cerrados RFC3339 + separador espacio + offset ±HH; fracciones 1–9 dígitos exactas con precisión declarada como la unidad power-of-thousand más gruesa que representa el instante; zona explícita obligatoria; offsets validados ≤23h/59m porque la stdlib no los valida en todos los layouts; >9 dígitos, zone-less, ±HHMM y garbage rechazados), 6 fixtures nuevas (1 positiva con los lexemas demostrados verbatim + 5 negativas por razón), specs con date override + regeneración de manifest y evidencias G-03/G-01-wire, probe nuevo `gamma_date_variants_counterexample` que pinea la corrección y conserva el fallo anterior en el registro; test unitarios de aceptación/rechazo/scoping; checkpoint S02 exclusivo `81aadb5`. FASE B: sync real read-only en 3 pasadas contra `gamma-api.polymarket.com` (eventos/markets, filter `closed=false`); corrección de 5 defectos demostrados por el servicio vivo durante el cierre — (1) fracciones de 2 dígitos (segunda demostración real que extendió la gramática a 1–9 dígitos con regla de exactitud 1–3→ms/4–6→µs/7–9→ns), (2) páginas `/events` que exceden el record cap de capture (4.5 MiB reales > 4 MiB default → composition root fija `MaxRecordBytes` 16 MiB), (3) `capture.segPaths` no seguía el rename active→sealed al rotar (fix root-cause de 1 línea contra el contrato documentado del propio paquete; blocker del lector en el mismo proceso — única desviación del scope nominal de FASE B, justificada y disclosed), (4) dedup de revisiones y relaciones sólo contra la última revisión → `UNIQUE(entity_id|relationship_id, content_hash)` violado por oscilación A→B→A de datos vivos (dedup ahora contra la historia completa; current pointer permanece en la última revisión), (5) `source_at` cortaba la flecha multibyte `→` por bytes (UTF-8 inválido en la columna, demostrado al normalizar fechas reales) + reporte de drain fallido que se ocultaba tras otro error; drain en sub-batches acotados por bytes (1 MiB) + `WriteTimeout` 30 s en el composition root para mantener transacciones breves; 4 tests G-04 nuevos (oscilación de contenido, oscilación de relación, source_at rune exacto, declaración combinada fetch_error+drain_failed) + 2 subtests de fault injection para los reads de historia + test de filas corruptas; evidencia G-04 re-emitida con 20 escenarios (16+4) y Portion actualizada; actualización documental (Estado actual, Tareas, Bitácora del proyecto y este run).
- **Artefactos afectados:** repo `~/go/src/github.com/xKoRx/polymarket-engine` — checkpoint `81aadb5` (S02-C1: internal/protocol + testdata/protocol) y `527dc73` (S04-C1: internal/catalog, internal/capture/writer.go [desviación mínima justificada y disclosed], cmd/engine/catalog.go, testdata/catalog/evidence); vault — [[Polymarket Engine — MVP]] (Estado actual, Tareas, Bitácora) y este agent run.

## Evidencia

- **Validaciones ejecutadas:** FASE A: `go test ./internal/protocol/...` verde, `-race` verde, `go vet` limpio. FASE B y global: `go build ./...` OK; `go vet ./...` OK; `go test ./...` verde (9 paquetes); `go test -race ./...` verde; `go mod tidy` sin diff; `git diff --check` limpio. Coverage: módulo 95.1%; catalog 95.1% aislado / 95.0% en corrida global (covermode atomic), gamma 97.5%, cmd 89.6% (seam `os.Exit` heredado de S01/S03/S04 — excepción continuada, no extendida), slice S04 combinado 93.9% (composición: catalog 95.0 + gamma 97.5 + cmd 89.6 — cmd domina el denominador por sus statements del composition root). G-03 PASS (47 fixtures; probe del contraejemplo M2-S04 incluido); G-01 wire PASS re-emitido; G-04 PASS 20/20 escenarios con evidencia física `testdata/catalog/evidence/g04.json` (16 originales + 4 correctivos).
- **Resultado observable:** sync real read-only ×3 pasadas sobre data-dir limpio con la implementación corregida — pasada 1: 2,100 eventos + 2,100 mercados observados, 22,555 + 1,411 revisiones nuevas, 689–863 duplicados ya en pasada 1 (mercados anidados en eventos dedup contra /markets), 54 cuarentenas genuinas (`outcome_arrays_missing`, mercados reales sin arrays; re-declaradas por pasada como auditoría con capture_seq propio); pasadas 2–3: 0 entidades nuevas, 18,388+2,001+2,100 duplicados idénticos y 1,913/3,202/99 revisiones nuevas TODAS atribuidas a cambio real del servicio (diff campo a campo: `dates.updatedAt` del servidor en el 100% de los casos + `prices` 315 + `unknown_fields` 245 en la muestra de 4,264); pass 3 de markets: 0 revisiones nuevas (idempotencia exacta); `catalog inspect` muestra known_at/observed_at/source_at (UTF-8 válido)/source_ref/capture_ref/outcomes; `journal verify` exit 0 (88→129 records, ~197 MB, 16 segmentos post-rotación, contigüidad, frontera durable y cross-check de cursors); known-at sin regresión verificada por SQL sobre los 83,956 entities (first_known_at == revisión 1 en el 100%, 0 regresiones). Techo no documentado del TPM: Gamma rechaza `offset > 2000` con HTTP 422 en `/events` y `/markets` (sondeado 2000/2050/2100/2150) — la caminata del universo activo termina partial honesto con `fetch_error`; documentado como gap upstream.
- **Limitaciones de la evidencia:** la cobertura completa del universo activo es upstream-imposible vía offset pagination por el techo 422 (keyset sigue sin documentar; gap declarado, no inventado); las revisiones nuevas entre pasadas contra el servicio vivo no son cero porque Gamma modifica `updatedAt`/precios — la atribución a cambio real está demostrada por diff de contenido, no asumida; el fix de `capture/writer.go` es la única modificación fuera del scope nominal de FASE B (root-cause de un blocker demostrado, sin cambio de contrato, surfaced para revisión del manager).

## Evaluación

- **Correctness:** la gramática del parser es cerrada y Gamma-scoped (sin parser global permisivo; Data v2 sigue rechazando las variantes nuevas, probado); precisión declarada sin redondeo (1–9 dígitos exactos, >9 rechazado); zona explícita obligatoria y offsets validados; dedup de historial como conjunto de contenido alineado con la UNIQUE del schema; durable-before-projection y cursor bajo la frontera durable intactos; ninguna fecha rechazada de más ni inventada de más.
- **Autonomy:** dos fases secuenciales respetadas (checkpoint S02 separado antes de tocar S04); defectos nuevos demostrados por el servicio vivo se corrigieron root-cause con prueba de regresión cada uno en vez de improvisar alrededor; la única desviación de scope (capture) está documentada con justificación y precedencia (ajuste mínimo análogo al aceptado en S04→persist).
- **Efficiency:** el mecanismo de evidencia existente (PM_ENGINE_EMIT_EVIDENCE) reutilizado sin nuevos canales; harness de tests compartido; los 4 escenarios correctivos viven dentro del registro G-04 en lugar de un gate paralelo.
- **Tool use:** sólo recursos locales + curl read-only contra el endpoint público documentado para sondear el techo de offset y los tamaños de página; sin secretos, sin push.
- **Overall:** cierre completo del blocker funcional de S04 con hallazgos adicionales convertidos en correcciones verificadas; el estado del upstream S02 mejora además a S05 (parser de fechas Gamma vigente contra el wire real).

## Resultado

- **Outcome:** **`M2_S04_PASS`** — parser Gamma corregido (G-03 PASS, checkpoint `81aadb5`), sync real read-only terminado y validado en 3 pasadas (partial honesto por techo upstream de offset ≤2000, documentado), cuarentena temporal indebida resuelta (0 cuarentenas por fechas; 54 genuinas explicadas), idempotencia demostrada sobre datos vivos, known-at/recovery sin regresión, coverage conforme (catalog 95.0–95.1/gamma 97.5/cmd 89.6 excepción heredada/módulo 95.1), suite global + race verdes, G-04 PASS 20/20 con evidencia física. Checkpoints `81aadb5` (S02-C1) y `527dc73` (S04-C1) en `main` sin push. G-06 sigue PARTIAL hasta S06; `LIVE_DISABLED` sin cambios.
- **Rework posterior:** revisión manager de la desviación capture (`internal/capture/writer.go`, 1 línea + comentario: `segPaths` sigue el rename active→sealed en rotate/finalSeal) — revertible en un commit si se objetan los términos; techo de offset 422 queda como gap upstream documentado (resolución posible vía keyset si Polymarket lo documenta, fuera de este mandato); siguiente `M2-S05` autorizado por el mandato en caso de PASS.
- **Aprendizaje para comparar herramientas:** la primera pasada real con la corrección falsó de inmediato la gramática cerrada inicial (2 dígitos, no sólo 5) — iterar contra el servicio vivo dentro del mismo mandato, con contraejemplos pineados y tests de regresión por cada defecto, convierte el descubrimiento en corrección verificable sin gates intermedios; el "comportamiento honesto por diseño" (quarantine/partial) volvió a localizar cada defecto en el componente exacto que lo poseía.
