---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-08"
updated: "2026-09-08"
area: "[[Echo]]"
project: "[[Echo — E-01 Canonical SDK Foundation S0]]"
application:
entities:
  - "[[Echo — Live Platform V1]]"
related:
  - "[[Echo SDK — Canonical Contract Final Freeze Review — Fable 5.1]]"
  - "[[2026-09-08-echo-e01-canonicalization-boundary-session-feedback]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: review
task_complexity: high
outcome: success
verification: verified
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-08-zcode-glm-5.3-flash-echo-e01-canonicalization-boundary-decision

## Trabajo

- **Objetivo:** resolver el diseño de frontera de `C()` (E-01): garantizar canonical JSON, schema-agnostic, UTF-8 válido y determinista sin mantener una segunda implementación material de internals privados de `encoding/json`; evaluar opciones A–D y cerrar una recomendación única.
- **Alcance atribuible:** decisión de diseño `IMPLEMENTATION_CORRECTION` (opción B: autoridad canónica sobre bytes JSON válidos, lane Go-value sobre comportamiento público documentado del stdlib, strictness UTF-8 en el byte-gate único de `canonicalJSON`); especificación de WP-G T26–T29 (allowed files `v3/sdk/contracts/wire/canonicalize.go` + `canonicalize_test.go`, keep/remove de `2be12e23`, tests de regresión obligatorios, gates). Sin código implementado, sin push.
- **Artefactos afectados:** [[Echo — E-01 Canonical SDK Foundation S0]] (estado, WP-G, bitácora, decisiones). Repo `xKoRx/echo` sin modificación (SDD sin contradicción: TASKS T02 satisfecho verbatim; corpus sin goldens dependientes del precheck).

## Evidencia

- **Lectura física:** `specs/FEAT-SDK-CANONICAL-CONTRACT/{SPEC,PLAN,TASKS}.md`; `v3/sdk/contracts/wire/**` completo; freeze FR-4a/FR-4b ([[Echo SDK — Canonical Contract Final Freeze Review — Fable 5.1]]); mirror identificado ~350 líneas (`selectJSONFields`/`dominantJSONField`/`jsonTagName`/`isValidJSONTagName`/`replacedByCustomMarshaler`/`walkUTF8*`/`walkUTF8MapKey` + caches), contenido 100% en `canonicalize.go` (grep sin referencias externas).
- **Probes físicos go1.25.5** (scratch /tmp, sin tocar repo): ciclos → `encountered a cycle` (error stdlib); referencias compartidas acíclicas OK; `Marshaler` con output UTF-8 inválido pasa el stdlib sin error pero queda invalid UTF-8 en bytes → lo captura el byte-gate existente de `canonicalJSON`; `Marshaler` con JSON inválido → error stdlib; string Go inválida → U+FFFD (comportamiento público documentado, encode.go:84/264); `map[string]json.RawMessage` con valor inválido atraviesa stdlib y queda invalid UTF-8 → byte-gate. `typeFields` confirmada privada en encode.go:1117; `encoding/json/v2/` experimental presente en el toolchain.
- **Argumento frozen:** FR-4a define `C()` sobre payloads ("mismo resultado para payloads válidos", "UTF-8 válido sin BOM") y exige implementable en MQL5/JS sin librería — la autoridad es bytes; el precheck de grafos Go fue endurecimiento post-freeze de la cadena de correcciones, no semántica congelada; ningún AC ni golden lo exige.

## Resultado

- **Outcome:** success — decisión cerrada y materializada en el subproyecto; estado E-01 se mantiene `implementation complete / verification pending` hasta que el manager acepte y un NORMAL nuevo implemente WP-G.
- **Riesgo residual registrado:** lane Go-value converge inputs UTF-8 inválidos vía U+FFFD documentado (el lane raw/ingestión permanece estricto); único acoplamiento restante es comportamiento público documentado del stdlib, no internals.
