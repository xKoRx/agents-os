---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-20"
updated: "2026-09-20"
area: "[[Echo]]"
project: "[[Echo + Echo Forge — Deferred Certification Backlog]]"
application: "[[xKoRx/symphony]]"
entities:
  - "[[Echo Forge]]"
related:
  - "[[Echo Forge — F-04 Magic allocation, version seal and handoff]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: coding
task_complexity: high
outcome: success
verification: passed
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-20-zcode-glm-5.3-flash-f05c-cert-f04-01-c11-sqx-zip-magic-readback-fix

## Trabajo

- **Objetivo:** F05C-CERT-F04-01-C11 — corrección mínima, probada y verificable del quinto defecto de CERT-F04-01 (read-back Magic en `.sqx` reales contenedor ZIP), veredicto máximo `SOURCE_FIX_READY_FOR_MANAGER_REVIEW`; sin cerrar F04-01, sin release, sin campaña, sin RERUN-6.
- **Alcance atribuible a esta combinación superficie×modelo:** bootstrap Agents OS + routing aranea y lectura del contrato F-04 y la evidencia RERUN-5; verificación de baseline sin drift (`66faa42…` local == origin) y worktree aislado `~/aranea/work/f04-cert-f04-01-c11/symphony-c11` (branch `codex/f05-sqx-zip-magic-readback-fix`, dirty operacional del checkout principal intacto); G0 inspección estructural del artefacto auténtico (4506976 B, SHA256 `9ac377b9…` == durable, ZIP válido, entrada canónica `strategy_Portfolio.xml` 32618 B con `<value>26090011010</value>`); G1 fix en `sqx/adapters/magic-readback/readback.go` (detección `PK\x03\x04`, `archive/zip` en memoria, entrada exacta case-sensitive, duplicadas ambiguas, límite de descompresión documentado 16 MiB, CRC/truncamiento fail-closed, parser XML compartido, ruta XML plano histórica explícita sin fallback, sentinels tipados, sin tocar FromMQ5/parseMagicValue/VerifyMagicReadback/consumer/exporter/allocator); G2 tests RED contra baseline con la firma exacta del error de producción (T1 auténtico con gate fail-closed por env var `SQX_AUTHENTIC_FIXTURE`, T2 ZIP controlado, T3 fixture del seal convertido a ZIP real); G3 matriz negativa completa en adapter y consumer; G4 camino auténtico `26090011010` byte-exacto con determinismo y no-mutación; G5 gates (gofmt/vet/diff-check, coverage 100%, seal 19/19, conjunto de fallo byte-idéntico al baseline con 16 fallos preexistentes, anti-test-masking sin skips, fixture de benchmark contaminado por tests históricos restaurado); G7 commit único `2993da07d453521d81589413edb344e16ac1a9c6`, push normal y read-back de origin (HEAD/parent/3 archivos); G8 registro vault completo.
- **Artefactos afectados:** repositorio — 3 archivos autorizados (`readback.go`, `readback_test.go`, `forge_seal_handoff_test.go`) en commit `2993da0…` sobre `codex/f05-sqx-zip-magic-readback-fix`; vault — delta C11 en [[Echo + Echo Forge — Deferred Certification Backlog]], checkpoint [[Echo Forge]], este agent-run, change_log `2026-09-20-f05c-cert-f04-01-c11-sqx-zip-magic-readback-fix`; workdir local `~/aranea/work/f04-cert-f04-01-c11/` (fixture 600, logs RED/GREEN con exit codes, RED diff, worktree baseline de comparación). Sin cambios en Echo, sin SPEC, sin release, sin IAM, sin efectos físicos.

## Evidencia

- **Validaciones ejecutadas:** (1) G0 — `sha256sum` + `stat` del fixture == durable (`9ac377b9a2b459b28ecefcf2c8166d8d200b2c47d4a4b2351cb8344d5635120d`, 4506976); inspección Python `zipfile`: `testzip` None, 5+ entradas con name/size/CRC, `strategy_Portfolio.xml` exacto 32618 B crc `9ec67337`, MagicNumber `26090011010` en `<Variables><variable>`, XML interno SHA256 `7e1f33fd…9522`. (2) RED — `go test -count=1 -v ./sqx/adapters/magic-readback/ -run 'TestFromSQX_AuthenticRERUN5SQX|TestFromSQX_ZIPContainerWithAuthenticShape'` exit 1 con `sqx xml parse: XML syntax error on line 3: illegal character code U+0003` en ambos; seal `TestForgeSealHandoff_SealsAndDeliversStructuralMember` exit 1 con `member … sqx readback: … U+0003 (CONTRACT_CONFLICT, retryable: false)`; logs `logs/red-*.log`, diff RED `logs/red-tests-diff-against-baseline.patch`, checkout SHA `logs/red-checkout-sha.txt`. (3) GREEN — `magic-readback` 6/6 PASS (coverage 100%: `magicFromSQXZip`/`findMagicVariable`/`parseMagicValue`/`FromMQ5` al 100%), `TestForgeSealHandoff` 19/19 PASS, G4 auténtico exit 0 en 2 procesos con size+SHA256 del archivo idénticos antes/después. (4) Comparación baseline — `go test ./sqx/activities/worker/ ./sqx/core/...` en worktree `symphony-baseline` @ `66faa42`: listas `--- FAIL` byte-idénticas (16 preexistentes MT5Reconcile/MT5ScoreShadow, `diff` vacío). (5) G7 — `git push` normal (rama nueva, sin force), `git ls-remote` == `2993da07d453521d81589413edb344e16ac1a9c6`, `diff-tree` == 3 archivos autorizados, parent `66faa42debba18cd0dd09de127ae1374ea2a07f0`.
- **Resultado observable:** veredicto **`SOURCE_FIX_READY_FOR_MANAGER_REVIEW`**; `CERT-F04-01 = BLOCKED / READY TO RERUN-6` (pendiente review manager → release C12 → RERUN-6). El fix hace que el read-back lea el formato efectivo del productor (ZIP → `strategy_Portfolio.xml` → MagicNumber) conservando el contrato XML histórico y el fail-closed íntegro.
- **Limitaciones de la evidencia:** sólo el `.sqx` del miembro 1 (`26090011010`) está disponible legítimamente en local — los de `26090011011`/`26090011012` no fueron ejercitados ni declarados; los tests ZIP sintéticos cubren la estructura común, no sustituyen los bytes de esos miembros; el límite de 16 MiB es documentado y su rechazo se prueba estrechándolo en test (no generando 16 MiB reales).

## Evaluación

- **Correctness:** cada afirmación del fix quedó anclada a evidencia ejecutada — RED con la firma exacta del error de producción contra `66faa42`, GREEN con los bytes auténticos byte-verificados, conjunto de fallo de suites idéntico al baseline por comparación de listas, y read-back de origin tras el push. El defecto de aserción inicial en `TestForgeSealHandoff_ZIPMagicMismatchFailsClosedWithoutAnyEffect` (substring equivocado) se detectó por fallo del test y se corrigió contra la clase de error real `magic_mismatch`.
- **Autonomy:** misión completa sin escalaciones; ningún efecto físico (sin RERUN-6, sin MT5, sin campañas, sin releases, sin IAM); scope respetado (3 archivos; el `phase4_performance.json` contaminado por corridas de tests históricos fue restaurado, no commiteado).
- **Efficiency:** RED y GREEN en el mismo worktree con baseline separado para comparación de fallos; cobertura del gap (93.7% → 100%) cerrada con tests de variantes XML documentadas y de header local corrupto, no con tests artificiales.
- **Tool use:** bash local (git, go, python3/zipfile, sha256sum, ln para el sibling `../sdk` @ `c85594440f67`), lectura MCP none-required; escrituras remotas = push normal del branch nuevo a origin.
- **Overall:** conversión del defecto #5 en un fix cerrado de source con RED/GREEN sobre bytes reales, listo para revisión independiente del manager; la cadena de certificación permanece intacta y write-once.

## Resultado

- **Outcome:** success — commit `2993da07d453521d81589413edb344e16ac1a9c6` publicado en `codex/f05-sqx-zip-magic-readback-fix` con parent exacto `66faa42…`; coverage 100% del paquete; 0 fallos nuevos vs baseline; `SOURCE_FIX_READY_FOR_MANAGER_REVIEW`. `MIGRATION: NONE`. Allocations `26090011001–012` write-once intactas.
- **Rework posterior:** revisión independiente del manager del diff `66faa42…2993da0` → release/rollout C12 desde `codex/f05-release-prep` (fast-forward) → RERUN-6 desde cero con identidades todas nuevas. CERT-F04-02 sigue bloqueada por F04-01; join con Echo sin cerrar.
- **Aprendizaje para comparar herramientas:** el gate auténtico como test fail-closed por env var (FAIL — nunca SKIP — ante fixture ausente/incorrecto) conecta el unit test con la evidencia física sin versionar 4.5 MB en Git; y la comparación de listas `--- FAIL` baseline vs fix sigue siendo la forma más barata de probar cero regresión en repos con rojo preexistente.
