---
type: agent_run
schema_version: 1
scope: session
created: "2026-09-17"
updated: "2026-09-17"
area: "[[Echo]]"
project: "[[Echo + Echo Forge — Deferred Certification Backlog]]"
application: "[[xKoRx/symphony]]"
entities:
  - "[[Echo Forge]]"
related:
  - "[[Echo Forge — F-04 Magic allocation, version seal and handoff]]"
  - "[[Echo Forge — F-05-I Release Matrix and Read Surface Contract]]"
aliases: []
agent_surface: "[[ZCode]]"
agent_model: GLM-5.3-Flash
model_source: host
task_type: coding
task_complexity: medium
outcome: success
verification: passed
evaluator: agent
user_rework: unknown
source_session:
load_policy: manual
indexable: false
index_priority: never
tags:
  - kind/agent-run
  - scope/session
---

# Agent Run — 2026-09-17-zcode-glm-5.3-flash-f05c-cert-f04-01-c4-microfix-release-rollout

## Trabajo

- **Objetivo:** F05C-CERT-F04-01-C4 — aplicar el microfix UTF-8 exigido por la revisión manager del source C3, integrarlo en `codex/f05-release-prep`, publicar release nueva resuelta por `release-authority` y desplegarla en flota completa (microfix → source authority → release → rollout), terminando en `RELEASE/ROLLOUT PASS` o `BLOCKED` fundamentado. Sin campañas, sin CERT-F04-01, sin infraestructura, sin ETCD, sin watcher de certificación, sin tocar `f03cert`.
- **Alcance atribuible a esta combinación superficie×modelo:** bootstrap Agents OS; análisis del gap en `decodeCompileLogText`; implementación del microfix + regresiones M1–M4; demostración fail-vs-C3 con worktree temporal; source gate (tests enfocados + equivalencia de fallos con baseline); fast-forward de `codex/f05-release-prep` con read-back; resolución de versión por autoridad remota; build/publicación con `deploy_release.sh --release-only`; verificación de buildinfo/hashes/manifest remoto; rollout Linux por SSH; rollout Windows por evidence publisher; redacción del prompt maestro `F05C-CERT-F04-01-RERUN-3` (NO ejecutado); registro documental.
- **Artefactos afectados:** repo `xKoRx/symphony` — commit `a440ac4ed730747b1ae6b07557cb6204102f16ac` en `codex/f04-cert-f04-01-encoding-fix` (2 archivos) y fast-forward puro de `codex/f05-release-prep` al mismo SHA; release `0.2.100` publicada en MinIO bucket `deploy` (6 objetos + manifest). Vault — delta fechado + `Estado de entrada` + `Próxima tarea` en [[Echo + Echo Forge — Deferred Certification Backlog]], este agent-run, change_log `2026-09-17-f05c-cert-f04-01-c4-microfix-release-rollout`, prompt RERUN-3 en el workspace de la misión. Dirty foráneo preservado intacto y sin stage (`deploy/manifest.json` actualizado por el pipeline, efecto operativo esperado; `specs/FEAT-SQX-STRATEGY-EVALUATION/fixtures/phase4_performance.json` intacto).

## Evidencia

- **Microfix:** `utf8.Valid(raw)` exigido en el branch sin BOM de `decodeCompileLogText`; UTF-16LE/BE, patrón `Result: 0 errors` y bytes durables intactos. Commit único sobre C3 (`18d0905…` parent exacto), push + read-back remoto.
- **M1–M4:** M1/M2 (byte UTF-8 inválido al principio/final de un log con `Result: 0 errors`) rechazados; M3 (UTF-8 válido no ASCII) aceptado; M4 compiler y seal rechazan los mismos bytes vía el helper compartido. Contra C3 sin microfix (worktree temporal @ `18d0905`): M1/M2/M4 FALLAN, M3 pasa; con el fix todo pasa; worktree eliminado sin residuo.
- **Source gate:** `FINAL_SOURCE_SHA=a440ac4…`; C3 ancestro inmediato; 2 archivos en el segundo commit; diff acumulado contra `49fce32…` = 4 archivos autorizados de C3. T1–T8 PASS; `./sqx/core/capabilities/...` OK; `./sqx/adapters/mt5` raíz OK (C4/C5/durable); MT5CompilePersist 7/7 + wiring workflow PASS; 75 fallos de fixtures históricos idénticos al baseline C3 (comparación de listas normalizadas).
- **Versión:** `release-authority` AUTO → `published_version=0.2.99`, `remote_line_max=0.2.99`, `authority_state=CONSISTENT`, candidato `0.2.100`; preflight target `AVAILABLE`; `NEW_RELEASE=0.2.100`.
- **Build:** 6 artifacts desde `a440ac4…` (symphony 40317088 B `9fbf73b2…`, sqx-watcher 32682144 B `d0dac89c…`, start-symphony-worker.sh 310 B, run-symphony 233 B, promtail-worker.yaml 355 B, sqx-mt5-worker.exe 37249024 B `b36216c0facf4aa79454c165ceac1e41202aed39e7b67078ce3e575bc04cc8f3`); `vcs.revision=a440ac4…`, `vcs.time=2026-09-17T22:38:01Z`, `vcs.modified=true` (dirty no-Go explicado). Manifest publicado 22:44:45Z; read-back `release-authority --target 0.2.100` → `CONSISTENT`/`EXACT_MATCH`.
- **Rollout Linux:** Zeus PID 2742882 (22:46:39Z, WorkerID `2742882@sqx-ulab-zeus-0`), Hera PID 1338299 (22:45:51Z, `1338299@sqx-ulab-hera-0`), Kronos PID 1302804 (22:47:08Z, `1302804@sqx-ulab-kron-0`); los 3 CURRENT `0.2.100`, binario SHA256 == artifact, poller `sqx-prop/sqx-main-queue` + ESTABLISHED :7233, un worker por host. Arranque efímero previo del stager en Zeus/Kronos (restart_backoff); estado final singleton.
- **Rollout Windows:** evidence publisher 22:48:10Z (`partial=false`, SYSTEM, freshness <15 min): worker PID 5496 desde 22:45:03Z en `C:\ProgramData\Stager\releases\0.2.100\bin\sqx-mt5-worker.exe`, SHA256 == artifact, padre `stager-runtime` PID 19472, `CURRENT/PENDING=0.2.100`, poller ESTABLISHED → :7233, singleton.
- **Limitaciones de la evidencia:** `vcs.modified=true` no desglosado por archivo (clase explicada 0.2.98/0.2.99); convergencia Windows observada sólo vía evidence publisher (canal certificado vigente); sin bytes del compile log físico auténtico (queda para el re-run).

## Evaluación

- **Correctness:** cada condición del veredicto demostrada en la capa que posee la semántica; el defecto se reprodujo contra C3 antes de corregir y la corrección se verificó en ambos gates; ninguna suite fallida declarada verde (equivalencia con baseline probada por diff normalizado).
- **Autonomy:** cero STOP; el pipeline canónico resolvió autoridad/build/publicación sin reintentos; convergencia Windows inmediata.
- **Efficiency:** un solo ciclo microfix→release; verificación de flota en paralelo por perfiles SSH certificados.
- **Tool use:** git/go locales, worktrees temporales para repro y baseline, pipeline canónico, MCP `aranea-ssh` (operators certificados) y `mt5-kronos-operator` read-only sobre evidence JSON; sin edición manual de objetos MinIO ni ejecutables.
- **Overall:** cadena microfix→source→release→runtime trazable de punta a punta con un mecanismo de evidencia por capa.

## Resultado

- **Outcome:** success — veredicto **`RELEASE/ROLLOUT PASS`**; `CERT-F04-01 = BLOCKED / READY TO RERUN` (el rollout no constituye certificación física).
- **Rework posterior:** ninguno conocido. Prompt maestro `F05C-CERT-F04-01-RERUN-3` redactado y persistido, NO ejecutado: re-run desde cero sobre `0.2.100`/`a440ac4…` con identidades todas nuevas, allocations `26090011001–004` conservadas, captura obligatoria del compile log durable auténtico (key/size/SHA256/BOM) para cerrar la regresión T8.
- **Aprendizaje para comparar herramientas:** el gate fail-closed de encoding necesita validar también el branch sin BOM — la confianza en el caso BOM (bien probado) ocultó el caso passthrough; el par worktree temporal + diff de listas de fallos normalizadas volvió barato demostrar equivalencia con baseline sin ampliar scope.
